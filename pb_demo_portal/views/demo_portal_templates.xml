<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============ Registration page ============ -->
    <template id="register_page" name="Payobook Demo Registration">
        <t t-call="website.layout">
            <t t-set="no_header" t-value="true"/>
            <t t-set="no_footer" t-value="true"/>
            <div class="pbdemo-wrap">
                <div class="pbdemo-grid">
                    <!-- Left: pitch -->
                    <aside class="pbdemo-pitch">
                        <div class="pbdemo-brand">
                            <span class="pbdemo-logo">P</span>
                            <span>Payobook</span>
                        </div>
                        <h1>Experience enterprise payroll, live.</h1>
                        <p class="pbdemo-lead">Get instant access to a fully-loaded Payobook
                            environment — thousands of employees, real payroll history,
                            multi-country schemes, analytics and integrations.</p>
                        <ul class="pbdemo-points">
                            <li><span class="pbdemo-tick"/>4,500 employees across 6 divisions</li>
                            <li><span class="pbdemo-tick"/>Formula-driven payroll history &amp; analytics</li>
                            <li><span class="pbdemo-tick"/>Multilingual payslips &amp; 25+ integrations</li>
                            <li><span class="pbdemo-tick"/>Run payroll end-to-end in minutes — guided by AI</li>
                        </ul>
                    </aside>

                    <!-- Right: form -->
                    <main class="pbdemo-card">
                        <h2>Start your demo</h2>
                        <p class="pbdemo-sub">Tell us a little about you. We'll email a secure link to set your password.</p>
                        <div t-if="error" class="pbdemo-error" role="alert" t-esc="error"/>
                        <form action="/demo/register/submit" method="post" class="pbdemo-form">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="pbdemo-row">
                                <label>Full name
                                    <input type="text" name="name" t-att-value="values.get('name', '')" required="required" placeholder="Jane Tran"/>
                                </label>
                                <label>Company
                                    <input type="text" name="company" t-att-value="values.get('company', '')" required="required" placeholder="Acme Corp"/>
                                </label>
                            </div>
                            <label>Business email
                                <input type="email" name="email" t-att-value="values.get('email', '')" required="required" placeholder="jane@acme.com"/>
                            </label>
                            <div class="pbdemo-row">
                                <label>Mobile number
                                    <input type="text" name="mobile" t-att-value="values.get('mobile', '')" required="required" placeholder="+84 90 123 4567"/>
                                </label>
                                <label>Country
                                    <select name="country_id" required="required">
                                        <option value="">Select…</option>
                                        <t t-foreach="countries" t-as="c">
                                            <option t-att-value="c.id"
                                                    t-att-selected="'selected' if values.get('country_id') == str(c.id) else None"
                                                    t-esc="c.name"/>
                                        </t>
                                    </select>
                                </label>
                            </div>
                            <div class="pbdemo-row">
                                <label>Industry
                                    <select name="industry" required="required">
                                        <option value="">Select…</option>
                                        <t t-foreach="industries" t-as="ind">
                                            <option t-att-value="ind"
                                                    t-att-selected="'selected' if values.get('industry') == ind else None"
                                                    t-esc="ind"/>
                                        </t>
                                    </select>
                                </label>
                                <label>Company size
                                    <select name="company_size" required="required">
                                        <option value="">Select…</option>
                                        <t t-foreach="company_sizes" t-as="sz">
                                            <option t-att-value="sz"
                                                    t-att-selected="'selected' if values.get('company_size') == sz else None"
                                                    t-esc="sz"/>
                                        </t>
                                    </select>
                                </label>
                            </div>
                            <button type="submit" class="pbdemo-submit">Get my demo access</button>
                            <p class="pbdemo-fine">Already have access?
                                <a href="/web/login">Sign in</a>.</p>
                        </form>
                    </main>
                </div>
            </div>
        </t>
    </template>

    <!-- ============ Confirmation page ============ -->
    <template id="register_done" name="Payobook Demo Registration Done">
        <t t-call="website.layout">
            <t t-set="no_header" t-value="true"/>
            <t t-set="no_footer" t-value="true"/>
            <div class="pbdemo-wrap pbdemo-center">
                <main class="pbdemo-card pbdemo-done">
                    <div class="pbdemo-check"/>
                    <h2 t-if="not already">Check your inbox</h2>
                    <h2 t-if="already">You're already registered</h2>
                    <p t-if="not already">We've sent a secure link to <strong t-esc="email"/>.
                        Click it to set your password, then you'll be signed straight into your
                        Payobook demo.</p>
                    <p t-if="already">An account already exists for <strong t-esc="email"/>.
                        You can sign in or reset your password.</p>
                    <a class="pbdemo-submit" href="/web/login">Go to sign in</a>
                </main>
            </div>
        </t>
    </template>

    <!-- ============ Private-demo enquiry page ============ -->
    <template id="private_demo_page" name="Payobook Private Demo Request">
        <t t-call="website.layout">
            <t t-set="no_header" t-value="true"/>
            <t t-set="no_footer" t-value="true"/>
            <div class="pbdemo-wrap">
                <div class="pbdemo-grid">
                    <!-- Left: pitch -->
                    <aside class="pbdemo-pitch">
                        <div class="pbdemo-brand">
                            <span class="pbdemo-logo">P</span>
                            <span>Payobook</span>
                        </div>
                        <span class="pbdemo-kicker">Private demo</span>
                        <h1>Payroll, tailored to your business.</h1>
                        <p class="pbdemo-lead">Book a one-on-one walkthrough with a Payobook
                            specialist. We'll shape the session around your countries, headcount
                            and pay rules — and answer anything on migration, compliance and cost.</p>
                        <ul class="pbdemo-points">
                            <li><span class="pbdemo-tick"/>A guided tour built around <b>your</b> payroll</li>
                            <li><span class="pbdemo-tick"/>Multi-country statutory &amp; formula deep-dive</li>
                            <li><span class="pbdemo-tick"/>Migration, integrations &amp; pricing Q&amp;A</li>
                            <li><span class="pbdemo-tick"/>No obligation — just a real conversation</li>
                        </ul>
                    </aside>

                    <!-- Right: form -->
                    <main class="pbdemo-card">
                        <h2>Request your private demo</h2>
                        <p class="pbdemo-sub">Tell us a little about you and we'll be in touch to set up a time.</p>
                        <div t-if="error" class="pbdemo-error" role="alert" t-esc="error"/>
                        <form action="/demo/private/submit" method="post" class="pbdemo-form">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="pbdemo-row">
                                <label>Full name <span class="pbdemo-req">*</span>
                                    <input type="text" name="name" t-att-value="values.get('name', '')" required="required" placeholder="Jane Tran"/>
                                </label>
                                <label>Company <span class="pbdemo-req">*</span>
                                    <input type="text" name="company" t-att-value="values.get('company', '')" required="required" placeholder="Acme Corp"/>
                                </label>
                            </div>
                            <div class="pbdemo-row">
                                <label>Business email <span class="pbdemo-req">*</span>
                                    <input type="email" name="email" t-att-value="values.get('email', '')" required="required" placeholder="jane@acme.com"/>
                                </label>
                                <label>Phone <span class="pbdemo-req">*</span>
                                    <input type="text" name="mobile" t-att-value="values.get('mobile', '')" required="required" placeholder="+84 90 123 4567"/>
                                </label>
                            </div>
                            <div class="pbdemo-row">
                                <label>Country
                                    <select name="country_id">
                                        <option value="">Select…</option>
                                        <t t-foreach="countries" t-as="c">
                                            <option t-att-value="c.id"
                                                    t-att-selected="'selected' if values.get('country_id') == str(c.id) else None"
                                                    t-esc="c.name"/>
                                        </t>
                                    </select>
                                </label>
                                <label>Company size
                                    <select name="company_size">
                                        <option value="">Select…</option>
                                        <t t-foreach="company_sizes" t-as="sz">
                                            <option t-att-value="sz"
                                                    t-att-selected="'selected' if values.get('company_size') == sz else None"
                                                    t-esc="sz"/>
                                        </t>
                                    </select>
                                </label>
                            </div>
                            <label>Anything you'd like us to focus on?
                                <textarea name="message" rows="3" placeholder="e.g. We run payroll in Vietnam &amp; Singapore and want to see the migration path." t-esc="values.get('message', '')"/>
                            </label>
                            <button type="submit" class="pbdemo-submit">Request my private demo</button>
                            <p class="pbdemo-fine">Prefer the instant shared demo?
                                <a href="/demo">Start now</a>.</p>
                        </form>
                    </main>
                </div>
            </div>
        </t>
    </template>

    <!-- ============ Private-demo confirmation ============ -->
    <template id="private_demo_done" name="Payobook Private Demo Request Done">
        <t t-call="website.layout">
            <t t-set="no_header" t-value="true"/>
            <t t-set="no_footer" t-value="true"/>
            <div class="pbdemo-wrap pbdemo-center">
                <main class="pbdemo-card pbdemo-done">
                    <div class="pbdemo-check"/>
                    <h2>Request received</h2>
                    <p>Thanks — we've sent a confirmation to <strong t-esc="email"/> and a
                        Payobook specialist will reach out shortly to arrange your private demo.</p>
                    <a class="pbdemo-submit" href="/demo">Explore the shared demo meanwhile</a>
                </main>
            </div>
        </t>
    </template>
</odoo>
