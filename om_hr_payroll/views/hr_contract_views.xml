<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Root Menus -->
    <!--
    <menuitem id="menu_hr_payroll_root"
              name="Payroll"
              sequence="50"
              action="action_internal_user_portal"
              web_icon="om_hr_payroll,static/description/icon.png"/>
    -->
    <!-- NOTE: this used to carry action="pb_hr_flow.action_hr_flow_wizard".
         pb_hr_flow DEPENDS on om_hr_payroll, so on a fresh database that
         reference can never resolve ("External ID not found in the system:
         pb_hr_flow.action_hr_flow_wizard") — a base module may not point at a
         downstream module's action. It was also dead weight: pb_dashboard
         (depth 6) loads after om_hr_payroll (depth 4) and re-points this same
         menu to its own cockpit in views/pb_dashboard_action.xml, which is what
         every database in the estate actually holds. Re-pointing from
         pb_hr_flow instead would have CHANGED that, because pb_hr_flow (depth 8)
         loads after pb_dashboard and would win. So the root menu simply carries
         no action of its own; whoever owns the landing screen sets it. -->
    <menuitem id="menu_hr_payroll_root"
              name="Payroll"
              sequence="50"
              web_icon="om_hr_payroll,static/description/icon.png"/>

<!--
    <menuitem id="menu_hr_payroll_configuration"
              name="Configuration" parent="menu_hr_payroll_root"
              sequence="100"
              groups="om_hr_payroll.group_hr_payroll_manager"/>
-->
    <menuitem id="menu_hr_payroll_configuration"
              name="Configuration" parent="menu_hr_payroll_root"
              sequence="100"
              />



    <!-- Contract View -->
    <record id="hr_contract_form_inherit" model="ir.ui.view">
        <field name="name">hr.contract.view.form.inherit</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='job_id']" position="after">
                <!-- NOT required: payroll here is defined by a scheme, and a
                     contract on such a tenant has no salary structure to pick.
                     Demanding one made the contract form unsaveable on any
                     database whose payroll does not use structures — which is
                     every one of them except where a structure is deliberately
                     in use. Where structures ARE used, the field is still here
                     and still the thing the standard engine reads. -->
                <field name="struct_id"/>
                <field name="type_id" required="1"/>
            </xpath>
            <xpath expr="//field[@name='job_id']" position="before">
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="currency_id" invisible="1"/>
            </xpath>

            <xpath expr="//field[@name='resource_calendar_id']" position="after">
                <field name="schedule_pay"/>

            </xpath>

            <xpath expr="//page[@name='information']" position="after">
                <page name="other" position="move"/>
            </xpath>

            <xpath expr="//group[@name='salary']" position="after">
                <group> 
                        <field name="tupart" widget="radio"  options="{'horizontal':true}" />
                        <field name="shuipart" widget="radio"  options="{'horizontal':true}" />
                </group>

                    <group>
                        <field name="dependents"/>                 
                        <field name="location"/>
                        <field name="costcenter"/>
                    </group>
                    <group>
                        <field name="hirestatus"/>                 
                    </group>



                <separator string="Contract Components" />
                <field name="advantages_ids">
                    <list editable="bottom">
                        <field
                            name="advantage_template_id"
                            options="{'no_quick_create':True,'no_create_edit':True,'no_open': True,}"
                        />
                        <field name="advantage_template_code" />
                        <field name="advantage_lower_bound" />
                        <field name="advantage_upper_bound" />
                        <field name="amount" />
                    </list>
                </field>
            </xpath>
            
            <!-- Biztint-->
            <!--
            <xpath expr="//group[@name='salary_info']" position="after">
                <group string="Monthly Advantages in Cash" name='allowances'>
                    <label for="hra"/>
                    <div class="o_row">
                        <field name="hra" nolabel="1"/>
                        <span>/ month</span>
                    </div>
                    <label for="da"/>
                    <div class="o_row">
                        <field name="da" nolabel="1"/>
                        <span>/ month</span>
                    </div>
                    <label for="travel_allowance"/>
                    <div class="o_row">

                        <field name="travel_allowance" nolabel="1"/>
                        <span>/ month</span>
                    </div>
                    <label for="meal_allowance"/>
                    <div class="o_row">

                        <field name="meal_allowance" nolabel="1"/>
                        <span>/ month</span>
                    </div>
                    <label for="medical_allowance"/>
                    <div class="o_row">

                        <field name="medical_allowance" nolabel="1"/>
                        <span>/ month</span>
                    </div>
                    <label for="other_allowance"/>
                    <div class="o_row">

                        <field name="other_allowance" nolabel="1"/>
                        <span>/ month</span>
                    </div>
                </group>
            </xpath>
            -->
        </field>
    </record>

    <record id="hr_contract_advantage_template_view_form" model="ir.ui.view">
        <field name="name">hr.contract.advantage.template.form</field>
        <field name="model">hr.contract.advantage.template</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Advantage Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="default_value"/>
                            <field name="lower_bound"/>
                            <field name="upper_bound"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="hr_contract_advantage_template_view_tree" model="ir.ui.view">
        <field name="name">hr.contract.advantage.template.tree</field>
        <field name="model">hr.contract.advantage.template</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="code"/>
                <field name="lower_bound"/>
                <field name="upper_bound"/>
                <field name="default_value"/>
            </list>
        </field>
    </record>

    <record id="hr_contract_advantage_template_action" model="ir.actions.act_window">
        <field name="name">Contract Advantage Templates</field>
        <field name="res_model">hr.contract.advantage.template</field>
    </record>

    <menuitem
        id="hr_contract_advantage_template_menu_action"
        action="hr_contract_advantage_template_action"
        parent="menu_hr_payroll_configuration"
        sequence="50"/>

</odoo>
