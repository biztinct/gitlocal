<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
      Sudima Phase M — the Analytics menu forest is RETIRED.

      This file used to publish `menu_hr_analytics_root` plus eight children
      under om_hr_payroll's payroll root: a pre-design-system native menu tree
      over 14 report models. The single entry point is now the Insights cockpit
      (`pb_insights`, sidebar > Insights), whose report gallery launches each of
      these actions by xmlid.

      Nothing else is removed: every ir.actions.act_window, view and model in
      this module STAYS, off-menu, as the admin fallback (C18.42 / C18.55) and
      as the gallery's destinations. Dropping the menuitems is reversible — the
      records return by restoring this file and re-running `-u`.

      Menuitems retired here (their actions still resolve):
        menu_hr_analytics_root
          menu_hr_analytics_dashboard        -> action_prepare_hr_analytics_dashboard
          menu_hr_analytics_personnel_costs  -> action_view_hr_analytics_personnel_costs
          menu_hr_analytics_statutory        -> action_view_hr_analytics_statutory_contrib
          menu_hr_analytics_headcount        -> action_view_hr_analytics_headcount
          menu_hr_analytics_dependents       -> action_view_hr_analytics_dependents
          menu_hr_analytics_budget           -> action_view_hr_analytics_budget_variance
          menu_hr_analytics_annual           -> action_view_hr_analytics_annual_costs
          menu_formula_config_analytics      -> action_prepare_formula_config_analytics
    -->

</odoo>
