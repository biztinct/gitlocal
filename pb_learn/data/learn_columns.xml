<?xml version="1.0" encoding="utf-8"?>
<!-- GENERATED FILE. Do not edit.
         Source: docs/tutorial_poc/author/ · Regenerate: python3 docs/tutorial_poc/author/tools/gen_learn_data.py
         Hand edits are erased on the next run and fail the CI check. -->
<!-- The per-screen micro-glossary: what a KPI tile or a chip counts. Curated, not read from ir.model.fields — most of these are computed tiles with no field behind them to read a help string from. -->
<odoo>

    <record id="col_runpayroll_eligible_employees" model="learn.column">
        <field name="screen">runpayroll</field>
        <field name="key">eligible_employees</field>
        <field name="sequence">10</field>
        <field name="label">Eligible employees</field>
        <field name="body">How many people this configuration would compute a payslip for, given the period and the division you have chosen. It is the count to read before pressing Compute: a number far below the division's headcount almost always means the month's attendance import has not been committed yet.</field>
    </record>

    <record id="col_runpayroll_payslips" model="learn.column">
        <field name="screen">runpayroll</field>
        <field name="key">payslips</field>
        <field name="sequence">20</field>
        <field name="label">Payslips</field>
        <field name="body">How many payslip records the compute created for this run. It should match the eligible count you read beforehand; if it does not, something was excluded during the compute and is worth finding before the run moves on.</field>
    </record>

    <record id="col_runpayroll_computed" model="learn.column">
        <field name="screen">runpayroll</field>
        <field name="key">computed</field>
        <field name="sequence">30</field>
        <field name="label">Computed</field>
        <field name="body">How many of those payslips the formula configuration was able to evaluate all the way through. A payslip that exists but did not compute has no net figure, so it would leave somebody unpaid rather than underpaid.</field>
    </record>

    <record id="col_runpayroll_need_review" model="learn.column">
        <field name="screen">runpayroll</field>
        <field name="key">need_review</field>
        <field name="sequence">40</field>
        <field name="label">Need review</field>
        <field name="body">How many payslips the engine flagged for a human to read — an unusual variance, a zero net, an input it could not reconcile. A flag is a question, not an error, and clearing one means understanding it rather than dismissing it.</field>
    </record>

    <record id="col_runpayroll_exceptions" model="learn.column">
        <field name="screen">runpayroll</field>
        <field name="key">exceptions</field>
        <field name="sequence">50</field>
        <field name="label">Exceptions</field>
        <field name="body">The list behind the Need review count, one line per payslip with the reason the engine raised it. It is where the judgement in a pay run actually lives — everything else on this screen computed itself.</field>
    </record>

    <record id="col_payruns_pay_runs" model="learn.column">
        <field name="screen">payruns</field>
        <field name="key">pay_runs</field>
        <field name="sequence">10</field>
        <field name="label">Pay runs</field>
        <field name="body">How many runs exist within the date and division filters currently active. It is a scoped count, not a total: changing a chip below changes this number without anything having happened in the database.</field>
    </record>

    <record id="col_payruns_in_pipeline" model="learn.column">
        <field name="screen">payruns</field>
        <field name="key">in_pipeline</field>
        <field name="sequence">20</field>
        <field name="label">In pipeline</field>
        <field name="body">Runs that have left draft and have not reached done — they are sitting at somebody's approval gate. It is not a count of work you owe; "Awaiting your approval" is that.</field>
    </record>

    <record id="col_payruns_awaiting_your_approval" model="learn.column">
        <field name="screen">payruns</field>
        <field name="key">awaiting_your_approval</field>
        <field name="sequence">30</field>
        <field name="label">Awaiting your approval</field>
        <field name="body">Runs sitting at a gate that your groups let you act on. This is the only number on the band that is about you, and the one to read first every morning of payroll week — nothing here moves until you move it.</field>
    </record>

    <record id="col_payruns_completed" model="learn.column">
        <field name="screen">payruns</field>
        <field name="key">completed</field>
        <field name="sequence">40</field>
        <field name="label">Completed</field>
        <field name="body">Runs that have passed every gate. Completion is what unlocks the bank file, the journals and the payments — and it is also the point after which a correction becomes a retro line rather than an edit.</field>
    </record>

    <record id="col_payruns_net_paid" model="learn.column">
        <field name="screen">payruns</field>
        <field name="key">net_paid</field>
        <field name="sequence">50</field>
        <field name="label">Net paid</field>
        <field name="body">The total net of completed runs inside the current filters. Runs still in the pipeline are deliberately excluded: this is money that has passed every gate, not money that is expected to.</field>
    </record>

    <record id="col_payslips_payslips" model="learn.column">
        <field name="screen">payslips</field>
        <field name="key">payslips</field>
        <field name="sequence">10</field>
        <field name="label">Payslips</field>
        <field name="body">How many payslips this run contains, before any filter chip is applied. If it is not the headcount you expected, the problem is in the run rather than on this screen.</field>
    </record>

    <record id="col_payslips_net_total" model="learn.column">
        <field name="screen">payslips</field>
        <field name="key">net_total</field>
        <field name="sequence">20</field>
        <field name="label">Net total</field>
        <field name="body">The sum of every net figure in this run — what will actually leave the bank account. Compare it with last month before approving: a variance you cannot explain in one sentence is a variance you have not checked.</field>
    </record>

    <record id="col_payslips_gross_total" model="learn.column">
        <field name="screen">payslips</field>
        <field name="key">gross_total</field>
        <field name="sequence">30</field>
        <field name="label">Gross total</field>
        <field name="body">The sum of earnings before any deduction — base, allowances and overtime added together across the run. The gap between this and the net total is the statutory deductions plus tax, and that gap should move roughly in step with headcount.</field>
    </record>

    <record id="col_payslips_approved" model="learn.column">
        <field name="screen">payslips</field>
        <field name="key">approved</field>
        <field name="sequence">40</field>
        <field name="label">Approved</field>
        <field name="body">How many payslips in this run have reached the done stage, against the total. A payslip carries its own position in the chain, which is not always the run's — that difference shows up here when one slip has been held back for a question.</field>
    </record>

    <record id="col_payslips_need_review" model="learn.column">
        <field name="screen">payslips</field>
        <field name="key">need_review</field>
        <field name="sequence">50</field>
        <field name="label">Need review</field>
        <field name="body">Payslips the engine flagged for a human to read — an unusual variance, a zero net, an input it could not reconcile. A flag is a question, not an error, and clearing one means understanding it rather than dismissing it.</field>
    </record>

    <record id="col_import_import_batches" model="learn.column">
        <field name="screen">import</field>
        <field name="key">import_batches</field>
        <field name="sequence">10</field>
        <field name="label">Import batches</field>
        <field name="body">Every batch ever started, whatever became of it. A batch is a file or a connector pull with a period attached — it is the unit you go back to when something in a month looks wrong.</field>
    </record>

    <record id="col_import_completed" model="learn.column">
        <field name="screen">import</field>
        <field name="key">completed</field>
        <field name="sequence">20</field>
        <field name="label">Completed</field>
        <field name="body">Batches that reached commit. Only these have written anything: a batch that stopped earlier changed nothing at all, which is the whole design of the guided flow.</field>
    </record>

    <record id="col_import_in_progress" model="learn.column">
        <field name="screen">import</field>
        <field name="key">in_progress</field>
        <field name="sequence">30</field>
        <field name="label">In progress</field>
        <field name="body">Batches that have been loaded but not committed. They are safe to leave — nothing is written — but a batch left here on the day of the run is a month of inputs that never arrived.</field>
    </record>

    <record id="col_import_with_errors" model="learn.column">
        <field name="screen">import</field>
        <field name="key">with_errors</field>
        <field name="sequence">40</field>
        <field name="label">With errors</field>
        <field name="body">Batches holding at least one row the importer could not place. Each of those rows is a person: unresolved, they become a payslip that is missing or computed on the wrong figure.</field>
    </record>

    <record id="col_import_connectors" model="learn.column">
        <field name="screen">import</field>
        <field name="key">connectors</field>
        <field name="sequence">50</field>
        <field name="label">Connectors</field>
        <field name="body">Configured links to a source system that can pull data without anybody retyping it. A connector that has quietly stopped syncing looks exactly like a connector that is working until the month it matters.</field>
    </record>

    <record id="col_importwizard_rows_loaded" model="learn.column">
        <field name="screen">importwizard</field>
        <field name="key">rows_loaded</field>
        <field name="sequence">10</field>
        <field name="label">Rows loaded</field>
        <field name="body">How many rows the importer read out of the source. If this is not the row count of your file, the file was truncated or a sheet was missed — and that is a better thing to find here than after the compute.</field>
    </record>

    <record id="col_importwizard_matched" model="learn.column">
        <field name="screen">importwizard</field>
        <field name="key">matched</field>
        <field name="sequence">20</field>
        <field name="label">Matched</field>
        <field name="body">Rows the importer tied to an existing employee with no ambiguity. These are the rows that will become inputs without anybody looking at them again, so the count matters more than the percentage above it.</field>
    </record>

    <record id="col_importwizard_new_employees" model="learn.column">
        <field name="screen">importwizard</field>
        <field name="key">new_employees</field>
        <field name="sequence">30</field>
        <field name="label">New employees</field>
        <field name="body">Rows the importer could not tie to anybody and that you have marked as a person who does not exist yet. Committing creates them, so this number is a hiring decision arriving through a spreadsheet — worth a second look.</field>
    </record>

    <record id="col_importwizard_need_attention" model="learn.column">
        <field name="screen">importwizard</field>
        <field name="key">need_attention</field>
        <field name="sequence">40</field>
        <field name="label">Need attention</field>
        <field name="body">Rows the importer could not place on its own: no matching employee, a duplicate, or a value it could not read. Every one of these is a person, and committing with any of them open produces a payslip that is missing or wrong.</field>
    </record>

    <record id="col_importwizard_confidence_score" model="learn.column">
        <field name="screen">importwizard</field>
        <field name="key">confidence_score</field>
        <field name="sequence">50</field>
        <field name="label">Confidence score</field>
        <field name="body">The share of rows read without ambiguity. It is not a grade and there is no pass mark — the number that matters is the count of rows underneath it, because 98% of fifty rows still leaves one person unaccounted for.</field>
    </record>

    <record id="col_fullfinal_leavers_this_period" model="learn.column">
        <field name="screen">fullfinal</field>
        <field name="key">leavers_this_period</field>
        <field name="sequence">10</field>
        <field name="label">Leavers this period</field>
        <field name="body">Employees whose contract ends inside this period. Each one needs settling here AND excluding from the normal monthly run — a leaver in both is paid twice, and recovering that is a legal conversation rather than a payroll one.</field>
    </record>

    <record id="col_fullfinal_pending_settlement" model="learn.column">
        <field name="screen">fullfinal</field>
        <field name="key">pending_settlement</field>
        <field name="sequence">20</field>
        <field name="label">Pending settlement</field>
        <field name="body">Leavers whose final amount has been worked out but not yet paid. Settlements have legal deadlines, so this count is a clock rather than a queue.</field>
    </record>

    <record id="col_fullfinal_settled" model="learn.column">
        <field name="screen">fullfinal</field>
        <field name="key">settled</field>
        <field name="sequence">30</field>
        <field name="label">Settled</field>
        <field name="body">The total already paid out as final settlements in this period. It sits outside the monthly run's net total, which is why the two numbers never add up to what left the bank.</field>
    </record>

    <record id="col_proration_prorated_payslips" model="learn.column">
        <field name="screen">proration</field>
        <field name="key">prorated_payslips</field>
        <field name="sequence">10</field>
        <field name="label">Prorated payslips</field>
        <field name="body">Payslips in this run paid for part of a month rather than all of it — joiners, leavers and unpaid leave. Every one of them is a likely question from the employee, and this screen is the answer written down before it is asked.</field>
    </record>

    <record id="col_proration_standard_working_days" model="learn.column">
        <field name="screen">proration</field>
        <field name="key">standard_working_days</field>
        <field name="sequence">20</field>
        <field name="label">Standard working days</field>
        <field name="body">The divisor every proration factor is built from, taken from the division's configuration. It is working days and not calendar days, and reading it as calendar days is the mistake that turns an explanation into an argument.</field>
    </record>

    <record id="col_retro_retro_lines" model="learn.column">
        <field name="screen">retro</field>
        <field name="key">retro_lines</field>
        <field name="sequence">10</field>
        <field name="label">Retro lines</field>
        <field name="body">Corrections belonging to a period that is already closed, paid in the current run. Each is a deliberate decision not to reopen an approved month, which is what keeps everything already reported from that month true.</field>
    </record>

    <record id="col_retro_total_adjustment" model="learn.column">
        <field name="screen">retro</field>
        <field name="key">total_adjustment</field>
        <field name="sequence">20</field>
        <field name="label">Total adjustment</field>
        <field name="body">The sum of the retro lines in this run. It is added on top of the month's own earnings, so the run's net total will be higher than the same headcount would normally produce — worth saying out loud before somebody asks about the variance.</field>
    </record>

    <record id="col_retro_oldest_source_period" model="learn.column">
        <field name="screen">retro</field>
        <field name="key">oldest_source_period</field>
        <field name="sequence">30</field>
        <field name="label">Oldest source period</field>
        <field name="body">The furthest-back month any line here is correcting. The older it is, the more reporting has already been done on the original figure — which is exactly why the correction is a retro line and never an edit to that month.</field>
    </record>

    <record id="col_formula_components" model="learn.column">
        <field name="screen">formula</field>
        <field name="key">components</field>
        <field name="sequence">10</field>
        <field name="label">Components</field>
        <field name="body">Every named piece of a division's rulebook: the inputs, the earnings, the deductions and the totals. A payslip line exists because a component here produced it, so a missing line is a missing component rather than a bug in the compute.</field>
    </record>

    <record id="col_formula_depends_on" model="learn.column">
        <field name="screen">formula</field>
        <field name="key">depends_on</field>
        <field name="sequence">20</field>
        <field name="label">Depends on</field>
        <field name="body">The components this one reads to produce its own value. Change any of them and this line moves — which makes this the list to check before you edit an input rather than after somebody notices the output.</field>
    </record>

    <record id="col_formula_used_by" model="learn.column">
        <field name="screen">formula</field>
        <field name="key">used_by</field>
        <field name="sequence">30</field>
        <field name="label">Used by</field>
        <field name="body">The components that read THIS one. It is the blast radius of a rename or a deletion, written down: everything listed here would lose an input, and a formula that lost an input does not always fail loudly.</field>
    </record>

    <record id="col_formula_live_preview" model="learn.column">
        <field name="screen">formula</field>
        <field name="key">live_preview</field>
        <field name="sequence">40</field>
        <field name="label">Live preview</field>
        <field name="body">The whole configuration evaluated against one sample employee, line by line. It is the cheapest possible check on an edit — and it is one person, which is why it is a first look rather than the evidence.</field>
    </record>

    <record id="col_formula_simulate" model="learn.column">
        <field name="screen">formula</field>
        <field name="key">simulate</field>
        <field name="sequence">50</field>
        <field name="label">Simulate</field>
        <field name="body">The edited configuration run against a period that has already been paid, so you can compare what it would have produced with what was actually paid. This is the evidence the preview is not: everybody, not one person.</field>
    </record>

    <record id="col_statutory_contributions" model="learn.column">
        <field name="screen">statutory</field>
        <field name="key">contributions</field>
        <field name="sequence">10</field>
        <field name="label">Contributions</field>
        <field name="body">The total of both legs — what employees have deducted plus what the company pays on top — for the period on display. It is a cost figure, not a deduction figure, so it is roughly three times what appears on the payslips.</field>
    </record>

    <record id="col_statutory_employee_leg" model="learn.column">
        <field name="screen">statutory</field>
        <field name="key">employee_leg</field>
        <field name="sequence">20</field>
        <field name="label">Employee leg</field>
        <field name="body">The part deducted from payslips — 10.5% of the registered insurance base under the current policy. This is the only half an employee ever sees, and it is the half they ask about.</field>
    </record>

    <record id="col_statutory_employer_leg" model="learn.column">
        <field name="screen">statutory</field>
        <field name="key">employer_leg</field>
        <field name="sequence">30</field>
        <field name="label">Employer leg</field>
        <field name="body">The part the company pays on top of salary — 21.5% of the same base, twice what the employee pays. It never appears in anybody's net, which is why it is invisible in every conversation about pay unless somebody puts it on the table.</field>
    </record>

    <record id="col_statutory_policies" model="learn.column">
        <field name="screen">statutory</field>
        <field name="key">policies</field>
        <field name="sequence">40</field>
        <field name="label">Policies</field>
        <field name="body">How many insurance policy records exist, active and archived together. More than one is normal and healthy: a rate change is a new record, so the count grows every time the law does.</field>
    </record>

    <record id="col_statutory_tax_tables" model="learn.column">
        <field name="screen">statutory</field>
        <field name="key">tax_tables</field>
        <field name="sequence">50</field>
        <field name="label">Tax tables</field>
        <field name="body">One per tax year, holding the bands and the two relief figures. Last year's stays because last year's payslips were computed by it and still have to be explainable.</field>
    </record>

    <record id="col_statutory_dependents" model="learn.column">
        <field name="screen">statutory</field>
        <field name="key">dependents</field>
        <field name="sequence">60</field>
        <field name="label">Dependents</field>
        <field name="body">Registered dependants across the company, each worth 4,400,000 ₫ a month off taxable income. Registration is the point: an unregistered dependant is relief the employee is entitled to and is not getting.</field>
    </record>

    <record id="col_structures_structures" model="learn.column">
        <field name="screen">structures</field>
        <field name="key">structures</field>
        <field name="sequence">10</field>
        <field name="label">Structures</field>
        <field name="body">Legacy Odoo salary structures still held by this company. They are history rather than configuration: new pay logic belongs in a formula configuration, and these exist so old payslips can still be explained.</field>
    </record>

    <record id="col_structures_salary_rules" model="learn.column">
        <field name="screen">structures</field>
        <field name="key">salary_rules</field>
        <field name="sequence">20</field>
        <field name="label">Salary rules</field>
        <field name="body">The individual rules inside those structures — the older equivalent of a component. Reading one tells you how a payslip from before the migration got its number.</field>
    </record>

    <record id="col_structures_categories" model="learn.column">
        <field name="screen">structures</field>
        <field name="key">categories</field>
        <field name="sequence">30</field>
        <field name="label">Categories</field>
        <field name="body">How the rules are grouped for reporting — basic, allowance, deduction, net. The grouping is what a payslip's subtotals are built from, which is why two structures with the same rules can still print differently.</field>
    </record>

    <record id="col_structures_employees_covered" model="learn.column">
        <field name="screen">structures</field>
        <field name="key">employees_covered</field>
        <field name="sequence">40</field>
        <field name="label">Employees covered</field>
        <field name="body">How many people are still paid through a structure rather than a formula configuration. On a migrated company this trends to zero, and zero is the signal that a division has finished moving — not that the structure can be deleted.</field>
    </record>

    <record id="col_structures_countries" model="learn.column">
        <field name="screen">structures</field>
        <field name="key">countries</field>
        <field name="sequence">50</field>
        <field name="label">Countries</field>
        <field name="body">How many country rule sets are represented here. A structure carries its country's statutory assumptions, so a structure from the wrong country is wrong in ways that do not look wrong.</field>
    </record>

    <record id="col_integrations_connectors" model="learn.column">
        <field name="screen">integrations</field>
        <field name="key">connectors</field>
        <field name="sequence">10</field>
        <field name="label">Connectors</field>
        <field name="body">Configured links to a source system that can pull data in without anybody retyping it — an HR system, a time clock, the bank. Each one is a place a wrong row can enter payroll without passing a human.</field>
    </record>

    <record id="col_integrations_connected" model="learn.column">
        <field name="screen">integrations</field>
        <field name="key">connected</field>
        <field name="sequence">20</field>
        <field name="label">Connected</field>
        <field name="body">How many connectors currently hold working credentials. It says nothing about whether data is arriving: connected describes the login, and the last-sync time describes the data.</field>
    </record>

    <record id="col_integrations_errors" model="learn.column">
        <field name="screen">integrations</field>
        <field name="key">errors</field>
        <field name="sequence">30</field>
        <field name="label">Errors</field>
        <field name="body">Connectors whose last attempt failed. One here in the week before payroll is a month of inputs that will not arrive — and the payslips computed without them will look perfectly normal.</field>
    </record>

    <record id="col_integrations_synced_records" model="learn.column">
        <field name="screen">integrations</field>
        <field name="key">synced_records</field>
        <field name="sequence">40</field>
        <field name="label">Synced records</field>
        <field name="body">How many records have come through successfully. Compare it with last month rather than reading it alone: a number that stopped growing is a connector that stopped working.</field>
    </record>

    <record id="col_integrations_field_mappings" model="learn.column">
        <field name="screen">integrations</field>
        <field name="key">field_mappings</field>
        <field name="sequence">50</field>
        <field name="label">Field mappings</field>
        <field name="body">How many fields in the source system are wired to a field here. A mapping that was never made is a column that silently arrives empty, and the payslip line it feeds simply computes on zero.</field>
    </record>

    <record id="col_integrations_staged_records" model="learn.column">
        <field name="screen">integrations</field>
        <field name="key">staged_records</field>
        <field name="sequence">60</field>
        <field name="label">Staged records</field>
        <field name="body">Rows that arrived but have not been taken into payroll yet. A count that is climbing is the clearest single sign of a broken sync — those rows are somebody's attendance, waiting.</field>
    </record>

    <record id="col_dashboard_headcount" model="learn.column">
        <field name="screen">dashboard</field>
        <field name="key">headcount</field>
        <field name="sequence">10</field>
        <field name="label">Headcount</field>
        <field name="body">Everybody the company currently employs, whether or not they were in the last run. It describes the company rather than the month, so it will read almost the same tomorrow — a figure to notice when it MOVES, not one to act on today.</field>
    </record>

    <record id="col_dashboard_monthly_payroll" model="learn.column">
        <field name="screen">dashboard</field>
        <field name="key">monthly_payroll</field>
        <field name="sequence">20</field>
        <field name="label">Monthly payroll</field>
        <field name="body">The personnel cost of the current month, as computed so far. It is a cost figure rather than a payment figure: money still sitting in an unapproved run is counted here and has not left the company.</field>
    </record>

    <record id="col_dashboard_pending_approval" model="learn.column">
        <field name="screen">dashboard</field>
        <field name="key">pending_approval</field>
        <field name="sequence">30</field>
        <field name="label">Pending approval</field>
        <field name="body">How many runs are waiting on a signature, at any gate, across the whole company. It is not your personal queue — how many are YOURS is a question the Approvals screen answers, and confusing the two means chasing work that was never yours.</field>
    </record>

    <record id="col_dashboard_active_configurations" model="learn.column">
        <field name="screen">dashboard</field>
        <field name="key">active_configurations</field>
        <field name="sequence">40</field>
        <field name="label">Active configurations</field>
        <field name="body">How many formula configurations are switched on — normally one per division and cycle. A number that has grown without anybody adding a division is worth opening: an old configuration left active can still be selected by a run.</field>
    </record>

    <record id="col_approvals_at_officer_review" model="learn.column">
        <field name="screen">approvals</field>
        <field name="key">at_officer_review</field>
        <field name="sequence">10</field>
        <field name="label">At Officer review</field>
        <field name="body">Runs sitting at the first gate, waiting for the Payroll Officer to sign. A run only reaches this lane by being submitted, so anything still in draft is not counted here and is not waiting for anybody.</field>
    </record>

    <record id="col_approvals_at_hr_review" model="learn.column">
        <field name="screen">approvals</field>
        <field name="key">at_hr_review</field>
        <field name="sequence">20</field>
        <field name="label">At HR review</field>
        <field name="body">Runs the Officer has already passed, waiting at {{hrTierName}}. The lane names the tier that has to act next, which makes it the answer to "who do I chase" rather than a status word.</field>
    </record>

    <record id="col_approvals_at_finance_approval" model="learn.column">
        <field name="screen">approvals</field>
        <field name="key">at_finance_approval</field>
        <field name="sequence">30</field>
        <field name="label">At Finance approval</field>
        <field name="body">The last gate before done. A run here has been read twice already, which is exactly why the reading at this tier is about totals rather than lines — and why a wrong line has to have been caught earlier.</field>
    </record>

    <record id="col_approvals_net_at_stake" model="learn.column">
        <field name="screen">approvals</field>
        <field name="key">net_at_stake</field>
        <field name="sequence">40</field>
        <field name="label">Net at stake</field>
        <field name="body">The net of every run in the pipeline added together — money that is one or more signatures away from leaving the company's account and can still be stopped by anybody who finds a reason. After the last gate that stops being true.</field>
    </record>

    <record id="col_employees_headcount" model="learn.column">
        <field name="screen">employees</field>
        <field name="key">headcount</field>
        <field name="sequence">10</field>
        <field name="label">Headcount</field>
        <field name="body">Everybody on the roster within the filters currently active. It counts people the company employs, which is not the same as people the last run paid — that count lives on Workforce Analytics, and the gap between them is where a missing payslip hides.</field>
    </record>

    <record id="col_employees_running_contracts" model="learn.column">
        <field name="screen">employees</field>
        <field name="key">running_contracts</field>
        <field name="sequence">20</field>
        <field name="label">Running contracts</field>
        <field name="body">Contracts in force today. Payroll computes from the contract, so somebody on the roster whose contract is still in draft is on this screen and will not be in the run — which is why this number is worth comparing with headcount.</field>
    </record>

    <record id="col_employees_expiring_30" model="learn.column">
        <field name="screen">employees</field>
        <field name="key">expiring_30</field>
        <field name="sequence">30</field>
        <field name="label">Expiring in 30 days</field>
        <field name="body">Contracts that end within the next month. Each one is either a renewal somebody has to sign or a leaver somebody has to settle, and both are cheaper to handle now than as a surprise proration on a payslip.</field>
    </record>

    <record id="col_employees_new_this_month" model="learn.column">
        <field name="screen">employees</field>
        <field name="key">new_this_month</field>
        <field name="sequence">40</field>
        <field name="label">New this month</field>
        <field name="body">People who joined inside the current period. They are the most likely rows to be prorated and the most likely to have bank details missing, so they are worth reading one by one rather than as a count.</field>
    </record>

    <record id="col_employees_monthly_wage_bill" model="learn.column">
        <field name="screen">employees</field>
        <field name="key">monthly_wage_bill</field>
        <field name="sequence">50</field>
        <field name="label">Monthly wage bill</field>
        <field name="body">The registered contract bases added together — what the company has agreed to pay, before overtime, allowances or deductions. It is also the base insurance is charged on, which is why it moves when a contract is renewed rather than when a month is busy.</field>
    </record>

    <record id="col_employees_payroll_ready" model="learn.column">
        <field name="screen">employees</field>
        <field name="key">payroll_ready</field>
        <field name="sequence">60</field>
        <field name="label">Payroll-ready</field>
        <field name="body">The share of people who have everything needed to actually be paid: a running contract, a wage on it and usable bank details. Anybody outside this share computes perfectly and does not receive the money, which is the failure with the longest gap between cause and symptom.</field>
    </record>

    <record id="col_contracts_running" model="learn.column">
        <field name="screen">contracts</field>
        <field name="key">running</field>
        <field name="sequence">10</field>
        <field name="label">Running</field>
        <field name="body">Contracts in force today, which is the set payroll will compute from. A contract that starts next week is not here yet and a person on it is not in this month's run either.</field>
    </record>

    <record id="col_contracts_draft" model="learn.column">
        <field name="screen">contracts</field>
        <field name="key">draft</field>
        <field name="sequence">20</field>
        <field name="label">Draft</field>
        <field name="body">Contracts that have been prepared and not put in force. A draft pays nothing at all — it is the state to hunt for in the week before a run, because the employee is on every other screen and simply absent from the payslips.</field>
    </record>

    <record id="col_contracts_expired" model="learn.column">
        <field name="screen">contracts</field>
        <field name="key">expired</field>
        <field name="sequence">30</field>
        <field name="label">Expired</field>
        <field name="body">Contracts whose end date has passed. Each is either a leaver who still needs settling in Full &amp; Final or a renewal that was never signed, and the second one is paid up to the end date and then stops.</field>
    </record>

    <record id="col_contracts_average_wage" model="learn.column">
        <field name="screen">contracts</field>
        <field name="key">average_wage</field>
        <field name="sequence">40</field>
        <field name="label">Average wage</field>
        <field name="body">The wage bill divided by the number of contracts in the current filters. It is a scoped average, so it moves when you change a chip without anything having changed in the database — useful for comparing divisions, misleading as a headline.</field>
    </record>

    <record id="col_insights_net_payroll" model="learn.column">
        <field name="screen">insights</field>
        <field name="key">net_payroll</field>
        <field name="sequence">10</field>
        <field name="label">Net payroll</field>
        <field name="body">The headline: what actually left, or will leave, the bank account for the run this board is reading. It is read off the payslips themselves rather than a reporting copy, so it cannot disagree with the payslips underneath it.</field>
    </record>

    <record id="col_insights_cost_story" model="learn.column">
        <field name="screen">insights</field>
        <field name="key">cost_story</field>
        <field name="sequence">20</field>
        <field name="label">Cost story</field>
        <field name="body">The same measure over three, six or twelve months. The window chip decides which story it tells, and a figure quoted without saying which window it came from is a figure somebody will contradict with the same screen.</field>
    </record>

    <record id="col_insights_statutory_split" model="learn.column">
        <field name="screen">insights</field>
        <field name="key">statutory_split</field>
        <field name="sequence">30</field>
        <field name="label">Statutory split</field>
        <field name="body">How much of the company's payroll cost is the law rather than salary: the employee leg deducted from payslips beside the employer leg paid on top. The second one never appears in anybody's net, so it is invisible in every conversation about pay unless somebody puts it on the table.</field>
    </record>

    <record id="col_explorer_measure" model="learn.column">
        <field name="screen">explorer</field>
        <field name="key">measure</field>
        <field name="sequence">10</field>
        <field name="label">Measure</field>
        <field name="body">What is being counted — net pay, gross, a contribution, a headcount. It is the first thing to choose and the most common thing to get wrong: most wrong answers here are the right filter applied to the wrong measure.</field>
    </record>

    <record id="col_explorer_break_down_by" model="learn.column">
        <field name="screen">explorer</field>
        <field name="key">break_down_by</field>
        <field name="sequence">20</field>
        <field name="label">Break down by</field>
        <field name="body">The dimension the rows are grouped into — division, department, cycle, month. Changing it does not change the total, only how the same total is divided up, which is the quickest way to check that a number is what you think it is.</field>
    </record>

    <record id="col_explorer_where" model="learn.column">
        <field name="screen">explorer</field>
        <field name="key">where</field>
        <field name="sequence">30</field>
        <field name="label">Where</field>
        <field name="body">Every filter currently applied, shown as a removable tag. The tags are part of the answer: the same measure with one tag removed is a different figure, and the two look identical once they have been pasted into an email.</field>
    </record>

    <record id="col_workforcean_employees_paid" model="learn.column">
        <field name="screen">workforcean</field>
        <field name="key">employees_paid</field>
        <field name="sequence">10</field>
        <field name="label">Employees paid</field>
        <field name="body">How many people the runs in scope actually produced a payslip for. It is not headcount: somebody employed all month whose contract sat in draft is counted on People and not here, and that difference is the point of the tile.</field>
    </record>

    <record id="col_workforcean_joiners" model="learn.column">
        <field name="screen">workforcean</field>
        <field name="key">joiners</field>
        <field name="sequence">20</field>
        <field name="label">Joiners</field>
        <field name="body">People who first appear in a run inside this period. Almost all of them are prorated, which means almost all of them are a question somebody will ask about their first payslip.</field>
    </record>

    <record id="col_workforcean_leavers" model="learn.column">
        <field name="screen">workforcean</field>
        <field name="key">leavers</field>
        <field name="sequence">30</field>
        <field name="label">Leavers</field>
        <field name="body">People whose last run falls in this period. Each one needs settling in Full &amp; Final AND excluding from the ordinary monthly run — a leaver in both is paid twice, and recovering that is a legal conversation rather than a payroll one.</field>
    </record>

    <record id="col_workforcean_cost_per_head" model="learn.column">
        <field name="screen">workforcean</field>
        <field name="key">cost_per_head</field>
        <field name="sequence">40</field>
        <field name="label">Cost per head</field>
        <field name="body">The run's cost divided by the people it paid. It is the only figure on this screen that survives a headcount change, which makes it the honest way to compare two months — a total against a total mostly measures how many people were in each.</field>
    </record>

    <record id="col_govreports_period" model="learn.column">
        <field name="screen">govreports</field>
        <field name="key">period</field>
        <field name="sequence">10</field>
        <field name="label">Period</field>
        <field name="body">The month being filed for. It is a month rather than a date range because the authorities ask for months, and a filing generated before every run in that month is done is short by however many payslips are still in the pipeline.</field>
    </record>

    <record id="col_govreports_country" model="learn.column">
        <field name="screen">govreports</field>
        <field name="key">country</field>
        <field name="sequence">20</field>
        <field name="label">Country</field>
        <field name="body">Which country's filing set is on display. The tiles are decided by the active company's country rather than by what Payobook can produce, and the chips only appear when a company runs payroll in more than one.</field>
    </record>

</odoo>
