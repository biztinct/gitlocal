<odoo>
    <template id="portal_my_home_menu_payslip" name="Portal layout : payslip menu entries" inherit_id="portal.portal_breadcrumbs" priority="30">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'payslip'" t-attf-class="breadcrumb-item #{'active ' if not payslip else ''}">
                <a t-if="payslip" t-attf-href="/my/payslips?{{ keep_query() }}">Payslips</a>
                <t t-else="">Payslips</t>
            </li>
            <li t-if="payslip" class="breadcrumb-item active">
                <t t-esc="payslip.name"/> 
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_payslip" name="Show Payslips" inherit_id="portal.portal_my_home" customize_show="True" priority="30">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Payslips</t>
                <t t-set="url" t-value="'/my/payslips'"/>
                <t t-set="placeholder_count" t-value="'payslip_count'"/> 
            </t>
        </xpath>
    </template>

    <template id="portal_my_payslips" name="My Payslips">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Payslips</t> 
            </t>

            <t t-if="not payslips">
                <p>There are currently no payslips for your account.</p>
            </t>

            <t t-if="payslips" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Payslip #</th>
                        <th>Period</th> 
                        <th class="text-center">Status</th>
                        <th class="text-end">Net Pay</th> 
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="payslips" t-as="payslip">
                        <tr>
                            <td>
                                <a t-att-href="payslip.get_portal_url()" t-att-title="payslip.name">
                                    <t t-esc="payslip.name"/> 
                                </a>
                            </td>
                            <td>
                                <span t-field="payslip.date_from"/> - <span t-field="payslip.date_to"/>
                            </td>
                            <td class="tx_status text-center"> 
                                <span t-attf-class="badge rounded-pill text-bg-#{payslip.state == 'done' and 'success' or payslip.state == 'cancel' and 'warning' or 'info'}">
                                    <t t-esc="payslip.state.capitalize()"/> 
                                </span>
                            </td>
                            <td class="text-end">
                                <span t-field="payslip.line_ids.filtered(lambda line: line.code == 'NETPAY')[0].amount" t-options="{&quot;widget&quot;: &quot;monetary&quot;, &quot;display_currency&quot;: payslip.company_id.currency_id}"/> 
                            </td>
                        </tr>
                    </t>
                </tbody>
            </t>
        </t>
    </template>

<template id="portal_payslip_page" name="Payslip Portal Template" inherit_id="portal.portal_sidebar" primary="True">
    <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
 <!--       <t t-set="o_portal_fullwidth_alert"> 
            <t t-call="portal.portal_back_in_edit_mode">
                <t t-set="backend_url" t-value="'/web#model=hr.payslip&amp;id=%s&amp;action=%s&amp;view_type=form' % (payslip.id, payslip.env.ref('om_hr_payroll.action_view_hr_payslip').id)"/> 
            </t>
        </t>
-->
        <div class="row mt16 o_portal_payslip_sidebar"> 
            <t t-call="portal.portal_record_sidebar">
                <t t-set="classes" t-value="'col-lg-auto d-print-none'"/>
                <t t-set="title">
                    <h2 class="mb-0">
                        <span t-field="payslip.line_ids.filtered(lambda line: line.code == 'NETPAY')[0].amount" t-options="{&quot;widget&quot;: &quot;monetary&quot;, &quot;display_currency&quot;: payslip.company_id.currency_id}"/>
                    </h2>
                    <span t-field="payslip.name"/> 
                    <div class="small"><i class="fa fa-calendar-o"/> <span t-field="payslip.date_to"/></div> 
                </t>

                <t t-set="entries">
                    <ul class="list-group list-group-flush flex-wrap flex-row flex-lg-column">
                        <li class="list-group-item flex-grow-1">
                            <div class="o_download_pdf btn-toolbar flex-sm-nowrap">
                                <div class="btn-group  flex-grow-1 me-1 mb-1">
                                    <a class="btn btn-secondary btn-block o_download_btn" t-att-href="payslip.get_portal_url(report_type='pdf', download=True)" title="Download"><i class="fa fa-download"/> Download</a>
                                </div>
                                <div class="btn-group flex-grow-1 mb-1">
                                    <a class="btn btn-secondary btn-block o_print_btn o_portal_invoice_print" t-att-href="payslip.get_portal_url(report_type='pdf')" title="Print" target="_blank"><i class="fa fa-print"/> Print</a>
                                </div>
                            </div>
                        </li>
                    </ul>
                </t>
            </t>

            <div id="payslip_content" class="col-12 col-lg"> 
                <div class="o_portal_html_view shadow p-3">
                    <div class="o_portal_html_loader text-center">
                        <i class="fa fa-circle-o-notch fa-spin fa-2x fa-fw text-black-50"></i>
                    </div>
                    <iframe id="payslip_html" class="mt8 mb8" width="100%" height="100%" frameborder="0" scrolling="no" t-att-src="payslip.get_portal_url(report_type='html')"/> 
                </div>
                                <!-- chatter -->
                <div id="payslip_communication" class="mt-4">
                    <h2>History</h2>
                    <t t-call="portal.message_thread"/>
                </div>
            </div>
        </div>
    </xpath>
</template>



</odoo>