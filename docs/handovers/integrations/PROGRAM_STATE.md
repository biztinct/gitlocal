# Integrations Program — resume state

> **PROGRAM COMPLETE 2026-08-20 — all four cycles shipped, deployed, live-validated. Ledger closed at W145.** Reports: `CYCLE2_REPORT.md`, `CYCLE3_REPORT.md`, `CYCLE4_REPORT.md` (C1's report was lost to a session pause; its substance is in ledger W127–W130 + commit messages). Open owner decisions are listed at the bottom of this file.

_Last updated: 2026-08-20 (ACTIVE — owner resumed). C2 closeout agent running (stalled twice; resumed with stall-proof-deploy + incremental-report constraints). Fable pre-ran Cycle 4's read-only abm inspection: config id 7 "AB Mauri Payroll Vietnam" (draft, 40 inputs — codes match legacy inventory 1:1, see CYCLE4 §Pre-verified facts); **secrets comparison CLOSED: legacy-production params shown by owner are DIFFERENT from the hardcoded pair (old separate OAuth client); owner ruling = leave the code, BACKLOG the scrub until the new Integration credential screens are owner-tested (then also revoke the old client in Zoho console)**; abm modules installed at pre-program versions (C4's -u list confirmed); pb_demo correctly absent on abm. Next: C2 report → finalize C3 ⟲C2 → launch C3 → finalize C4 ⟲C3 → launch C4._

**Cycle 2 incident + recovery**: the C2 agent stalled MID-DEPLOY (after its 4 feature commits 741f345c/c8f19083/e4a8421c/d0e5d1f3, after rsync, after service stop, BEFORE the `-u`) leaving payobook.com 502. Fable completed the window: verified disk=repo versions, ran detached `-u` unit (i2fix, EXIT=0), restarted service — public 200, acme 200, all six modules at repo versions on payobook. The agent was then resumed from transcript to finish tests/Chrome validation/ledger/`CYCLE2_REPORT.md`._

**To resume**: check Cycle 2's outcome — read `CYCLE2_REPORT.md` if present, else reconstruct from git log (feat commits on pb_formula_studio/pb_settings/pb_import_advanced/pb_hub/pb_integrations after 5e9bbb5a) + ledger W131+ + live check (Mapping Studio action tag `pb_mapping_studio` reachable, module versions repo↔live). If clean → finalize `CYCLE3_ZOHO_CATALOG.md`'s ⟲C2 markers → launch Opus on it → then write + run Cycle 4 (ABM). Owner follow-ups pending: current admin credential (W129 — ash/admin1234 stale); the ~120+38 unpushed commits decision (pre-existing, not this program's).

**Approved plan**: `/Users/adity/.claude/plans/integrations-functionality-can-be-generic-truffle.md` (context, exploration findings, 4-cycle design). Owner decisions locked: phased Fable→Opus; one connector = many endpoints; ABM = catalog+mappings only (no live creds); abm target config = **"AB Mauri Payroll Vietnam"**; secrets = compare hardcoded `hr_zoho_staging.py:208-210` vs abm's `zoho.*` ir.config_parameters and REPORT before any action.

| Cycle | Handover | Status |
|---|---|---|
| 1 — endpoint model + navigation | `CYCLE1_ENDPOINTS_AND_NAV.md` | **COMPLETE & DEPLOYED** — commits 745d736d..5e9bbb5a (5 features + 2 fixes + ledger W127-W130); live-verified: payobook `pb_hr_payroll_formula` 19.0.1.49.0, 53 endpoint rows, login 200. Its report text was lost to the pause; ledger + commit messages carry the substance. NOTE W129: ash/admin1234 is stale — validation uses a temp shell-created single-company user. Degrade rail cf2d197a: tenant DBs without the feeds table must keep working. |
| 2 — Mapping Studio | `CYCLE2_MAPPING_STUDIO.md` | **COMPLETE & DEPLOYED** — 10 commits (741f345c..a06ae36c), 85 tests 0-failed, full live validation (report: `CYCLE2_REPORT.md`), ledger →W136, test-10 click count = 6. Two defects found+fixed live. Handed to C3: pb_hub palette gating bug, board-vs-studio count honesty line, preview_transform exception hygiene. Agent stalled 3× + died 1× (network); recoveries documented in the report's deploy table. |
| 3 — Zoho catalog + prepopulation | `CYCLE3_ZOHO_CATALOG.md` (FINAL) | **COMPLETE & DEPLOYED** — 9 commits (5479e66c..d0027c16), 109 tests 0-failed, live-validated (Zoho connector: 10 feeds, 3 ABM badges; report: `CYCLE3_REPORT.md`), ledger →W140. FOUND+FIXED an engine bug: safe_eval nocopy removal meant every python transformation rule returned default since the Odoo 19 port (efbb64b5). Filter namespace is `rec` not `record`. Palette "bug" dissolved (MAX_ROWS=12 + IA seq 2000+) — owner decision pending on ⌘K fold ordering. |
| 4 — ABM tenant seeding + secrets report | `CYCLE4_ABM_SEEDING.md` (FINAL) | **COMPLETE** — commits e64b314f/8c8ba59c/cb46f1fe/66747680/b7ede970 (report: `CYCLE4_REPORT.md`), ledger closed →W145. abm upgraded (EXIT 0/0, 99 data rows byte-identical to payobook), seeded connector "Zoho People (ABM)" disconnected/no-creds: 15 evidence-cited wires + honest unmatched tables (25 inputs / 26 fields, 4 deliberate), 8 rules, 7 feeds w/ 3 ABM badges; idempotency proven (run 2 = 0 creates); Chrome-validated on abm.payobook.com (0 console errors); temp user removed. SURPRISE: owner had hand-made connector id 1 on abm (2026-08-19) holding the REAL legacy credentials — left untouched; secrets verdict closed by md5: DIFFERENT. |

**To resume**: read this file, the plan file, then Cycle 1's outcome (report/commits/live state). If C1 is clean → finalize CYCLE2 draft against reality → launch Opus on it → continue the chain (C2 → C3 → C4), updating this file each cycle.

## Open owner decisions (post-program)
1. **Two Zoho connectors on abm**: the owner's hand-made one (id 1, holds real credentials) and the program-seeded one (41 mappings, 7 feeds, 8 rules, disconnected). Either paste credentials into the seeded one and archive id 1, or move the seeded config onto id 1.
2. **Backlog**: scrub hardcoded Zoho creds + UserError echo (om_hr_payroll/models/hr_zoho_staging.py:208-220) after the new credential screens are owner-tested; revoke the OLD client in Zoho console (separate client — revoking cannot break the new config).
3. **BASESALARY / Other_Allowance**: wire from P_Salary's `Salary`? (Legacy never did — they came from the spreadsheet.)
4. **EMPLOYEENAME**: reproduce the legacy `full_name_vn or first_name` fallback (needs a backend-seeded python transform)?
5. **No_of_Dependents flat field vs the DEPCOUNT computed rule** — computed rule is currently wired.
6. **⌘K fold**: re-sequence the 5 Setup deep-links above the palette's 12-row fold?
7. **~75 commits unpushed on 19.1** (predates + includes this program) — push decision.
