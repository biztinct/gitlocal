# Approval Matrix — conventions & gotcha ledger (AM-ledger)

Every Approval Matrix phase handover references this file. Implementers **append** a numbered `AM<n>` entry whenever they hit a new gotcha or make a convention decision; never rewrite earlier entries.

## Standing rules (verbatim from the owner, binding on every phase)

- **Design bar:** "extreme WOW, intuitive, out-of-this-world, best in class" — hero moment, zero dead-ends, plain language, purposeful motion, bulk ergonomics; Lucide icons, never emoji; Chrome-MCP validation of every user-facing change.
- **White-label:** the word "Odoo" must never appear in any user-visible string (labels, help, errors, toasts, emails, `.po` msgstr). Technical identifiers (`from odoo import`, xml ids, module names, log lines, comments) stay as they are.
- **Flexibility ruling (12 Sep 2026):** the business decides. Any process may be set to "No approval needed". There are **no hard minimums** (not two people for money out, not an independence rule, not a coverage pass). The system **warns, offers options and records the publisher's confirmation** of each warning. Structural errors only (bad schema, unknown role, two bindings that tie at the same rank) block a publish.
- **Clean replacement:** no payroll is live. Old engines (pay run `PB_TIER`, `officer_review` setting, `wfp.approval.step`, the two extra inboxes) are removed, not kept beside the new one. Every process ships a default workflow equal to today's route, so day one behaves the same.
- **Nothing approves on its own:** escalation reminds, escalates, optionally reassigns to a configured backup. Never auto-approves. A fast lane is a *published choice*, not an escalation outcome.
- **Fail closed at runtime:** no binding, a tie, a paused binding, a missing person, a revoked grant → submission/decision is refused with an actionable message. Never fall back to "all HR users", never pick the newest id.
- **Configuring a person never grants access.** Eligibility = resolved seat AND active user AND the object's own access. The picker says "Needs permission" and links to Access.
- **Server is the authority.** Every RPC re-checks access and scope; UI booleans are cosmetic. No `sudo()` to make a path work; `sudo()` only for reads the gate already allowed (audit console pattern).
- **Commit per feature**, explicit staging, reviewer-focused message, end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; never push.
- **Report in plain technical prose**; owner-facing wording is Fable's job.

## Module and dependency rules

- `biz_approval_workflow` (engine) depends only on `biz_approval_chain, hr, mail`. It must NOT depend on `biz_access`, `pb_settings`, `pb_hub`, `pb_group`, any `pb_*`. Reason: `biz_access → pb_hub/pb_settings → om_hr_payroll/pb_hr_payroll_base` would cycle through `pb_hr_payroll_formula` once the scheme adapter exists.
- `pb_approval_config` (UI) depends on `biz_approval_workflow, pb_hub, pb_settings, pb_sidebar, pb_import_kit, pb_group`. It must NOT depend on `pb_payruns`, `pb_home_hub`, `pb_mission`, `pb_approval`, `pb_team` (cycle via `pb_home_hub → pb_approval → pb_payruns`). Hubs depend on it, never the reverse.
- Adapters live in the business module (`pb_payruns`, `pb_hr_payroll_formula`, `pb_records`, `pb_pay_delivery`, `pb_hr_workforce`, `biz_access`, …) and add `biz_approval_workflow` to their `depends`.
- Division/scheme are Payobook concepts. The engine sees only opaque `scope_key` strings supplied by the adapter in precedence order (most specific first). Payobook adapters build them as `scheme:<id>|division:<id>`, `scheme:<id>`, `division:<id>`, `` (company).

## Odoo 19 gotchas that apply here (verified in this repo)

- `_sql_constraints` is silently ignored → use `_name_uniq = models.Constraint('unique(a,b)', 'message')` class attributes.
- `res.users.groups_id` → `group_ids`; membership checks via `user.all_group_ids` / `has_group`.
- `res.groups` has no `category_id`; define groups with `name` + `implied_ids` (+ `privilege_id` only if needed, and never `&`/`<` in a privilege name).
- `ir.cron` has no `numbercall`/`doall`; `nextcall` computed in Python, not an eval expression; `post_init_hook` runs on install only → mirror in a migration for `-u`.
- `hr.payslip.run` has NO `company_id` in this build → `getattr(run, 'company_id', False)`.
- Recordsets do not keep instance attributes; pass state through context lists.
- Private (`_`-prefixed) methods cannot be called over JSON-RPC.
- Odoo 19 removed the `<report>` shortcut and search `<group expand string>`; write explicit records.
- `safe_eval` has no `nocopy`. Never expose Python/SQL/domain evaluation to configuration; conditions are typed fact/operator/value only.
- Tests: `@tagged('post_install', '-at_install')`, `TransactionCase`, users created with `group_ids: [(6,0,[...])]`, `with_context(no_reset_password=True)`.

## Deploy contract (from CLAUDE.md, summarised)

1. One addons dir on the live box: `/odoo/odoo-server/addons` (ssh alias `Payobook19v2`). `/odoo/custom/addons` is a guard FILE — never recreate.
2. `sudo rm -rf /tmp/deployX && mkdir -p /tmp/deployX`, `rsync -az --exclude=__pycache__ --exclude='*.pyc' <modules> Payobook19v2:/tmp/deployX/`, then **per module** `sudo rsync -a --delete /tmp/deployX/<m>/ /odoo/odoo-server/addons/<m>/`. NEVER `--delete` with the addons root as destination.
3. Never deploy vendored standard addons (`web`, `hr`, `hr_*`, `mail`, …).
4. Stop the service; run the `-i/-u` in a detached unit with a sentinel (`systemd-run --collect --unit=x-install /bin/bash /tmp/x_run.sh`; script does `sudo -u odoo python3 /odoo/odoo-server/odoo-bin -c /etc/odoo-server.conf -d <db> -i <new> -u <changed> --stop-after-init > /tmp/x.log 2>&1; echo EXIT=$? >> /tmp/x.log; touch /tmp/x.done`); poll the sentinel; grep `EXIT=` and `Traceback|CRITICAL|ERROR`; start the service; confirm `Registry loaded` and a page loads.
5. Upgrade **every** database: `payobook`, `payobook_template`, and every tenant DB (`psql -l`; today at least `abm`, `acme`, `rize`). Tenant rule: tenants get everything the master gets except `pb_tenants`, `pb_demo`, `pb_demo_portal`, `pb_website`. Backup before installing on a tenant (`pg_dump -Fc`), install, verify.
6. After JS/SCSS: `DELETE FROM ir_attachment WHERE url LIKE '/web/assets/%'` per DB **and** bump `web.assets.version` ir.config_parameter, then restart.
7. Verify: hash each module tree both sides (skip `__pycache__`, `*.pyc`, `.DS_Store`); compare manifest version to `ir_module_module.latest_version` per DB (normalise the `19.0.` prefix).
8. Tests on the server: scratch DB (`createdb -T payobook_template <db>` or fresh), `odoo-bin -c /etc/odoo-server.conf -d <db> -i <module> --test-enable --stop-after-init --http-port=8199 --gevent-port=8198 --log-level=test`, with the service running is fine on those ports. `--test-enable` only runs tests for modules it installs/updates.
9. Never `pkill -f odoo-bin` in an ssh one-liner (self-matches). Kill leftover masters by PID. Never `systemctl daemon-reload` on the box.
10. Remove any screenshots/PNGs you generate; keep other sessions' files.

## Ledger entries

- **AM1 (design, 12 Sep):** workflow definitions are stored as one versioned JSON document per revision (`definition` Json, `schema_version` int), validated by a server-side serializer. No child step tables for authoring; the published snapshot is the runtime truth. Rationale: the builder edits the whole route atomically, comparisons between revisions are whole-document, and the POC's route logic maps 1:1.
- **AM2 (design, 12 Sep):** scope resolution is adapter-driven `scope_keys` in precedence order + `kind_key`. Exact `kind_key` beats `any` within a rank. Two effective bindings at the same rank and kind = publish-time error, runtime fail-closed.
- **AM3 (design, 12 Sep):** the engine ships one real business object of its own, `biz.approval.generic.request` ("Other request": title, description, amount, currency, requester). It is the test fixture for every model-level acceptance case and a real feature (ask for a sign-off on anything).
- **AM4 (design, 12 Sep):** warnings vs errors. `errors[]` block publish; `warnings[]` each carry a stable `code` and are stored on the published version as `confirmations` (code, text, user, stamp). Codes so far: `fast_lane`, `single_person`, `independence_off`, `coverage_not_run`, `coverage_gap:<scope>`, `no_backup:<step>`, `notify_only_route`, `money_single_person`.
- **AM5 (P1 build, 12 Sep):** `who.mode = 'team'` carries its own bounded list — `{"mode": "team", "user_ids": [...], "label": "Vietnam payroll desk"}` — because P1 has no team object and the engine may not invent one. A pool `biz.approval.role` is the other way to express "any one of". If P2 adds a real team model, it must keep filling `user_ids` at publish time so the published snapshot stays self-contained (design §9: the snapshot is the runtime truth).
- **AM6 (P1 build, 12 Sep):** the engine's public `resolve_binding(company_id, process_key, scope_keys, kind_key, at)` returns a **dict** (`binding_id`, `version_id`, `workflow_name`, `trace`, `error`), not the handover's 3-tuple: preview and the "Why this route?" panel must be able to SHOW a broken route rather than raise on it, and a recordset is not JSON. The internal `_resolve_binding` keeps the tuple with a 4th `error` element. Error codes: `no_route`, `ambiguous_route`, `paused`, `no_version`.
- **AM7 (P1 build, 12 Sep):** every adapter ends its `scope_keys` with `''`, so a role with `fallback_to_company = False` would be unreachable. `biz.approval.responsibility.resolve` therefore **strips the trailing company rank** when the role forbids the fallback AND a narrower scope was asked for; `['']` on its own still resolves, because then the company IS the scope. Without this the "each area needs its own person" switch does nothing.
- **AM8 (P1 build, 12 Sep):** `biz.approval.request.maker_uids` stays the frozen Json list, and is mirrored on create into a technical M2M `maker_user_ids`. A Json field cannot appear in a record-rule domain, and "the people who prepared it may see it" has to be a rule, not a method.
- **AM9 (P1 build, 12 Sep):** the repeated-person rule is applied when the **seats are built at submission**, not at step activation. Principals are frozen at submission, so both moments give the same answer, and doing it at build time makes `preview` and the real request byte-identical — which is the promise "Try an example" makes.
- **AM10 (P1 build, 12 Sep):** decisions and events refuse `write`/`unlink` for everyone, at model level. Consequence: `biz.approval.decision.company_id` is a PLAIN column the engine sets, never a stored related — a stored related rewrites itself when the source changes and would hit its own immutability guard. Same reason `biz.approval.workflow.version.write` has to allow `company_id`, `process_id`, `summary` and `route_labels` through on a published row: those are the ORM keeping its own bookkeeping, not an edit.
- **AM11 (P1 build, 12 Sep):** outbox delivery is the 5-minute cron only; no `cr.postcommit` kick. A post-commit delivery needs a second cursor and would run outside a test transaction, so it cannot be tested and can double-send. Rows are still written in the same transaction as the event, so nothing is ever told about something that did not happen.
- **AM12 (P1 build, 12 Sep):** `preview` and `coverage_scan` are gated by `group_approval_config`, not left open. Both resolve responsibilities across a whole company, so an open preview would be a people-directory read for anyone.
- **AM13 (P1 build, 12 Sep) — DEPLOY BLOCKED:** the live box `Payobook19v2` (3.104.113.197) was unreachable for the whole P1 session — ssh timeout, port 22 closed, no HTTP on the apex either — and there is no local Odoo, postgres or container runtime on this machine. So §6 of the P1 handover (deploy, install on every DB, run T01–T34, Chrome check) did **not** run. What did run: the pure-Python half (`definition.py`) exercised standalone, 33 checks, 0 failures; plus an AST pass over the module confirming every `self._method`, every `env['model']`, every `create()` key and every ACL/record-rule reference resolves. **The first job of the next session is to deploy the three modules and run the suite before anything is built on top of it.**
- **AM14 (Fable, 12 Sep) — LOCAL RUNTIME + DEFERRED DEPLOY WAVE:** the live box stayed down after P1 (DNS still 3.104.113.197; ports 22 and 443 closed; the owner has been told). A local Odoo 19 runtime is being set up at `/Users/adity/odoo19` (Postgres 16 via Homebrew, Python 3.12 venv, official `odoo/odoo` 19.0 checkout FIRST in `addons_path`, then the gitlocal repo root — so the repo's vendored old `web`/`hr`/`mail` copies are shadowed exactly as on the server). `README.md` and `runtests.sh <module> [db]` live there. **Every phase from P1 onward runs its tests locally (`/Users/adity/odoo19/runtests.sh <module>`) and validates its screens with Chrome MCP against `http://localhost:8069`.** Deployment to `Payobook19v2` is deferred to a single deploy wave once the box answers; each phase report must therefore end with a "deploy checklist" (modules to install/upgrade per DB, asset ritual yes/no, migrations, seeds) so the wave can be run without re-reading the code.
- **AM15 (Fable, 12 Sep, local test run of P1):** two gotchas found the moment the suite ran. (a) **`pool` is a reserved attribute on every model class** in Odoo 19 (`model_classes.is_model_definition` asserts `cls.pool is None`), so a field named `pool` makes the whole registry fail with a bare `AssertionError` at "Loading module …". The role flag is now **`biz.approval.role.is_pool`** (definition JSON `role_info` still uses the key `'pool'`). Never name a field `pool`, `env`, `id`, `ids`, `_fields`. (b) **`assertRaises` in Odoo 19's `TransactionCase` does not accept a tuple of exception classes** (`issubclass(exception, AccessError)` → `TypeError`); assert one class — model-level immutability guards raise `UserError`, seat-eligibility guards raise `AccessError`. Result after both fixes: `biz_approval_workflow: 48 tests, 0 failed, 0 errors` on the local runtime.
- **AM16 (Fable, 12 Sep):** a fresh install of anything that pulls `pb_payruns` fails at `pb_payruns/views/hr_payslip_run_kanban.xml:7` with `Field "journal_id" does not exist in model "hr.payslip.run"` — the field comes from `om_hr_payroll_account`, which `pb_payruns/__manifest__.py:17` does not declare. The live DBs never noticed because the accounting module was already installed. Until P3 fixes it (declare the dependency, or stop listing `credit_note`/`journal_id` in the kanban), local runs must add it: `EXTRA=om_hr_payroll_account /Users/adity/odoo19/runtests.sh <module>` (`runtests.sh` now accepts `EXTRA=<mod,mod>` to co-install).
- **AM17 (Fable, 12 Sep):** `om_hr_payroll_account` cannot be installed on a fresh local DB: its `data/hr_payroll_account_data.xml:10` references `account.1_expense_salary` / `account.1_salary_payable`, ids no current chart template creates (tried `l10n_generic_coa`). It is installed on the live DBs from an older era. Consequence: `pb_audit`'s local test run (and anything else pulling `pb_payruns`) is blocked ONLY by the AM16 kanban field until P3 lands; P3 must make `pb_payruns` install cleanly without `om_hr_payroll_account` (keep the accounting hook behind `'om_hr_payroll_account' in self.env.registry._init_modules` / `hasattr` guards, as `_accountless` already does). Until then P1's T34 (audit `workflow` source) is verified only by its unit test inside `pb_audit`, which cannot run locally yet.
- **AM18 (P2 build, 12 Sep) — PRESETS ARE PYTHON, NOT DATA:** the handover asked for `data/presets.xml`. A preset is a piece of COPY (a title and a sentence that both need `_t()`) plus a definition document, and there is nothing useful to do with it in the database between the press and the draft it creates — no screen lists preset *records*, nothing points at one, and a data row would have to carry a Json definition that no view could ever edit. They live in `matrix_facade._preset_rows()` and are served by `get_matrix`. Six of them: officer/mgr_hr/joint/tiers/four/blank. **Deviation from the handover, deliberate.**
- **AM19 (P2 build, 12 Sep) — THE FILE SET IS TEN JS FILES, NOT SIXTEEN:** the handover listed `matrix_tab.js`, `step_card.js`, `example_panel.js`, `coverage.js`, `publish.js`, `request_drawer.js`, `decision_modals.js`, `ask_drawer.js` as separate modules. Shipped as: `sentence.js`, `picker_drawer.js`, `builder.js`, `people_tab.js`, `history_tab.js`, `scheme_panel.js`, `matrix_room.js`, `request_drawer.js`, `inbox.js`, `palette.js`. The builder's four wizard steps share one state object and one save path, so splitting them across five files would have meant five components passing the same eight props to each other; the example panel and the coverage panel are sub-TEMPLATES (`pb_approval_config.ExamplePanel`) called from both the People and Safeguards steps, which is what keeps them from drifting into two. The decision modals are inside `RequestDrawer` for the same reason: they close over its `decide()`. **Deviation from the handover, deliberate.**
- **AM20 (P2 build, 12 Sep) — `_area_labels` / `AREA_ICONS` LIVE ON THE PROCESS MODEL, IN THE UI MODULE:** the engine's `biz.approval.process` has an `area` selection but no idea what icon an area wears, and it must not learn — it has to stay able to run in a product that has never heard of payroll. `pb_approval_config.models.seed` therefore `_inherit`s `biz.approval.process` and adds `AREA_ICONS` (a class dict) plus `_area_labels()`. Both the Matrix and the inbox read them, so the area headers and the inbox filter chips can never disagree. The static-contract test asserts every name in `AREA_ICONS` exists in the shared `ic()` set, because an icon name the SERVER sends is exactly as invisible a typo as one in the markup.
- **AM21 (P2 build, 12 Sep) — THE HOME LENS GATE IS NOW EMPTY, ON PURPOSE:** `APPROVAL_GATE` in `pb_home_hub/static/src/js/home_hub.js` was `pb.approval._APPROVAL_GROUPS` verbatim, because the lens was the pay-run board and only a payroll officer could read one. The lens is now `PbInbox` — every process, and every person ever asked to decide anything. A payroll gate would hide it from most of the people it is for. Nothing is given away: `biz.approval.request`'s record rules narrow every read to the requests a person sent in, prepared or holds a seat on. The export is KEPT (empty) because `home_hub_palette.js` imports it and must follow the lens exactly. `pb_hub/static/src/js/hub_palette_entries.js`'s `approvals` row was repointed from the bare tag `pb_approval` to `{xmlid: "pb_home_hub.action_pb_home_hub", lens: "approvals"}` with `groups: []` and `requires: "pb_home_hub"` — named as a plain string, so `pb_hub` still depends on nothing above it. `pb_home_hub/tests/test_home_hub.py` had two cases asserting the old gate and the old component; both are rewritten, not deleted.
- **AM22 (P2 build, 12 Sep) — A METHOD NAMED `stepNumber` TRIPS THE "no JS globals in a template" GREP:** `pb_group`'s static-contract test looks for the SUBSTRING `Number(` in every `t-` expression, and `stepNumber(st)` contains it. The check in this module uses a word boundary (`(?<![\w.])Number\(`) instead. Anybody cloning that test into a module with a `stepNumber`, `toString`, `keyObject` or similar method needs the same fix; the substring form produces a failure that looks like a real finding and is not.
- **AM23 (P2 build, 12 Sep) — THE HOME LENS COULD NOT BE WALKED LOCALLY:** `pb_home_hub` depends on `pb_payhub → pb_payruns`, and `pb_payruns` cannot install on a fresh local DB for the reason AM16/AM17 already record (`hr_payslip_run_kanban.xml:7` needs `journal_id` from `om_hr_payroll_account`, which itself cannot install locally). So the Approvals lens swap is proved by `pb_home_hub/tests/test_home_hub.py` (rewritten to assert the empty gate and the new component) rather than by a browser walk, and those tests have NOT been run — they cannot be on this machine. The inbox itself was walked in full through its own client action (`pb_approval_config.action_pb_approval_inbox`), which mounts the identical `PbInbox`. **The deploy wave must run `pb_home_hub`'s suite and open Home → Approvals as its first check.**
- **AM24 (P2 build, 12 Sep) — THE KIT CAPITALIZES EVERY BADGE:** `.pbim-badge` sets `text-transform: capitalize`, which is right for a one-word status and wrong for a sentence — the browser walk showed "3 Thing(S) To Confirm" and "Not Connected Yet". The POC had a `.badge.sentence` opt-out; this module instead sets `text-transform: none` on every `.pbim-badge` inside its two roots, because every badge it writes is already in the case it should be read in. Any surface whose badges are sentences needs the same line.
- **AM25 (P2 build, 12 Sep) — A UNIT IS A WORD, NOT A TYPE:** adapters put the SHAPE of a fact in the same slot as its unit, so the request drawer rendered "No bool" beside a yes/no answer and "any" for a kind. `pb.approval.inbox._facts` now drops any unit whose value is one of `definition.FACT_TYPES`, and maps a kind back through the adapter's own `capabilities['kinds']` labels. **Every adapter P3-P5 writes should put a real unit (a currency code, "hours") in `unit` or leave it empty — never the type.**
- **AM26 (P2 build, 12 Sep) — AN INSTALL HOOK PUBLISHES AS A ROBOT:** the seed's `engine.publish()` ran as whoever is acting during an install, so History's first line read "OdooBot published …" — a vendor name on a customer-facing screen. Fixed by publishing as the company's own administrator. It has to be `with_user(publisher).sudo()` and not one or the other: a company created a moment ago has no members at all, so the `company_id in company_ids` record rule refuses that person their own default route, while `sudo()` lifts the rule WITHOUT changing `env.uid` — and `biz.approval.event._log` forces `user_id = env.uid`, so the trail keeps the honest name. Any later seed that writes events needs the same pairing.
- **AM27 (P2 build, 12 Sep) — GAPS ARE ONLY GAPS WHERE A ROUTE ASKS:** `get_people` counted an empty seat as a gap only for a *part of the business*, so a company with no divisions showed "every seat has somebody in it" while the seat its published route needed was empty. Company-wide empties now count too — but only for responsibilities some workflow in that company actually names (`_roles_in_use`, read from the definition documents, drafts included). Without that filter all nine catalogue responsibilities would report as problems on day one.
- **AM28 (P2 build, 12 Sep) — SENTENCES BUILT AROUND AN INTERPOLATION:** the translation export is what finds these, and it found one that was untranslatable (`The <n> already under way keep…` exported the bare word "The"). That one is fixed as a single `_t` string in a getter. **Several clause-level fragments remain** in the templates (". Requests from those places cannot be sent in until somebody is named.", "thing(s) to confirm", "seat(s) have nobody in them:", "In use now: v", "Compared with version", …). They are translated and they read correctly in Vietnamese, but a language that reorders the count would be stuck with them. A later i18n pass should fold each into one string with a placeholder; new copy should not add more.
- **AM29 (P2 build, 12 Sep) — `--i18n-export` IS GONE IN ODOO 19:** `odoo-bin server` no longer has the option. Export a `.pot` with the wizard instead, from a database with the module installed: `env['base.language.export'].create({'lang': '__new__', 'format': 'po', 'modules': [(6, 0, module.ids)]})`, then `act_getfile()` and base64-decode `wiz.data`. The header it writes says "Odoo Server 19.0" in `Project-Id-Version`, which is a real msgstr and therefore a user-visible string — rewrite it to "Payobook 19.0" before committing.
- **AM30 (P3 build, 12 Sep) — AM16 CLOSED, AND THE FIX WAS THE VIEW, NOT THE DEPENDENCY:** `pb_payruns` listed `journal_id` in its kanban `<field>` set, its card and its search view, and grouped by it. The field belongs to `om_hr_payroll_account`, which cannot be installed on a fresh database at all (AM17), so DECLARING the dependency was never an option — the only way out was to stop naming the field. The "group by Journal" filter is replaced by "group by Division", which is what the cards already show. `runtests.sh pb_payruns` now installs with no `EXTRA`.
- **AM31 (P3 build, 12 Sep) — `hr.payslip.run.state` IS REPLACED, NOT ADDED TO:** `om_hr_payroll` ships `draft/level1/level2/done/cancel` on the run and `pb_payruns` used to insert `level0` with `selection_add`. P3 redefines the whole selection (`fields.Selection(selection=[...])` on the inheriting model) as `draft/approval_pending/done/cancel`. Consequence nobody can skip: the two base values disappear, so a run sitting in one of them becomes unreadable — which is why the `pre-` migration ABORTS the whole upgrade, by name, if any exists. The base module's own `done_payslip_run`, `action_payslip_run_level1_done` and `action_payslip_run_level2_done` still EXIST (they are defined upstream and cannot be deleted from a child), so each is overridden: the first is re-pointed at `action_approval_submit`, the other two refuse with a message naming Approvals. A method that still writes `level1` is a live hole, not dead code.
- **AM32 (P3 build, 12 Sep) — A SOURCE REVISION MUST NOT INCLUDE `write_date`:** the handover specified the pay-run stamp as sha256 over `(slip id, net, gross, write_date)`. Sending a run in moves every payslip from Draft to Waiting, which stamps a NEW `write_date` on all of them — and the engine re-computes `_approval_context()` at apply time and compares. The stamp would therefore differ from itself the instant it was taken, and every approval would be refused as "this changed after it was sent in". The stamp is `(slip id, net, gross)` only: what an approver signs for is the money. **Any adapter that freezes a revision must ask whether its own freeze changes the thing it is stamping.**
- **AM33 (P3 build, 12 Sep) — `copy()` ON A RUN DUPLICATES ITS PAYSLIPS:** `hr.payslip.run.slip_ids` is a plain one2many with no `copy=False`, so the obvious implementation of "split into review groups" (`self.copy({'slip_ids': [(6, 0, [])]})`) would have duplicated every payslip — the exact opposite of conserving them. The children are `create()`d from the source's own dates and credit-note flag instead, and the split then asserts three things before it removes the source: every payslip landed in exactly one child, the children's nets add back up to the source's, and the source is empty.
- **AM34 (P3 build, 12 Sep) — THE ANALYTICS HOLE WAS NOT THE `sudo()`:** `payroll_analytics_approval` finished pay runs with `runs_to_finalize.sudo().action_payslip_run_level2_done()`. Removing only the sudo would have left a pay run being APPROVED from a reporting screen, by nobody, with no seat and no recorded decision — which is what the whole engine exists to prevent. The entire block is gone: approving an analytics record marks the analytics approved and touches no run. Its `('state', 'in', ['level2', 'done'])` payslip domains became `['verify', 'done']`, and the dashboard integration's "every run in level2" became "every run waiting for its approval".
- **AM35 (P3 build, 12 Sep) — THE SCHEME ACTIVATION GUARD LIVES IN `pb_payruns`:** "a scheme cannot go live with nobody to approve its pay runs" is a sentence about two modules. `hr.formula.config` belongs to the formula engine, which must not learn about approvals; the engine must not learn about pay schemes. `pb_payruns` already depends on both, so `action_activate` is overridden there. The check reads the route under `sudo()` — it is a GUARD, and refusing to activate because the acting user may not read the approval set-up would be the wrong answer to the wrong question.
- **AM36 (P3 build, 12 Sep) — THE BLUEPRINT'S APPROVALS PILL IS NOT A REMEMBERED PRESS:** the other two Connect tasks store "somebody said they were done". This one asks the engine, every read, because a route that exists exists whether or not anybody ticked a box and a route with an empty seat is not finished however many times they did. `bp_task_set(..., 'configured')` is refused for `approvals` with a plain reason. The ONE thing that is still remembered is a deliberate skip, which is a statement about the person rather than about the route. `DEFAULT_OPTIONAL_STATUS['approvals']` gained the normal shape and the old stored word `'info'` is coerced to `'not_started'`.
- **AM37 (P3 build, 12 Sep) — pb_learn's REPLICA SCREENS STILL DRAW THE OLD LADDER, DELIBERATELY:** `learn_live.py` (which reads the REAL database) is corrected — the state map is `draft/approval_pending/done`, "submitted" means the run left draft, and "past the officer" is replaced by "a decision has been recorded", which is the only question answerable under a route that may have one step, five, or none. `static/src/engine/fixture.js` and `screens.js` are NOT: they are a teaching MOCK of the product's screens, their lane names and lesson prose are content rather than code, and rewriting them is a content pass. **Deviation, recorded, and the next content pass owns it.**
- **AM38 (P3 build, 12 Sep) — PRE-EXISTING LOCAL FAILURE, NOT THIS PHASE'S:** `pb_payruns/tests/test_payslip_line_access.py::test_06_a_plain_employee_sees_only_its_own_lines` fails on the local runtime with `AccessError: You are not allowed to access 'Pay Slip'` — a plain employee has no `hr.payslip` read ACL at all here. It failed identically before any P3 code was written (first clean install run of the phase). It needs a module the local runtime does not install, or an ACL the live databases have from an older era; either way it is not a regression and it is not fixed here.
- **AM39 (P3 build, 12 Sep) — `_approval_capabilities()['scope_levels']` IS A LIST OF LABELS:** the handover asked for `['scheme', 'division']`. The only other implementation (the generic fallback in `matrix_facade._capabilities`) sends `[_('Whole company')]` — labels. Nothing reads the key today, and the day something does it will be putting it on a screen, so the pay-run adapter sends `[_('Pay scheme'), _('Division')]`. **Deviation from the handover, deliberate.**
- **AM40 (P3 build, 13 Sep) — CHOOSING A ROUTE IS NOT A PERMISSION QUESTION:** the first browser press of "Submit for approval" was refused with "You don't have access to this yet · Formula Engine/Formula Manager". Working out WHICH published route applies to a pay run means reading its pay scheme, the employees' teams and the division those teams sit in — and a payroll officer who may open a run and send it in has no reason to be allowed to open a scheme. `_pb_review_groups`, `_pb_kind_key` and the maker/subject lookup in `_approval_context` now read under `sudo()`; the gate is unchanged (`submit` re-checks the run's own write access) and every seat, decision and later read still runs as the real person. **Every adapter has this shape: the facts a route is chosen ON are read for the engine, not handed to the reader.**
- **AM41 (P3 build, 13 Sep) — AN OPEN SEAT IS NOT A TURN:** `pb_awaiting_me` matched any open seat on the request, and every seat on every included step is created open at submission — so a run three steps from Finance already sat in the Finance approver's queue. The search needs `step_id.status = 'active'`. Anything else that asks "is this waiting for me?" needs the same clause.
- **AM42 (P3 build, 13 Sep) — THE LOCAL RUNTIME CANNOT INSTALL `pb_blueprint` AT ALL:** `post_init_hook` runs `certify_module_templates`, and the VN country pack fails its own certification on a fresh database — seven figures out (`Full month · local[NET] exp 27,035,000 got 26,307,500`, PIT and TAXABLE likewise). Nothing in P3 touches the pack or the tax arithmetic; it is a pre-existing local-runtime failure of the same family as AM17. Consequence: `runtests.sh pb_blueprint` cannot run here, and the guided setup's Connect card could not be walked in a browser. Its server half is covered by `pb_blueprint/tests/test_connect.py` (rewritten), which will run on the deploy wave.
- **AM43 (P3 build, 13 Sep) — THE REFERENCE MACHINE TAKES ~1 HOUR PER FULL INSTALL, AND TWO AT ONCE TAKE FIVE:** two concurrent `runtests.sh` runs dropped to 3-4 modules per ten minutes; serially each is ~25-60 minutes. Run them ONE AT A TIME. For a re-run after a code change, skip the rebuild entirely: `odoo-bin -c odoo-local.conf -d am_test_<module> -u <module> --test-enable --test-tags /<module> --stop-after-init` against the database the previous run left behind takes about two minutes.
- **AM44 (P3 build, 13 Sep) — A PAYSLIP IN A RUN IS FINISHED BY THE RUN, INCLUDING IN DRAFT:** the payslip review screen drove a three-rung ladder on a SINGLE payslip beside the run's own approval. The bank export pays every payslip in state `done`, so that was a way to get one person's money out of a run nobody had sent in. `action_payslip_done` (and the two level actions) now refuse for any payslip whose run is `draft` or `approval_pending`. Deliberately NOT for a run that is already `done`: a refund raised off a finished run is the caller's own business, and `om_hr_payroll.refund_sheet` copies a payslip with its run link intact.

