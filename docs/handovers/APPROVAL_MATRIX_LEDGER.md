# Approval Matrix — conventions & gotcha ledger (AM-ledger)

Every Approval Matrix phase handover references this file. Implementers **append** a numbered `AM<n>` entry whenever they hit a new gotcha or make a convention decision; never rewrite earlier entries.

## Standing rules (verbatim from the owner, binding on every phase)

- **Design bar:** "extreme WOW, intuitive, out-of-this-world, best in class" — hero moment, zero dead-ends, plain language, purposeful motion, bulk ergonomics; Lucide icons, never emoji; Chrome-MCP validation of every user-facing change.
- **White-label:** the word "Odoo" must never appear in any user-visible string (labels, help, errors, toasts, emails, `.po` msgstr). Technical identifiers (`from odoo import`, xml ids, module names, log lines, comments) stay as they are.
- **Flexibility ruling (12 Sep 2026):** the business decides. Any process may be set to "No approval needed". There are **no hard minimums** (not two people for money out, not an independence rule, not a coverage pass). The system **warns, offers options and records the publisher's confirmation** of each warning. Structural errors only (bad schema, unknown role, two bindings that tie at the same rank) block a publish.
- **Clean replacement:** no payroll is live. Old engines (pay run `PB_TIER`, `officer_review` setting, `wfp.approval.step`, the two extra inboxes) are removed, not kept beside the new one. Every process ships a default workflow equal to today's route, so day one behaves the same.
- **Nothing approves on its own:** escalation reminds, escalates, optionally reassigns to a configured backup. Never auto-approves. A fast lane is a *published choice*, not an escalation outcome.
- **Fail closed at runtime:** no binding, a tie, a paused binding, a missing person, a revoked grant → submission/decision is refused with an actionable message. Never fall back to "all HR users", never pick the newest id.
- **Configuring a person never grants access.** Eligibility = resolved seat AND active user AND the object's own access. The picker says "Needs permission" and links to Access.
- **Server is the authority.** Every RPC re-checks access and scope; UI booleans are cosmetic. No `sudo()` to make a path work; `sudo()` only for reads the gate already allowed (audit console pattern).
- **Commit per feature**, explicit staging, reviewer-focused message, end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; never push.
- **Report in plain technical prose**; owner-facing wording is Fable's job.

## Module and dependency rules

- `biz_approval_workflow` (engine) depends only on `biz_approval_chain, hr, mail`. It must NOT depend on `biz_access`, `pb_settings`, `pb_hub`, `pb_group`, any `pb_*`. Reason: `biz_access → pb_hub/pb_settings → om_hr_payroll/pb_hr_payroll_base` would cycle through `pb_hr_payroll_formula` once the scheme adapter exists.
- `pb_approval_config` (UI) depends on `biz_approval_workflow, pb_hub, pb_settings, pb_sidebar, pb_import_kit, pb_group`. It must NOT depend on `pb_payruns`, `pb_home_hub`, `pb_mission`, `pb_approval`, `pb_team` (cycle via `pb_home_hub → pb_approval → pb_payruns`). Hubs depend on it, never the reverse.
- Adapters live in the business module (`pb_payruns`, `pb_hr_payroll_formula`, `pb_records`, `pb_pay_delivery`, `pb_hr_workforce`, `biz_access`, …) and add `biz_approval_workflow` to their `depends`.
- Division/scheme are Payobook concepts. The engine sees only opaque `scope_key` strings supplied by the adapter in precedence order (most specific first). Payobook adapters build them as `scheme:<id>|division:<id>`, `scheme:<id>`, `division:<id>`, `` (company).

## Odoo 19 gotchas that apply here (verified in this repo)

- `_sql_constraints` is silently ignored → use `_name_uniq = models.Constraint('unique(a,b)', 'message')` class attributes.
- `res.users.groups_id` → `group_ids`; membership checks via `user.all_group_ids` / `has_group`.
- `res.groups` has no `category_id`; define groups with `name` + `implied_ids` (+ `privilege_id` only if needed, and never `&`/`<` in a privilege name).
- `ir.cron` has no `numbercall`/`doall`; `nextcall` computed in Python, not an eval expression; `post_init_hook` runs on install only → mirror in a migration for `-u`.
- `hr.payslip.run` has NO `company_id` in this build → `getattr(run, 'company_id', False)`.
- Recordsets do not keep instance attributes; pass state through context lists.
- Private (`_`-prefixed) methods cannot be called over JSON-RPC.
- Odoo 19 removed the `<report>` shortcut and search `<group expand string>`; write explicit records.
- `safe_eval` has no `nocopy`. Never expose Python/SQL/domain evaluation to configuration; conditions are typed fact/operator/value only.
- Tests: `@tagged('post_install', '-at_install')`, `TransactionCase`, users created with `group_ids: [(6,0,[...])]`, `with_context(no_reset_password=True)`.

## Deploy contract (from CLAUDE.md, summarised)

1. One addons dir on the live box: `/odoo/odoo-server/addons` (ssh alias `Payobook19v2`). `/odoo/custom/addons` is a guard FILE — never recreate.
2. `sudo rm -rf /tmp/deployX && mkdir -p /tmp/deployX`, `rsync -az --exclude=__pycache__ --exclude='*.pyc' <modules> Payobook19v2:/tmp/deployX/`, then **per module** `sudo rsync -a --delete /tmp/deployX/<m>/ /odoo/odoo-server/addons/<m>/`. NEVER `--delete` with the addons root as destination.
3. Never deploy vendored standard addons (`web`, `hr`, `hr_*`, `mail`, …).
4. Stop the service; run the `-i/-u` in a detached unit with a sentinel (`systemd-run --collect --unit=x-install /bin/bash /tmp/x_run.sh`; script does `sudo -u odoo python3 /odoo/odoo-server/odoo-bin -c /etc/odoo-server.conf -d <db> -i <new> -u <changed> --stop-after-init > /tmp/x.log 2>&1; echo EXIT=$? >> /tmp/x.log; touch /tmp/x.done`); poll the sentinel; grep `EXIT=` and `Traceback|CRITICAL|ERROR`; start the service; confirm `Registry loaded` and a page loads.
5. Upgrade **every** database: `payobook`, `payobook_template`, and every tenant DB (`psql -l`; today at least `abm`, `acme`, `rize`). Tenant rule: tenants get everything the master gets except `pb_tenants`, `pb_demo`, `pb_demo_portal`, `pb_website`. Backup before installing on a tenant (`pg_dump -Fc`), install, verify.
6. After JS/SCSS: `DELETE FROM ir_attachment WHERE url LIKE '/web/assets/%'` per DB **and** bump `web.assets.version` ir.config_parameter, then restart.
7. Verify: hash each module tree both sides (skip `__pycache__`, `*.pyc`, `.DS_Store`); compare manifest version to `ir_module_module.latest_version` per DB (normalise the `19.0.` prefix).
8. Tests on the server: scratch DB (`createdb -T payobook_template <db>` or fresh), `odoo-bin -c /etc/odoo-server.conf -d <db> -i <module> --test-enable --stop-after-init --http-port=8199 --gevent-port=8198 --log-level=test`, with the service running is fine on those ports. `--test-enable` only runs tests for modules it installs/updates.
9. Never `pkill -f odoo-bin` in an ssh one-liner (self-matches). Kill leftover masters by PID. Never `systemctl daemon-reload` on the box.
10. Remove any screenshots/PNGs you generate; keep other sessions' files.

## Ledger entries

- **AM1 (design, 12 Sep):** workflow definitions are stored as one versioned JSON document per revision (`definition` Json, `schema_version` int), validated by a server-side serializer. No child step tables for authoring; the published snapshot is the runtime truth. Rationale: the builder edits the whole route atomically, comparisons between revisions are whole-document, and the POC's route logic maps 1:1.
- **AM2 (design, 12 Sep):** scope resolution is adapter-driven `scope_keys` in precedence order + `kind_key`. Exact `kind_key` beats `any` within a rank. Two effective bindings at the same rank and kind = publish-time error, runtime fail-closed.
- **AM3 (design, 12 Sep):** the engine ships one real business object of its own, `biz.approval.generic.request` ("Other request": title, description, amount, currency, requester). It is the test fixture for every model-level acceptance case and a real feature (ask for a sign-off on anything).
- **AM4 (design, 12 Sep):** warnings vs errors. `errors[]` block publish; `warnings[]` each carry a stable `code` and are stored on the published version as `confirmations` (code, text, user, stamp). Codes so far: `fast_lane`, `single_person`, `independence_off`, `coverage_not_run`, `coverage_gap:<scope>`, `no_backup:<step>`, `notify_only_route`, `money_single_person`.
- **AM5 (P1 build, 12 Sep):** `who.mode = 'team'` carries its own bounded list — `{"mode": "team", "user_ids": [...], "label": "Vietnam payroll desk"}` — because P1 has no team object and the engine may not invent one. A pool `biz.approval.role` is the other way to express "any one of". If P2 adds a real team model, it must keep filling `user_ids` at publish time so the published snapshot stays self-contained (design §9: the snapshot is the runtime truth).
- **AM6 (P1 build, 12 Sep):** the engine's public `resolve_binding(company_id, process_key, scope_keys, kind_key, at)` returns a **dict** (`binding_id`, `version_id`, `workflow_name`, `trace`, `error`), not the handover's 3-tuple: preview and the "Why this route?" panel must be able to SHOW a broken route rather than raise on it, and a recordset is not JSON. The internal `_resolve_binding` keeps the tuple with a 4th `error` element. Error codes: `no_route`, `ambiguous_route`, `paused`, `no_version`.
- **AM7 (P1 build, 12 Sep):** every adapter ends its `scope_keys` with `''`, so a role with `fallback_to_company = False` would be unreachable. `biz.approval.responsibility.resolve` therefore **strips the trailing company rank** when the role forbids the fallback AND a narrower scope was asked for; `['']` on its own still resolves, because then the company IS the scope. Without this the "each area needs its own person" switch does nothing.
- **AM8 (P1 build, 12 Sep):** `biz.approval.request.maker_uids` stays the frozen Json list, and is mirrored on create into a technical M2M `maker_user_ids`. A Json field cannot appear in a record-rule domain, and "the people who prepared it may see it" has to be a rule, not a method.
- **AM9 (P1 build, 12 Sep):** the repeated-person rule is applied when the **seats are built at submission**, not at step activation. Principals are frozen at submission, so both moments give the same answer, and doing it at build time makes `preview` and the real request byte-identical — which is the promise "Try an example" makes.
- **AM10 (P1 build, 12 Sep):** decisions and events refuse `write`/`unlink` for everyone, at model level. Consequence: `biz.approval.decision.company_id` is a PLAIN column the engine sets, never a stored related — a stored related rewrites itself when the source changes and would hit its own immutability guard. Same reason `biz.approval.workflow.version.write` has to allow `company_id`, `process_id`, `summary` and `route_labels` through on a published row: those are the ORM keeping its own bookkeeping, not an edit.
- **AM11 (P1 build, 12 Sep):** outbox delivery is the 5-minute cron only; no `cr.postcommit` kick. A post-commit delivery needs a second cursor and would run outside a test transaction, so it cannot be tested and can double-send. Rows are still written in the same transaction as the event, so nothing is ever told about something that did not happen.
- **AM12 (P1 build, 12 Sep):** `preview` and `coverage_scan` are gated by `group_approval_config`, not left open. Both resolve responsibilities across a whole company, so an open preview would be a people-directory read for anyone.
- **AM13 (P1 build, 12 Sep) — DEPLOY BLOCKED:** the live box `Payobook19v2` (3.104.113.197) was unreachable for the whole P1 session — ssh timeout, port 22 closed, no HTTP on the apex either — and there is no local Odoo, postgres or container runtime on this machine. So §6 of the P1 handover (deploy, install on every DB, run T01–T34, Chrome check) did **not** run. What did run: the pure-Python half (`definition.py`) exercised standalone, 33 checks, 0 failures; plus an AST pass over the module confirming every `self._method`, every `env['model']`, every `create()` key and every ACL/record-rule reference resolves. **The first job of the next session is to deploy the three modules and run the suite before anything is built on top of it.**
- **AM14 (Fable, 12 Sep) — LOCAL RUNTIME + DEFERRED DEPLOY WAVE:** the live box stayed down after P1 (DNS still 3.104.113.197; ports 22 and 443 closed; the owner has been told). A local Odoo 19 runtime is being set up at `/Users/adity/odoo19` (Postgres 16 via Homebrew, Python 3.12 venv, official `odoo/odoo` 19.0 checkout FIRST in `addons_path`, then the gitlocal repo root — so the repo's vendored old `web`/`hr`/`mail` copies are shadowed exactly as on the server). `README.md` and `runtests.sh <module> [db]` live there. **Every phase from P1 onward runs its tests locally (`/Users/adity/odoo19/runtests.sh <module>`) and validates its screens with Chrome MCP against `http://localhost:8069`.** Deployment to `Payobook19v2` is deferred to a single deploy wave once the box answers; each phase report must therefore end with a "deploy checklist" (modules to install/upgrade per DB, asset ritual yes/no, migrations, seeds) so the wave can be run without re-reading the code.
- **AM15 (Fable, 12 Sep, local test run of P1):** two gotchas found the moment the suite ran. (a) **`pool` is a reserved attribute on every model class** in Odoo 19 (`model_classes.is_model_definition` asserts `cls.pool is None`), so a field named `pool` makes the whole registry fail with a bare `AssertionError` at "Loading module …". The role flag is now **`biz.approval.role.is_pool`** (definition JSON `role_info` still uses the key `'pool'`). Never name a field `pool`, `env`, `id`, `ids`, `_fields`. (b) **`assertRaises` in Odoo 19's `TransactionCase` does not accept a tuple of exception classes** (`issubclass(exception, AccessError)` → `TypeError`); assert one class — model-level immutability guards raise `UserError`, seat-eligibility guards raise `AccessError`. Result after both fixes: `biz_approval_workflow: 48 tests, 0 failed, 0 errors` on the local runtime.
- **AM16 (Fable, 12 Sep):** a fresh install of anything that pulls `pb_payruns` fails at `pb_payruns/views/hr_payslip_run_kanban.xml:7` with `Field "journal_id" does not exist in model "hr.payslip.run"` — the field comes from `om_hr_payroll_account`, which `pb_payruns/__manifest__.py:17` does not declare. The live DBs never noticed because the accounting module was already installed. Until P3 fixes it (declare the dependency, or stop listing `credit_note`/`journal_id` in the kanban), local runs must add it: `EXTRA=om_hr_payroll_account /Users/adity/odoo19/runtests.sh <module>` (`runtests.sh` now accepts `EXTRA=<mod,mod>` to co-install).
- **AM17 (Fable, 12 Sep):** `om_hr_payroll_account` cannot be installed on a fresh local DB: its `data/hr_payroll_account_data.xml:10` references `account.1_expense_salary` / `account.1_salary_payable`, ids no current chart template creates (tried `l10n_generic_coa`). It is installed on the live DBs from an older era. Consequence: `pb_audit`'s local test run (and anything else pulling `pb_payruns`) is blocked ONLY by the AM16 kanban field until P3 lands; P3 must make `pb_payruns` install cleanly without `om_hr_payroll_account` (keep the accounting hook behind `'om_hr_payroll_account' in self.env.registry._init_modules` / `hasattr` guards, as `_accountless` already does). Until then P1's T34 (audit `workflow` source) is verified only by its unit test inside `pb_audit`, which cannot run locally yet.
- **AM18 (P2 build, 12 Sep) — PRESETS ARE PYTHON, NOT DATA:** the handover asked for `data/presets.xml`. A preset is a piece of COPY (a title and a sentence that both need `_t()`) plus a definition document, and there is nothing useful to do with it in the database between the press and the draft it creates — no screen lists preset *records*, nothing points at one, and a data row would have to carry a Json definition that no view could ever edit. They live in `matrix_facade._preset_rows()` and are served by `get_matrix`. Six of them: officer/mgr_hr/joint/tiers/four/blank. **Deviation from the handover, deliberate.**
- **AM19 (P2 build, 12 Sep) — THE FILE SET IS TEN JS FILES, NOT SIXTEEN:** the handover listed `matrix_tab.js`, `step_card.js`, `example_panel.js`, `coverage.js`, `publish.js`, `request_drawer.js`, `decision_modals.js`, `ask_drawer.js` as separate modules. Shipped as: `sentence.js`, `picker_drawer.js`, `builder.js`, `people_tab.js`, `history_tab.js`, `scheme_panel.js`, `matrix_room.js`, `request_drawer.js`, `inbox.js`, `palette.js`. The builder's four wizard steps share one state object and one save path, so splitting them across five files would have meant five components passing the same eight props to each other; the example panel and the coverage panel are sub-TEMPLATES (`pb_approval_config.ExamplePanel`) called from both the People and Safeguards steps, which is what keeps them from drifting into two. The decision modals are inside `RequestDrawer` for the same reason: they close over its `decide()`. **Deviation from the handover, deliberate.**
- **AM20 (P2 build, 12 Sep) — `_area_labels` / `AREA_ICONS` LIVE ON THE PROCESS MODEL, IN THE UI MODULE:** the engine's `biz.approval.process` has an `area` selection but no idea what icon an area wears, and it must not learn — it has to stay able to run in a product that has never heard of payroll. `pb_approval_config.models.seed` therefore `_inherit`s `biz.approval.process` and adds `AREA_ICONS` (a class dict) plus `_area_labels()`. Both the Matrix and the inbox read them, so the area headers and the inbox filter chips can never disagree. The static-contract test asserts every name in `AREA_ICONS` exists in the shared `ic()` set, because an icon name the SERVER sends is exactly as invisible a typo as one in the markup.
- **AM21 (P2 build, 12 Sep) — THE HOME LENS GATE IS NOW EMPTY, ON PURPOSE:** `APPROVAL_GATE` in `pb_home_hub/static/src/js/home_hub.js` was `pb.approval._APPROVAL_GROUPS` verbatim, because the lens was the pay-run board and only a payroll officer could read one. The lens is now `PbInbox` — every process, and every person ever asked to decide anything. A payroll gate would hide it from most of the people it is for. Nothing is given away: `biz.approval.request`'s record rules narrow every read to the requests a person sent in, prepared or holds a seat on. The export is KEPT (empty) because `home_hub_palette.js` imports it and must follow the lens exactly. `pb_hub/static/src/js/hub_palette_entries.js`'s `approvals` row was repointed from the bare tag `pb_approval` to `{xmlid: "pb_home_hub.action_pb_home_hub", lens: "approvals"}` with `groups: []` and `requires: "pb_home_hub"` — named as a plain string, so `pb_hub` still depends on nothing above it. `pb_home_hub/tests/test_home_hub.py` had two cases asserting the old gate and the old component; both are rewritten, not deleted.
- **AM22 (P2 build, 12 Sep) — A METHOD NAMED `stepNumber` TRIPS THE "no JS globals in a template" GREP:** `pb_group`'s static-contract test looks for the SUBSTRING `Number(` in every `t-` expression, and `stepNumber(st)` contains it. The check in this module uses a word boundary (`(?<![\w.])Number\(`) instead. Anybody cloning that test into a module with a `stepNumber`, `toString`, `keyObject` or similar method needs the same fix; the substring form produces a failure that looks like a real finding and is not.
- **AM23 (P2 build, 12 Sep) — THE HOME LENS COULD NOT BE WALKED LOCALLY:** `pb_home_hub` depends on `pb_payhub → pb_payruns`, and `pb_payruns` cannot install on a fresh local DB for the reason AM16/AM17 already record (`hr_payslip_run_kanban.xml:7` needs `journal_id` from `om_hr_payroll_account`, which itself cannot install locally). So the Approvals lens swap is proved by `pb_home_hub/tests/test_home_hub.py` (rewritten to assert the empty gate and the new component) rather than by a browser walk, and those tests have NOT been run — they cannot be on this machine. The inbox itself was walked in full through its own client action (`pb_approval_config.action_pb_approval_inbox`), which mounts the identical `PbInbox`. **The deploy wave must run `pb_home_hub`'s suite and open Home → Approvals as its first check.**
- **AM24 (P2 build, 12 Sep) — THE KIT CAPITALIZES EVERY BADGE:** `.pbim-badge` sets `text-transform: capitalize`, which is right for a one-word status and wrong for a sentence — the browser walk showed "3 Thing(S) To Confirm" and "Not Connected Yet". The POC had a `.badge.sentence` opt-out; this module instead sets `text-transform: none` on every `.pbim-badge` inside its two roots, because every badge it writes is already in the case it should be read in. Any surface whose badges are sentences needs the same line.
- **AM25 (P2 build, 12 Sep) — A UNIT IS A WORD, NOT A TYPE:** adapters put the SHAPE of a fact in the same slot as its unit, so the request drawer rendered "No bool" beside a yes/no answer and "any" for a kind. `pb.approval.inbox._facts` now drops any unit whose value is one of `definition.FACT_TYPES`, and maps a kind back through the adapter's own `capabilities['kinds']` labels. **Every adapter P3-P5 writes should put a real unit (a currency code, "hours") in `unit` or leave it empty — never the type.**
- **AM26 (P2 build, 12 Sep) — AN INSTALL HOOK PUBLISHES AS A ROBOT:** the seed's `engine.publish()` ran as whoever is acting during an install, so History's first line read "OdooBot published …" — a vendor name on a customer-facing screen. Fixed by publishing as the company's own administrator. It has to be `with_user(publisher).sudo()` and not one or the other: a company created a moment ago has no members at all, so the `company_id in company_ids` record rule refuses that person their own default route, while `sudo()` lifts the rule WITHOUT changing `env.uid` — and `biz.approval.event._log` forces `user_id = env.uid`, so the trail keeps the honest name. Any later seed that writes events needs the same pairing.
- **AM27 (P2 build, 12 Sep) — GAPS ARE ONLY GAPS WHERE A ROUTE ASKS:** `get_people` counted an empty seat as a gap only for a *part of the business*, so a company with no divisions showed "every seat has somebody in it" while the seat its published route needed was empty. Company-wide empties now count too — but only for responsibilities some workflow in that company actually names (`_roles_in_use`, read from the definition documents, drafts included). Without that filter all nine catalogue responsibilities would report as problems on day one.
- **AM28 (P2 build, 12 Sep) — SENTENCES BUILT AROUND AN INTERPOLATION:** the translation export is what finds these, and it found one that was untranslatable (`The <n> already under way keep…` exported the bare word "The"). That one is fixed as a single `_t` string in a getter. **Several clause-level fragments remain** in the templates (". Requests from those places cannot be sent in until somebody is named.", "thing(s) to confirm", "seat(s) have nobody in them:", "In use now: v", "Compared with version", …). They are translated and they read correctly in Vietnamese, but a language that reorders the count would be stuck with them. A later i18n pass should fold each into one string with a placeholder; new copy should not add more.
- **AM29 (P2 build, 12 Sep) — `--i18n-export` IS GONE IN ODOO 19:** `odoo-bin server` no longer has the option. Export a `.pot` with the wizard instead, from a database with the module installed: `env['base.language.export'].create({'lang': '__new__', 'format': 'po', 'modules': [(6, 0, module.ids)]})`, then `act_getfile()` and base64-decode `wiz.data`. The header it writes says "Odoo Server 19.0" in `Project-Id-Version`, which is a real msgstr and therefore a user-visible string — rewrite it to "Payobook 19.0" before committing.
- **AM30 (P3 build, 12 Sep) — AM16 CLOSED, AND THE FIX WAS THE VIEW, NOT THE DEPENDENCY:** `pb_payruns` listed `journal_id` in its kanban `<field>` set, its card and its search view, and grouped by it. The field belongs to `om_hr_payroll_account`, which cannot be installed on a fresh database at all (AM17), so DECLARING the dependency was never an option — the only way out was to stop naming the field. The "group by Journal" filter is replaced by "group by Division", which is what the cards already show. `runtests.sh pb_payruns` now installs with no `EXTRA`.
- **AM31 (P3 build, 12 Sep) — `hr.payslip.run.state` IS REPLACED, NOT ADDED TO:** `om_hr_payroll` ships `draft/level1/level2/done/cancel` on the run and `pb_payruns` used to insert `level0` with `selection_add`. P3 redefines the whole selection (`fields.Selection(selection=[...])` on the inheriting model) as `draft/approval_pending/done/cancel`. Consequence nobody can skip: the two base values disappear, so a run sitting in one of them becomes unreadable — which is why the `pre-` migration ABORTS the whole upgrade, by name, if any exists. The base module's own `done_payslip_run`, `action_payslip_run_level1_done` and `action_payslip_run_level2_done` still EXIST (they are defined upstream and cannot be deleted from a child), so each is overridden: the first is re-pointed at `action_approval_submit`, the other two refuse with a message naming Approvals. A method that still writes `level1` is a live hole, not dead code.
- **AM32 (P3 build, 12 Sep) — A SOURCE REVISION MUST NOT INCLUDE `write_date`:** the handover specified the pay-run stamp as sha256 over `(slip id, net, gross, write_date)`. Sending a run in moves every payslip from Draft to Waiting, which stamps a NEW `write_date` on all of them — and the engine re-computes `_approval_context()` at apply time and compares. The stamp would therefore differ from itself the instant it was taken, and every approval would be refused as "this changed after it was sent in". The stamp is `(slip id, net, gross)` only: what an approver signs for is the money. **Any adapter that freezes a revision must ask whether its own freeze changes the thing it is stamping.**
- **AM33 (P3 build, 12 Sep) — `copy()` ON A RUN DUPLICATES ITS PAYSLIPS:** `hr.payslip.run.slip_ids` is a plain one2many with no `copy=False`, so the obvious implementation of "split into review groups" (`self.copy({'slip_ids': [(6, 0, [])]})`) would have duplicated every payslip — the exact opposite of conserving them. The children are `create()`d from the source's own dates and credit-note flag instead, and the split then asserts three things before it removes the source: every payslip landed in exactly one child, the children's nets add back up to the source's, and the source is empty.
- **AM34 (P3 build, 12 Sep) — THE ANALYTICS HOLE WAS NOT THE `sudo()`:** `payroll_analytics_approval` finished pay runs with `runs_to_finalize.sudo().action_payslip_run_level2_done()`. Removing only the sudo would have left a pay run being APPROVED from a reporting screen, by nobody, with no seat and no recorded decision — which is what the whole engine exists to prevent. The entire block is gone: approving an analytics record marks the analytics approved and touches no run. Its `('state', 'in', ['level2', 'done'])` payslip domains became `['verify', 'done']`, and the dashboard integration's "every run in level2" became "every run waiting for its approval".
- **AM35 (P3 build, 12 Sep) — THE SCHEME ACTIVATION GUARD LIVES IN `pb_payruns`:** "a scheme cannot go live with nobody to approve its pay runs" is a sentence about two modules. `hr.formula.config` belongs to the formula engine, which must not learn about approvals; the engine must not learn about pay schemes. `pb_payruns` already depends on both, so `action_activate` is overridden there. The check reads the route under `sudo()` — it is a GUARD, and refusing to activate because the acting user may not read the approval set-up would be the wrong answer to the wrong question.
- **AM36 (P3 build, 12 Sep) — THE BLUEPRINT'S APPROVALS PILL IS NOT A REMEMBERED PRESS:** the other two Connect tasks store "somebody said they were done". This one asks the engine, every read, because a route that exists exists whether or not anybody ticked a box and a route with an empty seat is not finished however many times they did. `bp_task_set(..., 'configured')` is refused for `approvals` with a plain reason. The ONE thing that is still remembered is a deliberate skip, which is a statement about the person rather than about the route. `DEFAULT_OPTIONAL_STATUS['approvals']` gained the normal shape and the old stored word `'info'` is coerced to `'not_started'`.
- **AM37 (P3 build, 12 Sep) — pb_learn's REPLICA SCREENS STILL DRAW THE OLD LADDER, DELIBERATELY:** `learn_live.py` (which reads the REAL database) is corrected — the state map is `draft/approval_pending/done`, "submitted" means the run left draft, and "past the officer" is replaced by "a decision has been recorded", which is the only question answerable under a route that may have one step, five, or none. `static/src/engine/fixture.js` and `screens.js` are NOT: they are a teaching MOCK of the product's screens, their lane names and lesson prose are content rather than code, and rewriting them is a content pass. **Deviation, recorded, and the next content pass owns it.**
- **AM38 (P3 build, 12 Sep, PROVEN 13 Sep) — A TEST THAT NEEDED A MODULE IT DID NOT DEPEND ON:** `pb_payruns/tests/test_payslip_line_access.py::test_06_a_plain_employee_sees_only_its_own_lines` failed on the local runtime with `AccessError: You are not allowed to access 'Pay Slip'`. Not a P3 regression, and the proof is one grep: the ONLY `ir.model.access` row in the repository that lets a plain internal user (`base.group_user`) read `hr.payslip` is `pb_me_portal/security/ir.model.access.csv:9` — and `pb_me_portal` is not in `pb_payruns`' 52-module dependency closure. The search is therefore refused at MODEL level, before any record rule is consulted, so the case could never have passed on a database installed from `pb_payruns` alone; it passed on the live boxes only because the ESS module happens to be installed there. `git log` on the module's access file shows no P3 commit. The case now checks for that access row and skips with the reason named — checked as the ROW rather than the module, because the row is what it actually needs.

- **AM39 (P3 build, 12 Sep) — `_approval_capabilities()['scope_levels']` IS A LIST OF LABELS:** the handover asked for `['scheme', 'division']`. The only other implementation (the generic fallback in `matrix_facade._capabilities`) sends `[_('Whole company')]` — labels. Nothing reads the key today, and the day something does it will be putting it on a screen, so the pay-run adapter sends `[_('Pay scheme'), _('Division')]`. **Deviation from the handover, deliberate.**
- **AM40 (P3 build, 13 Sep) — CHOOSING A ROUTE IS NOT A PERMISSION QUESTION:** the first browser press of "Submit for approval" was refused with "You don't have access to this yet · Formula Engine/Formula Manager". Working out WHICH published route applies to a pay run means reading its pay scheme, the employees' teams and the division those teams sit in — and a payroll officer who may open a run and send it in has no reason to be allowed to open a scheme. `_pb_review_groups`, `_pb_kind_key` and the maker/subject lookup in `_approval_context` now read under `sudo()`; the gate is unchanged (`submit` re-checks the run's own write access) and every seat, decision and later read still runs as the real person. **Every adapter has this shape: the facts a route is chosen ON are read for the engine, not handed to the reader.**
- **AM41 (P3 build, 13 Sep) — AN OPEN SEAT IS NOT A TURN:** `pb_awaiting_me` matched any open seat on the request, and every seat on every included step is created open at submission — so a run three steps from Finance already sat in the Finance approver's queue. The search needs `step_id.status = 'active'`. Anything else that asks "is this waiting for me?" needs the same clause.
- **AM42 (P3 build, 13 Sep) — THE LOCAL RUNTIME CANNOT INSTALL `pb_blueprint` AT ALL:** `post_init_hook` runs `certify_module_templates`, and the VN country pack fails its own certification on a fresh database — seven figures out (`Full month · local[NET] exp 27,035,000 got 26,307,500`, PIT and TAXABLE likewise). Nothing in P3 touches the pack or the tax arithmetic; it is a pre-existing local-runtime failure of the same family as AM17. Consequence: `runtests.sh pb_blueprint` cannot run here, and the guided setup's Connect card could not be walked in a browser. Its server half is covered by `pb_blueprint/tests/test_connect.py` (rewritten), which will run on the deploy wave.
- **AM43 (P3 build, 13 Sep) — THE REFERENCE MACHINE TAKES ~1 HOUR PER FULL INSTALL, AND TWO AT ONCE TAKE FIVE:** two concurrent `runtests.sh` runs dropped to 3-4 modules per ten minutes; serially each is ~25-60 minutes. Run them ONE AT A TIME. For a re-run after a code change, skip the rebuild entirely: `odoo-bin -c odoo-local.conf -d am_test_<module> -u <module> --test-enable --test-tags /<module> --stop-after-init` against the database the previous run left behind takes about two minutes.
- **AM44 (P3 build, 13 Sep) — A PAYSLIP IN A RUN IS FINISHED BY THE RUN, INCLUDING IN DRAFT:** the payslip review screen drove a three-rung ladder on a SINGLE payslip beside the run's own approval. The bank export pays every payslip in state `done`, so that was a way to get one person's money out of a run nobody had sent in. `action_payslip_done` (and the two level actions) now refuse for any payslip whose run is `draft` or `approval_pending`. Deliberately NOT for a run that is already `done`: a refund raised off a finished run is the caller's own business, and `om_hr_payroll.refund_sheet` copies a payslip with its run link intact.
- **AM45 (P3 build, 13 Sep) — "SKIP THE REST" REACHES APPROVALS NOW, AND ONE TEST PINNED THE OLD ANSWER:** `bp_skip_rest` used to exclude the approvals task by name, so `test_skip_the_rest_only_touches_what_nobody_started` asserted `skipped == ['payslip']`. Approvals is an ordinary optional task in P3 (handover §1.6), so the list is `['approvals', 'payslip']`. The case is updated and a second one added: a person who OPENED the approvals card and came back out has touched it, and the bulk action must leave it `in_progress` — "only what nobody started" is the promise, and approvals is not an exception to it.
- **AM46 (P3 build, 13 Sep) — THE FINISH PAGE NAMED THE BACKGROUND ACCOUNT:** `bp_evidence`'s `run_by` was `tests_run_by.name` raw, so on any database where `biz_debrand` has not renamed that account's partner — a scratch test database, and any install-hook or cron run — the Finish page read "Last checked Tuesday by OdooBot". `pb_blueprint`'s own white-label test caught it the moment the module was installed without `biz_debrand`. Now `_run_by_name()` returns `_("System")` for the superuser and the account's own name for anybody else. Deliberately NOT a debranding rule of its own: `biz_debrand` renames that partner on a live box and this returns the same word either way; what it adds is that a database without it cannot leak the raw login through this payload. **Any payload that echoes a user name should ask whether the superuser can be the one it is echoing.**

- **AM45 (P4 build, 13 Sep) — TWO CATALOGUE ROWS NAMED THINGS THAT COULD NOT HOLD A REQUEST:** `process_timesheet` named `hr.attendance.weekentry`, a TransientModel with zero stored fields, and `process_scheme` named `hr.formula.config`, which is the SUBJECT of a change rather than the request to make one. A `biz.approval.request` carries `res_model`/`res_id` and re-reads its record at apply time, so a vacuumed transient row is a request pointing at nothing. Both rows are repointed (`pb.timesheet.packet`, `pb.scheme.proposal`) — and repointed **in the adapter module's own seed**, not in `pb_approval_config/data/processes.xml`, because that file is `noupdate="1"` and an existing database would keep the old name forever. Any later adapter whose catalogue row names the wrong model must do the same.
- **AM46 (P4 build, 13 Sep) — A STAMP READ BACK OFF ITS OWN SNAPSHOT IS NOT A STAMP:** the timesheet packet stores `days_json` + `source_hash` at submission, and the obvious `_approval_context()` reads `source_hash` off the row. The engine compares that value at apply time against the one frozen at submission — so it would always equal itself and the whole "this changed after it was sent in" rail would be decoration. `_scan_week()` is therefore a PURE read of the punches and overtime, `_write_snapshot()` uses it, and `_live_stamp()` re-takes the stamp from the source on every call. The companion of AM32: that one says do not stamp what your own freeze changes; this one says do not stamp what your own snapshot froze.
- **AM47 (P4 build, 13 Sep) — AN INPUT RUNG GOES IN THE MODULE THAT DEPENDS ON THE ONE IT ANSWERS OVER:** the handover put the approved-timesheet hour rung in `pb_workforce_payroll_bridge`. It cannot live there: to ANSWER for `OTHRS150/200/300/NGT` and `BONHRS` instead of the bridge's live-overtime values, the override must sit ABOVE the bridge in the MRO, and the only thing that puts it there is depending on the bridge. It lives in `pb_timesheet_approval/models/hr_payslip.py`. **Deviation from the handover, forced.** The same reasoning binds every future rung that REPLACES rather than adds.
- **AM48 (P4 build, 13 Sep) — A NEW PROVENANCE `via` IS THREE FILES, AND TWO OF THEM ARE GUARDED BY A SET-EQUALITY TEST:** `input_provenance.VIAS` (the vocabulary), `pb_source_atlas.VIA_LABELS` (the words a reader sees) and `pb_formula_studio._JOURNEY_VIA_BUCKETS` (which family the landing tab counts it in). `pb_formula_studio/tests/test_journey_view.py::test_01a` asserts set equality in BOTH directions, so adding to one and not the other fails a suite in a module you may not have touched. `src` is a closed list of nine and was not extended: an approved week is the employee's own record, so it is `employee_field` with `via='timesheet_packet'`, exactly as the overtime and business-trip streams already are.
- **AM49 (P4 build, 13 Sep) — THE INBOX LEARNED TWO HOOKS, NOT TWO PROCESSES:** a pay run is counted in payslips and a week in hours, and `_card_count` could only count payslips and people. Rather than teach `inbox_facade` what a week is, the adapter mixin gained `_approval_card_count(request)` and `_approval_detail(request)`; the facade asks the record and falls back to the facts it already recognised. `_approval_detail` returns a SHAPE (`title`, `columns`, `rows[{head,sub,cells,tone}]`, `chips`, `note`), shape-checked and clipped in the facade before it reaches the drawer, so a malformed payload from a future adapter degrades to "nothing more to show" rather than to a blank screen. Any adapter with something worth drawing under the facts implements those two and writes no JS.
- **AM50 (P4 build, 13 Sep) — "THEIR MANAGER" DOES NOT WORK OFF `subject_uids` FOR A WORKFORCE:** the mixin's default `_approval_manager_uids` maps `subject_uids` → `hr.employee` → `parent_id.user_id`, which needs the SUBJECT to have a login. Most people who clock in do not have one, so on `pb.timesheet.packet` the default resolved to nobody and every week blocked. The packet reads `employee_id.parent_id.user_id` directly. Every adapter about a person rather than about a batch needs the same override.
- **AM51 (P4 build, 13 Sep) — THE SCHEME PROCESS KEY IS `scheme`, NOT `scheme_change`:** the handover names `_approval_process_key = 'scheme_change'` throughout; the catalogue row shipped in P2 is `key=scheme`, and a key that matches nothing makes `_approval_process()` refuse with "not set up for approvals yet". The code uses `scheme`. **Deviation from the handover, forced by the data.**
- **AM52 (P4 build, 13 Sep) — THE PAYROLL ENGINE GETS THE APPROVAL ENGINE, NEVER THE MATRIX UI:** the handover added `biz_approval_workflow, pb_approval_config` to `pb_hr_payroll_formula`. Only the first went in. `pb_approval_config` pulls `pb_hub`, `pb_settings`, `pb_sidebar`, `pb_import_kit` and `pb_group`, and `pb_hr_payroll_formula` is the module every payroll install already needs — the adapter needs the engine and nothing else. `pb_formula_studio` already depended on `pb_approval_config`, which is where the panels live. **Deliberate deviation.**
- **AM53 (P4 build, 13 Sep) — THE LIVE-SCHEME ISOLATION RULE HOLDS THE MONEY FIELDS, NOT EVERY FIELD:** the handover asked for `hr.formula.rule.write` to refuse on an active config. It refuses for `excel_formula`, `python_formula`, `constant_value` and `column_type` — what a component PAYS — and for `unlink`. A rename, a sequence, a payslip position, a column role, a source binding: none of those move money, and a gate that held them would put "create a branch and propose it" in front of fixing a typo. `create` is not held either: a new component reaches a live scheme through the studio's own guarded flows, and the first write of its formula is caught. **Deliberate narrowing; if a later phase wants the wider rule it is one constant (`MONEY_FIELDS`).**
- **AM54 (P4 build, 13 Sep) — THE FINAL APPROVER OF A SCHEME CHANGE CARRIES IT OUT, AS THEMSELVES:** safety rail 7 says no `sudo()` to apply, so `pb.scheme.proposal._approval_apply` calls `config.check_access('write')` before it does anything. Consequence for every live database: a route whose last step is a country director who holds no Formula role will be approved and then refuse to apply, landing the request in `approved` with a block reason. That is the designed behaviour and not a fault — but it has to be checked when the route is published, and it is in the P4 deploy doc.
- **AM55 (P4 build, 13 Sep) — ODOO 19 SEARCH VIEWS REJECT `<group expand="0" string="…">`, AND SAY SO ABOUT THE WRONG ELEMENT:** the failure is `RELAXNG_ERR_EXTRACONTENT: Element search has extra content: field`, pointing at the first `<field>` in the view, and the actual offender is the attributes on the group-by `<group>` at the bottom. Write a bare `<group>` containing only `<filter>` rows. (The ledger already recorded that `<report>` and this attribute pair are gone; this is what the error looks like when you forget.)
- **AM56 (P4 build, 13 Sep) — A FORMULA COMPONENT WITH NO CATEGORY TAKES A PAYSLIP DOWN AT INSERT:** `hr.payslip.line.category_id` is NOT NULL, and `_create_payslip_lines_from_formulas` writes a line for every component marked "appears on payslip". A fixture (or a real scheme) with a category-less component fails with `NotNullViolation` from deep inside the computation, naming a column rather than a component. Give every component a category, or `appears_on_payslip=False`.
- **AM57 (P4 build, 13 Sep) — A MODEL ADDED IN MODULE A AND TESTED FROM MODULE B NEEDS `EXTRA=A`:** `runtests.sh` clones `am_tpl` and updates ONLY the module under test, so a new table defined in another module does not exist in that database and every case fails with `relation "…" does not exist`. Request `RUN-TESTS: B EXTRA=A`. The same trap makes a cross-module constant look out of step: module B's copy is new, module A's is the one in the database.
- **AM58 (P4 build, 13 Sep) — THE GUIDED SETUP'S FINISH STEP NEVER NAMED ACTIVATION:** handover item 13 asks for Finish's "activate later" advice to point at "Propose for approval". There is no such advice: `blueprint_finish.py`, `finish_text.js` and `finish.xml` contain no "activate", "go live" or equivalent copy at all (the blueprint deliberately never activates). Nothing to repoint; the Connect card's scheme-change line, which was a promise of a future panel, now mounts the real one.
- **AM59 (P4 build, 13 Sep) — `pay_period` HAD NO BUCKET, AND THE LANDING TAB HAD BEEN COUNTING IT AS A DEFAULT:** AM48's set-equality gate caught a hole that was not this phase's: the pay-period source added `via='pay_period'` to `input_provenance.VIAS` and to the Atlas labels, but never to `pb_formula_studio._JOURNEY_VIA_BUCKETS`, so every value the run's own dates answered has been reported on the Journey landing tab as "nothing fed it, it fell back to its default". It is bucketed `fallback` — it fills only what nothing else filled and can never take a number off a spreadsheet or a feed, which is what that family means. Found only because this phase added a second `via` and ran the suite that owns the pair.
- **AM60 (P4 build, 13 Sep) — A SEAT ON A WEEK IS ALSO PERMISSION TO READ THAT WEEK:** `biz.approval.engine.decide` re-checks `record.check_access('read')` before it lets anybody decide, and most HR leads and line managers hold no workforce role at all — so the route gave them a seat and the ORM then refused them the record the seat was about (`AccessError … doesn't have 'read' access to Weekly Timesheet Packet`). Fixed the way AM8 fixed the same shape on the engine: a technical `seat_user_ids` M2M on `pb.timesheet.packet`, written by an override of `biz.approval.request.seat.create` (so a hand-over or a late reassignment carries the read with it), and ONE record-rule clause that reads it. A record rule is a domain and cannot reach an answer that has no column. **Every adapter whose record is not already readable by its approvers needs this.**
- **AM61 (P4 build, 13 Sep) — THE ISOLATION RULE IS NOT BYPASSED FOR LEGACY TESTS, THE TESTS CHOOSE THE OTHER STATE:** `test_rd46_person_preview` edits a formula on a LIVE scheme, which is the point of the panel it tests, and P4 refuses that wherever a scheme-change route is published — which the seed does for every company. The ruling: no blanket bypass and no loosening of the gate. The case retires its own company's scheme-change binding in `setUpClass` — putting the test company in the OTHER state the product supports, a business that has not asked for that check — and says so. Any later test that edits a live scheme has the same three honest choices: branch it, publish the fast lane, or be a business with no route.
- **AM62 (P4 build, 13 Sep) — A FORK MILESTONE WITH NO VERSION HIGH-WATER MARK LOSES A SAME-TRANSACTION MERGE:** `branch_create` anchored its fork milestone with `milestone.record()`, which leaves `version_hwm` at -1 — the "legacy milestone" path, where `_ms_hwm` falls back to "the last version row created at or before the milestone's TIMESTAMP". Inside one transaction every row shares one timestamp (Postgres `now()` is the transaction clock), so the boundary swallowed the merge's own version rows, `release_approve` found "no changes since the last milestone" and a merge that really did rewrite the parent sealed no release. `branch_create` now uses `_seal_milestone`, which stamps the id boundary. W86's lesson in its most total form: any milestone that might be sealed in the same request as the edits it caps must carry an id boundary, never a timestamp.
- **AM63 (P4 build, 13 Sep) — A PAYSLIP LINE NEEDS A CATEGORY *AND* A SALARY RULE:** AM56's sibling. `hr.payslip.line.salary_rule_id` is NOT NULL too, so a fixture that gets past the category still dies at INSERT from inside `_create_payslip_lines_from_formulas`. A test that only needs a payslip COMPUTED (not printed) should mark its components `appears_on_payslip=False`, which writes no lines at all; a test that needs lines has to build a scheme whose components map to real salary rules, the way the certified country starters do.
- **AM64 (P4 walk, 13 Sep) — "Missing not-null constraint on pb.scheme.proposal.kind/state" IS AM57's SYMPTOM, NOT A DATA FAULT:** a server started on a database where `pb_timesheet_approval` was installed but `pb_hr_payroll_formula` was not updated logs that warning at registry load. The model is in the REGISTRY (Python is imported for every installed module's dependencies) while its table was never created, so `_auto_init` never applied the constraints. Both fields are correct — `state` has `default='draft'`, `kind` is required with no default because a proposal that does not say what it asks for is not a proposal. The check is `SELECT to_regclass('pb_scheme_proposal')`; if it is null the owning module was not updated in that database, and the scheme gate silently does nothing there (`_record_unchecked` wraps its own `create`, so nothing crashes).
- **AM65 (P4 walk, 13 Sep) — THE WALK CONFIRMED THE FAIL-CLOSED LOOP END TO END:** the first week sent in blocked with *"Nobody holds \"HR lead\" for Walk Line 1 yet"* — the department name, because that company has no divisions — and naming an HR lead plus `repair` mended it with no resubmission. The independence rail then refused the administrator, who had created the punches, their own approval ("You prepared this"), which is why the walk needed a manager who had prepared nothing. Both are the designed behaviour and both are worth expecting on the deploy wave.
- **AM66 (P4 walk, 13 Sep) — AN OFFICER'S OWN SEARCH ON `hr.attendance` RETURNS NOTHING FOR OTHER PEOPLE, AND `write([])` RETURNS TRUE:** a walk that checked the freeze by searching for a punch as the attendance officer and writing to it "passed" while touching nothing — the officer record rule on `hr.attendance` is own-records-only (which is why the grid facade reads under `sudo()`). Re-run as an administrator, all three paths — create, write, unlink — refused with the named message. **Any guard proved through a search must check that the search found something.**
- **AM67 (P4 walk, 13 Sep) — FOUR BUTTONS THAT ALL SAY "PROPOSE FOR APPROVAL" SAY WHAT THE PRESS COSTS, NOT WHAT IT DOES:** the first cut of the studio's lifecycle row replaced every direct verb with one generic label. On a LIVE scheme the Activate button is hidden, so the only button left carrying those words was the one that RETIRES the scheme — a destructive act wearing a procedural label. `schemeVerb(direct, propose)` now takes the specific wording per button (propose putting it live / retiring it / the release / the roll-back). **A gate that renames a button must keep the button's own verb inside the new one.**
- **AM68 (P4 walk, 13 Sep) — THE RELEASE NOW NAMES ITS PROPOSAL, BOTH WAYS ROUND:** the walk's merge was approved by three people, sealed "Release v1" — and the release's `proposal_id` was empty, because only the proposal had been written with the release's id. The release is the row somebody reads six months later, and "who agreed to this?" is asked of it. `_approval_apply` now writes `proposal_id` (and `state='approved'`) onto the release it produced.
- **AM69 (harness, 13 Sep) — `runtests.sh` CANNOT RECREATE A DATABASE THE LOCAL SERVER IS SERVING, AND THE OLD LOG STAYS BEHIND:** a suite run while `odoo-bin --http-port=8090` holds `am_test_<module>` fails at `createdb` (exit 1, no test output) and the previous `tests_<module>.log` keeps its earlier timestamp and result lines, so a grep of the log reads as a green re-run. Always compare the log's timestamp with the launch time, and stop the 8090 server (or serve a differently named clone) before re-running the suite whose database it holds.
- **AM70 (P5 build, 13 Sep) — A ROUTE THAT ONLY AN INSTALL LAYS IS A ROUTE NO LIVE DATABASE HAS:** every P5 adapter seeded its default route from `post_init_hook`, which runs on install ONLY — and every live database, and every `runtests.sh` database (a clone of `am_tpl`, then `-u`), already has these modules. Fourteen test failures, all one sentence: *"There is no approval set up for this yet."* Every adapter module now carries a migration that lays its own routes on `-u`. **A phase that adds an adapter adds a migration, not just a hook.**
- **AM71 (P5 build, 13 Sep) — "ANY TWO OF THESE FIVE" IS NOT EXPRESSIBLE, AND NEITHER IS A JOINT STEP OVER TWO ROLES:** the handover asked for the payment release to be "Joint: Finance approver + Country director". A step names ONE `who`, and a `role` resolves to ONE person, so `kind='joint'` over a role is a step with one seat wearing the word "joint"; and the pool mode (`is_pool`) closes on the FIRST answer, so it cannot express "two of the five signatories" either. The release ships as two sequential `approve` steps, which really is two signatures from two different people. N-of-M over a pool is a genuine engine gap for a later phase.
- **AM72 (P5 build, 13 Sep) — `pb.division` HAS NO `company_id`:** which companies a division belongs to is computed from its department links into a non-stored `company_ids`, so `search([('company_id','=',x)])` raises `Invalid field pb.division.company_id in condition`. Search them all and filter in Python, exactly as `matrix_facade._capabilities` already does.
- **AM73 (P5 build, 13 Sep) — "NO ROUTE" IS NOT THE SAME AS "NO APPROVAL NEEDED", AND ONLY ONE OF THEM WORKS:** four existing suites needed to say "this company has not asked for that check". Deactivating the binding is the obvious move and is WRONG: the engine fails closed on a missing route, so nothing can be sent in at all. The published fast lane is the state the product actually offers, and it is now a real capability — `biz.approval.seed.set_no_approval_needed(company, process_key)` — used by the tests and available to any later screen. AM61's ruling with the trap it did not yet know about.
- **AM74 (P5 build, 13 Sep) — `approval_request_id` IS COMPUTED WITH `@api.depends()` AND THEREFORE NEVER INVALIDATES:** no dependencies at all is deliberate (it is worked out on every read and can never disagree with its record), but nothing evicts the cache. Every adapter's door reads it BEFORE submitting, to refuse a second press — so the cached `False` survived the submit and the record said it had not been sent in. `biz.approval.engine.submit` now invalidates those two fields once, rather than every adapter doing it and one forgetting.
- **AM75 (P5 build, 13 Sep) — A `post-` MIGRATION IN A MODULE EVERYTHING DEPENDS ON RUNS TOO EARLY TO SEE ANY OF IT:** `pb_approval_config.seed_adapters` is a duck-typed loop over the registry, and its `post-` script runs while that module is loading — before every adapter module, all of which depend on it. The loop silently found nothing and the routes were laid later, which read as "the seed is not idempotent". Odoo runs `end-` scripts after the whole graph is loaded (`loading.py` STEP 3.5). **Any relay that asks the registry what it contains belongs in an `end-` script.**
- **AM76 (P5 build, 13 Sep) — DO NOT STAMP `write_date` TO ANSWER "HAS THIS MOVED?":** the arrivals batch froze `employee.write_date` and compared it at apply time. It moves for reasons that have nothing to do with the change, and INSIDE ONE TRANSACTION it does not move at all — Postgres `now()` is the transaction clock (AM62) — so a stamp taken and compared in one request always equals itself. Stamp the VALUES. And not via the adapter's own "what would I write" helper: that one CREATES a department when the payload names a new one, so a read-only stamp would have written to the database, twice. The companion of AM32 and AM46.
- **AM77 (P5 walk, 13 Sep) — A STATE IS NOT A STAMP, AT THE LAST DOOR:** `pb.payment.release._approval_apply` refused unless the bank file read `approved`. The state says somebody approved it; only the hash says the bytes are still the ones they approved. A file edited between approval and release would have gone out as if nothing had happened — at the last door before the money is gone. Safety rail 2 asked for the hash at the release and it was not there until the browser walk found it. **Wherever a record points at another record's approval, re-check the thing that was approved, not the word.**
- **AM78 (P5 walk, 13 Sep) — A SUPERSEDED RECORD MUST NOT BORROW THE "NOT YET" MESSAGE:** downloading a replaced bank file answered *"This file has not been approved yet… It is with its approver"* — about a file that WAS approved and then replaced, sending its reader to look for somebody who signed it weeks ago. Every state that cannot do a thing needs its OWN sentence; a single `if not ready` branch will always lie to some of them.
- **AM79 (P5 build, 13 Sep) — THE INBOX DREW NOTHING FOR A DETAIL WITH CHIPS AND NO ROWS:** `inbox_facade._detail` dropped any payload without `rows`, so "Everybody in this pay run is in the file" — with the bank and the filename beside it — was silent on exactly the reassuring case. Rows OR chips is enough; the table itself draws only when there are rows.
- **AM80 (P5 build, 13 Sep) — A ROLE SCOPED 'area' IS LOOKED UP UNDER WHATEVER SCOPE KEYS THE REQUEST HAS:** the pay-data route asked for the HR lead at `'area'`, and a pay-data file's scope keys are SCHEME keys — so the responsibility was looked up under `scheme:7`, which nobody holds, and (the role forbidding the company fall-back, AM7) every file blocked on a seat that cannot exist. Use `'area'` only where the request's own scope really is a part of the business; a scheme-scoped request asks a company-wide role at `'company'`.
- **AM81 (P5 build, 13 Sep) — AN OVERRIDE THAT RUNS AFTER `super().action_process()` MUST ASK WHETHER ANYTHING WAS PROCESSED:** `pb_hr_fullandfinal` built full-and-final settlements straight after `super()`, and once that `super()` became "ask for approval" it was building settlements out of a file nobody had agreed to. Guarded on the batch really reaching `done`. **Every `super()` that becomes a request breaks every caller that assumed it did the work** — the same question has to be asked of `pb_comp_ben`'s award feed (which now carries the sentinel, because an approved award must not be approved twice) and of the RD54 refresh cron (which deliberately does not, because a scheduled fetch that writes pay data is pay data).
- **AM82 (P5 build, 13 Sep) — TWO MANIFEST DESCRIPTIONS MADE EVERY REGISTRY LOAD LOG AN ERROR:** `ir.module.module.description_html` renders the manifest description as reStructuredText on every load, and `(ERROR/3) Unexpected indentation` from `pb_audit` (an indented bullet list with no blank line before it) and `pb_compliance_hub` (a literal block introduced by one colon) had been in every upgrade log for months. It looks like a real fault and is not, which is what makes a real one easy to miss.
- **AM83 (P6 build, 13 Sep) — ONE SHIM FOR ELEVEN LADDERS, AND IT LIVES INSIDE THE CHAIN MIXIN ITSELF:** `biz_approval_workflow/models/chain_shim.py` declares `_name = 'biz.approval.chain.mixin'` with `_inherit = ['biz.approval.chain.mixin', 'biz.approval.adapter.mixin']` — it EXTENDS the abstract mixin every consumer already inherits and folds the adapter contract into it, so all eleven consumers gain `approval_request_id`, the adapter methods and the engine wiring without a line of their own. What each consumer still has to say is data, not behaviour, and it is one `register_chain(model, process_key, submit_state, driven, …)` call at import time. `CHAIN_PROCESS_KEYS` is that registry.
- **AM84 (P6 build, 13 Sep) — A CONSUMER'S OWN `_approval_can` OVERRIDE COMES BEFORE THE SHIM'S, SO THE COSMETIC BUTTONS STILL ANSWER THE OLD QUESTION:** Odoo's MRO puts a model's own classes ahead of the classes it inherits, so `pb.asset.request._approval_can` (which admits the line manager by name) runs first and never reaches the shim's "do you hold a seat?" answer. Consequence: under a published route the `can_*` view booleans can still offer a button to a group holder who has no seat, and the press is then refused by the engine with "This step is not waiting for you." That is a refusal, not a hole — `_advance_state` IS intercepted (no consumer overrides it) and every decision goes through `biz.approval.engine.decide`, which re-checks the seat, the account and the record's own access. A later phase that wants the buttons to match the route exactly has to change the eleven `_approval_can` overrides, not the shim.
- **AM85 (P6 build, 13 Sep) — THE ENGINE HAD NO WAY TO TELL A RECORD THAT THE ROUTE HAD MOVED ON:** `_approval_apply` fires at the END and `_approval_reject` on a no; there was nothing for the middle. A business object with its own status ladder would therefore have sat at "sent in" until the very last approver, however many steps the route had. `biz.approval.engine._do_approve` now calls `record._approval_advance(request)` after every decision that does not finish the request (swallowed and logged: a consumer that cannot follow its own route must never be able to undo a decision a person really made). The shim maps it: after k decided steps the record wears `driven[k-1]`, capped, and the LAST state in `driven` is reached only by `_approval_apply`. A route with fewer steps than the ladder has degrades correctly — the record jumps to the final state when the request is carried out.
- **AM86 (P6 build, 13 Sep) — AN OVERRIDE THAT APPLIED *BEFORE* ADVANCING THE CHAIN BECOMES AN APPLY ON THE FIRST YES:** `hr.attendance.correction.action_approve` wrote the punch and THEN advanced the chain, which was right when the chain was one rung long. Under a route of three it would have corrected the attendance record on the first approver's press. The apply moved into `_approval_apply`. AM81's family, from the other end: there it was a `super()` that became a request, here it is a caller that assumed the last rung was the only rung. **Every consumer whose approve button does work BEFORE `_advance_state` has this bug** — the other ten do their work in `_after_approval_transition`, which the shim fires from `_approval_apply`, so they are already right.
- **AM87 (P6 build, 13 Sep) — DO NOT STAMP WHAT THE LAST APPROVER IS THERE TO DECIDE:** `pb.rnr.nomination.action_recognise(amount)` writes the prize onto the record and then says yes. The generic revision stamp covers the facts, the amount among them, so the value the final approver had just set made the request differ from itself and the engine refused to carry it out ("This changed after it was sent in"). `_chain_revision_values` is overridden there to stamp the story, the person and the value shown — what everybody in the route is actually agreeing to. The third member of the AM32/AM46/AM76 family: do not stamp what your own freeze changes, what your own snapshot froze, or what the decision itself sets.
- **AM88 (P6 build, 13 Sep) — A JSON LIST CANNOT BE A QUEUE:** "everything waiting about somebody who works for me" is a search, and `biz.approval.request.subject_uids` is a Json list, which cannot appear in a domain (AM8's lesson, second occurrence). `subject_user_ids` mirrors it as a real Many2many, written by `submit` and back-filled by a migration for every request that already exists. Anything the product will ever want to FILTER on has to be a column, however frozen the truth behind it is.
- **AM89 (P6 build, 13 Sep) — THE ENGINE MAY NOT LEARN WHAT A DIVISION IS, SO IT HOLDS A HOOK AND PAYOBOOK FILLS IT:** eleven adapters needed "which part of the business is this about", and the answer is a `pb.division`, which the engine must never hear of (AM2). `chain_shim` keeps two module-level registries — `SCOPE_RESOLVERS` (one record → its scope keys) and `SCOPE_CATALOGUES` (a company → every scope) — and `pb_approval_config/models/seed.py` registers the Payobook answer at import time. With nothing registered every request is scoped to its company, which is the honest answer for a product that has no such concept.
- **AM90 (P6 build, 13 Sep) — EVERY DEFAULT ROUTE THIS PHASE SHIPS ASKS FOR A ROLE AT `'company'` SCOPE, ON PURPOSE:** AM80 says a role is looked up under the REQUEST's own scope keys, and every chain consumer's keys start with `division:<id>`. `hr_lead` forbids the company fall-back, so an `'area'` step would have blocked every request in every division on day one, on a seat nobody had been given. `role_step()` therefore defaults to `scope='company'` and the docstring says why. Narrowing a route to one part of the business stays a deliberate choice a business makes, with the seats to match.
- **AM91 (P6 build, 13 Sep) — A MIGRATION IS NOT ENOUGH FOR A FRESH DATABASE, AND A HOOK IS NOT ENOUGH FOR AN UPGRADE:** AM70 fixed the upgrade case and left the other half open. A brand-new database runs NO migration at all, and the adapter modules do not depend on `pb_approval_config` (nor it on them), so whichever installs second is invisible to the first. Every P6 adapter module therefore ships BOTH: a `post-` migration for `-u` and a `post_init_hook` for a fresh install, with `pb_approval_config`'s `end-` relay as the belt-and-braces third. All idempotent. Three modules already had a `post_init_hook` (`pb_bank_ocr`, `pb_contract_lifecycle`, `pb_pay`) and the seed is chained into it rather than replacing it.
- **AM92 (P6 build, 13 Sep) — A MANY2MANY DECLARED ON AN ABSTRACT MIXIN GETS ITS OWN TABLE PER CONSUMER:** `seat_user_ids` ("a seat is also a read", AM60) is declared once on the chain mixin and materialises as `pb_asset_request_res_users_rel`, `hr_attendance_correction_res_users_rel` and so on — one relation table per concrete model, automatically. What could NOT be shared is the record rule: a rule names one model, so each consumer module ships its own eight-line rule reading that column. `pb_pay` and `pb_comp_ben` deliberately have none: neither gives an ordinary internal user any ACL read at all, so a rule could not widen anything — their routes name roles whose holders hold the matching group, which is what the deploy doc says to check.
- **AM93 (P6 build, 13 Sep) — TODAY'S LADDER BEAT THE HANDOVER WHERE THE TWO DISAGREED:** the handover asked for "Their manager" at the front of the pay-change and pay-review routes. There is no manager rung in today's ladder, and `pb.pay.change` gives an ordinary internal user no read at all — so the route would have handed a seat to somebody the ORM then refused the record. Both ship with today's roles-only ladder (HR lead → Finance approver when above guidance → Country director). Same reasoning kept the employee-record route checking everything: the handover's fast lane for "phone and address" would have waived every field that screen can propose, because all five of them are contact details on this build.
- **AM94 (P6 build, 13 Sep) — THE SECOND APPROVALS QUEUE IS GONE, AND THE DOOR IT LEFT BEHIND IS THE POINT:** `pb.team` is removed — model, cockpit and stylesheet — and `pb_team` stays installed as a redirect to the Workforce Approvals lens, because an action is a link somebody may have saved. Its overtime "clean" verdict moved to `hr.overtime.request._approval_batch_safe` (a new adapter hook, default False, fail-closed), which is the module that knows what a ceiling is. `pb_mission` drops the `pb_team` dependency and gains `pb_approval_config`; the ambient dock now reads `pb.approval.inbox.get_team_data`/`act` — the SAME payload shape, so the dock itself barely changed and gained eleven kinds of request.
- **AM95 (P6 build, 13 Sep) — THE ENGINE'S OWN SUITE HAD NEVER MET THE PRODUCT'S OWN SEED, AND ONE SCAN MADE A COMPANY TAKE HALF A MINUTE TO CREATE:** `biz_approval_workflow`'s 48 cases were last run at P1, before `pb_approval_config` existed. Since P2 that module's `res.company.create` has given EVERY new company a published default route and a first responsibility — so the fixtures' own `bind()` hit `_check_no_tie` ("Other request already covers exactly this place…") 40 times, `t10 no route refuses` found a route, and `t33 coverage finds the gap` found the seeded company-wide holder filling it. The fixtures now end (never delete) whatever a product seeded for their two companies, in `tests/common.py::_clear_seeded`: these cases are about a company that has NOT been set up yet, and that state has to be asked for now rather than assumed. **Any suite that builds its own route for a company it created has the same collision.** The same run took 1829s where it used to take under a minute: `validate_for_publish` runs the whole-coverage scan, which asks every adapter for every place its process happens and resolves every step against each — once per adapter per company, fifteen times over, with `pb.division.company_ids` (computed, non-stored) at the bottom of it. Seeding now passes `approval_skip_coverage=True`, which is honest rather than cheap: the seed confirms every warning it is given anyway, and the run records `coverage_not_run`, the code that has always meant exactly that. A publisher's own press still runs the scan.
- **AM96 (P6 build, 13 Sep) — A SEAT IS ALSO A READ, AND THE THREE MODULES THAT NEEDED IT MOST WERE NOT CHAIN CONSUMERS:** AM60's rule was applied to the eleven chain consumers through the shim and forgotten for the three adapters written by hand. Every one of them failed the same way — `biz.approval.engine.decide` re-checks `record.check_access('read')`, and the person a route names is exactly the person the old ACL never imagined. `hr.leave`: reading somebody else's leave needs a holidays role, and the HR lead may hold none (the manager step is safe only because the time-off module hands a leave manager its Responsible role automatically). `hr.overtime.request`: reading somebody else's overtime needs the attendance MANAGER role and the first step is their line manager. `pb.support.access`: only an administrator could read the table at all, and the whole point of the phase is that somebody else agrees to it. All three now carry `seat_user_ids` with a READ-ONLY record rule — and `pb.support.access` needed a second rule granting `base.group_system` everything, because a rule written for `base.group_user` applies to an administrator too and would have emptied the support screen for the people it was built for.
- **AM97 (P6 build, 13 Sep) — WHO MAY RECORD TIME OFF IS THE TIME-OFF MODULE'S QUESTION, NOT A GROUP CHECK:** `_approval_apply` demanded `group_hr_holidays_user` before approving a leave, which refused the single most ordinary approver there is — the employee's own leave manager, who holds no officer group and is nevertheless allowed by hr_holidays itself. The apply now TRIES the module's own `action_approve` and turns its refusal into the sentence the person reads ("…is not allowed to record it (why). Ask somebody who looks after approvals to move this step"). The rule generalises: an adapter must ask the owning module whether this person may do the thing, never re-implement the answer.
- **AM98 (P6 build, 13 Sep) — A FAST LANE THAT COULD NOT BE CARRIED OUT MUST STILL SAY NO OUT LOUD:** on a "No approval needed" route the engine applies the change inside the very same call and records any failure on the request — quietly. For a door that used to raise the refusal synchronously (the roles board's "every permission in this role is also in another one they hold") that turned a refusal into a cheerful "done". `pb.access`'s three doors now re-raise the block reason, which also unwinds the request written a moment earlier, because nothing happened. **Every adapter whose door used to raise a domain refusal has to check this.**
- **AM99 (P6 build, 13 Sep) — A FIXED DATE IN A FIXTURE IS A TIME BOMB, AND ONE WENT OFF:** `pb_timeoff`'s suite anchored its leaves to `date(2026, 8, 12)`, which stopped being the future on 12 August 2026 — and an allocation starts the day it is made, so a leave in the past has no allocation covering it and `test_05_balance_math` failed with "You do not have any allocation for this time off type" for a reason that had nothing to do with balances. Pre-existing, found because P6 made that suite run. Anchors are worked out from `date.today()` now. The same class of fixture bug bit the new leave cases from the other side: a leave has to fall on days the calendar says are worked, or the time-off module refuses it before any route sees it.
- **AM100 (P6 build, 13 Sep) — A LEGACY SUITE MEETS A NEW ROUTE IN ONE OF TWO HONEST WAYS:** seat the person it already acts as (`pb_timeoff`: its officer becomes the HR lead the route asks), or publish the fast lane for that process in its own company (`biz_access`'s three older suites, `pb_tenancy`'s login suites). Never weaken the gate and never make the product's own seed conditional on being under test. Which one is right depends on what the suite is ABOUT: the time-off cases are about the officer deciding, so they seat them; the access and support cases are about what happens behind the door, so they publish the choice every company is allowed to make and keep testing the door.
- **AM101 (P6 close, 15 Sep) — "NOBODY HOLDS IT TODAY" IS NOT "IT WAS NEVER SET UP":** the three responsibility seeders (`seed_helper.fill_role_from_group`, `pb_payruns/models/approval_seed.py`, `pb_approval_config/models/seed.py`) looked for an ACTIVE holder and created one when none was found. A seat the business had ended was therefore quietly re-seated on the next upgrade, and `test_u12_running_the_seed_again_creates_nothing_new` caught it because the config fixture ends every seeded seat first (8 → 14 rows over two runs). The existence check is now made with `active_test=False` on (company, role, scope ''): once a seat has ever been set up for a company, no seeder touches it again. The whole-coverage check is what tells the business about the gap, not a seeder.
- **AM102 (P6 close, 15 Sep) — THE TEST ENVIRONMENT'S OWN USER IS THE INACTIVE BACKGROUND ACCOUNT:** `TransactionCase.env.user` is `base.user_root`, which is `active=False`. A fixture that seats `cls.env.user` as an approver builds a seat the engine correctly refuses (`engine.py` "Your account is no longer in use", the check AM100 says never to relax). Seat `base.user_admin` (added to the fixture company) and act through it; leave the filing as the background account so the decider is never the submitter. Companion to AM100: `pb_timeoff`'s officer is now the administrator.
- **AM103 (P6 walk, 15 Sep) — ONE PAYLOAD, TWO READERS, ONE OF THEM PRINTS IT RAW:** the inbox card formats `when` through `sentence.js::when()` (UTC in, reader's local time out); the Workforce dock, moved onto the same inbox payload in P6, printed the same field as it came — "2026-09-14 21:20:04" beside a card saying "Tue 15 Sept, 07:20". Whenever a screen is moved onto another screen's payload, walk every field it prints and ask which side formats it. The record stepper's "Sent in · 2026-09-14 21:20" is the OLD chain widget's own raw stamp (biz_approval_chain, pre-P6) — a debt for P7 polish, not a P6 regression.
- **AM104 (P7 build, 15 Sep) — A PROPOSAL IS AN ENGINE OBJECT, NOT THIRTEEN COPIES OF ONE:** Phase 7's whole shape is doors whose PRESS is the change — there is no record to approve because the record is created (or overwritten) by the press itself. Thirteen adapters each needed the same object: what was asked for, what the world looked like at the time, and nothing else, until somebody says yes. `biz.approval.proposal.mixin` (in the ENGINE, which learns nothing: `kind` and `payload` are opaque, the facts come from the caller, the apply is a method on the business model) carries the four parts that would otherwise drift — the snapshot re-check, the gate the original door required, the audit line and the fast lane. A concrete proposal model is then ~60 lines: a `kind` table, a `_live_snapshot`, and one `_apply_<kind>` per door. `PROPOSAL_MODELS` is filled by `_register_hook`, which is what lets ONE seat-is-a-read bridge cover all thirteen (AM92's rule still applies to the RULE, which is per model).
- **AM105 (P7 build, 15 Sep) — THE STAMP COVERS THE PAYLOAD AND THE SNAPSHOT IS CHECKED BY NAME:** `biz.approval.engine._run_apply` already re-computes `_approval_context()['source_revision']` at apply time and refuses a mismatch — with a generic sentence. If the proposal's revision covered the LIVE snapshot, that generic refusal would fire first and the approver would never learn WHICH value moved. So `source_revision` is a hash of the PAYLOAD, which never changes, and `_proposal_moved()` does the comparison explicitly and names every field that differs. The fourth member of the AM32/AM46/AM76/AM87 family, from a new angle: do not let a general rail pre-empt a specific one that can say more.
- **AM106 (P7 build, 15 Sep) — `env.su` IS THE WRONG QUESTION AT A GATE, AND IT IS WRONG ON EXACTLY THE PATH THAT NEEDS IT:** a proposal is written down under `sudo()` (the engine's own bookkeeping — the door has already checked the permission, and `_apply_payload` runs as the real person). Under a published fast lane the engine then applies it INSIDE that same call, so `self.env.su` is True at the moment `_proposal_check_gate()` runs. A gate written as `if self.env.su or user.has_group(...)` would therefore wave through every fast-lane press. `sudo()` does not change `env.uid`, so `self.env.user` is the real person either way — ask that, and `_is_superuser()` for the install hooks.
- **AM107 (P7 build, 15 Sep) — A MODULE THAT EXTENDS A MODEL CANNOT BE IMPORTED FROM THE FILE THAT DEFINES IT:** `letter.py` needed one constant from `letter_approval.py`, and `letter_approval.py` declares `_name = 'pb.hr.letter'` with `_inherit = ['pb.hr.letter', …]`. Importing it from `letter.py` runs its class body BEFORE `class PbHrLetter` exists, and Odoo then tries to extend a model that is not in the registry. The constant is declared in the file that owns the model and imported the other way. The same trap does NOT apply to a module that only defines NEW models (`bands_approval`, `fx_approval`, `verdict_approval`, `unlock_approval`, `month_approval`), which is why those are imported from their doors freely — and why `pb_people_advanced/models/__init__.py` lists the proposal FIRST.
- **AM108 (P7 build, 15 Sep) — A `write()` HOOK IS THE ONE DOOR THAT HAS TO ASK THE ROUTE FIRST:** every other Phase-7 door writes the proposal down and lets the engine decide, because a fast lane applies inside the same call and the answer is the one the screen always had. A proposal made inside `write()` cannot do that: `write` must return True, and returning True for a change that has not happened is a screen that lies. So the four `write` hooks (the VN insurance policy, the VN tax band, the connector's fetch schedule, `res.currency.rate`, the company-wide pay-review limits) call `route_mode()` first and leave the write COMPLETELY alone unless a real multi-step route is published. `route_mode` returns `none` / `fast` / `route`, and `none` behaves like `fast` here on purpose — refusing every edit on a database that has never heard of the process would be a gate nobody asked for (the other half of AM73).
- **AM109 (P7 build, 15 Sep) — FOUR PROPOSAL MODELS LIVE IN THE PAYROLL ENGINE BECAUSE A CATALOGUE ROW NAMES ONE MODEL:** `statutory` has doors in `pb_statutory`, `pb_hr_payroll_vietnam` and `pb_formula_studio`; `mappings` in `pb_hr_payroll_formula` and `pb_formula_studio`; `schememap` in `pb_scheme_map` and `pb_formula_studio`; `demo` in `pb_demo` and `pb_demo_seed`, which do not know about each other. AM45 is why they cannot be two rows. The proposal therefore has to sit in the one module every caller reaches, and for all four that is `pb_hr_payroll_formula`. The cost is ONE new dependency edge in the whole phase — `pb_statutory → pb_hr_payroll_formula` — which is the cheapest of the options that were on the table. **Deviation from the handover's "each module adds its own", deliberate and forced by the one-model-per-row rule.**
- **AM110 (P7 build, 15 Sep) — A PLATFORM ROW IS SEEDED WHERE THE PLATFORM IS, AND ASKED FOR TWICE:** `pb.tenant.proposal._approval_seed_default` returns False when `pb.tenant` is not in the registry, and `pb.demo.proposal`'s does the same when neither demo model is. A tenant database therefore HAS the catalogue row (it comes from `pb_approval_config`, which every database has) and no route for it, which is the honest state: the row is in the list and nothing on that database can raise one. The `tenant` route is also the only one in the product that names the same responsibility twice — two `platform_owner` steps with different keys, because AM71 says a "joint" step over a role is one seat wearing the word, and two sequential steps really are two signatures.
- **AM111 (P7 build, 15 Sep) — THE TYPED SLUG MOVED IN FRONT OF THE PROPOSAL, NOT BEHIND IT:** the fleet's destructive doors check `confirm_slug == tenant.slug` before they do anything. Keeping that check where it was — before the proposal is written — is what stops a typo becoming a REQUEST about the wrong customer, which is worse than a refused press because somebody else then reads it and agrees to it. The slug is carried on the proposal (`payload['typed']`) so the drawer can show what was typed; it is not re-checked at apply time, because by then two owners have read the customer's name on the request.
- **AM112 (P7 build, 15 Sep) — THE WHITE-LABEL GATE IS CASE-SENSITIVE AND COMMENT-BLIND, OR IT IS USELESS:** a repo-wide grep for `odoo` over 44 modules returns 61 hits and all 61 are legitimate — engineers' notes about Odoo 19's own behaviour, `@odoo-module` headers, a provenance key literally spelled `"odoo"`, a `.mc-prov.odoo` css class, and two test files that assert this very rule. The gate that works looks for the whole word **`Odoo`**, capitalised (which is how a reader would ever see it), AFTER stripping `<!-- -->` and `/* */` across lines, skipping `static/tests/`, and skipping a short list of framework tokens. It found nineteen real leaks and every one was the same: `Project-Id-Version: Odoo Server 19.0` in a `.po` header, which AM29 warned about and which nobody had swept since. All nineteen are now `Payobook 19.0`. **A first attempt swept the whole repository with `sed` and touched 5,444 files including every vendored standard addon; it was reverted with `git checkout` and re-done against the module list only. Never run a repo-wide rewrite from a shell one-liner in this tree.**
- **AM113 (P7 build, 15 Sep) — THE IMPORTER GUESSES A ROUTE AND NEVER A PERSON:** the approval matrix every payroll implementation writes in a spreadsheet maps cleanly onto this product — ten rows, ten processes, and the reviewer/final columns are steps. What does NOT map is a name: "Nithya (HR Manager)" is a human being, and matching that to a login by similarity would put the wrong person in an approval chain silently. Named approvers become `pb.approval.proposed.person` rows that say where they came from and wait for somebody to pick the account. Two more refusals of the same kind: a threshold ("<=200 h/person/year") becomes a NOTE and never a condition, because a condition invented from that text would be a silent lie about what the route checks; and an SLA that cannot be read as days ("Case-by-case") sets no due date and says so. Proven against the real RIZE workbook: 10 rows, 10 recognised, 7 SLAs parsed, joint cells split into two signatures, a slash left alone ("HR/Payroll" is one team's name).
- **AM114 (P7 build, 15 Sep) — FIVE DOORS HAD NO PERMISSION CHECK AT ALL, AND THAT WAS THE BIGGER HALF OF THE PHASE:** `pb.statutory.wizard.create_policy`/`create_tax_table` (the numbers the whole country pays), `pb.filing.flow.generate` (a file addressed to a tax authority), `pb.people.onboard.wizard.create_employee` and `pb.people.contract.wizard.create_contract` (a person on the payroll at any salary) — none of them asked anything. Two more were too weak: `vietnam.insurance.adjustment.action_apply` admitted a payroll USER, and `pb.close.unlock_days` asked for an officer while the lock model it calls asks for a manager, so the bulk door was the cheaper way to the wider right. Every one is now gated with the rule its own family already had. **A missing gate is not a phase's incidental finding; where a route is being added to a door, the first question is what that door checked before.**
- **AM115 (P7 build, 15 Sep) — AN APPROVAL UPSTREAM IS NOT A SECOND APPROVAL DOWNSTREAM:** `pb.pip.case.action_terminate` is reached from `pb.resignation._on_resignation_approved` — inside the approval transaction of a resignation somebody has ALREADY agreed to. Putting the verdict route in front of it would have left every improvement plan open for ever after a resignation. The hook calls it with the apply flag on and says why. AM81's family from the third side: there it was a `super()` that became a request, then a caller that assumed the last rung was the only rung, and here it is a caller whose own approval already covers the thing.
- **AM116 (harness, 15 Sep) — `am_tpl` HAS TO BE REFRESHED WITH `-u all` WHENEVER A PHASE ADDS MODELS TO A MODULE THAT IS NOT THE ONE UNDER TEST:** `runtests.sh <m>` clones the template and updates ONLY `<m>`, so a model added in another module has no table in that clone and `ir_model_inherit.parent_id` comes back NULL for anything that extends it. The symptom is not "relation does not exist" (AM57's shape, which at least names the table) but a spray of unrelated failures in modules nobody touched. The fix is one command before the round: back the template up (`createdb -T am_tpl am_tpl_p6bak`) and run `-u all` on `am_tpl`. Phase 7 added eleven models across eleven modules, so every suite in round 1 was measured against a database that did not have them.
- **AM117 (P7 round 1, 15 Sep) — A RELATED SELECTION'S `.selection` IS A CALLABLE, AND IT TOOK DOWN FOUR MODULES:** `pb.hr.letter.letter_type` is `related='template_id.letter_type'`; on this build the related field's `selection` attribute is a FUNCTION that resolves to the base field, so `dict(field.selection)` and any iteration of it raise `TypeError: 'function' object is not iterable`. It sat in `_approval_capabilities()`, which is what EVERY whole-catalogue scan calls on every adapter — so one letter broke the Matrix, the configuration suite, the probation seed and the pay screens, in modules whose own code was fine. **Build a Selection's labels from the list the model is built from, never from `_fields[...].selection`, in anything a catalogue scan reaches.** The same reading found `PAY_LETTER_TYPES` naming five keys this product does not have, so "does this letter quote pay?" was silently always no.
- **AM118 (P7 round 1, 15 Sep) — `manager_mode: False` IS A PUBLISH-TIME REFUSAL, NOT A HINT:** `definition.validate` refuses a `manager` step on a process whose adapter says it has no manager mode — correctly, because a step that can only resolve to nobody is a route that blocks everything. The proposal mixin answered a flat False, which is right for a rate, a band or a customer and wrong for the verdict proposal, whose first step IS the employee's own manager. The route failed validation, `lay()` returned False before the binding, and every verdict then met "there is no approval set up for this yet" — a failure three steps away from its cause. `_proposal_manager_mode` is now a class attribute, and a proposal that sets it True must also override `_approval_manager_uids` (AM50).
- **AM119 (P7 round 1, 15 Sep) — A `write` HOOK THAT PROPOSES TAKES THE ORM'S CONSTRAINTS OUT OF THE PATH:** `pb_hr_payroll_formula`'s own schedule suite found it — `conn.sync_day_of_month = 31` raised no `ValidationError`, because the write became a proposal and `@api.constrains` never ran. Left alone, a bad value would have been written down, sent to somebody, agreed to, and blown up at apply time in front of the approver, about a mistake the typist should have been told about at once. `biz.approval.proposal.mixin.precheck(record, vals)` applies the values to an in-memory copy (`record.new(vals, origin=record)`) and runs `_validate_fields` on it: nothing written, nothing rolled back, no second copy of the rule. **Every `write` hook in the programme calls it.**
- **AM120 (P7 round 1, 15 Sep) — A HELD FIELD SET THAT IS ONE FIELD TOO WIDE IS A HOLE, NOT A GATE:** `cron_pull_enabled` went into the fetch-schedule held set because it sits beside the schedule fields on the form. `integration_cron.py` says in its own words why it is NOT one of them — switching a fetch ON is not a change to WHEN it runs — and holding it meant the hourly dispatcher never saw a connector somebody had just turned on. Five cases across two suites, and a real product hole rather than a test problem. **Take the field list from the code that already reasoned about it, never from the form.**
- **AM121 (P7 round 1, 15 Sep) — A SUITE A PHASE NEVER RAN IS A PHASE THAT DID NOT FINISH:** `pb_pay`'s pay-review suite was asserting the ladder of a product Phase 6 had already replaced — all four rungs driven by one test user — and nobody found out, because P6 ran nine suites and this was not one of them. Meeting the route honestly took four people, not three: the route's independence rail refuses somebody their own approval, so the review is sent in by a preparer who decides none of it, and the case that had the HR lead approving their own first rung now asserts what the product actually promises. **A phase that puts a module's records on a route runs that module's whole suite, or it has not finished.**
- **AM122 (P7 round 1, 15 Sep) — TWO `pb_tenants` CAPACITY CASES ARE ENVIRONMENT-ONLY ON THIS MACHINE AND ALWAYS WERE:** `alert_service._memory_reading` reads `/proc/meminfo` and `/proc/self/status`, which do not exist on macOS, so `total_mb` and `rss_mb` are 0 and `test_t8_the_capacity_reading_is_real` fails with `0 not greater than 0`. `test_t8_a_full_machine_refuses_the_next_customer` is its neighbour and touches no approval door. Neither is a P7 regression, neither is chased, and both pass on the Linux box the platform actually runs on.
