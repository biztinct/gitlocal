<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">

  <!-- Searchable many2one combobox -->
  <t t-name="pb_formula_studio.CfgCombo">
    <div class="cfg-combo" t-ref="root">
      <button type="button" class="cfg-combo-control ce-input" t-on-click="toggle">
        <span t-attf-class="cfg-combo-lbl {{ !displayLabel ? 'ph' : '' }}"><t t-esc="displayLabel || props.placeholder || '— none —'"/></span>
        <svg class="cfg-combo-chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
      </button>
      <div t-if="state.open" class="cfg-combo-menu" t-att-style="state.menuStyle">
        <input class="cfg-combo-search" t-ref="search" placeholder="Type to search…" t-att-value="state.q" t-on-input="onInput" t-on-keydown="onKey"/>
        <div class="cfg-combo-list">
          <div class="cfg-combo-opt none" t-on-click="() => this.pick(false)">— none —</div>
          <t t-foreach="filtered" t-as="o" t-key="o.id">
            <div t-attf-class="cfg-combo-opt {{ o.id === props.value ? 'sel' : '' }} {{ o_index === state.active ? 'active' : '' }}"
                 t-on-click="() => this.pick(o.id)" t-on-mouseenter="() => { state.active = o_index; }"
                 t-esc="label(o)"/>
          </t>
          <div t-if="!filtered.length" class="cfg-combo-empty">No matches</div>
        </div>
      </div>
    </div>
  </t>

  <t t-name="pb_formula_studio.PbFormulaStudio">
    <div class="pbfs">
      <!-- loading -->
      <div t-if="!state.loaded" class="pbfs-loading"><div class="pbfs-spinner"/><span>Loading Formula Studio…</span></div>

      <!-- empty -->
      <div t-elif="state.empty" class="pbfs-empty-state">
        <h2>No formula configurations yet</h2>
        <p>Create your first payroll calculation to get started.</p>
        <button class="pbfs-btn primary" t-on-click="openWizard">New Formula Config</button>
      </div>

      <!-- main -->
      <div t-else="" class="pbfs-main">
        <!-- top bar -->
        <header class="pbfs-top">
          <div class="pbfs-cfg">
            <button class="pbfs-cfg-name" data-coach="fs-config" t-on-click="toggleConfigPicker">
              <span class="nm" t-esc="state.config.name"/>
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
            </button>
            <span t-if="state.config.country" class="pbfs-chip" t-esc="state.config.country"/>
            <span class="pbfs-chip cur" t-esc="'Currency ' + (state.config.currency || '')"/>
            <span t-attf-class="pbfs-state {{ stageCls() }}"><span class="d"/><t t-esc="stageLabel()"/></span>
            <!-- config picker dropdown -->
            <div t-if="state.configPickerOpen" class="pbfs-picker">
              <div t-foreach="state.configs" t-as="cfg" t-key="cfg.id" class="pbfs-picker-row" t-on-click="() => this.pickConfig(cfg.id)">
                <div class="pp-main">
                  <span class="nm" t-esc="cfg.name"/>
                  <span class="meta" t-esc="cfg.rule_count + ' components · ' + cfg.state"/>
                </div>
                <button class="pbfs-picker-gear" t-on-click="(ev) => this.openConfigSettings(cfg.id, ev)" title="Configuration settings">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                </button>
                <button class="pbfs-picker-del" t-on-click="(ev) => this.askDeleteConfig(cfg, ev)" title="Delete configuration">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
                </button>
              </div>
              <div class="pbfs-picker-row new" t-on-click="openWizard">
                <span class="nm">＋ New configuration</span>
                <span class="meta">Guided setup</span>
              </div>
            </div>
          </div>
          <div class="pbfs-top-right">
            <div class="pbfs-ring" title="Validation health">
              <svg width="46" height="46">
                <circle cx="23" cy="23" r="19" stroke="#EEF1F5" stroke-width="5" fill="none"/>
                <circle cx="23" cy="23" r="19" stroke="#0F8A63" stroke-width="5" fill="none" stroke-linecap="round"
                        t-att-stroke-dasharray="ring(state.config.score).dash"
                        t-att-stroke-dashoffset="ring(state.config.score).offset"
                        transform="rotate(-90 23 23)"/>
              </svg>
              <span class="val" t-esc="state.config.score"/>
            </div>
            <div class="pbfs-seg" data-coach="fs-views">
              <button t-att-class="state.view === 'cards' ? 'active' : ''" t-on-click="() => this.setView('cards')">
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>Cards
              </button>
              <button t-att-class="state.view === 'grid' ? 'active' : ''" t-on-click="() => this.setView('grid')">
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18M15 3v18"/></svg>Grid
              </button>
              <button t-att-class="state.view === 'test' ? 'active' : ''" t-on-click="openTest" title="Test &amp; validate">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 2h6M9 2v6.5L4.5 18a2 2 0 0 0 1.8 3h11.4a2 2 0 0 0 1.8-3L15 8.5V2"/><path d="M7 14h10"/></svg>Test
              </button>
              <button t-att-class="state.view === 'settings' ? 'active' : ''" t-on-click="openSettings" title="Configuration settings">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>Settings
              </button>
            </div>
            <button class="pbfs-btn ai" data-coach="fs-payai" t-on-click="openAI">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg>Ask PayAI
            </button>
            <button class="pbfs-btn primary" t-on-click="advance"><t t-esc="nextLabel()"/></button>
          </div>
        </header>

        <!-- ============ CONFIG SETTINGS SURFACE ============ -->
        <t t-if="state.view === 'settings' and state.settings">
        <div class="pbcfg">
          <!-- workflow rail -->
          <div class="pbcfg-rail">
            <div class="pbcfg-stages">
              <t t-foreach="[['draft','Draft'],['testing','Testing'],['validated','Validated'],['active','Active']]" t-as="stg" t-key="stg[0]">
                <div t-attf-class="pbcfg-stage {{ cfgStageCls(stg[0]) }}">
                  <span class="dot"><t t-if="cfgStageCls(stg[0]) === 'done'">✓</t></span>
                  <span class="lb" t-esc="stg[1]"/>
                </div>
              </t>
            </div>
            <div class="pbcfg-rail-side">
              <div class="pbfs-ring" title="Validation health">
                <svg width="42" height="42">
                  <circle cx="21" cy="21" r="19" stroke="#EEF1F5" stroke-width="5" fill="none"/>
                  <circle cx="21" cy="21" r="19" stroke="#0F8A63" stroke-width="5" fill="none" stroke-linecap="round"
                          t-att-stroke-dasharray="ring(state.settings.status.score).dash"
                          t-att-stroke-dashoffset="ring(state.settings.status.score).offset"
                          transform="rotate(-90 21 21)"/>
                </svg>
                <span class="val" t-esc="state.settings.status.score"/>
              </div>
              <span t-attf-class="pbcfg-vbadge {{ state.settings.status.validation_status }}" t-esc="state.settings.status.validation_status"/>
            </div>
          </div>
          <!-- stage actions -->
          <div class="pbcfg-actions">
            <button t-if="cfgState === 'draft'" class="pbfs-btn primary" t-on-click="startTesting">Start Testing</button>
            <button t-if="cfgState === 'testing'" class="pbfs-btn primary" t-on-click="validateCfg">Validate</button>
            <button t-if="cfgState === 'validated'" class="pbfs-btn primary" t-on-click="activateCfg">Activate</button>
            <button t-if="cfgState !== 'draft'" class="pbfs-btn ghost" t-on-click="setDraftCfg">Set to Draft</button>
            <button t-if="!state.settings.status.rule_count" class="pbfs-btn ghost" t-on-click="importExcelCfg">Import from Excel</button>
            <button t-if="state.settings.status.rule_count" class="pbfs-btn ghost" t-on-click="regenerateFormulas">Regenerate Formulas</button>
            <button t-if="cfgState !== 'archived'" class="pbfs-btn danger" t-on-click="archiveCfg">Archive</button>
          </div>
          <!-- alerts -->
          <div t-if="state.settings.status.has_errors and state.settings.status.error_details" class="ed-errbox" t-esc="state.settings.status.error_details"/>
          <!-- T1.6: bare has_circular_ref flag replaced by the human cycle path(s) -->
          <div t-if="graphCycles.length" class="ed-errbox pbfs-cyc-alert">
            <div t-foreach="graphCycles" t-as="cy" t-key="cy_index" class="cyc-alert-row">
              <span t-esc="cy.human_explanation"/>
              <button class="pbfs-link" t-on-click="() => this.showCyclePath(cy)">Show path</button>
            </div>
          </div>

          <!-- tabs -->
          <div class="pbcfg-tabs">
            <t t-foreach="[['setup','Setup'],['automation','Automation'],['intelligence','Intelligence']]" t-as="tb" t-key="tb[0]">
              <button t-attf-class="{{ state.settingsTab === tb[0] ? 'on' : '' }}" t-on-click="() => this.setSettingsTab(tb[0])" t-esc="tb[1]"/>
            </t>
          </div>

          <div t-if="state.settingsError" class="ce-banner err">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
            <span t-esc="state.settingsError"/>
          </div>

          <div class="pbcfg-body">
            <!-- SETUP -->
            <t t-if="state.settingsTab === 'setup'">
              <div class="pbcfg-grid">
                <div class="ce-sec">
                  <div class="ce-sec-h">Essentials</div>
                  <div class="ce-field"><label>Name</label><input class="ce-input" t-att-value="state.setDraft.name" t-on-input="(ev)=>this.setCfgField('name',ev)"/></div>
                  <div class="ce-grid2" style="margin-top:12px">
                    <div class="ce-field"><label>Country</label>
                      <select class="ce-input" t-on-change="(ev)=>this.setCfgField('country_code',ev)">
                        <option t-foreach="cfgMeta('country_codes')" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.setDraft.country_code === o.value" t-esc="o.label"/>
                      </select></div>
                    <div class="ce-field"><label>Cycle type</label>
                      <select class="ce-input" t-on-change="(ev)=>this.setCfgField('cycle_type',ev)">
                        <option t-foreach="cfgMeta('cycle_types')" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.setDraft.cycle_type === o.value" t-esc="o.label"/>
                      </select></div>
                    <div t-if="state.settings.meta.multi_company" class="ce-field"><label>Company</label>
                      <CfgCombo options="cfgMeta('companies')" value="state.setDraft.company_id" placeholder="'Company'" onSelect="(id) => this.pickCfgM2O('company_id', id)"/></div>
                  </div>
                </div>

                <div class="ce-sec">
                  <div class="ce-sec-h">Accounting</div>
                  <div class="ce-sec-sub">Where payslips post in your books.</div>
                  <div class="ce-field"><label>Payroll journal</label>
                    <CfgCombo options="cfgMeta('journals')" value="state.setDraft.payroll_journal_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('payroll_journal_id', id)"/></div>
                  <div class="ce-field" style="margin-top:12px"><label>Default debit account</label>
                    <CfgCombo options="cfgMeta('accounts')" value="state.setDraft.debit_account_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('debit_account_id', id)"/></div>
                  <div class="ce-field" style="margin-top:12px"><label>Default credit account</label>
                    <CfgCombo options="cfgMeta('accounts')" value="state.setDraft.credit_account_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('credit_account_id', id)"/></div>
                </div>

                <div class="ce-sec pbcfg-readonly">
                  <div class="ce-sec-h">Components</div>
                  <div class="pbcfg-stat"><span class="k">Total</span><span t-esc="state.settings.status.rule_count"/></div>
                  <div class="pbcfg-stat"><span class="k">Formulas</span><span t-esc="state.settings.status.formula_rule_count"/></div>
                  <div class="pbcfg-stat"><span class="k">Inputs</span><span t-esc="state.settings.status.input_rule_count"/></div>
                  <button class="pbfs-btn soft sm" style="margin-top:10px" t-on-click="() => this.setView('cards')">Edit components →</button>
                </div>
              </div>

              <div class="ce-sec pbcfg-adv">
                <button class="ce-sec-toggle" t-on-click="toggleCfgAdv">
                  <svg t-attf-class="chev {{ state.cfgAdvOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                  Advanced
                </button>
                <t t-if="state.cfgAdvOpen">
                  <div class="ce-grid2" style="margin-top:14px">
                    <div class="ce-field"><label>Payroll structure</label>
                      <CfgCombo options="cfgMeta('structures')" value="state.setDraft.structure_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('structure_id', id)"/></div>
                    <div class="ce-field"><label>Data source connector</label>
                      <CfgCombo options="cfgMeta('connectors')" value="state.setDraft.connector_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('connector_id', id)"/></div>
                    <div class="ce-field"><label>Reference code</label><input class="ce-input mono" t-att-value="state.setDraft.code" t-on-input="(ev)=>this.setCfgField('code',ev)"/></div>
                  </div>
                  <label class="ce-tog" style="margin-top:14px"><input type="checkbox" t-att-checked="state.setDraft.use_color_coded_excel_import" t-on-change="(ev)=>this.setCfgField('use_color_coded_excel_import',ev)"/><span>Use color-coded Excel import</span></label>
                </t>
              </div>
            </t>

            <!-- AUTOMATION -->
            <t t-elif="state.settingsTab === 'automation'">
              <div class="pbcfg-grid">
                <div class="ce-sec">
                  <div class="ce-sec-h">Proration</div>
                  <label class="ce-tog"><input type="checkbox" t-att-checked="state.setDraft.use_proration" t-on-change="(ev)=>this.setCfgField('use_proration',ev)"/><span>Enable proration</span></label>
                  <t t-if="state.setDraft.use_proration">
                    <div class="ce-grid2" style="margin-top:14px">
                      <div class="ce-field"><label>Proration basis</label>
                        <select class="ce-input" t-on-change="(ev)=>this.setCfgField('proration_basis',ev)">
                          <option t-foreach="cfgMeta('proration_bases')" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.setDraft.proration_basis === o.value" t-esc="o.label"/>
                        </select></div>
                      <div class="ce-field"><label>Rounding (decimals)</label><input class="ce-input" type="number" t-att-value="state.setDraft.proration_rounding" t-on-change="(ev)=>this.setCfgField('proration_rounding',ev)"/></div>
                    </div>
                    <label class="ce-label" style="margin-top:14px; display:block">Prorated components</label>
                    <div class="ce-pal-grp">
                      <button t-foreach="cfgMeta('components')" t-as="o" t-key="o.id" t-attf-class="ce-chip col {{ cfgM2MHas('proration_component_ids', o.id) ? 'on' : '' }}" t-on-click="()=>this.toggleCfgM2M('proration_component_ids', o.id)" t-att-title="o.name">
                        <span class="ce-chip-col" t-esc="o.col"/><span class="ce-chip-nm" t-esc="o.name"/>
                      </button>
                    </div>
                  </t>
                </div>

                <div class="ce-sec">
                  <div class="ce-sec-h">Retroactive adjustments</div>
                  <label class="ce-tog"><input type="checkbox" t-att-checked="state.setDraft.use_auto_retro" t-on-change="(ev)=>this.setCfgField('use_auto_retro',ev)"/><span>Enable auto retro</span></label>
                  <div t-if="state.setDraft.use_auto_retro" class="ce-field" style="margin-top:14px"><label>Retro target component</label>
                    <CfgCombo options="cfgMeta('components')" value="state.setDraft.retro_component_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('retro_component_id', id)"/></div>
                </div>
              </div>
            </t>

            <!-- INTELLIGENCE (T1.5 + T1.6) -->
            <t t-elif="state.settingsTab === 'intelligence'">
              <!-- circular references first — the loudest signal -->
              <div t-if="graphCycles.length" class="ce-sec pbfs-intel-cyc">
                <div class="ce-sec-h">Circular references</div>
                <div class="ce-sec-sub">These components depend on each other in a loop — the engine cannot resolve them.</div>
                <t t-foreach="graphCycles" t-as="cy" t-key="cy_index">
                  <div class="intel-cyc-row">
                    <span class="cyc-msg" t-esc="cy.human_explanation"/>
                    <button class="pbfs-btn ghost sm" t-on-click="() => this.showCyclePath(cy)">Show path</button>
                  </div>
                </t>
              </div>

              <div class="pbcfg-grid pbfs-intel">
                <div class="ce-sec">
                  <div class="ce-sec-h">Execution order</div>
                  <div class="ce-sec-sub">The order the engine evaluates formulas — every dependency is computed before the components that use it.</div>
                  <div class="intel-order">
                    <t t-foreach="execOrderItems" t-as="it" t-key="it.id">
                      <div class="intel-ord-row" t-att-data-col="it.col">
                        <span class="intel-step" t-esc="it_index + 1"/>
                        <span class="intel-col" t-esc="it.col"/>
                        <span class="intel-nm" t-esc="it.name"/>
                        <span t-if="it.is_valid" class="intel-ok" title="Valid">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 4 4 10-10"/></svg>
                        </span>
                        <span t-else="" class="intel-bad" title="Check this formula">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                        </span>
                      </div>
                    </t>
                    <div t-if="!execOrderItems.length" class="intel-empty">No formula components yet.</div>
                  </div>
                </div>

                <div class="ce-sec">
                  <div class="ce-sec-h">Unused components</div>
                  <div class="ce-sec-sub">Nothing depends on these and they don't appear on payslips — safe to archive.</div>
                  <div class="intel-unused">
                    <t t-foreach="unusedItems" t-as="u" t-key="u.id">
                      <div class="intel-un-row" t-att-data-col="u.col">
                        <span class="intel-col" t-esc="u.col"/>
                        <span class="intel-nm" t-esc="u.name"/>
                        <span class="intel-typ" t-esc="u.type"/>
                        <button t-if="state.canEdit" class="pbfs-btn danger sm" t-on-click="() => this.deleteComponent(u.id)" title="Archive (delete) this component">Archive</button>
                      </div>
                    </t>
                    <div t-if="!unusedItems.length" class="intel-empty">No unused components — every component is referenced or shown on a payslip.</div>
                  </div>
                </div>
              </div>
            </t>
          </div>

          <div class="pbcfg-foot">
            <button class="pbfs-btn ghost" t-on-click="revertSettings">Revert</button>
            <button class="pbfs-btn primary" t-att-disabled="state.settingsBusy" t-on-click="saveSettings">Save settings</button>
          </div>
        </div>
        </t>

        <!-- ============ TEST &amp; VALIDATE WORKBENCH ============ -->
        <t t-elif="state.view === 'test'">
        <div class="pbtest">
          <!-- left rail -->
          <div class="pbtest-rail">
            <div class="pbtest-rail-head">
              <div class="pbtest-rail-title">Test Samples</div>
              <div class="pbtest-btnrow">
                <button class="pbfs-btn soft sm" t-on-click="addManualSample" title="Enter input values by hand">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg>Manual
                </button>
                <div class="pbtest-gen">
                  <button class="pbfs-btn primary sm" t-on-click="toggleTestGen">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>Generate
                  </button>
                  <div t-if="state.testGenOpen" class="pbtest-gen-menu">
                    <div class="gm-rand">
                      <div class="gm-rl">Random samples</div>
                      <div class="gm-rrow">
                        <label class="gm-f"><span>Count</span><input type="number" min="1" t-att-value="state.randomCount" t-on-change="(ev)=>this.setRandomField('randomCount',ev)"/></label>
                        <label class="gm-f"><span>Min salary</span><input type="number" t-att-value="state.randomMin" t-on-change="(ev)=>this.setRandomField('randomMin',ev)"/></label>
                        <label class="gm-f"><span>Max salary</span><input type="number" t-att-value="state.randomMax" t-on-change="(ev)=>this.setRandomField('randomMax',ev)"/></label>
                      </div>
                      <button class="pbfs-btn soft sm gm-go" t-on-click="generateRandom">Generate</button>
                    </div>
                    <button class="gm-item" t-on-click="() => this.generateFromWizard('employees')">From employees <small>pull contract values</small></button>
                    <button class="gm-item" t-on-click="() => this.generateFromWizard('payslips')">From payslips <small>inputs + expected (regression)</small></button>
                  </div>
                </div>
                <button class="pbfs-btn soft sm" t-on-click="triggerImport" title="Import input values from an Excel file">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>Import Excel
                </button>
                <button class="pbfs-btn soft sm" t-on-click="exportTestTemplate" title="Download a blank input template (.xlsx)">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>Export Template
                </button>
                <input type="file" t-ref="testFile" accept=".xlsx" style="display:none" t-on-change="onImportFile"/>
              </div>
            </div>
            <button class="pbfs-btn soft sm pbtest-runall" t-on-click="runAllTests">Run all tests</button>
            <div class="pbtest-list">
              <div t-if="!state.test.samples.length" class="pbtest-empty">No samples yet — Generate one to start.</div>
              <div t-foreach="state.test.samples" t-as="s" t-key="s.id"
                   t-attf-class="pbtest-item {{ s.id === state.testSampleId ? 'active' : '' }}"
                   t-on-click="() => this.selectSample(s.id)">
                <div class="nm" t-esc="s.name"/>
                <span t-attf-class="pbcfg-vbadge {{ s.verdict }}" t-esc="s.verdict"/>
                <button class="pbtest-del" t-on-click="(ev) => { ev.stopPropagation(); this.deleteSample(s.id); }" title="Delete sample">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
                </button>
              </div>
            </div>
          </div>

          <!-- detail -->
          <div class="pbtest-detail">
            <t t-if="state.testDetail">
              <div class="pbtest-dhead">
                <input class="pbtest-name" t-att-value="state.testDetail.name" t-on-change="renameSample"/>
                <span class="pbtest-src" t-esc="state.testDetail.source_type"/>
                <span t-attf-class="pbcfg-vbadge big {{ state.testDetail.verdict }}" t-esc="state.testDetail.verdict"/>
                <div class="pbtest-dact">
                  <button class="pbfs-btn primary sm" t-on-click="openInFormulaView">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
                    <t t-esc="state.testDetail.source_type === 'manual' ? 'Calculate in Formula View' : 'Open in Formula View'"/>
                  </button>
                  <button t-if="!state.testDetail.has_expected" class="pbfs-btn soft sm" t-on-click="snapshotExpected">Snapshot as expected</button>
                  <button t-if="state.testDetail.has_expected" class="pbfs-btn ghost sm" t-on-click="clearExpected">Clear expected</button>
                </div>
              </div>

              <!-- verdict banner -->
              <t t-set="fails" t-value="state.testDetail.rows.filter(r => r.column_type !== 'input' &amp;&amp; r.expected !== null &amp;&amp; r.expected !== undefined &amp;&amp; !r.passed)"/>
              <div t-if="!state.testDetail.has_expected" class="pbtest-banner preview">
                <b>Preview only.</b> No expected baseline yet — the outputs below are just computed. Click <b>Snapshot as expected</b> to freeze them as the pass/fail baseline, or generate from payslips for real expected values.
              </div>
              <div t-elif="state.testDetail.verdict === 'passed'" class="pbtest-banner passed"><b>Passed.</b> Every output matches its expected baseline.</div>
              <div t-else="" t-attf-class="pbtest-banner {{ state.testDetail.verdict }}"><b><t t-esc="state.testDetail.verdict === 'warning' ? 'Warning' : 'Failed'"/>.</b> <t t-esc="fails.length"/> output<t t-if="fails.length !== 1">s</t> differ from the expected baseline — flagged ✗ below.</div>

              <!-- INPUTS first (edit at the top, results recompute below) -->
              <div class="ce-sec">
                <button class="ce-sec-toggle" t-on-click="toggleTestInputs">
                  <svg t-attf-class="chev {{ state.testInputsOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                  Inputs (<t t-esc="state.testDetail.rows.filter(x => x.column_type === 'input').length"/>) <span class="pbtest-hint">edit to recompute live</span>
                </button>
                <t t-if="state.testInputsOpen">
                  <div class="pbtest-inputs" style="margin-top:14px">
                    <t t-foreach="state.testDetail.rows" t-as="r" t-key="r.code">
                      <div t-if="r.column_type === 'input'" class="pbtest-inrow">
                        <span class="lbl"><span class="c" t-esc="r.column_letter"/><t t-esc="r.name"/></span>
                        <input class="ce-input" type="number" step="any" t-att-value="r.input" t-on-input="(ev) => this.onTestInput(r.code, ev)"/>
                      </div>
                    </t>
                    <div t-if="!state.testDetail.rows.some(x => x.column_type === 'input')" class="pbtest-hint">This config has no input components.</div>
                  </div>
                </t>
              </div>

              <!-- RESULTS below inputs -->
              <div class="ce-sec">
                <div class="ce-sec-h">Results<t t-if="state.testDetail.has_expected"> · expected vs computed</t></div>
                <table class="pbcfg-table">
                  <thead><tr><th>Col</th><th>Component</th><th t-if="state.testDetail.has_expected">Expected</th><th>Computed</th><th t-if="state.testDetail.has_expected">Diff</th><th t-if="state.testDetail.has_expected"></th></tr></thead>
                  <tbody>
                    <t t-foreach="state.testDetail.rows" t-as="r" t-key="r.code">
                      <tr t-if="r.column_type !== 'input'" t-attf-class="{{ state.testDetail.has_expected &amp;&amp; r.expected !== null &amp;&amp; r.expected !== undefined &amp;&amp; !r.passed ? 'failrow' : '' }}">
                        <td t-esc="r.column_letter"/><td t-esc="r.name"/>
                        <td t-if="state.testDetail.has_expected" t-esc="tcell(r.expected)"/>
                        <td t-esc="tcell(r.computed)"/>
                        <td t-if="state.testDetail.has_expected" t-esc="(r.expected !== null &amp;&amp; r.expected !== undefined) ? (r.discrepancy ? r.discrepancy.toFixed(2)+'%' : '0%') : '—'"/>
                        <td t-if="state.testDetail.has_expected">
                          <span t-if="r.expected !== null &amp;&amp; r.expected !== undefined" t-attf-class="pbtest-tick {{ r.passed ? 'ok' : 'no' }}"><t t-esc="r.passed ? '✓' : '✗'"/></span>
                          <span t-else="" class="pbtest-tick na">—</span>
                        </td>
                      </tr>
                    </t>
                  </tbody>
                </table>
              </div>
            </t>
            <div t-else="" class="pbtest-noselect">Select or generate a sample to view its inputs &amp; results.</div>
          </div>
        </div>
        </t>

        <!-- empty config: "finish setup" resume panel (no dead-end) -->
        <t t-elif="!state.components.length">
        <div class="pbfs-build">
          <div class="pbfs-build-inner">
            <div class="pbfs-build-head">
              <h2>Finish setting up <t t-esc="state.config.name"/></h2>
              <p>This configuration has no components yet. Pick how you'd like to build it — you can mix and match afterwards.</p>
            </div>
            <div class="pbfs-build-grid">
              <button class="pbfs-build-card" t-on-click="importExcelInto">
                <span class="ic indigo"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg></span>
                <span class="t">Import from Excel</span>
                <span class="d">Upload a spreadsheet — sheets, columns &amp; formulas detected automatically.</span>
              </button>
              <button class="pbfs-build-card" t-on-click="() => this.applyStarter('vn_standard')">
                <span class="ic green"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg></span>
                <span class="t">Vietnam Standard starter</span>
                <span class="d">10 pre-wired components: Basic, allowances, SI/HI/UI, Gross &amp; Net.</span>
              </button>
              <button class="pbfs-build-card" t-on-click="addComponentQuick">
                <span class="ic blue"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></span>
                <span class="t">Add a component</span>
                <span class="d">Start from scratch and define each column yourself.</span>
              </button>
              <button class="pbfs-build-card" t-on-click="openAI">
                <span class="ic violet"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg></span>
                <span class="t">Ask PayAI to draft</span>
                <span class="d">Describe your payroll in plain language and let PayAI propose formulas.</span>
              </button>
            </div>
            <button class="pbfs-build-discard" t-on-click="discardConfig">Discard this draft</button>
          </div>
        </div>
        </t>

        <!-- 3-pane -->
        <t t-else="">
        <div class="pbfs-work">
          <svg id="pbfsArrows" class="pbfs-arrows"/>
          <!-- outline -->
          <div class="pbfs-outline" data-coach="fs-components">
            <div class="ol-head">
              <span class="t">Components</span>
              <div class="ol-actions">
                <button class="ol-act" data-coach="fs-add" t-on-click="addComponentQuick" title="Add a component">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                </button>
                <button class="ol-act" t-on-click="importExcelInto" title="Import from Excel">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                </button>
                <button t-attf-class="ol-arrows {{ state.arrowsOn ? 'on' : '' }}" data-coach="fs-arrows" t-on-click="toggleArrows" title="Dependency arrows">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                </button>
              </div>
            </div>
            <t t-foreach="visibleGroups" t-as="grp" t-key="grp">
              <div class="ol-grp" t-esc="grp"/>
              <div t-foreach="groupItems(grp)" t-as="c" t-key="c.id"
                   t-att-data-col="c.col"
                   t-attf-class="ol-item {{ c.id === state.selectedId ? 'active' : '' }} {{ isDep(c.col) ? 'dep' : '' }} {{ isCycleMember(c.col) ? 'cyc-hi' : '' }}"
                   t-attf-style="{{ depStyle(c) }}"
                   t-on-click="() => this.selectComponent(c.id)">
                <span class="ol-col" t-esc="c.col"/>
                <span class="ol-nm" t-esc="c.name"/>
                <span t-if="!c.is_valid" class="ol-err" title="Has errors"/>
                <span t-attf-class="ol-typ {{ c.type }}" t-esc="c.type"/>
              </div>
            </t>
          </div>

          <!-- editor: cards -->
          <div t-if="state.view === 'cards'" class="pbfs-editor">
            <div t-if="selected" t-attf-class="ed-card {{ editing ? 'is-editing' : '' }}">
              <t t-if="!editing">
              <div class="ed-hero" data-coach="fs-card">
                <div class="col-badge" t-esc="selected.col"/>
                <div style="flex:1; min-width:200px">
                  <h2><t t-esc="selected.name"/><span class="code-tag" t-esc="selected.code"/></h2>
                  <div class="sub" t-esc="'Column ' + selected.col + ' · ' + (selected.type === 'input' ? 'Input value' : selected.type === 'constant' ? 'Fixed constant' : 'Calculated formula') + (selected.category ? ' · ' + selected.category : '')"/>
                </div>
                <div class="ed-hero-side">
                  <div t-if="selected.is_valid" class="valid-ok"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m9 12 2 2 4-4"/><circle cx="12" cy="12" r="9"/></svg>Valid</div>
                  <div t-else="" class="valid-err"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>Check</div>
                  <div class="ed-actions">
                    <button class="pbfs-btn soft sm" t-on-click="openExplain" title="Explain in plain language">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 16v-4M12 8h.01"/></svg>Explain
                    </button>
                    <button class="pbfs-btn soft sm" data-coach="fs-editai" t-on-click="openAI" title="Edit with PayAI">
                      <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg>Edit with PayAI
                    </button>
                    <button class="pbfs-btn ghost sm" t-on-click="() => this.enterEdit(selected.id)" title="Edit this component">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.12 2.12 0 0 1 3 3L12 15l-4 1 1-4z"/></svg>Edit
                    </button>
                    <button class="pbfs-btn danger sm" t-on-click="() => this.deleteComponent(selected.id)" title="Delete this component">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>Delete
                    </button>
                  </div>
                </div>
              </div>
              <div class="ed-body">
                <div class="ed-label-row">
                  <span class="ed-label">Formula</span>
                  <div t-if="selected.type === 'formula'" class="ff-seg" data-coach="fs-namesletters" title="Toggle formula display">
                    <button t-att-class="!state.formulaShort ? 'on' : ''" t-on-click="() => this.setFormulaForm(false)">Names</button>
                    <button t-att-class="state.formulaShort ? 'on' : ''" t-on-click="() => this.setFormulaForm(true)">Letters</button>
                  </div>
                </div>
                <div class="formula-line" data-coach="fs-formula">
                  <t t-foreach="chips(selected.excel_formula)" t-as="tok" t-key="tok_index">
                    <span t-if="tok.kind === 'op'" t-attf-class="fop #{tok.ded ? 'fop-ded' : ''}" t-att-title="tok.ded ? 'This component is a deduction (a negative value) — it is subtracted from the total' : ''" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'func'" class="fchip ffunc" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'ref'" class="fchip fref clickable" t-on-click="() => this.scrollToCol(tok.col)" t-att-title="'Go to ' + (byCol(tok.col) ? byCol(tok.col).name : tok.col)">
                      <span t-if="state.formulaShort" class="ref solo" t-esc="tok.col"/>
                      <t t-else=""><span class="ref" t-esc="tok.col"/><t t-esc="tok.text"/></t>
                    </span>
                    <span t-elif="tok.kind === 'range'" class="fchip frange clickable" t-on-click="() => this.scrollToCol(tok.start)" t-att-title="'Range (' + tok.count + '): ' + tok.names.join(', ')">
                      <span class="ref" t-esc="tok.start"/><span class="rng-sep">…</span><span class="ref" t-esc="tok.end"/>
                      <span t-if="!state.formulaShort" class="rng-nm" t-esc="tok.startName + ' … ' + tok.endName"/>
                      <span class="rng-cnt" t-esc="tok.count"/>
                    </span>
                    <span t-elif="tok.kind === 'num'" class="fchip fnum" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'paren'" class="fparen" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'comma'" class="fcomma">,</span>
                    <span t-else="" class="fchip src" t-esc="tok.text"/>
                  </t>
                </div>
                <div class="dep-map" data-coach="fs-deps">
                  <div class="ed-label">Dependencies</div>
                  <div class="dep-row"><span class="lbl">Depends on</span>
                    <t t-if="selected.depends_on.length"><span t-foreach="selected.depends_on" t-as="d" t-key="d_index" class="dep-pill" t-esc="d"/></t>
                    <span t-else="" class="dep-pill">— none —</span>
                  </div>
                  <div class="dep-row"><span class="lbl">Used by</span>
                    <t t-if="selected.used_by.length"><span t-foreach="selected.used_by" t-as="d" t-key="d_index" class="dep-pill" t-esc="d"/></t>
                    <span t-else="" class="dep-pill">— none —</span>
                  </div>
                </div>
                <div t-if="!selected.is_valid and selected.validation_message" class="ed-errbox" t-esc="selected.validation_message"/>

                <!-- T1.6 — circular-reference explainer (only when this component is in a cycle) -->
                <div t-if="selectedCycle" class="pbfs-cycbox">
                  <div class="cyc-row">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2v6h-6"/><path d="M3 12a9 9 0 0 1 15-6.7L21 8"/><path d="M3 22v-6h6"/><path d="M21 12a9 9 0 0 1-15 6.7L3 16"/></svg>
                    <span class="cyc-msg" t-esc="selectedCycle.human_explanation"/>
                  </div>
                  <button class="pbfs-btn ghost sm" t-on-click="() => this.showCyclePath(selectedCycle)">Show path</button>
                </div>

                <!-- T1.4 — Insights: impact of this component (client BFS, instant) -->
                <t t-if="selected.type === 'formula'">
                  <t t-set="imp" t-value="selectedImpact"/>
                  <div class="pbfs-insights" data-coach="fs-insights">
                    <div class="ins-head"><span class="ins-t">Impact</span></div>
                    <div class="ins-sentence">
                      <t t-if="imp.down.length">
                        <b t-esc="imp.down.length"/> <t t-esc="imp.down.length === 1 ? 'component depends' : 'components depend'"/> on <b t-esc="selected.name"/><t t-if="imp.payvis.length">, <b t-esc="imp.payvis.length"/> shown on payslips</t>.
                      </t>
                      <t t-else="">Nothing depends on this yet.</t>
                    </div>
                    <div t-if="imp.up.length" class="ins-chips">
                      <span class="ins-lbl">Draws from</span>
                      <span class="pbfs-impact-chip" t-esc="imp.up.length + (imp.up.length === 1 ? ' component' : ' components')"/>
                    </div>
                    <t t-if="imp.down.length">
                      <button class="ins-toggle" t-on-click="toggleInsights">
                        <svg t-attf-class="chev {{ state.insightsOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                        <t t-esc="state.insightsOpen ? 'Hide downstream' : 'Show downstream (' + imp.down.length + ')'"/>
                      </button>
                      <div t-if="state.insightsOpen" class="ins-list">
                        <button t-foreach="imp.down" t-as="d" t-key="d.id" class="ins-item" t-on-click="() => this.selectComponent(d.id)" t-att-title="'Go to ' + d.name">
                          <span class="ins-col" t-esc="d.col"/>
                          <span class="ins-nm" t-esc="d.name"/>
                          <span t-if="d.appears_on_payslip" class="ins-badge">payslip</span>
                        </button>
                      </div>
                    </t>
                  </div>
                </t>

                <div t-if="selected.type === 'formula'" class="fc-section" data-coach="fs-flow">
                  <div class="fc-head">
                    <span class="fc-head-t">Calculation flow</span>
                    <span class="fc-legend-inline">
                      <span class="lg"><span class="sw" style="background:#0E7490"/>input</span>
                      <span class="lg"><span class="sw" style="background:#4F46E5"/>earning</span>
                      <span class="lg"><span class="sw" style="background:#B45309"/>deduction</span>
                      <span class="lg"><span class="sw" style="background:#059669"/>total</span>
                    </span>
                    <button class="fc-max" data-coach="fs-expand" t-on-click="openExpand" title="Expand flowchart">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 3h6v6"/><path d="M9 21H3v-6"/><path d="M21 3l-7 7"/><path d="M3 21l7-7"/></svg>Expand
                    </button>
                  </div>
                  <div class="fc-inline" t-on-click="openExpand" title="Click to expand">
                    <div class="fc-zoomwrap"><t t-call="pb_formula_studio.Flow"><t t-set="flow" t-value="inlineFlow"/></t></div>
                  </div>
                </div>
              </div>
              </t>

              <!-- ============ INLINE EDIT VIEW ============ -->
              <t t-else="">
                <div class="ce-head">
                  <div class="ce-badge" t-esc="state.draft.column_letter || selected.col"/>
                  <input class="ce-title" t-att-value="state.draft.name" placeholder="Component name"
                         t-on-input="(ev) => this.setDraftField('name', ev)"/>
                  <div class="ce-head-side">
                    <div class="ff-seg" title="Editing depth">
                      <button t-att-class="state.editScope === 'simple' ? 'on' : ''" t-on-click="() => this.setEditScope('simple')">Simple</button>
                      <button t-att-class="state.editScope === 'full' ? 'on' : ''" t-on-click="() => this.setEditScope('full')">Full</button>
                    </div>
                    <button class="pbfs-btn ghost sm" t-on-click="cancelEdit">Cancel</button>
                    <button class="pbfs-btn primary sm" t-att-disabled="state.editBusy" t-on-click="saveComponent">Save</button>
                  </div>
                </div>

                <div class="ce-body">
                  <div t-if="state.editError" class="ce-banner err">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                    <span t-esc="state.editError"/>
                  </div>
                  <!-- type chooser -->
                  <div class="ce-label">Type</div>
                  <div class="ce-types">
                    <button t-attf-class="ce-type {{ draftType === 'input' ? 'on' : '' }}" t-on-click="() => this.setDraftType('input')">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
                      <b>Input</b><small>From a data source</small>
                    </button>
                    <button t-attf-class="ce-type {{ draftType === 'formula' ? 'on' : '' }}" t-on-click="() => this.setDraftType('formula')">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16"/><path d="M9 4c-2 6-2 10-4 16"/><path d="M14 9l4 6"/><path d="M18 9l-4 6"/></svg>
                      <b>Formula</b><small>Calculated</small>
                    </button>
                    <button t-attf-class="ce-type {{ draftType === 'constant' ? 'on' : '' }}" t-on-click="() => this.setDraftType('constant')">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M8 12h8"/></svg>
                      <b>Constant</b><small>Fixed value</small>
                    </button>
                  </div>

                  <!-- FORMULA body -->
                  <t t-if="draftType === 'formula'">
                    <div class="ce-label-row">
                      <span class="ce-label">Formula</span>
                      <div t-if="state.liveValid" t-attf-class="ce-pill {{ state.liveValid.valid ? 'ok' : 'err' }}">
                        <t t-if="state.liveValid.valid">Valid</t><t t-else="">Check formula</t>
                      </div>
                    </div>
                    <input class="ce-input mono" t-ref="formulaInput" t-att-value="state.draft.excel_formula"
                           placeholder="e.g. =A1 + B1*0.08" t-on-input="onFormulaInput"/>
                    <div class="ce-pal">
                      <div class="ce-pal-grp">
                        <span class="ce-pal-lbl">Columns</span>
                        <button t-foreach="paletteColumns" t-as="c" t-key="c.id" class="ce-chip col"
                                t-on-click="() => this.insertToken(c.col + '1')" t-att-title="c.name">
                          <span class="ce-chip-col" t-esc="c.col"/><span class="ce-chip-nm" t-esc="c.name"/>
                        </button>
                      </div>
                      <div class="ce-pal-grp">
                        <span class="ce-pal-lbl">Operators</span>
                        <button t-foreach="paletteOps" t-as="op" t-key="op.t" class="ce-chip op" t-on-click="() => this.insertToken(op.t)" t-esc="op.g"/>
                      </div>
                      <div class="ce-pal-grp">
                        <span class="ce-pal-lbl">Functions</span>
                        <button t-foreach="paletteFns" t-as="fn" t-key="fn" class="ce-chip fn" t-on-click="() => this.insertToken(fn + '(')" t-esc="fn"/>
                      </div>
                    </div>
                    <div class="ce-label" style="margin-top:18px">Preview</div>
                    <div class="formula-line">
                      <span t-if="!draftChips.length" class="fchip src">empty</span>
                      <t t-foreach="draftChips" t-as="tok" t-key="tok_index">
                        <span t-if="tok.kind === 'op'" t-attf-class="fop #{tok.ded ? 'fop-ded' : ''}" t-att-title="tok.ded ? 'This component is a deduction (a negative value) — it is subtracted from the total' : ''" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'func'" class="fchip ffunc" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'ref'" class="fchip fref"><span class="ref" t-esc="tok.col"/><t t-esc="tok.text"/></span>
                        <span t-elif="tok.kind === 'range'" class="fchip frange" t-att-title="'Range (' + tok.count + '): ' + tok.names.join(', ')"><span class="ref" t-esc="tok.start"/><span class="rng-sep">…</span><span class="ref" t-esc="tok.end"/><span class="rng-cnt" t-esc="tok.count"/></span>
                        <span t-elif="tok.kind === 'num'" class="fchip fnum" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'paren'" class="fparen" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'comma'" class="fcomma">,</span>
                        <span t-else="" class="fchip src" t-esc="tok.text"/>
                      </t>
                    </div>
                    <div t-if="state.liveValid and !state.liveValid.valid and state.liveValid.message" class="ed-errbox" t-esc="state.liveValid.message"/>
                  </t>

                  <!-- INPUT body -->
                  <t t-elif="draftType === 'input'">
                    <div class="ce-grid2">
                      <div class="ce-field"><label>Source field mapping</label>
                        <input class="ce-input" t-att-value="state.draft.data_source_field" placeholder="e.g. basic_salary" t-on-input="(ev) => this.setDraftField('data_source_field', ev)"/></div>
                      <div class="ce-field"><label>Default value</label>
                        <input class="ce-input" type="number" step="any" t-att-value="state.draft.default_value" t-on-change="(ev) => this.setDraftField('default_value', ev)"/></div>
                    </div>
                  </t>

                  <!-- CONSTANT body -->
                  <t t-else="">
                    <div class="ce-field"><label>Constant value</label>
                      <input class="ce-input big" type="number" step="any" t-att-value="state.draft.constant_value" t-on-change="(ev) => this.setDraftField('constant_value', ev)"/></div>
                  </t>

                  <!-- ============ FULL sections ============ -->
                  <t t-if="state.editScope === 'full'">
                    <div class="ce-sec">
                      <div class="ce-sec-h">Identity</div>
                      <div class="ce-grid2">
                        <div class="ce-field"><label>Code</label>
                          <input class="ce-input mono" t-att-value="state.draft.code" t-on-input="(ev) => this.setDraftField('code', ev)"/></div>
                        <div class="ce-field"><label>Sequence</label>
                          <input class="ce-input" type="number" t-att-value="state.draft.sequence" t-on-change="(ev) => this.setDraftField('sequence', ev)"/></div>
                        <div class="ce-field"><label>Category</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('category_id', ev)">
                            <option value="">— none —</option>
                            <option t-foreach="state.fieldMeta.categories or []" t-as="c" t-key="c.id" t-att-value="c.id" t-att-selected="state.draft.category_id === c.id" t-esc="c.name"/>
                          </select></div>
                        <div class="ce-field"><label>Linked salary rule</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('salary_rule_id', ev)">
                            <option value="">— none —</option>
                            <option t-foreach="state.fieldMeta.salary_rules or []" t-as="r" t-key="r.id" t-att-value="r.id" t-att-selected="state.draft.salary_rule_id === r.id"><t t-esc="r.name"/> (<t t-esc="r.code"/>)</option>
                          </select></div>
                      </div>
                    </div>

                    <div class="ce-sec">
                      <div class="ce-sec-h">Appearance</div>
                      <div class="ce-grid2">
                        <div class="ce-field"><label>Number format</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('number_format', ev)">
                            <option t-foreach="state.fieldMeta.number_formats or []" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.draft.number_format === o.value" t-esc="o.label"/>
                          </select></div>
                        <div class="ce-field"><label>Decimal places</label>
                          <input class="ce-input" type="number" t-att-value="state.draft.decimal_places" t-on-change="(ev) => this.setDraftField('decimal_places', ev)"/></div>
                        <div class="ce-field"><label>Column width (px)</label>
                          <input class="ce-input" type="number" t-att-value="state.draft.column_width" t-on-change="(ev) => this.setDraftField('column_width', ev)"/></div>
                        <div class="ce-field"><label>Text align</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('text_align', ev)">
                            <option t-foreach="state.fieldMeta.text_aligns or []" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.draft.text_align === o.value" t-esc="o.label"/>
                          </select></div>
                      </div>
                      <div class="ce-toggles">
                        <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.appears_on_payslip" t-on-change="(ev) => this.setDraftField('appears_on_payslip', ev)"/><span>Appears on payslip</span></label>
                        <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.is_visible_in_grid" t-on-change="(ev) => this.setDraftField('is_visible_in_grid', ev)"/><span>Visible in grid</span></label>
                        <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.report_visible" t-on-change="(ev) => this.setDraftField('report_visible', ev)"/><span>Visible in reports</span></label>
                      </div>
                    </div>

                    <div class="ce-sec">
                      <button class="ce-sec-toggle" t-on-click="toggleAdvanced">
                        <svg t-attf-class="chev {{ state.advOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                        Advanced
                      </button>
                      <t t-if="state.advOpen">
                        <div class="ce-sec-h" style="margin-top:14px">Data source</div>
                        <div class="ce-grid2">
                          <div class="ce-field"><label>Data source</label>
                            <select class="ce-input" t-on-change="(ev) => this.setDraftField('data_source', ev)">
                              <option t-foreach="state.fieldMeta.data_sources or []" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.draft.data_source === o.value" t-esc="o.label"/>
                            </select></div>
                          <div t-if="state.draft.data_source === 'integration'" class="ce-field"><label>Integration connector</label>
                            <select class="ce-input" t-on-change="(ev) => this.setDraftField('integration_connector_id', ev)">
                              <option value="">— none —</option>
                              <option t-foreach="state.fieldMeta.connectors or []" t-as="c" t-key="c.id" t-att-value="c.id" t-att-selected="state.draft.integration_connector_id === c.id" t-esc="c.name"/>
                            </select></div>
                          <div t-if="state.draft.data_source === 'integration'" class="ce-field"><label>Source field</label>
                            <input class="ce-input" t-att-value="state.draft.source_field_mapping" t-on-input="(ev) => this.setDraftField('source_field_mapping', ev)"/></div>
                        </div>
                        <div class="ce-toggles">
                          <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.is_contract_component" t-on-change="(ev) => this.setDraftField('is_contract_component', ev)"/><span>Contract component</span></label>
                          <label t-if="state.draft.is_contract_component" class="ce-tog"><input type="checkbox" t-att-checked="state.draft.requires_new_contract" t-on-change="(ev) => this.setDraftField('requires_new_contract', ev)"/><span>Requires new contract</span></label>
                        </div>
                      </t>
                    </div>
                  </t>
                </div>
              </t>
            </div>
          </div>

          <!-- editor: grid (T2.1 — GridStudio component) -->
          <div t-else="" class="pbfs-gridwrap">
            <GridStudio components="state.components" preview="state.preview" graph="state.graph"
                        canEdit="state.canEdit" selectedId="state.selectedId"
                        onSelect="(id) => this.selectComponent(id)"
                        formatValue="(col) => this.previewVal(col)"
                        sampleName="sampleName" fieldMeta="state.fieldMeta"
                        onSaveFormula="(id, f) => this.gridSaveFormula(id, f)"
                        onValidateLive="(f, ex) => this.gridValidateLive(f, ex)"
                        onBulkUpdate="(ids, vals) => this.gridBulkUpdate(ids, vals)"
                        onTranslateFormula="(id, cols) => this.gridTranslateFormula(id, cols)"
                        onBulkSaveFormulas="(items) => this.gridBulkSaveFormulas(items)"/>
          </div>

          <!-- live preview -->
          <div class="pbfs-test" data-coach="fs-preview">
            <div class="tp-head"><span class="t">Live preview</span></div>
            <button class="tp-sel" t-on-click="cycleSample"><t t-esc="sampleName"/>
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg></button>
            <t t-if="!state.samples.length">
              <div class="tp-nosample">
                <p>No sample data yet — generate one to see live values.</p>
                <button class="pbfs-btn soft sm" t-on-click="generateSampleData">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>Generate sample data
                </button>
              </div>
            </t>
            <t t-foreach="state.components" t-as="c" t-key="c.id">
              <div t-att-data-col="c.col"
                   t-attf-class="tp-row {{ isDeduction(c) ? 'ded' : '' }} {{ isDep(c.col) ? 'dep' : '' }} {{ isOutcome(c.col) ? 'outcome' : '' }}"
                   t-attf-style="{{ depStyle(c) }}"
                   t-on-click="() => this.selectComponent(c.id)">
                <span class="nm"><span class="c" t-esc="c.col"/><t t-esc="c.name"/></span>
                <span class="v"><t t-if="isDeduction(c)">−</t><t t-esc="previewVal(c.col)"/></span>
              </div>
            </t>
          </div>
        </div>
        </t>
      </div>

      <!-- delete-configuration confirm -->
      <div t-if="state.confirmDel" class="pbfs-confirm-scrim" t-on-click="cancelDeleteConfig">
        <div class="pbfs-confirm" t-on-click="(ev) => ev.stopPropagation()">
          <div class="cf-ic">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><line x1="10" y1="11" x2="10" y2="17"/><line x1="14" y1="11" x2="14" y2="17"/></svg>
          </div>
          <h3>Delete this configuration?</h3>
          <p><b t-esc="state.confirmDel.name"/> and its <t t-esc="state.confirmDel.count"/> component<t t-if="state.confirmDel.count !== 1">s</t> will be permanently removed. This cannot be undone.</p>
          <div t-if="state.confirmDel.state === 'active'" class="cf-warn">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
            This configuration is <b>active</b> — deleting it may affect payroll that relies on it.
          </div>
          <div class="cf-actions">
            <button class="pbfs-btn ghost" t-on-click="cancelDeleteConfig">Cancel</button>
            <button class="pbfs-btn danger-solid" t-on-click="confirmDeleteConfig">Delete configuration</button>
          </div>
        </div>
      </div>

      <!-- Explain modal (T5.3) -->
      <div t-if="state.explainOpen" class="pbfs-explain-scrim" t-on-click="closeExplain">
        <div class="pbfs-explain" t-on-click="(ev) => ev.stopPropagation()">
          <div class="ex-head">
            <div class="ex-title">
              <span class="ex-col" t-esc="selected.col"/>
              <b t-esc="selected.name"/>
            </div>
            <div class="ex-lang">
              <button t-att-class="state.explainLang === 'en' ? 'on' : ''" t-on-click="() => this.setExplainLang('en')">EN</button>
              <button t-att-class="state.explainLang === 'vi' ? 'on' : ''" t-on-click="() => this.setExplainLang('vi')">VI</button>
            </div>
            <button class="ex-x" t-on-click="closeExplain"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>
          <div class="ex-body">
            <p class="ex-text" t-esc="state.explainText"/>
          </div>
          <div class="ex-foot">
            <span t-attf-class="ex-badge {{ state.explainSource }}">
              <t t-if="state.explainSource === 'ai'">PayAI</t>
              <t t-else="">Built-in</t>
            </span>
            <span t-if="state.explainBusy" class="ex-loading">
              <span class="ex-dot"/><span class="ex-dot"/><span class="ex-dot"/> PayAI is thinking…
            </span>
          </div>
        </div>
      </div>

      <!-- PayAI slide-in -->
      <div t-attf-class="pbfs-ai-scrim {{ state.aiOpen ? 'open' : '' }}" t-on-click="closeAI"/>
      <aside t-attf-class="pbfs-ai-panel {{ state.aiOpen ? 'open' : '' }}">
        <div class="ai-top">
          <div class="ico"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg></div>
          <div><b>PayAI Formula Assistant</b><small>
            <t t-if="state.aiLlm">Powered by <t t-esc="state.aiModel"/></t>
            <t t-else="">Describe a rule in plain English</t>
          </small></div>
          <button class="x" t-on-click="closeAI"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
        </div>
        <div class="ai-body">
          <div class="ai-msg"><div class="who">PayAI</div>
            <div class="ai-bubble bot">Tell me what a component should compute and I'll write the formula, mapping it to your existing columns. Try:</div>
            <div class="ai-chips">
              <span class="ai-chip" t-on-click="() => this.aiAskChip('Net is gross minus all deductions')">Net = gross − deductions</span>
              <span class="ai-chip" t-on-click="() => this.aiAskChip('Explain Net Salary')">Explain a formula</span>
            </div>
          </div>
          <t t-foreach="state.aiMsgs" t-as="m" t-key="m_index">
            <div class="ai-msg"><div class="who" t-esc="m.who === 'you' ? 'You' : 'PayAI'"/>
              <div t-attf-class="ai-bubble {{ m.who === 'you' ? 'user' : 'bot' }}" t-esc="m.text"/></div>
          </t>
          <div t-if="state.aiProposal" class="ai-proposal">
            <div class="ph">Proposed formula<t t-if="state.aiProposal.target_col"><t t-esc="' · column ' + state.aiProposal.target_col"/></t></div>
            <div class="pf"><t t-esc="state.aiProposal.human"/></div>
            <div class="map">
              <t t-foreach="state.aiProposal.mapping" t-as="mp" t-key="mp_index">
                <span class="mp"><b t-esc="mp.name"/> → <t t-esc="mp.col"/></span>
              </t>
            </div>
            <div class="pa">
              <button class="pbfs-btn primary" style="flex:1" t-on-click="applyProposal">
                <t t-if="state.aiProposal.target_id">Apply to <t t-esc="state.aiProposal.target_name"/></t>
                <t t-else="">Create as new component</t>
              </button>
              <button class="pbfs-btn ghost" t-on-click="discardProposal">Discard</button>
            </div>
          </div>
        </div>
        <div class="ai-input pbfs-ai-input">
          <input placeholder="e.g. overtime is 1.5 times hourly rate" t-on-keydown="aiAskInput"/>
          <button class="send" t-on-click="(ev) => this.aiAsk(ev.target.closest('.ai-input').querySelector('input').value)">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m4 12 16-8-6 16-3-7z"/></svg>
          </button>
        </div>
      </aside>

      <!-- Flowchart modal (Expand / drill-down) -->
      <div t-if="state.flowOpen" class="fc-scrim" t-on-click="closeFlow">
        <div class="fc-modal" t-on-click.stop="() => {}">
          <div class="fc-top">
            <div class="ico"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/><path d="M10 6.5h6a1 1 0 0 1 1 1V14"/></svg></div>
            <div><b t-esc="modalComp ? modalComp.name : 'Formula flow'"/><small>Calculation flow · = <t t-esc="modalComp ? previewVal(modalComp.col) : ''"/> for <t t-esc="sampleName"/></small></div>
            <div class="fc-crumbs">
              <t t-foreach="modalCrumbs" t-as="cb" t-key="cb_index">
                <span t-if="cb_index > 0">›</span>
                <span t-attf-class="cb {{ cb_index === modalCrumbs.length - 1 ? 'cur' : '' }}" t-on-click="() => this.crumbTo(cb_index)" t-esc="cb.name"/>
              </t>
            </div>
            <div class="fc-zoom">
              <button t-on-click="zoomOut" title="Zoom out"><svg viewBox="0 0 24 24" fill="none" stroke-width="2.4"><path d="M5 12h14"/></svg></button>
              <button class="zfit" t-on-click="zoomFit" title="Fit to window"><span class="zlabel">Fit</span></button>
              <button t-on-click="zoomIn" title="Zoom in"><svg viewBox="0 0 24 24" fill="none" stroke-width="2.4"><path d="M12 5v14M5 12h14"/></svg></button>
            </div>
            <button class="fc-x" t-on-click="closeFlow"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>
          <div class="fc-legend">
            <span class="lg"><span class="sw" style="background:#0E7490"/>Input / info</span>
            <span class="lg"><span class="sw" style="background:#4F46E5"/>Earning</span>
            <span class="lg"><span class="sw" style="background:#B45309"/>Deduction / decision</span>
            <span class="lg"><span class="sw" style="background:#059669"/>Total</span>
            <span class="lg" style="margin-left:auto;color:#4F46E5">▤ click a formula box to drill in</span>
          </div>
          <div class="fc-canvas" t-on-wheel="onWheel" t-on-pointerdown="onPanStart" t-on-pointermove="onPanMove" t-on-pointerup="onPanEnd" t-on-pointercancel="onPanEnd">
            <div class="fc-zoomwrap"><t t-call="pb_formula_studio.Flow"><t t-set="flow" t-value="modalFlow"/></t></div>
          </div>
        </div>
      </div>

      <!-- First-setup wizard -->
      <div t-if="state.wizardOpen" class="pbfs-wz-scrim">
        <div class="pbfs-wz">
          <div class="wz-rail">
            <h3>New Formula Config</h3>
            <div class="rsub">Set up a payroll calculation in 5 quick steps.</div>
            <t t-foreach="[[1,'Basics','Name, country, cycle'],[2,'Components','Pick a starter set'],[3,'Formulas','Review what gets created'],[4,'Test','Sample data'],[5,'Activate','Create &amp; open']]" t-as="st" t-key="st[0]">
              <div t-attf-class="wz-step {{ state.wizardStep === st[0] ? 'active' : (state.wizardStep > st[0] ? 'done' : '') }}">
                <div class="num"><t t-if="state.wizardStep > st[0]">✓</t><t t-else="" t-esc="st[0]"/></div>
                <div><div class="stt" t-esc="st[1]"/><div class="sd" t-esc="st[2]"/></div>
              </div>
            </t>
          </div>
          <div class="wz-main">
            <div class="wz-content">
              <!-- Step 1: Basics -->
              <t t-if="state.wizardStep === 1">
                <h2>The basics</h2>
                <p class="lead">Name the configuration and choose its country and pay cycle.</p>
                <div class="wz-fields">
                  <div class="field full"><label>Configuration name</label>
                    <input class="wz-input" t-att-value="state.wizardForm.name" t-on-input="(ev) => this.wizardSet('name', ev)" placeholder="e.g. Vietnam Monthly 2026"/>
                  </div>
                  <div class="field"><label>Country</label>
                    <select class="wz-input" t-on-change="(ev) => this.wizardSet('country_code', ev)">
                      <option value="VN" selected="1">Vietnam</option>
                      <option value="ID">Indonesia</option>
                      <option value="IN">India</option>
                      <option value="SG">Singapore</option>
                      <option value="TH">Thailand</option>
                      <option value="KH">Cambodia</option>
                      <option value="MY">Malaysia</option>
                    </select>
                  </div>
                  <div class="field"><label>Cycle type</label>
                    <select class="wz-input" t-on-change="(ev) => this.wizardSet('cycle_type', ev)">
                      <option value="regular" selected="1">Regular</option>
                      <option value="mid_cycle">Mid-Cycle</option>
                      <option value="end_cycle">End-Cycle</option>
                      <option value="full_final">Full &amp; Final</option>
                    </select>
                  </div>
                </div>
              </t>
              <!-- Step 2: Components -->
              <t t-elif="state.wizardStep === 2">
                <h2>How do you want to start?</h2>
                <p class="lead">Pick a starting point. You can add, remove or rewrite any component afterwards.</p>
                <div class="wz-tmpl">
                  <div t-foreach="state.wizardTemplates" t-as="tpl" t-key="tpl.key"
                       t-attf-class="tmpl-card {{ state.wizardForm.template === tpl.key ? 'sel' : '' }}"
                       t-on-click="() => this.pickTemplate(tpl.key)">
                    <b t-esc="tpl.name"/><p t-esc="tpl.desc"/>
                  </div>
                  <div class="tmpl-card" t-on-click="importExcel">
                    <b>Import from Excel</b><p>Upload a spreadsheet — headers, columns and formulas detected automatically.</p>
                  </div>
                </div>
              </t>
              <!-- Step 3: Formulas review -->
              <t t-elif="state.wizardStep === 3">
                <h2>Review the components</h2>
                <p class="lead"><t t-esc="wizardTpl.name"/> — these will be created. You'll tune them in the cockpit.</p>
                <div t-if="wizardTpl.preview and wizardTpl.preview.length" class="wz-preview">
                  <div t-foreach="wizardTpl.preview" t-as="p" t-key="p_index" class="cpr">
                    <span class="c" t-esc="p.col"/><t t-esc="p.name"/><span class="f" t-esc="p.f"/>
                  </div>
                  <div class="cpr more">…and the full statutory set</div>
                </div>
                <p t-else="" class="wz-blank">You'll start with a blank canvas and add components in the cockpit — or ask PayAI to draft them.</p>
              </t>
              <!-- Step 4: Test -->
              <t t-elif="state.wizardStep === 4">
                <h2>Test data</h2>
                <p class="lead">We'll seed a sample employee so the live preview works immediately. Generate more from real staff anytime, then run tests from the cockpit.</p>
                <div class="wz-note"><b>Sample — Standard</b> (Basic ₫15,000,000) will be added so you can see values right away.</div>
              </t>
              <!-- Step 5: Activate -->
              <t t-else="">
                <h2>Ready to create</h2>
                <p class="lead">Review and create. The configuration opens in the cockpit where you validate, test and activate.</p>
                <div class="wz-summary">
                  <div class="sr"><span>Name</span><b t-esc="state.wizardForm.name || '—'"/></div>
                  <div class="sr"><span>Country</span><b t-esc="state.wizardForm.country_code"/></div>
                  <div class="sr"><span>Cycle</span><b t-esc="state.wizardForm.cycle_type"/></div>
                  <div class="sr"><span>Starting point</span><b t-esc="wizardTpl.name || state.wizardForm.template"/></div>
                </div>
              </t>
            </div>
            <div class="wz-foot">
              <button class="pbfs-btn ghost" t-on-click="closeWizard">Cancel</button>
              <div style="margin-left:auto;display:flex;gap:10px;">
                <button t-if="state.wizardStep > 1" class="pbfs-btn ghost" t-on-click="wizardBack">Back</button>
                <button t-if="state.wizardStep &lt; 5" class="pbfs-btn primary" t-on-click="wizardNext">Continue</button>
                <button t-else="" class="pbfs-btn primary" t-on-click="wizardCreate" t-att-disabled="state.wizardBusy">
                  <t t-if="state.wizardBusy">Creating…</t><t t-else="">Create &amp; open</t>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </t>

  <!-- Reusable flowchart renderer -->
  <t t-name="pb_formula_studio.Flow">
    <div t-if="flow" class="fc-stage" t-attf-style="min-width:{{ flow.width }}px;min-height:{{ flow.height }}px;">
      <svg class="fc-edges" t-att-width="flow.width" t-att-height="flow.height">
        <t t-foreach="flow.edges" t-as="e" t-key="e_index">
          <path t-att-d="e.d" fill="none" t-att-stroke="e.color" stroke-width="2" class="fc-edge"/>
        </t>
      </svg>
      <t t-foreach="flow.nodes" t-as="n" t-key="n.id">
        <div class="fc-node" t-attf-style="left:{{ n.x }}px;top:{{ n.y }}px;">
          <t t-if="n.kind === 'if'">
            <div class="node-diamond"><div class="dmd" t-attf-style="border-color:{{ n.color }}"/><div class="dtext" t-attf-style="color:{{ n.color }}" t-esc="n.label"/></div>
          </t>
          <t t-elif="n.kind === 'result'">
            <div class="node-box node-result"><div class="nsub">result</div><div class="nlabel" t-esc="n.label"/><div class="nval" t-esc="n.value"/></div>
          </t>
          <t t-elif="n.kind === 'op'">
            <div class="node-box op-node">
              <div t-attf-class="op-badge {{ n.opCls }}" t-esc="n.label"/>
              <div class="nval" t-esc="n.value"/>
            </div>
          </t>
          <t t-else="">
            <div t-attf-class="node-box {{ n.formula ? 'clickable' : '' }}" t-attf-style="--nc:{{ n.color }};border-color:{{ n.color }}"
                 t-on-click="(ev) => this.flowNodeClick(ev, n)">
              <div t-if="n.sub" class="nsub" t-esc="n.sub"/>
              <div class="nlabel" t-esc="n.label"/>
              <div class="nval" t-esc="n.value"/>
              <div t-if="n.formula" class="drill">▤ open formula</div>
            </div>
          </t>
        </div>
      </t>
      <t t-foreach="flow.labels" t-as="l" t-key="l_index">
        <div t-attf-class="edge-label {{ l.cls }}" t-attf-style="left:{{ l.x }}px;top:{{ l.y }}px;" t-esc="l.text"/>
      </t>
    </div>
  </t>

</templates>
