<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">

  <!-- Searchable many2one combobox -->
  <t t-name="pb_formula_studio.CfgCombo">
    <div class="cfg-combo" t-ref="root">
      <button type="button" class="cfg-combo-control ce-input" t-on-click="toggle">
        <span t-attf-class="cfg-combo-lbl {{ !displayLabel ? 'ph' : '' }}"><t t-esc="displayLabel || props.placeholder || '— none —'"/></span>
        <svg class="cfg-combo-chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
      </button>
      <div t-if="state.open" class="cfg-combo-menu" t-att-style="state.menuStyle">
        <input class="cfg-combo-search" t-ref="search" placeholder="Type to search…" t-att-value="state.q" t-on-input="onInput" t-on-keydown="onKey"/>
        <div class="cfg-combo-list">
          <div class="cfg-combo-opt none" t-on-click="() => this.pick(false)">— none —</div>
          <t t-foreach="filtered" t-as="o" t-key="o.id">
            <div t-attf-class="cfg-combo-opt {{ o.id === props.value ? 'sel' : '' }} {{ o_index === state.active ? 'active' : '' }}"
                 t-on-click="() => this.pick(o.id)" t-on-mouseenter="() => { state.active = o_index; }"
                 t-esc="label(o)"/>
          </t>
          <div t-if="!filtered.length" class="cfg-combo-empty">No matches</div>
        </div>
      </div>
    </div>
  </t>

  <t t-name="pb_formula_studio.PbFormulaStudio">
    <div class="pbfs">
      <!-- loading -->
      <div t-if="!state.loaded" class="pbfs-loading"><div class="pbfs-spinner"/><span>Loading Formula Studio…</span></div>

      <!-- empty -->
      <div t-elif="state.empty" class="pbfs-empty-state">
        <h2>No formula configurations yet</h2>
        <p>Create your first payroll calculation to get started.</p>
        <button class="pbfs-btn primary" t-on-click="openWizard">New Formula Config</button>
      </div>

      <!-- main -->
      <div t-else="" class="pbfs-main">
        <!-- top bar — two tiers: identity row + toolbar row (biz responsive) -->
        <header class="pbfs-top">
          <div class="pbfs-top-row1">
            <div class="pbfs-cfg">
              <button class="pbfs-cfg-name" data-coach="fs-config" t-on-click="toggleConfigPicker">
                <span class="nm" t-esc="state.config.name"/>
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
              </button>
              <button class="pbfs-bureaubtn" data-coach="fs-bureau" t-on-click="openBureau" title="Bureau cockpit — all configurations at a glance">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="9" rx="1"/><rect x="14" y="3" width="7" height="5" rx="1"/><rect x="14" y="12" width="7" height="9" rx="1"/><rect x="3" y="16" width="7" height="5" rx="1"/></svg>
              </button>
              <span t-if="state.config.country" class="pbfs-chip biz-hide-md" t-esc="state.config.country"/>
              <span class="pbfs-chip cur biz-hide-md" t-esc="'Currency ' + (state.config.currency || '')"/>
              <span t-attf-class="pbfs-state {{ stageCls() }}"><span class="d"/><t t-esc="stageLabel()"/></span>
              <span t-if="state.config.is_branch" t-attf-class="pbfs-branchchip {{ state.config.branch_state === 'merged' ? 'merged' : '' }}" t-on-click="openBranches" title="This configuration is a branch — open Branches">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></svg>
                <t t-if="state.config.branch_state === 'merged'">Merged branch</t>
                <t t-else="">Branch of <t t-esc="state.config.parent_name"/></t>
              </span>
              <span t-if="state.config.is_variant" class="pbfs-branchchip variant" t-on-click="openVariants" title="This configuration is a variant — open Variants">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
                Variant of <t t-esc="state.config.master_name"/>
              </span>
              <span t-if="state.config.is_master" class="pbfs-branchchip master" t-on-click="openVariants" title="This is a master scheme — open Variants">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
                Master · <t t-esc="state.config.variant_count"/> variant<t t-if="state.config.variant_count !== 1">s</t>
              </span>
              <!-- config picker dropdown -->
              <div t-if="state.configPickerOpen" class="pbfs-picker">
                <div t-foreach="state.configs" t-as="cfg" t-key="cfg.id" class="pbfs-picker-row" t-on-click="() => this.pickConfig(cfg.id)">
                  <div class="pp-main">
                    <span class="nm" t-esc="cfg.name"/>
                    <span class="meta" t-esc="cfg.rule_count + ' components · ' + cfg.state"/>
                  </div>
                  <button class="pbfs-picker-gear" t-on-click="(ev) => this.openConfigSettings(cfg.id, ev)" title="Configuration settings">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                  </button>
                  <button class="pbfs-picker-del" t-on-click="(ev) => this.askDeleteConfig(cfg, ev)" title="Delete configuration">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
                  </button>
                </div>
                <div class="pbfs-picker-row new" t-on-click="openWizard">
                  <span class="nm">＋ New configuration</span>
                  <span class="meta">Guided setup</span>
                </div>
              </div>
            </div>
            <div class="pbfs-id-right">
              <div class="pbfs-ring" title="Validation health">
                <svg width="46" height="46">
                  <circle cx="23" cy="23" r="19" stroke="#EEF1F5" stroke-width="5" fill="none"/>
                  <circle cx="23" cy="23" r="19" stroke="#0F8A63" stroke-width="5" fill="none" stroke-linecap="round"
                          t-att-stroke-dasharray="ring(state.config.score).dash"
                          t-att-stroke-dashoffset="ring(state.config.score).offset"
                          transform="rotate(-90 23 23)"/>
                </svg>
                <span class="val" t-esc="state.config.score"/>
              </div>
              <button class="pbfs-btn primary" t-on-click="advance"><t t-esc="nextLabel()"/></button>
            </div>
          </div>
          <div class="pbfs-top-row2">
            <div class="pbfs-seg" data-coach="fs-views">
              <button t-att-class="state.view === 'cards' ? 'active' : ''" t-on-click="() => this.setView('cards')" title="Cards view">
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg><span class="tx">Cards</span>
              </button>
              <button data-coach="fs-view-grid" t-att-class="state.view === 'grid' ? 'active' : ''" t-on-click="() => this.setView('grid')" title="Grid view">
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18M15 3v18"/></svg><span class="tx">Grid</span>
              </button>
              <button t-att-class="state.view === 'test' ? 'active' : ''" t-on-click="openTest" title="Test &amp; validate">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 2h6M9 2v6.5L4.5 18a2 2 0 0 0 1.8 3h11.4a2 2 0 0 0 1.8-3L15 8.5V2"/><path d="M7 14h10"/></svg><span class="tx">Test</span>
              </button>
              <button t-att-class="state.view === 'compare' ? 'active' : ''" t-on-click="openCompare" title="Compare two periods">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h7M3 6l2-2M3 6l2 2M21 18h-7M21 18l-2-2M21 18l-2 2"/></svg><span class="tx">Compare</span>
              </button>
              <button t-att-class="state.view === 'settings' ? 'active' : ''" t-on-click="openSettings" title="Configuration settings">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg><span class="tx">Settings</span>
              </button>
            </div>
            <!-- W82 — tests-on-save chip: last save's sample-test verdict -->
            <div t-if="testChip" class="pbfs-testchip-wrap">
              <button t-attf-class="pbfs-testchip {{ testChip.kind }}" t-on-click.stop="toggleTestsFail"
                      t-att-title="testChip.kind === 'fail' ? (testChip.failed + ' sample test(s) failing — click for details') : 'All sample tests passing'">
                <svg t-if="testChip.kind === 'pass'" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>
                <svg t-else="" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                <span class="tx" t-esc="testChip.label"/>
              </button>
              <div t-if="state.testsFailOpen and state.tests and state.tests.failures.length" class="pbfs-testchip-pop" t-on-click.stop="">
                <div class="pbfs-testchip-pop-h">Failing samples</div>
                <button t-foreach="state.tests.failures" t-as="f" t-key="f_index" class="pbfs-testchip-row"
                        t-on-click="() => this.openTestsFailure(f.sample_id)">
                  <span class="s" t-esc="f.sample"/>
                  <span class="c" t-esc="f.code"/>
                  <span class="d">exp <t t-esc="f.expected"/> · got <t t-esc="f.computed"/></span>
                </button>
              </div>
            </div>
            <div class="pbfs-top-actions">
              <button class="pbfs-btn pbfs-cmdbtn" data-coach="fs-command" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Command Center","desc":"Every tool in one place. Press Ctrl or Cmd + K"}' t-on-click="openCommand" title="Command Center — every tool in one place (Ctrl/Cmd K)">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg>
                <span>Tools</span>
              </button>
              <button class="pbfs-btn soft pbfs-replaybtn biz-icon-only-lg biz-hide-lg" data-coach="fs-replay" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Execution replay","desc":"Watch a payslip compute step by step"}' t-on-click="openReplay" title="Execution replay — watch a payslip compute step by step">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
                <span>Replay</span>
              </button>
              <button class="pbfs-btn soft pbfs-whatifbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-whatif" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"What-if","desc":"Slide a rate and project the payroll cost"}' t-on-click="openWhatif" title="What-if — slide a rate and project the payroll cost">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="21" x2="4" y2="14"/><line x1="4" y1="10" x2="4" y2="3"/><line x1="12" y1="21" x2="12" y2="12"/><line x1="12" y1="8" x2="12" y2="3"/><line x1="20" y1="21" x2="20" y2="16"/><line x1="20" y1="12" x2="20" y2="3"/><line x1="1" y1="14" x2="7" y2="14"/><line x1="9" y1="8" x2="15" y2="8"/><line x1="17" y1="16" x2="23" y2="16"/></svg>
                <span>What-if</span>
              </button>
              <button class="pbfs-btn soft pbfs-psbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-payslip" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Payslip Studio","desc":"Design the payslip layout"}' t-on-click="openPayslip" title="Payslip Studio — design the payslip layout">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2h9l3 3v17l-3-2-3 2-3-2-3 2V2z"/><path d="M9 7h6M9 11h6M9 15h4"/></svg>
                <span>Payslip</span>
              </button>
              <button class="pbfs-btn soft pbfs-mapbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-mapping" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Mapping canvas","desc":"Map cycle carryover, API, import and scheme fields onto your components"}' t-on-click="openMapping" title="Mapping canvas — map cycle carryover, API, import and scheme fields onto your components">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="6" r="2.5"/><circle cx="6" cy="18" r="2.5"/><circle cx="18" cy="12" r="2.5"/><path d="M8.5 6.5C13 8 12 12 15.6 12M8.5 17.5C13 16 12 12 15.6 12"/></svg>
                <span>Mapping</span>
              </button>
              <button class="pbfs-btn soft pbfs-ratebtn biz-icon-only-lg biz-hide-lg" data-coach="fs-rates" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Rate tables","desc":"PIT brackets and other rate tables"}' t-on-click="openRates" title="Rate &amp; bracket tables (PIT, etc.)">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18"/></svg>
                <span>Tables</span>
                <span t-if="state.rateTables.length" class="pbfs-ratecount biz-keep" t-esc="state.rateTables.length"/>
              </button>
              <button class="pbfs-btn soft pbfs-offerbtn biz-icon-only-lg biz-hide-lg" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Offer calculator","desc":"Type hypothetical inputs, see the full breakdown"}' t-on-click="openOfferCalc" title="Offer calculator — type hypothetical inputs, see the full breakdown">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2"/><path d="M8 6h8M8 10h.01M12 10h.01M16 10h.01M8 14h.01M12 14h.01M16 14h4M8 18h.01M12 18h.01"/></svg>
                <span>Offer</span>
              </button>
              <!-- Tools ▾ overflow — absorbs the five buttons above below 1280px (CSS-gated, no resize JS) -->
              <div class="pbfs-more biz-more-menu biz-only-lg">
                <button class="pbfs-btn soft" t-on-click="toggleMore" title="More tools">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
                  <span class="biz-keep">More</span>
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:11px;height:11px"><path d="m6 9 6 6 6-6"/></svg>
                </button>
                <div t-if="state.moreOpen" class="biz-more-menu__panel">
                  <button t-on-click="() => this.pickTool('replay')">Replay</button>
                  <button t-on-click="() => this.pickTool('whatif')">What-if</button>
                  <button t-on-click="() => this.pickTool('payslip')">Payslip</button>
                  <button t-on-click="() => this.pickTool('mapping')">Mapping</button>
                  <button t-on-click="() => this.pickTool('rates')">
                    Tables<t t-if="state.rateTables.length"> (<t t-esc="state.rateTables.length"/>)</t>
                  </button>
                  <button t-on-click="openOfferCalc">Offer calculator</button>
                </div>
              </div>
              <button class="pbfs-btn soft pbfs-mapbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-depmap" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Dependency map","desc":"The whole configuration as a graph"}' t-on-click="openDepMap" title="Dependency map — the whole configuration as a graph">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="5" cy="6" r="2.4"/><circle cx="5" cy="18" r="2.4"/><circle cx="19" cy="12" r="2.4"/><path d="M7.3 6.6 16.7 11M7.3 17.4 16.7 13"/></svg>
                <span>Map</span>
              </button>
              <button t-attf-class="pbfs-btn soft pbfs-probbtn biz-icon-only-lg {{ problemLevel }}" data-coach="fs-problems" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Problems","desc":"Lint checks and rename-refactor"}' t-on-click="openProblems" title="Problems, lint &amp; rename-refactor">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 9v4M12 17h.01"/><path d="M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/></svg>
                <span>Problems</span>
                <span t-if="problemCount" class="pbfs-probcount biz-keep" t-esc="problemCount"/>
                <svg t-else="" class="pbfs-probok" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>
              </button>
              <button class="pbfs-btn soft pbfs-branchbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-branches" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Branches","desc":"Fork this config, trial a change, merge back"}' t-on-click="openBranches" title="Branches — fork this config, trial a change safely, merge back">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></svg>
                <span>Branches</span>
                <span t-if="state.config.branch_count" class="pbfs-branchn" t-esc="state.config.branch_count"/>
              </button>
              <button class="pbfs-btn soft pbfs-variantbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-variants" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Variants","desc":"One master scheme, many synced variants"}' t-on-click="openVariants" title="Variants — one master scheme, many synced variants">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
                <span>Variants</span>
                <span t-if="state.config.variant_count" class="pbfs-branchn" t-esc="state.config.variant_count"/>
              </button>
              <button class="pbfs-btn soft pbfs-legisbtn biz-icon-only-lg biz-hide-lg" data-coach="fs-legislation" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Legislation","desc":"Roll a statutory change across every configuration"}' t-on-click="openLegislation" title="Legislation — roll a statutory change across every configuration">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v18M4 7l8-4 8 4M6 21h12M5 7l-2 6a4 4 0 0 0 8 0L9 7M19 7l-2 6a4 4 0 0 0 8 0l-2-6"/></svg>
                <span>Legislation</span>
              </button>
              <button class="pbfs-btn soft pbfs-releasebtn" data-coach="fs-releases" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Releases","desc":"Review and sign off formula changes"}' t-on-click="openReleases" title="Releases — review and sign off formula changes">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>
                <span>Releases</span>
              </button>
              <button class="pbfs-btn soft pbfs-sharebtn biz-icon-only-lg biz-hide-lg" data-coach="fs-share" data-tooltip-template="pb_formula_studio.BtnTip" data-tooltip-position="bottom" data-tooltip-info='{"name":"Share for review","desc":"A read-only link for your client"}' t-on-click="openShare" title="Share for review — a read-only link for your client">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5 15.4 17.5M15.4 6.5 8.6 10.5"/></svg>
                <span>Share</span>
              </button>
              <button class="pbfs-btn ai" data-coach="fs-payai" t-on-click="openAI">
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg>Ask PayAI
              </button>
            </div>
          </div>
        </header>

        <!-- ============ CONFIG SETTINGS SURFACE ============ -->
        <t t-if="state.view === 'settings' and state.settings">
        <div class="pbcfg">
          <!-- workflow rail -->
          <div class="pbcfg-rail">
            <div class="pbcfg-stages">
              <t t-foreach="[['draft','Draft'],['testing','Testing'],['validated','Validated'],['active','Active']]" t-as="stg" t-key="stg[0]">
                <div t-attf-class="pbcfg-stage {{ cfgStageCls(stg[0]) }}">
                  <span class="dot"><t t-if="cfgStageCls(stg[0]) === 'done'">✓</t></span>
                  <span class="lb" t-esc="stg[1]"/>
                </div>
              </t>
            </div>
            <div class="pbcfg-rail-side">
              <div class="pbfs-ring" title="Validation health">
                <svg width="42" height="42">
                  <circle cx="21" cy="21" r="19" stroke="#EEF1F5" stroke-width="5" fill="none"/>
                  <circle cx="21" cy="21" r="19" stroke="#0F8A63" stroke-width="5" fill="none" stroke-linecap="round"
                          t-att-stroke-dasharray="ring(state.settings.status.score).dash"
                          t-att-stroke-dashoffset="ring(state.settings.status.score).offset"
                          transform="rotate(-90 21 21)"/>
                </svg>
                <span class="val" t-esc="state.settings.status.score"/>
              </div>
              <span t-attf-class="pbcfg-vbadge {{ state.settings.status.validation_status }}" t-esc="state.settings.status.validation_status"/>
            </div>
          </div>
          <!-- stage actions -->
          <div class="pbcfg-actions">
            <button t-if="cfgState === 'draft'" class="pbfs-btn primary" t-on-click="startTesting">Start Testing</button>
            <button t-if="cfgState === 'testing'" class="pbfs-btn primary" t-on-click="validateCfg">Validate</button>
            <button t-if="cfgState === 'validated'" class="pbfs-btn primary" t-on-click="activateCfg">Activate</button>
            <button t-if="state.settings.status.rule_count" class="pbfs-btn soft" data-coach="fs-simulate" t-on-click="openSimulate" title="Preview this config against last period's real payslips before activating">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="M7 14l4-4 3 3 5-6"/></svg>Simulate
            </button>
            <button t-if="cfgState !== 'draft'" class="pbfs-btn ghost" t-on-click="setDraftCfg">Set to Draft</button>
            <button t-if="!state.settings.status.rule_count" class="pbfs-btn ghost" t-on-click="importExcelCfg">Import from Excel</button>
            <button t-if="state.settings.status.rule_count" class="pbfs-btn ghost" t-on-click="regenerateFormulas">Regenerate Formulas</button>
            <button t-if="cfgState !== 'archived'" class="pbfs-btn danger" t-on-click="archiveCfg">Archive</button>
          </div>
          <!-- alerts -->
          <div t-if="state.settings.status.has_errors and state.settings.status.error_details" class="ed-errbox" t-esc="state.settings.status.error_details"/>
          <!-- T1.6: bare has_circular_ref flag replaced by the human cycle path(s) -->
          <div t-if="graphCycles.length" class="ed-errbox pbfs-cyc-alert">
            <div t-foreach="graphCycles" t-as="cy" t-key="cy_index" class="cyc-alert-row">
              <span t-esc="cy.human_explanation"/>
              <button class="pbfs-link" t-on-click="() => this.showCyclePath(cy)">Show path</button>
            </div>
          </div>

          <!-- tabs -->
          <div class="pbcfg-tabs">
            <t t-foreach="[['setup','Setup'],['automation','Automation'],['intelligence','Intelligence']]" t-as="tb" t-key="tb[0]">
              <button t-attf-class="{{ state.settingsTab === tb[0] ? 'on' : '' }}" t-on-click="() => this.setSettingsTab(tb[0])" t-esc="tb[1]"/>
            </t>
          </div>

          <div t-if="state.settingsError" class="ce-banner err">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
            <span t-esc="state.settingsError"/>
          </div>

          <div class="pbcfg-body">
            <!-- SETUP -->
            <t t-if="state.settingsTab === 'setup'">
              <div class="pbcfg-grid">
                <div class="ce-sec">
                  <div class="ce-sec-h">Essentials</div>
                  <div class="ce-field"><label>Name</label><input class="ce-input" t-att-value="state.setDraft.name" t-on-input="(ev)=>this.setCfgField('name',ev)"/></div>
                  <div class="ce-grid2" style="margin-top:12px">
                    <div class="ce-field"><label>Country</label>
                      <select class="ce-input" t-on-change="(ev)=>this.setCfgField('country_code',ev)">
                        <option t-foreach="cfgMeta('country_codes')" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.setDraft.country_code === o.value" t-esc="o.label"/>
                      </select></div>
                    <div class="ce-field"><label>Cycle type</label>
                      <select class="ce-input" t-on-change="(ev)=>this.setCfgField('cycle_type',ev)">
                        <option t-foreach="cfgMeta('cycle_types')" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.setDraft.cycle_type === o.value" t-esc="o.label"/>
                      </select></div>
                    <div t-if="state.settings.meta.multi_company" class="ce-field"><label>Company</label>
                      <CfgCombo options="cfgMeta('companies')" value="state.setDraft.company_id" placeholder="'Company'" onSelect="(id) => this.pickCfgM2O('company_id', id)"/></div>
                  </div>
                </div>

                <div class="ce-sec">
                  <div class="ce-sec-h">Accounting</div>
                  <div class="ce-sec-sub">Where payslips post in your books.</div>
                  <div class="ce-field"><label>Payroll journal</label>
                    <CfgCombo options="cfgMeta('journals')" value="state.setDraft.payroll_journal_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('payroll_journal_id', id)"/></div>
                  <div class="ce-field" style="margin-top:12px"><label>Default debit account</label>
                    <CfgCombo options="cfgMeta('accounts')" value="state.setDraft.debit_account_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('debit_account_id', id)"/></div>
                  <div class="ce-field" style="margin-top:12px"><label>Default credit account</label>
                    <CfgCombo options="cfgMeta('accounts')" value="state.setDraft.credit_account_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('credit_account_id', id)"/></div>
                </div>

                <div class="ce-sec pbcfg-readonly">
                  <div class="ce-sec-h">Components</div>
                  <div class="pbcfg-stat"><span class="k">Total</span><span t-esc="state.settings.status.rule_count"/></div>
                  <div class="pbcfg-stat"><span class="k">Formulas</span><span t-esc="state.settings.status.formula_rule_count"/></div>
                  <div class="pbcfg-stat"><span class="k">Inputs</span><span t-esc="state.settings.status.input_rule_count"/></div>
                  <button class="pbfs-btn soft sm" style="margin-top:10px" t-on-click="() => this.setView('cards')">Edit components →</button>
                </div>
              </div>

              <div class="ce-sec pbcfg-adv">
                <button class="ce-sec-toggle" t-on-click="toggleCfgAdv">
                  <svg t-attf-class="chev {{ state.cfgAdvOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                  Advanced
                </button>
                <t t-if="state.cfgAdvOpen">
                  <div class="ce-grid2" style="margin-top:14px">
                    <div class="ce-field"><label>Payroll structure</label>
                      <CfgCombo options="cfgMeta('structures')" value="state.setDraft.structure_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('structure_id', id)"/></div>
                    <div class="ce-field"><label>Data source connector</label>
                      <CfgCombo options="cfgMeta('connectors')" value="state.setDraft.connector_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('connector_id', id)"/></div>
                    <div class="ce-field"><label>Reference code</label><input class="ce-input mono" t-att-value="state.setDraft.code" t-on-input="(ev)=>this.setCfgField('code',ev)"/></div>
                  </div>
                  <label class="ce-tog" style="margin-top:14px"><input type="checkbox" t-att-checked="state.setDraft.use_color_coded_excel_import" t-on-change="(ev)=>this.setCfgField('use_color_coded_excel_import',ev)"/><span>Use color-coded Excel import</span></label>
                </t>
              </div>
            </t>

            <!-- AUTOMATION -->
            <t t-elif="state.settingsTab === 'automation'">
              <div class="pbcfg-grid">
                <div class="ce-sec">
                  <div class="ce-sec-h">Proration</div>
                  <label class="ce-tog"><input type="checkbox" t-att-checked="state.setDraft.use_proration" t-on-change="(ev)=>this.setCfgField('use_proration',ev)"/><span>Enable proration</span></label>
                  <t t-if="state.setDraft.use_proration">
                    <div class="ce-grid2" style="margin-top:14px">
                      <div class="ce-field"><label>Proration basis</label>
                        <select class="ce-input" t-on-change="(ev)=>this.setCfgField('proration_basis',ev)">
                          <option t-foreach="cfgMeta('proration_bases')" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.setDraft.proration_basis === o.value" t-esc="o.label"/>
                        </select></div>
                      <div class="ce-field"><label>Rounding (decimals)</label><input class="ce-input" type="number" t-att-value="state.setDraft.proration_rounding" t-on-change="(ev)=>this.setCfgField('proration_rounding',ev)"/></div>
                    </div>
                    <label class="ce-label" style="margin-top:14px; display:block">Prorated components</label>
                    <div class="ce-pal-grp">
                      <button t-foreach="cfgMeta('components')" t-as="o" t-key="o.id" t-attf-class="ce-chip col {{ cfgM2MHas('proration_component_ids', o.id) ? 'on' : '' }}" t-on-click="()=>this.toggleCfgM2M('proration_component_ids', o.id)" t-att-title="o.name">
                        <span class="ce-chip-col" t-esc="o.col"/><span class="ce-chip-nm" t-esc="o.name"/>
                      </button>
                    </div>
                  </t>
                </div>

                <div class="ce-sec">
                  <div class="ce-sec-h">Retroactive adjustments</div>
                  <label class="ce-tog"><input type="checkbox" t-att-checked="state.setDraft.use_auto_retro" t-on-change="(ev)=>this.setCfgField('use_auto_retro',ev)"/><span>Enable auto retro</span></label>
                  <div t-if="state.setDraft.use_auto_retro" class="ce-field" style="margin-top:14px"><label>Retro target component</label>
                    <CfgCombo options="cfgMeta('components')" value="state.setDraft.retro_component_id" placeholder="'— none —'" onSelect="(id) => this.pickCfgM2O('retro_component_id', id)"/></div>
                </div>
              </div>
            </t>

            <!-- INTELLIGENCE (T1.5 + T1.6) -->
            <t t-elif="state.settingsTab === 'intelligence'">
              <!-- circular references first — the loudest signal -->
              <div t-if="graphCycles.length" class="ce-sec pbfs-intel-cyc">
                <div class="ce-sec-h">Circular references</div>
                <div class="ce-sec-sub">These components depend on each other in a loop — the engine cannot resolve them.</div>
                <t t-foreach="graphCycles" t-as="cy" t-key="cy_index">
                  <div class="intel-cyc-row">
                    <span class="cyc-msg" t-esc="cy.human_explanation"/>
                    <button class="pbfs-btn ghost sm" t-on-click="() => this.showCyclePath(cy)">Show path</button>
                  </div>
                </t>
              </div>

              <div class="pbcfg-grid pbfs-intel">
                <div class="ce-sec">
                  <div class="ce-sec-h">Execution order</div>
                  <div class="ce-sec-sub">The order the engine evaluates formulas — every dependency is computed before the components that use it.</div>
                  <div class="intel-order">
                    <t t-foreach="execOrderItems" t-as="it" t-key="it.id">
                      <div class="intel-ord-row" t-att-data-col="it.col">
                        <span class="intel-step" t-esc="it_index + 1"/>
                        <span class="intel-col" t-esc="it.col"/>
                        <span class="intel-nm" t-esc="it.name"/>
                        <span t-if="it.is_valid" class="intel-ok" title="Valid">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 4 4 10-10"/></svg>
                        </span>
                        <span t-else="" class="intel-bad" title="Check this formula">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                        </span>
                      </div>
                    </t>
                    <div t-if="!execOrderItems.length" class="intel-empty">No formula components yet.</div>
                  </div>
                </div>

                <div class="ce-sec">
                  <div class="ce-sec-h">Unused components</div>
                  <div class="ce-sec-sub">Nothing depends on these and they don't appear on payslips — safe to archive.</div>
                  <div class="intel-unused">
                    <t t-foreach="unusedItems" t-as="u" t-key="u.id">
                      <div class="intel-un-row" t-att-data-col="u.col">
                        <span class="intel-col" t-esc="u.col"/>
                        <span class="intel-nm" t-esc="u.name"/>
                        <span class="intel-typ" t-esc="u.type"/>
                        <button t-if="state.canEdit" class="pbfs-btn danger sm" t-on-click="() => this.deleteComponent(u.id)" title="Archive (delete) this component">Archive</button>
                      </div>
                    </t>
                    <div t-if="!unusedItems.length" class="intel-empty">No unused components — every component is referenced or shown on a payslip.</div>
                  </div>
                </div>
              </div>
            </t>
          </div>

          <div class="pbcfg-foot">
            <button class="pbfs-btn ghost" t-on-click="revertSettings">Revert</button>
            <button class="pbfs-btn primary" t-att-disabled="state.settingsBusy" t-on-click="saveSettings">Save settings</button>
          </div>
        </div>
        </t>

        <!-- ============ TEST &amp; VALIDATE WORKBENCH ============ -->
        <t t-elif="state.view === 'test'">
        <!-- W83 — test coverage strip: what the samples do / don't exercise -->
        <div t-if="state.testCoverage and state.testCoverage.ok" class="pbcov">
          <div class="pbcov-main">
            <div t-attf-class="pbcov-pct {{ state.testCoverage.pct >= 80 ? 'good' : (state.testCoverage.pct >= 40 ? 'mid' : 'low') }}">
              <span class="n" t-esc="state.testCoverage.pct"/><span class="u">%</span>
            </div>
            <div class="pbcov-body">
              <div class="pbcov-title">Test coverage</div>
              <div class="pbcov-chips">
                <span class="pbcov-chip asserted"><b t-esc="state.testCoverage.asserted.length"/> asserted</span>
                <span class="pbcov-chip exercised"><b t-esc="state.testCoverage.exercised.length"/> exercised</span>
                <button t-attf-class="pbcov-chip untested {{ state.testCoverage.untested.length ? 'clk' : '' }}"
                        t-on-click="toggleCoverage" t-att-disabled="!state.testCoverage.untested.length">
                  <b t-esc="state.testCoverage.untested.length"/> untested
                  <svg t-if="state.testCoverage.untested.length" t-attf-class="chev {{ state.coverageOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"/></svg>
                </button>
                <span t-if="state.testCoverage.orphan_inputs.length" class="pbcov-chip orphan" t-attf-title="Inputs/constants no asserted formula reads: {{ state.testCoverage.orphan_inputs.map(o => o.code).join(', ') }}">
                  <b t-esc="state.testCoverage.orphan_inputs.length"/> unread input<t t-if="state.testCoverage.orphan_inputs.length !== 1">s</t>
                </span>
              </div>
            </div>
          </div>
          <div t-if="state.coverageOpen and state.testCoverage.untested.length" class="pbcov-list">
            <div class="pbcov-list-h">Untested calculated components — no sample verifies these</div>
            <button t-foreach="state.testCoverage.untested" t-as="u" t-key="u.rule_id" class="pbcov-row"
                    t-on-click="() => this.coverageJump(u.rule_id)" t-attf-title="Jump to {{ u.col }} in the grid">
              <span class="c" t-esc="u.col"/>
              <span class="nm" t-esc="u.name"/>
              <span class="cd" t-esc="u.code"/>
              <svg class="go" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
            </button>
          </div>
        </div>
        <div class="pbtest">
          <!-- left rail -->
          <div class="pbtest-rail">
            <div class="pbtest-rail-head">
              <div class="pbtest-rail-title">Test Samples</div>
              <div class="pbtest-btnrow">
                <button class="pbfs-btn soft sm" t-on-click="addManualSample" title="Enter input values by hand">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg>Manual
                </button>
                <div class="pbtest-gen">
                  <button class="pbfs-btn primary sm" t-on-click="toggleTestGen">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>Generate
                  </button>
                  <div t-if="state.testGenOpen" t-attf-class="pbtest-gen-menu {{ state.genMode ? 'wide' : '' }}">
                    <!-- default menu -->
                    <t t-if="!state.genMode">
                      <div class="gm-rand">
                        <div class="gm-rl">Random samples</div>
                        <div class="gm-rrow">
                          <label class="gm-f"><span>Count</span><input type="number" min="1" t-att-value="state.randomCount" t-on-change="(ev)=>this.setRandomField('randomCount',ev)"/></label>
                          <label class="gm-f"><span>Min salary</span><input type="number" t-att-value="state.randomMin" t-on-change="(ev)=>this.setRandomField('randomMin',ev)"/></label>
                          <label class="gm-f"><span>Max salary</span><input type="number" t-att-value="state.randomMax" t-on-change="(ev)=>this.setRandomField('randomMax',ev)"/></label>
                        </div>
                        <button class="pbfs-btn soft sm gm-go" t-on-click="generateRandom">Generate</button>
                      </div>
                      <button class="gm-item" t-on-click="() => this.generateFromWizard('employees')">From employees <small>pull contract values</small></button>
                      <button class="gm-item" t-on-click="() => this.generateFromWizard('payslips')">From payslips <small>inputs + expected (regression)</small></button>
                      <button class="gm-item" t-on-click="openBoundaryPanel">Boundary tests… <small>edge−1 / edge / edge+1 at every threshold</small></button>
                      <button class="gm-item" t-on-click="openAiPanel">AI suggest profiles… <small>realistic inputs, engine-computed baseline</small></button>
                    </t>

                    <!-- W84 — boundary panel -->
                    <t t-elif="state.genMode === 'boundary'">
                      <div class="gm-head"><button class="gm-back" t-on-click="backToGenMenu"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg></button><span>Boundary tests</span></div>
                      <div t-if="state.boundaryBusy and !state.boundaryCands" class="gm-note">Scanning formulas for edges…</div>
                      <t t-if="state.boundaryCands">
                        <div t-if="!state.boundaryCands.candidates.length" class="gm-note">No rate-table brackets or comparison thresholds found in this config.</div>
                        <t t-else="">
                          <div class="gm-sub">Reachable edges <small>(<t t-esc="state.boundaryCands.reachable"/> — pin an input; each makes 3 samples)</small></div>
                          <div class="gm-cands">
                            <t t-foreach="state.boundaryCands.candidates" t-as="c" t-key="c.key">
                              <label t-if="c.reachable" class="gm-cand">
                                <input type="checkbox" t-att-checked="state.boundaryPicks.includes(c.key)" t-on-change="() => this.toggleBoundaryPick(c.key)"/>
                                <span class="lb"><b t-esc="c.label"/><small t-esc="'via ' + c.formula_code"/></span>
                              </label>
                            </t>
                          </div>
                          <t t-set="unreach" t-value="state.boundaryCands.candidates.filter(c => !c.reachable)"/>
                          <t t-if="unreach.length">
                            <div class="gm-sub muted">Not reachable <small>(<t t-esc="unreach.length"/> — operand is computed; set inputs manually)</small></div>
                            <div class="gm-cands">
                              <div t-foreach="unreach" t-as="c" t-key="c.key" class="gm-cand off" t-att-title="c.reason">
                                <span class="lb"><b t-esc="c.label"/><small t-esc="c.reason"/></span>
                              </div>
                            </div>
                          </t>
                          <label class="gm-base">Clone inputs from
                            <select t-on-change="setBoundaryBase">
                              <option t-foreach="state.test.samples" t-as="bs" t-key="bs.id" t-att-value="bs.id" t-att-selected="bs.id === state.boundaryBase" t-esc="bs.name"/>
                            </select>
                          </label>
                          <button class="pbfs-btn primary sm gm-go" t-att-disabled="state.boundaryBusy" t-on-click="runBoundaryGen">Generate boundary samples</button>
                          <div t-if="state.boundaryResult" class="gm-note">
                            <b t-esc="state.boundaryResult.created"/> created<t t-if="state.boundaryResult.skipped">, <t t-esc="state.boundaryResult.skipped"/> already existed</t><t t-if="state.boundaryResult.capped">, <t t-esc="state.boundaryResult.capped"/> over the 60-per-run cap</t>.
                          </div>
                        </t>
                      </t>
                    </t>

                    <!-- W49 — AI panel -->
                    <t t-elif="state.genMode === 'ai'">
                      <div class="gm-head"><button class="gm-back" t-on-click="backToGenMenu"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg></button><span>AI suggest profiles</span></div>
                      <div t-if="state.aiBusy" class="gm-note">Asking PayAI for realistic profiles…</div>
                      <div t-if="state.aiError and !state.aiBusy" class="gm-note err" t-esc="state.aiError"/>
                      <t t-if="state.aiProps and !state.aiBusy">
                        <div t-if="!state.aiProps.length and !state.aiError" class="gm-note">No usable profiles returned.</div>
                        <div t-if="state.aiProps.length" class="gm-sub">Proposed profiles <small>(engine computes the baseline — the LLM never supplies outputs)</small></div>
                        <div t-if="state.aiProps.length" class="gm-cands">
                          <label t-foreach="state.aiProps" t-as="p" t-key="p_index" class="gm-cand">
                            <input type="checkbox" t-att-checked="state.aiPicks.includes(p_index)" t-on-change="() => this.toggleAiPick(p_index)"/>
                            <span class="lb"><b t-esc="p.name"/><small t-esc="p.rationale"/></span>
                          </label>
                        </div>
                        <div t-if="state.aiRejected.length" class="gm-note err">
                          <b t-esc="state.aiRejected.length"/> rejected: <t t-foreach="state.aiRejected" t-as="rj" t-key="rj_index"><t t-esc="rj.name + ' (' + rj.reason + ')'"/><t t-if="rj_index !== state.aiRejected.length - 1">; </t></t>
                        </div>
                        <button t-if="state.aiProps.length" class="pbfs-btn primary sm gm-go" t-att-disabled="state.aiBusy" t-on-click="acceptAiSamples">Add selected profiles</button>
                      </t>
                    </t>
                  </div>
                </div>
                <button class="pbfs-btn soft sm" t-on-click="triggerImport" title="Import input values from an Excel file">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>Import Excel
                </button>
                <button class="pbfs-btn soft sm" t-on-click="exportTestTemplate" title="Download a blank input template (.xlsx)">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>Export Template
                </button>
                <input type="file" t-ref="testFile" accept=".xlsx" style="display:none" t-on-change="onImportFile"/>
              </div>
            </div>
            <button class="pbfs-btn soft sm pbtest-runall" t-on-click="runAllTests">Run all tests</button>
            <button t-if="hasUnconfirmed" class="pbfs-btn soft sm pbtest-runall pbtest-confirmall" t-on-click="confirmAllBaselines">Confirm all baselines</button>
            <div class="pbtest-list">
              <div t-if="!state.test.samples.length" class="pbtest-empty">No samples yet — Generate one to start.</div>
              <div t-foreach="state.test.samples" t-as="s" t-key="s.id"
                   t-attf-class="pbtest-item {{ s.id === state.testSampleId ? 'active' : '' }}"
                   t-on-click="() => this.selectSample(s.id)">
                <div class="nm" t-esc="s.name"/>
                <span t-attf-class="pbcfg-vbadge {{ s.verdict }}" t-esc="s.verdict"/>
                <button class="pbtest-del" t-on-click="(ev) => { ev.stopPropagation(); this.deleteSample(s.id); }" title="Delete sample">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
                </button>
              </div>
            </div>
          </div>

          <!-- detail -->
          <div class="pbtest-detail">
            <t t-if="state.testDetail">
              <div class="pbtest-dhead">
                <input class="pbtest-name" t-att-value="state.testDetail.name" t-on-change="renameSample"/>
                <span class="pbtest-src" t-esc="state.testDetail.source_type"/>
                <span t-attf-class="pbcfg-vbadge big {{ state.testDetail.verdict }}" t-esc="state.testDetail.verdict"/>
                <span t-if="state.testDetail.has_expected and state.testDetail.expected_confirmed === false" class="pbtest-needs">needs confirmation</span>
                <div class="pbtest-dact">
                  <button class="pbfs-btn primary sm" t-on-click="openInFormulaView">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
                    <t t-esc="state.testDetail.source_type === 'manual' ? 'Calculate in Formula View' : 'Open in Formula View'"/>
                  </button>
                  <button t-if="state.testDetail.has_expected and state.testDetail.expected_confirmed === false" class="pbfs-btn soft sm" t-on-click="() => this.confirmBaseline(state.testDetail.id)">Confirm baseline</button>
                  <button t-if="!state.testDetail.has_expected" class="pbfs-btn soft sm" t-on-click="snapshotExpected">Snapshot as expected</button>
                  <button t-if="state.testDetail.has_expected" class="pbfs-btn ghost sm" t-on-click="clearExpected">Clear expected</button>
                </div>
              </div>

              <!-- verdict banner -->
              <t t-set="fails" t-value="state.testDetail.rows.filter(r => r.column_type !== 'input' &amp;&amp; r.expected !== null &amp;&amp; r.expected !== undefined &amp;&amp; !r.passed)"/>
              <div t-if="!state.testDetail.has_expected" class="pbtest-banner preview">
                <b>Preview only.</b> No expected baseline yet — the outputs below are just computed. Click <b>Snapshot as expected</b> to freeze them as the pass/fail baseline, or generate from payslips for real expected values.
              </div>
              <div t-elif="state.testDetail.expected_confirmed === false" class="pbtest-banner preview"><b>Awaiting confirmation.</b> This baseline was captured from the current engine output. It stays <b>out</b> of the pass/fail chip (counted as pending) until you <b>Confirm baseline</b>.</div>
              <div t-elif="state.testDetail.verdict === 'passed'" class="pbtest-banner passed"><b>Passed.</b> Every output matches its expected baseline.</div>
              <div t-else="" t-attf-class="pbtest-banner {{ state.testDetail.verdict }}"><b><t t-esc="state.testDetail.verdict === 'warning' ? 'Warning' : 'Failed'"/>.</b> <t t-esc="fails.length"/> output<t t-if="fails.length !== 1">s</t> differ from the expected baseline — flagged ✗ below.</div>

              <!-- INPUTS first (edit at the top, results recompute below) -->
              <div class="ce-sec">
                <button class="ce-sec-toggle" t-on-click="toggleTestInputs">
                  <svg t-attf-class="chev {{ state.testInputsOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                  Inputs (<t t-esc="state.testDetail.rows.filter(x => x.column_type === 'input').length"/>) <span class="pbtest-hint">edit to recompute live</span>
                </button>
                <t t-if="state.testInputsOpen">
                  <div class="pbtest-inputs" style="margin-top:14px">
                    <t t-foreach="state.testDetail.rows" t-as="r" t-key="r.code">
                      <div t-if="r.column_type === 'input'" class="pbtest-inrow">
                        <span class="lbl"><span class="c" t-esc="r.column_letter"/><t t-esc="r.name"/></span>
                        <input class="ce-input" type="number" step="any" t-att-value="r.input" t-on-input="(ev) => this.onTestInput(r.code, ev)"/>
                      </div>
                    </t>
                    <div t-if="!state.testDetail.rows.some(x => x.column_type === 'input')" class="pbtest-hint">This config has no input components.</div>
                  </div>
                </t>
              </div>

              <!-- RESULTS below inputs -->
              <div class="ce-sec">
                <div class="ce-sec-h">Results<t t-if="state.testDetail.has_expected"> · expected vs computed</t></div>
                <table class="pbcfg-table">
                  <thead><tr><th>Col</th><th>Component</th><th t-if="state.testDetail.has_expected">Expected</th><th>Computed</th><th t-if="state.testDetail.has_expected">Diff</th><th t-if="state.testDetail.has_expected"></th></tr></thead>
                  <tbody>
                    <t t-foreach="state.testDetail.rows" t-as="r" t-key="r.code">
                      <tr t-if="r.column_type !== 'input'" t-attf-class="{{ state.testDetail.has_expected &amp;&amp; r.expected !== null &amp;&amp; r.expected !== undefined &amp;&amp; !r.passed ? 'failrow' : '' }}">
                        <td t-esc="r.column_letter"/><td t-esc="r.name"/>
                        <td t-if="state.testDetail.has_expected" t-esc="tcell(r.expected)"/>
                        <td t-esc="tcell(r.computed)"/>
                        <td t-if="state.testDetail.has_expected" t-esc="(r.expected !== null &amp;&amp; r.expected !== undefined) ? (r.discrepancy ? r.discrepancy.toFixed(2)+'%' : '0%') : '—'"/>
                        <td t-if="state.testDetail.has_expected">
                          <span t-if="r.expected !== null &amp;&amp; r.expected !== undefined" t-attf-class="pbtest-tick {{ r.passed ? 'ok' : 'no' }}"><t t-esc="r.passed ? '✓' : '✗'"/></span>
                          <span t-else="" class="pbtest-tick na">—</span>
                        </td>
                      </tr>
                    </t>
                  </tbody>
                </table>
              </div>
            </t>
            <div t-else="" class="pbtest-noselect">Select or generate a sample to view its inputs &amp; results.</div>
          </div>
        </div>
        </t>

        <!-- W97 — period comparison view -->
        <t t-elif="state.view === 'compare'">
        <div class="pbcmp">
          <div class="pbcmp-head">
            <div class="pbcmp-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h7M3 6l2-2M3 6l2 2M21 18h-7M21 18l-2-2M21 18l-2 2"/></svg>
              <div>
                <b t-esc="state.cmpMode === 'budget' ? 'Budget vs actual' : 'Compare periods'"/>
                <small t-esc="state.cmpMode === 'budget' ? 'Fold a payrun against a per-component budget.' : 'Fold two payruns of this configuration component-by-component.'"/>
              </div>
            </div>
            <div class="pbcmp-right">
              <!-- W95 — mode toggle -->
              <div class="pbcmp-modes">
                <button t-attf-class="{{ state.cmpMode === 'period' ? 'on' : '' }}" t-on-click="() => this.setCmpMode('period')">vs Period</button>
                <button t-attf-class="{{ state.cmpMode === 'budget' ? 'on' : '' }}" t-on-click="() => this.setCmpMode('budget')">vs Budget</button>
              </div>

              <!-- period pickers -->
              <div t-if="state.cmpMode === 'period'" class="pbcmp-pickers">
                <label>Before
                  <select t-on-change="(ev) => this.onCmpPick('a', ev)">
                    <option value="">—</option>
                    <option t-foreach="state.cmpRuns" t-as="r" t-key="r.id" t-att-value="r.id" t-att-selected="r.id === state.cmpA" t-esc="r.name + ' · ' + r.slips"/>
                  </select>
                </label>
                <svg class="pbcmp-arr" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                <label>After
                  <select t-on-change="(ev) => this.onCmpPick('b', ev)">
                    <option value="">—</option>
                    <option t-foreach="state.cmpRuns" t-as="r" t-key="r.id" t-att-value="r.id" t-att-selected="r.id === state.cmpB" t-esc="r.name + ' · ' + r.slips"/>
                  </select>
                </label>
                <button class="pbfs-btn primary" t-att-disabled="state.cmpBusy" t-on-click="runCompare">
                  <t t-if="state.cmpBusy">Comparing… <t t-esc="state.cmpProgress"/>%</t>
                  <t t-else="">Compare</t>
                </button>
              </div>

              <!-- budget pickers -->
              <div t-else="" class="pbcmp-pickers">
                <label>Budget
                  <div class="pbcmp-budpick">
                    <select t-on-change="(ev) => this.onBudgetPick(ev)" t-att-disabled="!state.budgets.length">
                      <option value="" t-if="!state.budgets.length">— no budgets yet —</option>
                      <option t-foreach="state.budgets" t-as="b" t-key="b.id" t-att-value="b.id" t-att-selected="b.id === state.budgetSel" t-esc="b.name + (b.period_label ? ' · ' + b.period_label : '')"/>
                    </select>
                    <button t-if="state.budgetCanEdit and state.budgetSel" class="pbcmp-budedit" title="Edit budget" t-on-click="() => this.openBudgetEditor(state.budgetSel)">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>
                    </button>
                  </div>
                </label>
                <button class="pbcmp-newbud" t-if="state.budgetCanEdit" t-on-click="newBudget" title="New budget">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                  New budget
                </button>
                <svg class="pbcmp-arr" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                <label>Actual payrun
                  <select t-on-change="(ev) => this.onCmpPick('b', ev)">
                    <option value="">—</option>
                    <option t-foreach="state.cmpRuns" t-as="r" t-key="r.id" t-att-value="r.id" t-att-selected="r.id === state.cmpB" t-esc="r.name + ' · ' + r.slips"/>
                  </select>
                </label>
                <button class="pbfs-btn primary" t-att-disabled="state.cmpBusy" t-on-click="runBudgetCompare">
                  <t t-if="state.cmpBusy">Comparing… <t t-esc="state.cmpProgress"/>%</t>
                  <t t-else="">Compare</t>
                </button>
              </div>
            </div>
          </div>

          <div t-if="state.cmpBusy" class="pbcmp-progress"><div class="bar" t-attf-style="width:{{ state.cmpProgress }}%"/></div>

          <div t-if="!state.cmpRuns.length" class="pbcmp-empty">
            <b>No comparable payruns yet</b>
            <span>Run this configuration over at least two periods to compare them here.</span>
          </div>

          <t t-elif="state.cmpResult and !state.cmpResult.empty">
            <!-- ===== W95 (WP-H) budget-vs-actual layout ===== -->
            <t t-if="state.cmpResult.mode === 'budget'">
              <div class="pbcmp-stats">
                <div class="pbcmp-stat"><b t-esc="state.cmpCurrency + Math.round(state.cmpResult.total_budget).toLocaleString('en-US')"/><span>total budget</span></div>
                <div class="pbcmp-stat"><b t-esc="state.cmpCurrency + Math.round(state.cmpResult.total_actual).toLocaleString('en-US')"/><span>total actual</span></div>
                <div class="pbcmp-stat"><b t-attf-class="{{ state.cmpResult.total_delta >= 0 ? 'up' : 'dn' }}" t-esc="fmtSigned(Math.round(state.cmpResult.total_delta))"/><span>variance</span></div>
                <div class="pbcmp-stat"><b t-esc="state.cmpResult.coverage.unbudgeted.length"/><span>un-budgeted</span></div>
                <div class="pbcmp-stat"><b t-esc="state.cmpResult.coverage.orphan_lines.length"/><span>orphan lines</span></div>
              </div>

              <!-- coverage honesty (C7): both sets always shown -->
              <div class="pbcmp-cover">
                <div class="pbcov-card">
                  <div class="pbcov-h">Un-budgeted components</div>
                  <div t-if="state.cmpResult.coverage.unbudgeted.length" class="pbcov-chips">
                    <span t-foreach="state.cmpResult.coverage.unbudgeted" t-as="uc" t-key="uc" class="pbcov-chip" t-esc="uc"/>
                  </div>
                  <div t-else="" class="pbcov-none">Every component with actuals has a budget line.</div>
                </div>
                <div class="pbcov-card">
                  <div class="pbcov-h">Orphan budget lines</div>
                  <div t-if="state.cmpResult.coverage.orphan_lines.length" class="pbcov-chips">
                    <span t-foreach="state.cmpResult.coverage.orphan_lines" t-as="oc" t-key="oc" class="pbcov-chip gone" t-esc="oc"/>
                  </div>
                  <div t-else="" class="pbcov-none">No budget line points at a missing component.</div>
                </div>
              </div>

              <!-- variance table -->
              <div class="pbcmp-tablewrap">
                <table class="pbcmp-table">
                  <thead>
                    <tr>
                      <th class="s" t-on-click="() => this.cmpSetSort('code')">Component</th>
                      <th class="n" t-on-click="() => this.cmpSetSort('sum_a')">Budget</th>
                      <th class="n" t-on-click="() => this.cmpSetSort('sum_b')">Actual</th>
                      <th class="n" t-on-click="() => this.cmpSetSort('delta')">Δ</th>
                      <th class="n" t-on-click="() => this.cmpSetSort('delta_pct')">Δ%</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr t-foreach="cmpComponents" t-as="c" t-key="c.code" t-attf-class="{{ c.orphan ? 'orphan' : '' }}">
                      <td class="s">
                        <t t-esc="c.code"/>
                        <span t-if="c.orphan" class="pbcmp-gonetag">component gone</span>
                      </td>
                      <td class="n" t-esc="Math.round(c.sum_a).toLocaleString('en-US')"/>
                      <td class="n" t-esc="Math.round(c.sum_b).toLocaleString('en-US')"/>
                      <td class="n d" t-attf-style="{{ cmpHeat(c.delta) }}" t-esc="fmtSigned(Math.round(c.delta))"/>
                      <td class="n" t-esc="c.delta_pct == null ? '—' : (fmtSigned(c.delta_pct) + '%')"/>
                    </tr>
                  </tbody>
                </table>
              </div>
            </t>

            <!-- ===== period layout ===== -->
            <t t-else="">
            <!-- headline stats -->
            <div class="pbcmp-stats">
              <div class="pbcmp-stat"><b t-esc="state.cmpResult.matched.toLocaleString('en-US')"/><span>matched employees</span></div>
              <div class="pbcmp-stat"><b t-esc="state.cmpResult.net_moved.toLocaleString('en-US')"/><span><t t-esc="state.cmpResult.headline"/> moved</span></div>
              <div class="pbcmp-stat"><b t-esc="state.cmpResult.joiners.toLocaleString('en-US')"/><span>joiners</span></div>
              <div class="pbcmp-stat"><b t-esc="state.cmpResult.leavers.toLocaleString('en-US')"/><span>leavers</span></div>
              <button class="pbfs-btn soft pbcmp-narr" t-on-click="openNarrate" t-att-disabled="state.cmpNarrateBusy" title="Narrate this comparison">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
                <t t-if="state.cmpNarrateBusy">Narrating…</t><t t-else="">Narrate</t>
              </button>
            </div>

            <!-- cause candidates -->
            <div t-if="state.cmpResult.causes.length" class="pbcmp-causes">
              <div class="pbcmp-causes-h">Likely cause<t t-if="state.cmpResult.causes.length !== 1">s</t></div>
              <div t-foreach="state.cmpResult.causes" t-as="cz" t-key="cz.code" class="pbcmp-cause">
                <span class="cc-code" t-esc="cz.code"/>
                <span class="cc-cov">moved for <t t-esc="cz.coverage"/>% of changed employees</span>
                <span t-if="cz.attributed" class="cc-attr">edited <t t-esc="cz.when"/> (<t t-esc="cz.reason"/>)</span>
              </div>
            </div>

            <!-- W48 — narration card -->
            <div t-if="state.cmpNarrate" class="pbcmp-narr-card">
              <div class="pbcmp-narr-head">
                <div class="nh-left">
                  <b>Narrative</b>
                  <span t-attf-class="nh-src {{ state.cmpNarrate.source }}" t-esc="state.cmpNarrate.source === 'ai' ? 'AI-polished' : 'Built-in'"/>
                </div>
                <div class="nh-right">
                  <div class="nh-lang">
                    <button t-attf-class="{{ state.cmpNarrateLang === 'en' ? 'on' : '' }}" t-on-click="() => this.setNarrateLang('en')">EN</button>
                    <button t-attf-class="{{ state.cmpNarrateLang === 'vi' ? 'on' : '' }}" t-on-click="() => this.setNarrateLang('vi')">VI</button>
                  </div>
                  <button class="pbfs-btn ghost sm" t-on-click="copyNarrate" title="Copy narrative">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                  </button>
                </div>
              </div>
              <p t-foreach="state.cmpNarrate.blocks" t-as="b" t-key="b_index" class="pbcmp-narr-line" t-esc="b"/>
              <div t-if="state.cmpNarrateBusy" class="pbcmp-narr-busy">Narrating…</div>
            </div>

            <!-- component delta table -->
            <div class="pbcmp-tablewrap">
              <table class="pbcmp-table">
                <thead>
                  <tr>
                    <th class="s" t-on-click="() => this.cmpSetSort('code')">Component</th>
                    <th class="n" t-on-click="() => this.cmpSetSort('sum_a')">Before (<t t-esc="state.cmpResult.run_a.date_end"/>)</th>
                    <th class="n" t-on-click="() => this.cmpSetSort('sum_b')">After (<t t-esc="state.cmpResult.run_b.date_end"/>)</th>
                    <th class="n" t-on-click="() => this.cmpSetSort('delta')">Δ</th>
                    <th class="n" t-on-click="() => this.cmpSetSort('n_changed')">Employees</th>
                  </tr>
                </thead>
                <tbody>
                  <tr t-foreach="cmpComponents" t-as="c" t-key="c.code">
                    <td class="s" t-esc="c.code"/>
                    <td class="n" t-esc="c.sum_a.toLocaleString('en-US')"/>
                    <td class="n" t-esc="c.sum_b.toLocaleString('en-US')"/>
                    <td class="n d" t-attf-style="{{ cmpHeat(c.delta) }}" t-esc="fmtSigned(c.delta)"/>
                    <td class="n" t-esc="c.n_changed.toLocaleString('en-US')"/>
                  </tr>
                </tbody>
              </table>
            </div>

            <!-- movers drawer -->
            <div t-if="state.cmpResult.movers.length" class="pbcmp-movers">
              <button class="pbcmp-movers-h" t-on-click="toggleCmpMovers">
                <svg t-attf-class="chev {{ state.cmpMoversOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                Biggest employee movers (<t t-esc="state.cmpResult.headline"/>)
              </button>
              <div t-if="state.cmpMoversOpen" class="pbcmp-movers-list">
                <div t-foreach="state.cmpResult.movers" t-as="m" t-key="m_index" class="pbcmp-mover">
                  <span class="mv-emp" t-esc="m.emp || m.ref"/>
                  <span class="mv-ab"><t t-esc="m.a.toLocaleString('en-US')"/> → <t t-esc="m.b.toLocaleString('en-US')"/></span>
                  <span t-attf-class="mv-d {{ m.delta >= 0 ? 'up' : 'dn' }}" t-esc="fmtSigned(m.delta)"/>
                </div>
              </div>
            </div>
            </t>
          </t>

          <div t-elif="state.cmpResult and state.cmpResult.empty" class="pbcmp-empty">
            <b>No matched employees</b>
            <span>These two periods share no employees computed with this configuration.</span>
          </div>

          <div t-elif="!state.cmpBusy" class="pbcmp-idle">
            <t t-if="state.cmpMode === 'budget'">Pick a budget and a payrun, then hit Compare.</t>
            <t t-else="">Pick two periods and hit Compare.</t>
          </div>
        </div>
        </t>

        <!-- empty config: "finish setup" resume panel (no dead-end) -->
        <t t-elif="!state.components.length">
        <div class="pbfs-build">
          <div class="pbfs-build-inner">
            <div class="pbfs-build-head">
              <h2>Finish setting up <t t-esc="state.config.name"/></h2>
              <p>This configuration has no components yet. Pick how you'd like to build it — you can mix and match afterwards.</p>
            </div>
            <div class="pbfs-build-grid">
              <button class="pbfs-build-card" t-on-click="importExcelInto">
                <span class="ic indigo"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg></span>
                <span class="t">Import from Excel</span>
                <span class="d">Upload a spreadsheet — sheets, columns &amp; formulas detected automatically.</span>
              </button>
              <button class="pbfs-build-card" t-on-click="() => this.applyStarter('vn_standard')">
                <span class="ic green"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg></span>
                <span class="t">Vietnam Standard starter</span>
                <span class="d">10 pre-wired components: Basic, allowances, SI/HI/UI, Gross &amp; Net.</span>
              </button>
              <button class="pbfs-build-card" t-on-click="addComponentQuick">
                <span class="ic blue"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></span>
                <span class="t">Add a component</span>
                <span class="d">Start from scratch and define each column yourself.</span>
              </button>
              <button class="pbfs-build-card" t-on-click="openAI">
                <span class="ic violet"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg></span>
                <span class="t">Ask PayAI to draft</span>
                <span class="d">Describe your payroll in plain language and let PayAI propose formulas.</span>
              </button>
            </div>
            <button class="pbfs-build-discard" t-on-click="discardConfig">Discard this draft</button>
          </div>
        </div>
        </t>

        <!-- 3-pane (cards) / full-width workbench with drawers (grid) -->
        <t t-else="">
        <div t-attf-class="pbfs-work {{ state.view === 'grid' ? 'pbfs-work--full' : '' }}">
          <svg id="pbfsArrows" class="pbfs-arrows"/>
          <!-- outline (grid: always a drawer · cards: drawer below the md breakpoint) -->
          <div t-attf-class="pbfs-outline {{ state.view === 'grid' ? 'as-drawer' : 'md-drawer' }} {{ state.outlineDrawer ? 'open' : '' }}" data-coach="fs-components">
            <div class="ol-head">
              <span class="t">Components</span>
              <div class="ol-actions">
                <button class="ol-act ol-close" t-on-click="() => this.toggleDrawer('outline')" title="Close panel">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
                </button>
                <button class="ol-act" data-coach="fs-add" t-on-click="addComponentQuick" title="Add a component">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                </button>
                <button class="ol-act" t-on-click="importExcelInto" title="Import from Excel">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                </button>
                <button t-attf-class="ol-arrows {{ state.arrowsOn ? 'on' : '' }}" data-coach="fs-arrows" t-on-click="toggleArrows" title="Dependency arrows">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                </button>
              </div>
            </div>
            <t t-foreach="visibleGroups" t-as="grp" t-key="grp">
              <div class="ol-grp" t-esc="grp"/>
              <div t-foreach="groupItems(grp)" t-as="c" t-key="c.id"
                   t-att-data-col="c.col"
                   t-attf-class="ol-item {{ c.id === state.selectedId ? 'active' : '' }} {{ isDep(c.col) ? 'dep' : '' }} {{ isCycleMember(c.col) ? 'cyc-hi' : '' }}"
                   t-attf-style="{{ depStyle(c) }}"
                   t-on-click="() => this.selectComponent(c.id)">
                <span class="ol-col" t-esc="c.col"/>
                <span class="ol-nm" t-esc="c.name"/>
                <span t-if="!c.is_valid" class="ol-err" title="Has errors"/>
                <span class="ol-val" t-esc="previewVal(c.col)"/>
                <span t-attf-class="ol-typ {{ c.type }}" t-esc="c.type"/>
              </div>
            </t>
          </div>

          <!-- editor: cards -->
          <div t-if="state.view === 'cards'" class="pbfs-editor">
            <!-- small-screen toolbar: opens the two drawers (visible below md via CSS) -->
            <div class="pbfs-gridbar pbfs-cardbar">
              <button t-attf-class="pbfs-btn ghost sm {{ state.outlineDrawer ? 'active' : '' }}"
                      t-on-click="() => this.toggleDrawer('outline')" title="Components panel">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
                Components
              </button>
              <span class="pbfs-gridbar-spacer"/>
              <button t-attf-class="pbfs-btn ghost sm {{ state.previewDrawer ? 'active' : '' }}"
                      t-on-click="() => this.toggleDrawer('preview')" title="Live preview panel">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7z"/><circle cx="12" cy="12" r="3"/></svg>
                Preview · <t t-esc="sampleName"/>
              </button>
            </div>
            <div t-if="selected" t-attf-class="ed-card {{ editing ? 'is-editing' : '' }}">
              <t t-if="!editing">
              <div class="ed-hero" data-coach="fs-card">
                <div class="col-badge" t-esc="selected.col"/>
                <div style="flex:1; min-width:200px">
                  <h2><t t-esc="selected.name"/>
                    <t t-if="state.renameId === selected.id">
                      <span class="pbfs-rename">
                        <input class="pbfs-rename-in" t-att-value="state.renameVal" t-on-input="onRenameInput" t-on-keydown="onRenameKey" placeholder="CODE" autocomplete="off" spellcheck="false"/>
                        <button class="pbfs-rn-ok" t-att-disabled="state.renameBusy" t-on-click="commitRename" title="Rename (Enter)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg></button>
                        <button class="pbfs-rn-x" t-on-click="cancelRename" title="Cancel (Esc)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
                        <span t-if="state.renameErr" class="pbfs-rn-err" t-esc="state.renameErr"/>
                      </span>
                    </t>
                    <span t-else="" class="code-tag pbfs-code-edit" t-att-title="state.canEdit ? 'Rename code — references stay intact automatically' : ''" t-on-click="tryRename"><t t-esc="selected.code"/></span>
                  </h2>
                  <div class="sub" t-esc="'Column ' + selected.col + ' · ' + (selected.type === 'input' ? 'Input value' : selected.type === 'constant' ? 'Fixed constant' : 'Calculated formula') + (selected.category ? ' · ' + selected.category : '')"/>
                </div>
                <div class="ed-hero-side">
                  <div t-if="selected.is_valid" class="valid-ok"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m9 12 2 2 4-4"/><circle cx="12" cy="12" r="9"/></svg>Valid</div>
                  <div t-else="" class="valid-err"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>Check</div>
                  <div class="ed-actions">
                    <button class="pbfs-btn soft sm" t-on-click="openExplain" title="Explain in plain language">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 16v-4M12 8h.01"/></svg>Explain
                    </button>
                    <button class="pbfs-btn soft sm" data-coach="fs-history" t-on-click="openHistory" title="Version history">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v5h5"/><path d="M3.05 13A9 9 0 1 0 6 5.3L3 8"/><path d="M12 7v5l4 2"/></svg>History
                    </button>
                    <button t-attf-class="pbfs-btn soft sm pbfs-notesbtn {{ selected.review_open ? 'has-review' : '' }}" data-coach="fs-notes" t-on-click="openNotes" title="Comments &amp; review notes">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>Notes<span t-if="selected.note_count" class="pbfs-notes-badge" t-esc="selected.note_count"/>
                    </button>
                    <button class="pbfs-btn soft sm" data-coach="fs-editai" t-on-click="openAI" title="Edit with PayAI">
                      <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg>Edit with PayAI
                    </button>
                    <button class="pbfs-btn ghost sm" t-on-click="() => this.enterEdit(selected.id)" title="Edit this component">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.12 2.12 0 0 1 3 3L12 15l-4 1 1-4z"/></svg>Edit
                    </button>
                    <button class="pbfs-btn danger sm" t-on-click="() => this.deleteComponent(selected.id)" title="Delete this component">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>Delete
                    </button>
                  </div>
                </div>
              </div>
              <div class="ed-body">
                <div class="ed-label-row">
                  <span class="ed-label">Formula</span>
                  <div class="ed-label-tools">
                    <div t-if="selected.type === 'formula' and !rawActive" class="ff-seg" data-coach="fs-namesletters" title="Toggle formula display">
                      <button t-att-class="!state.formulaShort ? 'on' : ''" t-on-click="() => this.setFormulaForm(false)">Names</button>
                      <button t-att-class="state.formulaShort ? 'on' : ''" t-on-click="() => this.setFormulaForm(true)">Letters</button>
                    </div>
                    <div t-if="selected.type === 'formula' and state.canEdit" class="ff-seg pbfs-rawseg" data-coach="fs-rawmode" title="Edit as chips or as raw Excel text">
                      <button t-att-class="!state.rawMode ? 'on' : ''" t-on-click="() => this.setRawMode(false)">Chips</button>
                      <button t-att-class="state.rawMode ? 'on' : ''" t-on-click="() => this.setRawMode(true)">Text</button>
                    </div>
                  </div>
                </div>

                <!-- F12 — raw Excel textarea (edit-as-text) -->
                <div t-if="rawActive" class="pbfs-raw">
                  <textarea class="pbfs-raw-ta" t-ref="rawEditor" t-on-input="onRawInput" t-on-keydown="onRawKeydown" spellcheck="false" autocomplete="off" placeholder="=A2*0.1"/>
                  <div class="pbfs-raw-foot">
                    <span t-if="state.rawValid and state.rawValid.valid" class="pbfs-raw-pill ok"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>Valid</span>
                    <span t-elif="state.rawValid and !state.rawValid.valid" class="pbfs-raw-pill err"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg><t t-esc="state.rawValid.message"/></span>
                    <span t-else="" class="pbfs-raw-hint">Cmd/Ctrl+Enter to save</span>
                    <div class="pbfs-raw-actions">
                      <button t-if="state.rawDirty" class="pbfs-btn ghost sm" t-on-click="revertRaw">Revert</button>
                      <button class="pbfs-btn primary sm" t-att-disabled="state.rawBusy or !state.rawDirty or (state.rawValid and !state.rawValid.valid)" t-on-click="saveRaw">Save</button>
                    </div>
                  </div>
                </div>

                <div t-else="" class="formula-line" data-coach="fs-formula">
                  <t t-foreach="chips(selected.excel_formula)" t-as="tok" t-key="tok_index">
                    <span t-if="tok.kind === 'op'" t-attf-class="fop #{tok.ded ? 'fop-ded' : ''}" t-att-title="tok.ded ? 'This component is a deduction (a negative value) — it is subtracted from the total' : ''" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'func'" class="fchip ffunc" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'ref'" class="fchip fref clickable" t-att-data-hover-col="tok.col" t-on-click="() => this.scrollToCol(tok.col)" t-att-title="'Go to ' + (byCol(tok.col) ? byCol(tok.col).name : tok.col)">
                      <span t-if="state.formulaShort" class="ref solo" t-esc="tok.col"/>
                      <t t-else=""><span class="ref" t-esc="tok.col"/><t t-esc="tok.text"/></t>
                    </span>
                    <span t-elif="tok.kind === 'range'" class="fchip frange clickable" t-on-click="() => this.scrollToCol(tok.start)" t-att-title="'Range (' + tok.count + '): ' + tok.names.join(', ')">
                      <span class="ref" t-esc="tok.start"/><span class="rng-sep">…</span><span class="ref" t-esc="tok.end"/>
                      <span t-if="!state.formulaShort" class="rng-nm" t-esc="tok.startName + ' … ' + tok.endName"/>
                      <span class="rng-cnt" t-esc="tok.count"/>
                    </span>
                    <span t-elif="tok.kind === 'num'" class="fchip fnum" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'paren'" class="fparen" t-esc="tok.text"/>
                    <span t-elif="tok.kind === 'comma'" class="fcomma">,</span>
                    <span t-else="" class="fchip src" t-esc="tok.text"/>
                  </t>
                </div>
                <div class="dep-map" data-coach="fs-deps">
                  <div class="ed-label">Dependencies</div>
                  <div class="dep-row"><span class="lbl">Depends on</span>
                    <t t-if="selected.depends_on.length"><span t-foreach="selected.depends_on" t-as="d" t-key="d_index" class="dep-pill" t-esc="d"/></t>
                    <span t-else="" class="dep-pill">— none —</span>
                  </div>
                  <div class="dep-row"><span class="lbl">Used by</span>
                    <t t-if="selected.used_by.length"><span t-foreach="selected.used_by" t-as="d" t-key="d_index" class="dep-pill" t-esc="d"/></t>
                    <span t-else="" class="dep-pill">— none —</span>
                  </div>
                </div>
                <div t-if="!selected.is_valid and selected.validation_message" class="ed-errbox" t-esc="selected.validation_message"/>

                <!-- T1.6 — circular-reference explainer (only when this component is in a cycle) -->
                <div t-if="selectedCycle" class="pbfs-cycbox">
                  <div class="cyc-row">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2v6h-6"/><path d="M3 12a9 9 0 0 1 15-6.7L21 8"/><path d="M3 22v-6h6"/><path d="M21 12a9 9 0 0 1-15 6.7L3 16"/></svg>
                    <span class="cyc-msg" t-esc="selectedCycle.human_explanation"/>
                  </div>
                  <button class="pbfs-btn ghost sm" t-on-click="() => this.showCyclePath(selectedCycle)">Show path</button>
                </div>

                <!-- T1.4 — Insights: impact of this component (client BFS, instant) -->
                <t t-if="selected.type === 'formula'">
                  <t t-set="imp" t-value="selectedImpact"/>
                  <div class="pbfs-insights" data-coach="fs-insights">
                    <div class="ins-head"><span class="ins-t">Impact</span></div>
                    <div class="ins-sentence">
                      <t t-if="imp.down.length">
                        <b t-esc="imp.down.length"/> <t t-esc="imp.down.length === 1 ? 'component depends' : 'components depend'"/> on <b t-esc="selected.name"/><t t-if="imp.payvis.length">, <b t-esc="imp.payvis.length"/> shown on payslips</t>.
                      </t>
                      <t t-else="">Nothing depends on this yet.</t>
                    </div>
                    <div t-if="imp.up.length" class="ins-chips">
                      <span class="ins-lbl">Draws from</span>
                      <span class="pbfs-impact-chip" t-esc="imp.up.length + (imp.up.length === 1 ? ' component' : ' components')"/>
                    </div>
                    <t t-if="imp.down.length">
                      <button class="ins-toggle" t-on-click="toggleInsights">
                        <svg t-attf-class="chev {{ state.insightsOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                        <t t-esc="state.insightsOpen ? 'Hide downstream' : 'Show downstream (' + imp.down.length + ')'"/>
                      </button>
                      <div t-if="state.insightsOpen" class="ins-list">
                        <button t-foreach="imp.down" t-as="d" t-key="d.id" class="ins-item" t-on-click="() => this.selectComponent(d.id)" t-att-title="'Go to ' + d.name">
                          <span class="ins-col" t-esc="d.col"/>
                          <span class="ins-nm" t-esc="d.name"/>
                          <span t-if="d.appears_on_payslip" class="ins-badge">payslip</span>
                        </button>
                      </div>
                    </t>
                  </div>
                </t>

                <div t-if="selected.type === 'formula'" class="fc-section" data-coach="fs-flow">
                  <div class="fc-head">
                    <span class="fc-head-t">Calculation flow</span>
                    <span class="fc-legend-inline">
                      <span class="lg"><span class="sw" style="background:#0E7490"/>input</span>
                      <span class="lg"><span class="sw" style="background:#4F46E5"/>earning</span>
                      <span class="lg"><span class="sw" style="background:#B45309"/>deduction</span>
                      <span class="lg"><span class="sw" style="background:#059669"/>total</span>
                    </span>
                    <button class="fc-max" data-coach="fs-expand" t-on-click="openExpand" title="Expand flowchart">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 3h6v6"/><path d="M9 21H3v-6"/><path d="M21 3l-7 7"/><path d="M3 21l7-7"/></svg>Expand
                    </button>
                  </div>
                  <div class="fc-inline" t-on-click="openExpand" title="Click to expand">
                    <div class="fc-zoomwrap"><t t-call="pb_formula_studio.Flow"><t t-set="flow" t-value="inlineFlow"/></t></div>
                  </div>
                </div>
              </div>
              </t>

              <!-- ============ INLINE EDIT VIEW ============ -->
              <t t-else="">
                <div class="ce-head">
                  <div class="ce-badge" t-esc="state.draft.column_letter || selected.col"/>
                  <input class="ce-title" t-att-value="state.draft.name" placeholder="Component name"
                         t-on-input="(ev) => this.setDraftField('name', ev)"/>
                  <div class="ce-head-side">
                    <div class="ff-seg" title="Editing depth">
                      <button t-att-class="state.editScope === 'simple' ? 'on' : ''" t-on-click="() => this.setEditScope('simple')">Simple</button>
                      <button t-att-class="state.editScope === 'full' ? 'on' : ''" t-on-click="() => this.setEditScope('full')">Full</button>
                    </div>
                    <button class="pbfs-btn ghost sm" t-on-click="cancelEdit">Cancel</button>
                    <button class="pbfs-btn primary sm" t-att-disabled="state.editBusy" t-on-click="saveComponent">Save</button>
                  </div>
                </div>

                <div class="ce-body">
                  <div t-if="state.editError" class="ce-banner err">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                    <span t-esc="state.editError"/>
                  </div>
                  <!-- type chooser -->
                  <div class="ce-label">Type</div>
                  <div class="ce-types">
                    <button t-attf-class="ce-type {{ draftType === 'input' ? 'on' : '' }}" t-on-click="() => this.setDraftType('input')">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
                      <b>Input</b><small>From a data source</small>
                    </button>
                    <button t-attf-class="ce-type {{ draftType === 'formula' ? 'on' : '' }}" t-on-click="() => this.setDraftType('formula')">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16"/><path d="M9 4c-2 6-2 10-4 16"/><path d="M14 9l4 6"/><path d="M18 9l-4 6"/></svg>
                      <b>Formula</b><small>Calculated</small>
                    </button>
                    <button t-attf-class="ce-type {{ draftType === 'constant' ? 'on' : '' }}" t-on-click="() => this.setDraftType('constant')">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M8 12h8"/></svg>
                      <b>Constant</b><small>Fixed value</small>
                    </button>
                  </div>

                  <!-- FORMULA body -->
                  <t t-if="draftType === 'formula'">
                    <div class="ce-label-row">
                      <span class="ce-label">Formula</span>
                      <div t-if="state.liveValid" t-attf-class="ce-pill {{ state.liveValid.valid ? 'ok' : 'err' }}">
                        <t t-if="state.liveValid.valid">Valid</t><t t-else="">Check formula</t>
                      </div>
                    </div>
                    <input class="ce-input mono" t-ref="formulaInput" t-att-value="state.draft.excel_formula"
                           placeholder="e.g. =A1 + B1*0.08" t-on-input="onFormulaInput"/>
                    <div class="ce-pal">
                      <div class="ce-pal-grp">
                        <span class="ce-pal-lbl">Columns</span>
                        <button t-foreach="paletteColumns" t-as="c" t-key="c.id" class="ce-chip col"
                                t-on-click="() => this.insertToken(c.col + '1')" t-att-title="c.name">
                          <span class="ce-chip-col" t-esc="c.col"/><span class="ce-chip-nm" t-esc="c.name"/>
                        </button>
                      </div>
                      <div class="ce-pal-grp">
                        <span class="ce-pal-lbl">Operators</span>
                        <button t-foreach="paletteOps" t-as="op" t-key="op.t" class="ce-chip op" t-on-click="() => this.insertToken(op.t)" t-esc="op.g"/>
                      </div>
                      <div class="ce-pal-grp">
                        <span class="ce-pal-lbl">Functions</span>
                        <button t-foreach="paletteFns" t-as="fn" t-key="fn" class="ce-chip fn" t-on-click="() => this.insertToken(fn + '(')" t-esc="fn"/>
                      </div>
                    </div>
                    <div class="ce-label" style="margin-top:18px">Preview</div>
                    <div class="formula-line">
                      <span t-if="!draftChips.length" class="fchip src">empty</span>
                      <t t-foreach="draftChips" t-as="tok" t-key="tok_index">
                        <span t-if="tok.kind === 'op'" t-attf-class="fop #{tok.ded ? 'fop-ded' : ''}" t-att-title="tok.ded ? 'This component is a deduction (a negative value) — it is subtracted from the total' : ''" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'func'" class="fchip ffunc" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'ref'" class="fchip fref"><span class="ref" t-esc="tok.col"/><t t-esc="tok.text"/></span>
                        <span t-elif="tok.kind === 'range'" class="fchip frange" t-att-title="'Range (' + tok.count + '): ' + tok.names.join(', ')"><span class="ref" t-esc="tok.start"/><span class="rng-sep">…</span><span class="ref" t-esc="tok.end"/><span class="rng-cnt" t-esc="tok.count"/></span>
                        <span t-elif="tok.kind === 'num'" class="fchip fnum" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'paren'" class="fparen" t-esc="tok.text"/>
                        <span t-elif="tok.kind === 'comma'" class="fcomma">,</span>
                        <span t-else="" class="fchip src" t-esc="tok.text"/>
                      </t>
                    </div>
                    <div t-if="state.liveValid and !state.liveValid.valid and state.liveValid.message" class="ed-errbox" t-esc="state.liveValid.message"/>
                  </t>

                  <!-- INPUT body -->
                  <t t-elif="draftType === 'input'">
                    <div class="ce-grid2">
                      <div class="ce-field"><label>Source field mapping</label>
                        <input class="ce-input" t-att-value="state.draft.data_source_field" placeholder="e.g. basic_salary" t-on-input="(ev) => this.setDraftField('data_source_field', ev)"/></div>
                      <div class="ce-field"><label>Default value</label>
                        <input class="ce-input" type="number" step="any" t-att-value="state.draft.default_value" t-on-change="(ev) => this.setDraftField('default_value', ev)"/></div>
                    </div>
                  </t>

                  <!-- CONSTANT body -->
                  <t t-else="">
                    <div class="ce-field"><label>Constant value</label>
                      <input class="ce-input big" type="number" step="any" t-att-value="state.draft.constant_value" t-on-change="(ev) => this.setDraftField('constant_value', ev)"/></div>
                  </t>

                  <!-- ============ FULL sections ============ -->
                  <t t-if="state.editScope === 'full'">
                    <div class="ce-sec">
                      <div class="ce-sec-h">Identity</div>
                      <div class="ce-grid2">
                        <div class="ce-field"><label>Code</label>
                          <input class="ce-input mono" t-att-value="state.draft.code" t-on-input="(ev) => this.setDraftField('code', ev)"/></div>
                        <div class="ce-field"><label>Sequence</label>
                          <input class="ce-input" type="number" t-att-value="state.draft.sequence" t-on-change="(ev) => this.setDraftField('sequence', ev)"/></div>
                        <div class="ce-field"><label>Category</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('category_id', ev)">
                            <option value="">— none —</option>
                            <option t-foreach="state.fieldMeta.categories or []" t-as="c" t-key="c.id" t-att-value="c.id" t-att-selected="state.draft.category_id === c.id" t-esc="c.name"/>
                          </select></div>
                        <div class="ce-field"><label>Linked salary rule</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('salary_rule_id', ev)">
                            <option value="">— none —</option>
                            <option t-foreach="state.fieldMeta.salary_rules or []" t-as="r" t-key="r.id" t-att-value="r.id" t-att-selected="state.draft.salary_rule_id === r.id"><t t-esc="r.name"/> (<t t-esc="r.code"/>)</option>
                          </select></div>
                      </div>
                    </div>

                    <div class="ce-sec">
                      <div class="ce-sec-h">Appearance</div>
                      <div class="ce-grid2">
                        <div class="ce-field"><label>Number format</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('number_format', ev)">
                            <option t-foreach="state.fieldMeta.number_formats or []" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.draft.number_format === o.value" t-esc="o.label"/>
                          </select></div>
                        <div class="ce-field"><label>Decimal places</label>
                          <input class="ce-input" type="number" t-att-value="state.draft.decimal_places" t-on-change="(ev) => this.setDraftField('decimal_places', ev)"/></div>
                        <div class="ce-field"><label>Column width (px)</label>
                          <input class="ce-input" type="number" t-att-value="state.draft.column_width" t-on-change="(ev) => this.setDraftField('column_width', ev)"/></div>
                        <div class="ce-field"><label>Text align</label>
                          <select class="ce-input" t-on-change="(ev) => this.setDraftField('text_align', ev)">
                            <option t-foreach="state.fieldMeta.text_aligns or []" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.draft.text_align === o.value" t-esc="o.label"/>
                          </select></div>
                      </div>
                      <div class="ce-toggles">
                        <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.appears_on_payslip" t-on-change="(ev) => this.setDraftField('appears_on_payslip', ev)"/><span>Appears on payslip</span></label>
                        <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.is_visible_in_grid" t-on-change="(ev) => this.setDraftField('is_visible_in_grid', ev)"/><span>Visible in grid</span></label>
                        <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.report_visible" t-on-change="(ev) => this.setDraftField('report_visible', ev)"/><span>Visible in reports</span></label>
                      </div>
                    </div>

                    <div class="ce-sec">
                      <button class="ce-sec-toggle" t-on-click="toggleAdvanced">
                        <svg t-attf-class="chev {{ state.advOpen ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
                        Advanced
                      </button>
                      <t t-if="state.advOpen">
                        <div class="ce-sec-h" style="margin-top:14px">Data source</div>
                        <div class="ce-grid2">
                          <div class="ce-field"><label>Data source</label>
                            <select class="ce-input" t-on-change="(ev) => this.setDraftField('data_source', ev)">
                              <option t-foreach="state.fieldMeta.data_sources or []" t-as="o" t-key="o.value" t-att-value="o.value" t-att-selected="state.draft.data_source === o.value" t-esc="o.label"/>
                            </select></div>
                          <div t-if="state.draft.data_source === 'integration'" class="ce-field"><label>Integration connector</label>
                            <select class="ce-input" t-on-change="(ev) => this.setDraftField('integration_connector_id', ev)">
                              <option value="">— none —</option>
                              <option t-foreach="state.fieldMeta.connectors or []" t-as="c" t-key="c.id" t-att-value="c.id" t-att-selected="state.draft.integration_connector_id === c.id" t-esc="c.name"/>
                            </select></div>
                          <div t-if="state.draft.data_source === 'integration'" class="ce-field"><label>Source field</label>
                            <input class="ce-input" t-att-value="state.draft.source_field_mapping" t-on-input="(ev) => this.setDraftField('source_field_mapping', ev)"/></div>
                        </div>
                        <div class="ce-toggles">
                          <label class="ce-tog"><input type="checkbox" t-att-checked="state.draft.is_contract_component" t-on-change="(ev) => this.setDraftField('is_contract_component', ev)"/><span>Contract component</span></label>
                          <label t-if="state.draft.is_contract_component" class="ce-tog"><input type="checkbox" t-att-checked="state.draft.requires_new_contract" t-on-change="(ev) => this.setDraftField('requires_new_contract', ev)"/><span>Requires new contract</span></label>
                        </div>
                      </t>
                    </div>
                  </t>
                </div>
              </t>
            </div>
          </div>

          <!-- editor: grid (T2.1 — GridStudio component, full-width workbench) -->
          <div t-else="" class="pbfs-gridwrap">
            <div class="pbfs-gridbar">
              <button t-attf-class="pbfs-btn ghost sm {{ state.outlineDrawer ? 'active' : '' }}"
                      t-on-click="() => this.toggleDrawer('outline')" title="Components panel">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
                Components
              </button>
              <span class="pbfs-gridbar-spacer"/>
              <button t-attf-class="pbfs-btn ghost sm {{ state.previewDrawer ? 'active' : '' }}"
                      t-on-click="() => this.toggleDrawer('preview')" title="Live preview panel">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7z"/><circle cx="12" cy="12" r="3"/></svg>
                Preview · <t t-esc="sampleName"/>
              </button>
            </div>
            <GridStudio components="state.components" preview="state.preview" graph="state.graph"
                        canEdit="state.canEdit" selectedId="state.selectedId"
                        onSelect="(id) => this.selectComponent(id)"
                        formatValue="(col) => this.previewVal(col)"
                        extraPreviews="extraPinnedPreviews"
                        formatValueFor="(col, values) => this.previewValFrom(col, values)"
                        onUnpinSample="(sid) => this.unpinSample(sid)"
                        sampleName="sampleName" fieldMeta="state.fieldMeta"
                        scenarios="state.scenarios"
                        onSaveFormula="(id, f) => this.gridSaveFormula(id, f)"
                        onValidateLive="(f, ex) => this.gridValidateLive(f, ex)"
                        onBulkUpdate="(ids, vals) => this.gridBulkUpdate(ids, vals)"
                        onTranslateFormula="(id, cols) => this.gridTranslateFormula(id, cols)"
                        onBulkSaveFormulas="(items) => this.gridBulkSaveFormulas(items)"
                        onScenarioCreate="(ruleId) => this.gridScenarioCreate(ruleId)"
                        onScenarioSave="(sid, f) => this.gridScenarioSave(sid, f)"
                        onScenarioEval="(sid, sampleId) => this.gridScenarioEval(sid, sampleId)"
                        onScenarioPromote="(sid) => this.gridScenarioPromote(sid)"
                        onScenarioDiscard="(sid) => this.gridScenarioDiscard(sid)"
                        onReorder="(dragId, beforeId) => this.gridReorder(dragId, beforeId)"
                        onGroupByCategory="() => this.gridGroupByCategory()"
                        folds="state.folds"
                        onToggleFold="(k) => this.onToggleFold(k)"
                        formatSum="(n) => this.vnd(n)"
                        snippets="state.snippets"
                        pendingSnippet="state.pendingSnippet"
                        onSnippetConsumed="() => this.onSnippetConsumed()"/>
          </div>

          <!-- live preview (grid: always a drawer · cards: drawer below the md breakpoint) -->
          <div t-attf-class="pbfs-test {{ state.view === 'grid' ? 'as-drawer' : 'md-drawer' }} {{ state.previewDrawer ? 'open' : '' }}" data-coach="fs-preview">
            <div class="tp-head">
              <span class="t">Live preview</span>
              <button class="ol-act tp-close" t-on-click="() => this.toggleDrawer('preview')" title="Close panel">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
              </button>
            </div>
            <div class="tp-selrow">
              <button class="tp-sel" t-on-click="cycleSample"><t t-esc="sampleName"/>
                <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg></button>
              <button t-if="canPinSample" class="tp-pin" t-on-click="pinActiveSample"
                      title="Pin this sample as an extra value row in the grid">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 17v5"/><path d="M9 10.76a2 2 0 0 1-1.11 1.79l-1.78.9A2 2 0 0 0 5 15.24V16a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-.76a2 2 0 0 0-1.11-1.79l-1.78-.9A2 2 0 0 1 15 10.76V7a1 1 0 0 1 1-1 2 2 0 0 0 0-4H8a2 2 0 0 0 0 4 1 1 0 0 1 1 1z"/></svg>
              </button>
            </div>
            <div t-if="extraPinnedPreviews.length" class="tp-pins">
              <span t-foreach="extraPinnedPreviews" t-as="ex" t-key="ex.sample_id" class="tp-pinchip">
                <t t-esc="ex.name"/>
                <button class="tp-pinx" t-on-click="() => this.unpinSample(ex.sample_id)" title="Unpin">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
                </button>
              </span>
            </div>
            <t t-if="!state.samples.length">
              <div class="tp-nosample">
                <p>No sample data yet — generate one to see live values.</p>
                <button class="pbfs-btn soft sm" t-on-click="generateSampleData">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>Generate sample data
                </button>
              </div>
            </t>
            <t t-foreach="state.components" t-as="c" t-key="c.id">
              <div t-att-data-col="c.col"
                   t-attf-class="tp-row {{ isDeduction(c) ? 'ded' : '' }} {{ isDep(c.col) ? 'dep' : '' }} {{ isOutcome(c.col) ? 'outcome' : '' }}"
                   t-attf-style="{{ depStyle(c) }}"
                   t-on-click="() => this.selectComponent(c.id)">
                <span class="nm"><span class="c" t-esc="c.col"/><t t-esc="c.name"/></span>
                <span class="v"><t t-if="isDeduction(c)">−</t><t t-esc="previewVal(c.col)"/></span>
              </div>
            </t>
          </div>
        </div>
        </t>
      </div>

      <!-- delete-configuration confirm -->
      <div t-if="state.confirmDel" class="pbfs-confirm-scrim" t-on-click="cancelDeleteConfig">
        <div class="pbfs-confirm" t-on-click="(ev) => ev.stopPropagation()">
          <div class="cf-ic">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><line x1="10" y1="11" x2="10" y2="17"/><line x1="14" y1="11" x2="14" y2="17"/></svg>
          </div>
          <h3>Delete this configuration?</h3>
          <p><b t-esc="state.confirmDel.name"/> and its <t t-esc="state.confirmDel.count"/> component<t t-if="state.confirmDel.count !== 1">s</t> will be permanently removed. This cannot be undone.</p>
          <div t-if="state.confirmDel.state === 'active'" class="cf-warn">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
            This configuration is <b>active</b> — deleting it may affect payroll that relies on it.
          </div>
          <div class="cf-actions">
            <button class="pbfs-btn ghost" t-on-click="cancelDeleteConfig">Cancel</button>
            <button class="pbfs-btn danger-solid" t-on-click="confirmDeleteConfig">Delete configuration</button>
          </div>
        </div>
      </div>

      <!-- Explain modal (T5.3) -->
      <div t-if="state.explainOpen" class="pbfs-explain-scrim" t-on-click="closeExplain">
        <div class="pbfs-explain" t-on-click="(ev) => ev.stopPropagation()">
          <div class="ex-head">
            <div class="ex-title">
              <span class="ex-col" t-esc="selected.col"/>
              <b t-esc="selected.name"/>
            </div>
            <div class="ex-lang">
              <button t-att-class="state.explainLang === 'en' ? 'on' : ''" t-on-click="() => this.setExplainLang('en')">EN</button>
              <button t-att-class="state.explainLang === 'vi' ? 'on' : ''" t-on-click="() => this.setExplainLang('vi')">VI</button>
            </div>
            <button class="ex-x" t-on-click="closeExplain"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>
          <div class="ex-body">
            <p class="ex-text" t-esc="state.explainText"/>
          </div>
          <div class="ex-foot">
            <span t-attf-class="ex-badge {{ state.explainSource }}">
              <t t-if="state.explainSource === 'ai'">PayAI</t>
              <t t-else="">Built-in</t>
            </span>
            <span t-if="state.explainBusy" class="ex-loading">
              <span class="ex-dot"/><span class="ex-dot"/><span class="ex-dot"/> PayAI is thinking…
            </span>
          </div>
        </div>
      </div>

      <!-- Version history rail (F7) -->
      <div t-if="state.historyOpen" class="pbfs-hist-scrim" t-on-click="closeHistory">
        <div class="pbfs-hist" t-on-click="(ev) => ev.stopPropagation()">
          <div class="hs-head">
            <div class="hs-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v5h5"/><path d="M3.05 13A9 9 0 1 0 6 5.3L3 8"/><path d="M12 7v5l4 2"/></svg>
              <div>
                <b>Version history</b>
                <small t-if="state.historyData"><t t-esc="state.historyData.code"/> · <t t-esc="state.historyData.name"/></small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeHistory"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="hs-body">
            <div t-if="state.historyBusy and !state.historyData" class="hs-loading">Loading history…</div>

            <t t-if="state.historyData">
              <!-- current (live head) -->
              <div class="hs-row current">
                <div class="hs-meta">
                  <span class="hs-badge current">Current</span>
                  <span class="hs-who" t-esc="state.historyData.current.user"/>
                  <span class="hs-when" t-esc="state.historyData.current.date"/>
                </div>
                <div class="hs-formula" t-esc="state.historyData.current.excel_formula or '—'"/>
              </div>

              <div t-if="!state.historyData.versions.length" class="hs-empty">
                No earlier versions yet. Edits to this component will appear here.
              </div>

              <!-- past versions, newest first -->
              <div t-foreach="state.historyData.versions" t-as="v" t-key="v.seq" class="hs-row">
                <div class="hs-meta">
                  <span t-attf-class="hs-badge {{ v.reason }}" t-esc="v.reason_label"/>
                  <span class="hs-seq">v<t t-esc="v.seq"/></span>
                  <span class="hs-who" t-esc="v.user"/>
                  <span class="hs-when" t-esc="v.date"/>
                </div>
                <div class="hs-formula" t-esc="v.excel_formula or '—'"/>
                <div class="hs-actions">
                  <button class="pbfs-btn ghost xs" t-on-click="() => this.toggleDiff(v.seq)">
                    <t t-if="state.historyDiffSeq === v.seq">Hide diff</t>
                    <t t-else="">Diff vs current</t>
                  </button>
                  <button class="pbfs-btn soft xs" t-on-click="() => this.restoreVersion(v.seq)">Restore</button>
                </div>
                <!-- inline chip diff (this version → current) -->
                <div t-if="state.historyDiffSeq === v.seq" class="hs-diff">
                  <t t-if="state.historyDiffRuns === null"><span class="hs-diff-load">…</span></t>
                  <t t-elif="!state.historyDiffRuns.length"><span class="hs-diff-none">No formula change</span></t>
                  <t t-else="">
                    <t t-foreach="state.historyDiffRuns" t-as="run" t-key="run_index">
                      <t t-if="run.op === 'equal'">
                        <span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok eq" t-esc="tk"/>
                      </t>
                      <t t-elif="run.op === 'insert'">
                        <span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                      </t>
                      <t t-elif="run.op === 'delete'">
                        <span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/>
                      </t>
                      <t t-elif="run.op === 'replace'">
                        <span t-foreach="run.old" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/>
                        <span class="hs-arrow">→</span>
                        <span t-foreach="run.new" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                      </t>
                    </t>
                  </t>
                </div>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- Problems rail (F13) -->
      <div t-if="state.probOpen" class="pbfs-prob-scrim" t-on-click="closeProblems">
        <div class="pbfs-prob" t-on-click="(ev) => ev.stopPropagation()">
          <div class="pp-head">
            <div class="pp-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 9v4M12 17h.01"/><path d="M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/></svg>
              <div>
                <b>Problems</b>
                <small t-if="state.probData"><t t-esc="problemCount"/> found · <t t-esc="state.config.name"/></small>
              </div>
            </div>
            <div class="pp-head-side">
              <button class="pp-refresh" t-on-click="_loadProblems" title="Re-scan"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v5h5"/><path d="M3.05 13A9 9 0 1 0 6 5.3L3 8"/></svg></button>
              <button class="hs-x" t-on-click="closeProblems"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
            </div>
          </div>

          <!-- severity summary chips -->
          <div t-if="state.probData" class="pp-summary">
            <span t-attf-class="pp-chip error {{ (state.probData.counts.error||0) ? '' : 'off' }}"><i/><t t-esc="state.probData.counts.error||0"/> errors</span>
            <span t-attf-class="pp-chip warning {{ (state.probData.counts.warning||0) ? '' : 'off' }}"><i/><t t-esc="state.probData.counts.warning||0"/> warnings</span>
            <span t-attf-class="pp-chip hint {{ (state.probData.counts.hint||0) ? '' : 'off' }}"><i/><t t-esc="state.probData.counts.hint||0"/> hints</span>
          </div>

          <div class="pp-body">
            <div t-if="state.probBusy and !state.probData" class="hs-loading">Scanning…</div>

            <t t-if="state.probData">
              <div t-if="!problemCount" class="pp-clean">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="9"/></svg>
                <b>No problems found</b>
                <span>This configuration is valid, cycle-free and lint-clean.</span>
              </div>

              <t t-foreach="['error','warning','hint']" t-as="sev" t-key="sev">
                <t t-if="problemsOf(sev).length">
                  <div t-foreach="problemsOf(sev)" t-as="p" t-key="p.key" t-attf-class="pp-row {{ sev }}">
                    <span class="pp-dot"/>
                    <div class="pp-main">
                      <div class="pp-t"><t t-esc="p.title"/></div>
                      <div class="pp-d"><t t-esc="p.detail"/></div>
                    </div>
                    <button t-if="p.note_id" class="pp-resolve" t-on-click="() => this.resolveProblemNote(p)" title="Resolve this review note"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg></button>
                    <button t-if="p.rule_id" class="pp-go" t-on-click="() => this.gotoProblem(p)" title="Go to component"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17 17 7M8 7h9v9"/></svg></button>
                  </div>
                </t>
              </t>

              <!-- W54 — simplification suggestions (rate-table rewrites) -->
              <t t-if="simplifySuggestions.length">
                <div class="pp-simp-head">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m3 21 9-9"/><path d="M14.5 2.5 12 5l3.5 3.5L18 6a2.12 2.12 0 0 0-3.5-3.5z"/><path d="M9 6 6 9"/></svg>
                  Simplify — rate tables
                </div>
                <div t-foreach="simplifySuggestions" t-as="s" t-key="s.rule_id" t-attf-class="pp-simp {{ s.can_apply ? 'ok' : 'warn' }}">
                  <div class="pp-simp-t">
                    <span class="pp-simp-col"><t t-esc="s.col"/></span>
                    <b><t t-esc="s.name"/></b>
                    <span t-if="s.can_apply" class="pp-simp-badge"><t t-esc="s.brackets.length"/>-band progressive chain</span>
                    <span t-elif="!s.consistent" class="pp-simp-badge irr">irregular chain</span>
                    <span t-else="" class="pp-simp-badge irr">unverified</span>
                  </div>

                  <t t-if="s.can_apply">
                    <t t-set="db" t-value="simplifyDiff(s, 'before')"/>
                    <t t-set="da" t-value="simplifyDiff(s, 'after')"/>
                    <div class="pp-diff">
                      <div class="pp-diff-row"><span class="pp-diff-tag del">before</span><code><t t-esc="db[0]"/><mark class="del"><t t-esc="db[1]"/></mark><t t-esc="db[2]"/></code></div>
                      <div class="pp-diff-row"><span class="pp-diff-tag add">after</span><code><t t-esc="da[0]"/><mark class="add"><t t-esc="da[1]"/></mark><t t-esc="da[2]"/></code></div>
                    </div>
                    <div class="pp-simp-eq">
                      <b><t t-esc="s.table.code"/></b>
                      <t t-if="s.table.reuse"> · reuse existing table</t>
                      <t t-else=""> · new <t t-esc="s.brackets.length"/>-bracket table</t>
                      · equivalent on <t t-esc="s.equivalence.samples_matched"/>/<t t-esc="s.equivalence.samples_total"/> samples<t t-if="s.equivalence.probes_total"> + <t t-esc="s.equivalence.probes_matched"/>/<t t-esc="s.equivalence.probes_total"/> edge probes</t>
                      <t t-if="s.driver_kind === 'computed'"> (driver is computed — probes are synthetic)</t>
                    </div>
                    <div class="pp-simp-act">
                      <button class="pbfs-btn soft sm" t-on-click="() => this.gotoProblem({rule_id: s.rule_id, col: s.col})">View</button>
                      <button class="pbfs-btn primary sm" t-att-disabled="state.simplifyBusy === s.rule_id" t-on-click="() => this.applySimplify(s)">
                        <t t-esc="state.simplifyBusy === s.rule_id ? 'Applying…' : (s.table.reuse ? 'Use ' + s.table.code : 'Create table &amp; simplify')"/>
                      </button>
                    </div>
                  </t>
                  <t t-else="">
                    <div class="pp-simp-eq irr"><t t-esc="s.reason"/></div>
                    <div class="pp-simp-act">
                      <button class="pbfs-btn soft sm" t-on-click="() => this.gotoProblem({rule_id: s.rule_id, col: s.col})">View</button>
                    </div>
                  </t>
                </div>
              </t>
            </t>
          </div>
        </div>
      </div>

      <!-- Comments &amp; annotations rail (F15) -->
      <div t-if="state.notesOpen" class="pbfs-notes-scrim" t-on-click="closeNotes">
        <div class="pbfs-notes" t-on-click="(ev) => ev.stopPropagation()">
          <div class="nt-head">
            <div class="nt-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
              <div>
                <b>Notes</b>
                <small t-if="selected"><t t-esc="selected.col"/> · <t t-esc="selected.name"/></small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeNotes"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <!-- composer -->
          <div class="nt-composer" t-if="state.canEdit">
            <textarea class="nt-input" t-att-value="state.noteDraft" t-on-input="onNoteInput" t-on-keydown="onNoteKeydown" placeholder="Leave a comment… (Cmd/Ctrl+Enter to post)"/>
            <div class="nt-composer-foot">
              <label class="nt-review-toggle">
                <input type="checkbox" t-att-checked="state.noteReview" t-on-change="toggleNoteReview"/>
                <span class="nt-flag"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z"/><path d="M4 22v-7"/></svg>Review note</span>
              </label>
              <button class="pbfs-btn primary sm" t-att-disabled="!state.noteDraft.trim()" t-on-click="postNote">Post</button>
            </div>
          </div>

          <div class="nt-body">
            <div t-if="state.notesBusy and !state.notesData" class="nt-loading">Loading…</div>
            <t t-if="state.notesData">
              <div t-if="!state.notesData.notes.length" class="nt-empty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
                <span>No comments yet. Leave the first note on this component.</span>
              </div>
              <div t-foreach="state.notesData.notes" t-as="n" t-key="n.id" t-attf-class="nt-note {{ n.is_review ? 'review' : '' }} {{ n.resolved ? 'resolved' : '' }}">
                <div class="nt-note-head">
                  <span t-if="n.is_review" class="nt-badge"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z"/></svg><t t-if="n.resolved">Resolved</t><t t-else="">Review</t></span>
                  <span class="nt-author" t-esc="n.author"/>
                  <span class="nt-date" t-esc="n.date"/>
                </div>
                <div class="nt-note-body" t-esc="n.body"/>
                <div class="nt-note-actions">
                  <button t-if="n.is_review" class="pbfs-link" t-on-click="() => this.resolveNote(n)"><t t-if="n.resolved">Reopen</t><t t-else="">Resolve</t></button>
                  <button t-if="n.is_mine or state.canEdit" class="pbfs-link danger" t-on-click="() => this.deleteNote(n)">Delete</button>
                </div>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- Rate (bracket) tables editor (F11) -->
      <div t-if="state.ratesOpen" class="pbfs-rates-scrim" t-on-click="closeRates">
        <div class="pbfs-rates" t-on-click="(ev) => ev.stopPropagation()">
          <div class="pr-head">
            <div class="pr-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18"/></svg>
              <div>
                <b>Rate &amp; bracket tables</b>
                <small>Reference in any formula as <code>BRACKET(code, income)</code></small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeRates"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="pr-body">
            <!-- LIST view -->
            <t t-if="!state.rateEdit">
              <div t-if="!state.rateTables.length" class="pr-empty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18"/></svg>
                <b>No rate tables yet</b>
                <span>Create a progressive table (e.g. a PIT schedule) and call it from a formula with <code>BRACKET(PIT, ASSESS2)</code> — the ugly nested-IF is generated for you.</span>
              </div>
              <div t-foreach="state.rateTables" t-as="t" t-key="t.id" class="pr-card" t-on-click="() => this.editRateTable(t)">
                <div class="pr-card-main">
                  <div class="pr-card-t"><span class="pr-code" t-esc="t.code"/><span t-esc="t.name"/></div>
                  <div class="pr-card-sub"><t t-esc="t.brackets.length"/> bracket<t t-if="t.brackets.length !== 1">s</t><t t-if="rateTableUsage(t)"> · used by <t t-esc="rateTableUsage(t)"/></t></div>
                </div>
                <svg class="pr-chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
              </div>
              <button class="pbfs-btn primary pr-new" t-on-click="newRateTable">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12h14"/></svg>New rate table
              </button>
            </t>

            <!-- EDIT view -->
            <t t-else="">
              <div class="pr-form">
                <div class="pr-frow">
                  <label class="pr-fld pr-fld-code"><span>Code</span>
                    <input t-att-value="state.rateEdit.code" t-on-input="(ev) => this.setRateField('code', ev)" placeholder="PIT" spellcheck="false"/>
                  </label>
                  <label class="pr-fld pr-fld-name"><span>Name</span>
                    <input t-att-value="state.rateEdit.name" t-on-input="(ev) => this.setRateField('name', ev)" placeholder="Personal Income Tax"/>
                  </label>
                </div>

                <div class="pr-brackets">
                  <div class="pr-bhead"><span class="pr-bh-from">Income from</span><span class="pr-bh-rate">Marginal rate</span><span class="pr-bh-x"/></div>
                  <div t-foreach="state.rateEdit.brackets" t-as="b" t-key="b_index" class="pr-brow">
                    <div class="pr-bnum">
                      <input class="pr-in-lower" t-att-value="b.lower" t-on-input="(ev) => this.onBracketLower(b_index, ev)" inputmode="decimal"/>
                    </div>
                    <div class="pr-bpct">
                      <input class="pr-in-rate" t-att-value="ratePct(b.rate)" t-on-input="(ev) => this.onBracketRate(b_index, ev)" inputmode="decimal"/>
                      <span class="pr-pctsign">%</span>
                    </div>
                    <button class="pr-brm" t-on-click="() => this.removeBracket(b_index)" title="Remove bracket"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
                  </div>
                  <button class="pr-badd" t-on-click="addBracket"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12h14"/></svg>Add bracket</button>
                </div>

                <!-- live preview -->
                <div class="pr-preview">
                  <div class="pr-pv-row">
                    <label><span>At income</span>
                      <input t-att-value="state.ratePreviewIncome" t-on-input="setPreviewIncome" inputmode="decimal"/>
                    </label>
                    <div class="pr-pv-out">
                      <span class="pr-pv-lbl">computes</span>
                      <span class="pr-pv-val" t-esc="state.ratePreview ? vnd(state.ratePreview.result) : '—'"/>
                    </div>
                  </div>
                </div>

                <div t-if="state.rateErr" class="pr-err" t-esc="state.rateErr"/>

                <div class="pr-actions">
                  <button class="pbfs-btn ghost" t-on-click="cancelRateEdit">Cancel</button>
                  <button t-if="state.rateEdit.id" class="pbfs-btn danger" t-on-click="() => this.deleteRateTable(state.rateEdit)">Delete</button>
                  <button class="pbfs-btn primary" t-att-disabled="state.rateBusy" t-on-click="saveRateTable">Save table</button>
                </div>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- Mapping canvas overlay (F10) -->
      <div t-if="state.mapOpen" class="pbfs-map-scrim" t-on-click="closeMapping">
        <div class="pbfs-map" t-on-click="(ev) => ev.stopPropagation()">
          <div class="pm-head">
            <div class="pm-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="6" r="2.5"/><circle cx="6" cy="18" r="2.5"/><circle cx="18" cy="12" r="2.5"/><path d="M8.5 6.5C13 8 12 12 15.6 12M8.5 17.5C13 16 12 12 15.6 12"/></svg>
              <div>
                <b>Mapping canvas</b>
                <small t-if="state.mapData and state.mapData.ok and state.mapData.subtitle" t-esc="state.mapData.subtitle"/>
                <small t-else="">Wire the plugs — one canvas for every mapping surface</small>
              </div>
            </div>
            <div class="pm-head-actions">
              <select t-if="state.mapData and state.mapData.contexts and state.mapData.contexts.length" class="pm-ctx" t-on-change="setMapContext">
                <option t-foreach="state.mapData.contexts" t-as="ctx" t-key="ctx.id" t-att-value="ctx.id" t-att-selected="ctx.id === state.mapContextId" t-esc="ctx.name"/>
              </select>
              <t t-if="state.mapData and state.mapData.ok and state.mapData.can_edit">
                <span class="pm-stat"><b t-esc="mapAcceptedCount"/> mapped<t t-if="mapSuggestedCount"> · <b t-esc="mapSuggestedCount"/> suggested</t></span>
                <button t-if="state.mapData.supports_suggest" class="pbfs-btn ghost sm" t-att-disabled="state.mapBusy" t-on-click="mapSuggest">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/><circle cx="12" cy="12" r="3.2"/></svg>Auto-suggest
                </button>
                <button t-if="mapSuggestedCount" class="pbfs-btn primary sm" t-on-click="mapAcceptAll">Accept all ≥90%</button>
                <!-- W65 — mapping templates (api + cycle only) -->
                <t t-if="mapTemplatable">
                  <button class="pbfs-btn soft sm" title="Save this board as a reusable template" t-on-click="openTmplSave">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><path d="M17 21v-8H7v8M7 3v5h8"/></svg>Save as template…
                  </button>
                  <button class="pbfs-btn soft sm" title="Apply a saved template to this board" t-on-click="openTmplApply">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16v4H4zM4 12h10M4 17h10"/><path d="M18 12l3 3-3 3M21 15h-7"/></svg>Apply template…
                  </button>
                </t>
              </t>
              <button class="hs-x" t-on-click="closeMapping"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
            </div>
          </div>

          <!-- adapter tabs -->
          <div class="pm-tabs">
            <t t-foreach="mapTabs" t-as="tb" t-key="tb.key">
              <button t-attf-class="pm-tab {{ state.mapMode === tb.key ? 'on' : '' }}" t-on-click="() => this.setMapMode(tb.key)" t-esc="tb.label"/>
            </t>
          </div>

          <div class="pm-body">
            <div t-if="state.mapBusy and !state.mapData" class="pm-loading">Loading…</div>

            <!-- empty / no-pair / no-connector -->
            <div t-if="state.mapData and !state.mapData.ok" class="pm-empty">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="6" r="2.5"/><circle cx="6" cy="18" r="2.5"/><circle cx="18" cy="12" r="2.5"/><path d="M8.5 6.5C13 8 12 12 15.6 12M8.5 17.5C13 16 12 12 15.6 12"/></svg>
              <b t-if="state.mapData.reason === 'no_pair'">No paired cycle configuration</b>
              <b t-elif="state.mapData.reason === 'no_connector'">No integration connector</b>
              <b t-elif="state.mapData.reason === 'no_batch'">No import batch</b>
              <b t-else="">Nothing to map</b>
              <span t-if="state.mapData.reason === 'no_pair'">This surface wires a <b>Mid-Cycle</b> config to its <b>End-Cycle</b> counterpart. The current configuration has no sibling of the opposite cycle type in the same division.</span>
              <span t-elif="state.mapData.reason === 'no_connector'">Create an integration connector to map external source fields onto this scheme's inputs.</span>
              <span t-elif="state.mapData.reason === 'no_batch'">Import a file first — then map its columns onto this scheme's input components here.</span>
              <span t-else="">Open a mid-cycle or end-cycle configuration to wire its carryover components.</span>
            </div>

            <!-- the canvas -->
            <MappingCanvas t-if="state.mapData and state.mapData.ok"
                           leftItems="state.mapData.left" rightItems="state.mapData.right"
                           wires="mapWires"
                           leftTitle="state.mapData.left_title" rightTitle="state.mapData.right_title"
                           canEdit="state.mapData.can_edit" busy="state.mapBusy"
                           onAccept="(w) => this.mapAccept(w)"
                           onReject="(w) => this.mapReject(w)"
                           onDelete="(w) => this.mapDelete(w)"
                           onDraw="(l, r) => this.mapDraw(l, r)"
                           onSuggest="() => this.mapSuggest()"
                           onTransformPreview="(ref, d) => this.mapTransformPreview(ref, d)"
                           onTransformSave="(ref, v) => this.mapTransformSave(ref, v)"/>

            <!-- W65 — mapping template panel (save / apply) -->
            <t t-if="state.tmplMode">
              <div class="pm-tmpl-scrim" t-on-click="closeTmpl"/>
              <div class="pm-tmpl" t-on-click="(ev) => ev.stopPropagation()">
                <div class="pm-tmpl-head">
                  <b t-esc="state.tmplMode === 'save' ? 'Save board as template' : 'Apply a template'"/>
                  <button class="hs-x" t-on-click="closeTmpl"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
                </div>

                <!-- SAVE -->
                <t t-if="state.tmplMode === 'save'">
                  <p class="pm-tmpl-hint">Snapshots the <b t-esc="mapAcceptedCount"/> accepted wire<t t-if="mapAcceptedCount !== 1">s</t> on this board<t t-if="state.mapMode === 'api'"> (with their transforms)</t>. Templates apply by code, so you can reuse them across configurations.</p>
                  <input class="pm-tmpl-input" placeholder="Template name…" t-att-value="state.tmplName" t-on-input="onTmplName" t-on-keydown="onTmplKey"/>
                  <div class="pm-tmpl-actions">
                    <button class="pbfs-btn ghost sm" t-on-click="closeTmpl">Cancel</button>
                    <button class="pbfs-btn primary sm" t-att-disabled="state.tmplBusy" t-on-click="saveTmpl">Save template</button>
                  </div>
                </t>

                <!-- APPLY -->
                <t t-else="">
                  <div t-if="state.tmplBusy and !state.tmplList.length" class="pm-tmpl-hint">Loading…</div>
                  <div t-elif="!state.tmplList.length" class="pm-tmpl-empty">No saved templates for this surface yet. Wire a board and use <b>Save as template…</b>.</div>
                  <div t-else="" class="pm-tmpl-list">
                    <div t-foreach="state.tmplList" t-as="t" t-key="t.id" class="pm-tmpl-row">
                      <div class="pm-tmpl-row-main">
                        <b t-esc="t.name"/>
                        <small><t t-esc="t.line_count"/> wire<t t-if="t.line_count !== 1">s</t><t t-if="t.shared"> · shared</t><t t-if="t.connector_type"> · <t t-esc="t.connector_type"/></t></small>
                      </div>
                      <button class="pbfs-btn primary sm" t-att-disabled="state.tmplBusy" t-on-click="() => this.applyTmpl(t.id)">Apply</button>
                      <button t-if="t.can_delete" class="pm-tmpl-del" title="Delete template" t-on-click="() => this.deleteTmpl(t.id)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6"/></svg></button>
                    </div>
                  </div>

                  <!-- apply result summary — every bucket shown, unmatched never hidden (D-I6, C7) -->
                  <div t-if="state.tmplResult" class="pm-tmpl-result">
                    <div class="pm-tmpl-res-head"><b t-esc="state.tmplResult.applied.length"/> applied · <b t-esc="state.tmplResult.skipped_existing.length"/> skipped · <b t-esc="state.tmplResult.unmatched_sources.length + state.tmplResult.unmatched_targets.length"/> unmatched</div>
                    <div t-if="state.tmplResult.skipped_existing.length" class="pm-tmpl-res-sec"><span class="pm-tmpl-tag skip">Skipped (already wired)</span><t t-foreach="state.tmplResult.skipped_existing" t-as="s" t-key="'sk'+s_index"><code t-esc="s.source + ' → ' + s.target"/></t></div>
                    <div t-if="state.tmplResult.unmatched_sources.length" class="pm-tmpl-res-sec"><span class="pm-tmpl-tag miss">Source not on board</span><t t-foreach="state.tmplResult.unmatched_sources" t-as="u" t-key="'us'+u_index"><code t-esc="u"/></t></div>
                    <div t-if="state.tmplResult.unmatched_targets.length" class="pm-tmpl-res-sec"><span class="pm-tmpl-tag miss">Target not in scheme</span><t t-foreach="state.tmplResult.unmatched_targets" t-as="u" t-key="'ut'+u_index"><code t-esc="u"/></t></div>
                  </div>
                </t>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- Payslip Studio overlay (F9) -->
      <div t-if="state.psOpen" class="pbfs-ps-scrim" t-on-click="closePayslip">
        <div class="pbfs-ps" t-on-click="(ev) => ev.stopPropagation()">
          <div class="ps-head">
            <div class="ps-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2h9l3 3v17l-3-2-3 2-3-2-3 2V2z"/><path d="M9 7h6M9 11h6M9 15h4"/></svg>
              <div>
                <b>Payslip Studio</b>
                <small>Drag components into sections — the preview is what prints</small>
              </div>
            </div>
            <div class="ps-head-actions">
              <select t-if="state.psData and state.psData.samples.length" class="ps-sample" t-on-change="psSetSample">
                <option t-foreach="state.psData.samples" t-as="s" t-key="s.id" t-att-value="s.id" t-att-selected="s.id === state.psData.sample_id" t-esc="s.name"/>
              </select>
              <div class="ps-lang" t-on-click="psToggleLang" t-att-title="'Preview labels: ' + (state.psLang === 'vi' ? 'Vietnamese' : 'English')">
                <span t-att-class="state.psLang === 'en' ? 'on' : ''">EN</span><span t-att-class="state.psLang === 'vi' ? 'on' : ''">VI</span>
              </div>
              <button class="hs-x" t-on-click="closePayslip"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
            </div>
          </div>

          <div class="ps-body" t-if="state.psData and state.psData.ok">
            <!-- LEFT: layout canvas -->
            <div class="ps-canvas">
              <!-- tray of unplaced components -->
              <div t-attf-class="ps-tray {{ state.psOverZone === 'tray' ? 'over' : '' }}"
                   t-on-dragover="(ev) => this.psDragOverZone('tray', ev)" t-on-drop="(ev) => this.psDrop(false, ev)">
                <div class="ps-tray-head"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18"/></svg>Unplaced · <t t-esc="state.psData.tray.length"/></div>
                <div class="ps-chips">
                  <t t-foreach="state.psData.tray" t-as="c" t-key="c.id">
                    <div t-attf-class="ps-chip {{ c.group.toLowerCase() }} {{ state.psDragId === c.id ? 'dragging' : '' }}"
                         draggable="true" t-on-dragstart="(ev) => this.psDragStart(c, ev)" t-on-dragend="psDragEnd"
                         t-on-dragover="(ev) => this.psDragOverComp(c, ev)">
                      <span class="ps-chip-col" t-esc="c.col"/><span class="ps-chip-name" t-esc="c.name"/>
                    </div>
                  </t>
                  <div t-if="!state.psData.tray.length" class="ps-tray-empty">Every payslip component is placed in a section</div>
                </div>
              </div>

              <!-- sections -->
              <div class="ps-sections">
                <t t-foreach="state.psData.sections" t-as="s" t-key="s.id">
                  <div t-attf-class="ps-section sc-{{ s.color_key }} {{ state.psOverZone === s.id ? 'over' : '' }}"
                       t-on-dragover="(ev) => this.psDragOverZone(s.id, ev)" t-on-drop="(ev) => this.psDrop(s.id, ev)">
                    <div class="ps-sec-head">
                      <span class="ps-sec-dot"/>
                      <t t-if="state.psEditSec === s.id">
                        <input class="ps-sec-rename" t-att-value="s.label" t-on-blur="(ev) => this.psRenameSection(s, ev)" t-on-keydown="(ev) => this.psRenameKey(s, ev)" autofocus="autofocus"/>
                      </t>
                      <span t-else="" class="ps-sec-title" t-on-click="() => this.startSectionEdit(s)" t-esc="psSectionTitle(s)"/>
                      <span class="ps-sec-count" t-esc="s.components.length"/>
                      <div class="ps-sec-tools">
                        <div class="ps-colors">
                          <t t-foreach="state.psData.colors" t-as="ck" t-key="ck">
                            <button t-attf-class="ps-color sc-{{ ck }} {{ s.color_key === ck ? 'on' : '' }}" t-on-click="() => this.psSetSectionColor(s, ck)"/>
                          </t>
                        </div>
                        <button t-attf-class="ps-sec-btn {{ s.collapse_when_empty ? 'on' : '' }}" t-on-click="() => this.psToggleCollapse(s)" title="Hide this section when it has no visible lines">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10 10 0 0 1 12 20C5 20 1 12 1 12a18 18 0 0 1 5.06-5.94M9.9 4.24A9 9 0 0 1 12 4c7 0 11 8 11 8a18 18 0 0 1-2.16 3.19M1 1l22 22"/></svg>
                        </button>
                        <button class="ps-sec-btn danger" t-on-click="() => this.psDeleteSection(s)" title="Delete section (components return to the tray)">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>
                        </button>
                      </div>
                    </div>
                    <div class="ps-sec-body">
                      <t t-foreach="s.components" t-as="c" t-key="c.id">
                        <div t-attf-class="ps-line vis-{{ c.visibility }} {{ psVisible(c) ? '' : 'hidden' }} {{ state.psDragId === c.id ? 'dragging' : '' }} {{ state.psOverComp === c.id ? 'over' : '' }}"
                             draggable="true" t-on-dragstart="(ev) => this.psDragStart(c, ev)" t-on-dragend="psDragEnd"
                             t-on-dragover="(ev) => this.psDragOverComp(c, ev)">
                          <svg class="ps-grip" viewBox="0 0 24 24" fill="currentColor"><circle cx="9" cy="6" r="1.6"/><circle cx="15" cy="6" r="1.6"/><circle cx="9" cy="12" r="1.6"/><circle cx="15" cy="12" r="1.6"/><circle cx="9" cy="18" r="1.6"/><circle cx="15" cy="18" r="1.6"/></svg>
                          <span class="ps-line-name" t-esc="c.name"/>
                          <span class="ps-line-val" t-esc="psVal(c)"/>
                          <button class="ps-vis" t-attf-title="Visibility: {{ c.visibility }}" t-on-click="() => this.psCycleVisibility(c)">
                            <svg t-if="c.visibility === 'always'" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                            <svg t-elif="c.visibility === 'when_nonzero'" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" stroke-dasharray="3 3"/><circle cx="12" cy="12" r="3"/></svg>
                            <svg t-else="" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10 10 0 0 1 12 20C5 20 1 12 1 12a18 18 0 0 1 5.06-5.94M9.9 4.24A9 9 0 0 1 12 4c7 0 11 8 11 8a18 18 0 0 1-2.16 3.19M1 1l22 22"/></svg>
                          </button>
                        </div>
                      </t>
                      <div t-if="!s.components.length" class="ps-sec-empty">Drop components here</div>
                    </div>
                  </div>
                </t>
                <button class="ps-add-sec" t-on-click="psCreateSection">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12h14"/></svg>Add section
                </button>
              </div>
            </div>

            <!-- RIGHT: live payslip preview -->
            <div class="ps-preview">
              <div class="ps-slip">
                <div class="ps-slip-head">
                  <div class="ps-slip-title">Payslip preview</div>
                  <div class="ps-slip-sub" t-esc="state.psData.config.name"/>
                </div>
                <t t-foreach="state.psData.sections" t-as="s" t-key="s.id">
                  <div t-if="psSectionShown(s)" t-attf-class="ps-slip-sec sc-{{ s.color_key }}">
                    <div class="ps-slip-sec-title" t-esc="psSectionTitle(s)"/>
                    <t t-foreach="psSectionVisibleComps(s)" t-as="c" t-key="c.id">
                      <div class="ps-slip-line"><span t-esc="c.name"/><span t-attf-class="ps-slip-amt {{ c.is_deduction ? 'ded' : '' }}"><t t-if="c.is_deduction">−</t><t t-esc="psVal(c)"/></span></div>
                    </t>
                    <div class="ps-slip-subtotal"><span>Subtotal</span><span t-esc="vnd(psSectionTotal(s))"/></div>
                  </div>
                </t>
                <div class="ps-slip-net"><span>Net pay</span><span t-esc="vnd(psNet)"/></div>
              </div>
            </div>
          </div>

          <div t-elif="state.psBusy" class="ps-loading">Loading…</div>
        </div>
      </div>

      <!-- Execution replay overlay (B1) -->
      <div t-if="state.replayOpen" class="pbfs-replay-scrim" t-on-click="closeReplay">
        <div class="pbfs-replay" t-on-click="(ev) => ev.stopPropagation()">
          <div class="rp-head">
            <div class="rp-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
              <div>
                <b>Execution replay</b>
                <small t-if="state.replayData and state.replayData.ok">Watch <t t-esc="state.replayData.config.name"/> compute, one component at a time</small>
              </div>
            </div>
            <div class="rp-head-actions">
              <select t-if="state.replayData and state.replayData.ok and state.replayData.samples.length" class="pm-ctx" t-on-change="replaySetSample">
                <option t-foreach="state.replayData.samples" t-as="s" t-key="s.id" t-att-value="s.id" t-att-selected="s.id === state.replayData.sample_id" t-esc="s.name"/>
              </select>
              <button class="hs-x" t-on-click="closeReplay"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
            </div>
          </div>

          <t t-if="state.replayData and state.replayData.ok">
            <!-- transport controls -->
            <div class="rp-controls">
              <button class="rp-ctl" t-on-click="replayReset" title="Reset"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="19 20 9 12 19 4 19 20"/><line x1="5" y1="19" x2="5" y2="5"/></svg></button>
              <button class="rp-ctl" t-on-click="replayPrev" t-att-disabled="state.replayStep &lt; 0" title="Previous"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg></button>
              <button class="rp-ctl play" t-on-click="replayPlayPause" title="Play / pause">
                <svg t-if="!state.replayPlaying" viewBox="0 0 24 24" fill="currentColor" stroke="none"><polygon points="6 4 20 12 6 20 6 4"/></svg>
                <svg t-else="" viewBox="0 0 24 24" fill="currentColor" stroke="none"><rect x="6" y="5" width="4" height="14" rx="1"/><rect x="14" y="5" width="4" height="14" rx="1"/></svg>
              </button>
              <button class="rp-ctl" t-on-click="replayNext" t-att-disabled="replayDone" title="Next"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg></button>
              <span class="rp-step">Step <t t-esc="state.replayStep + 1"/> / <t t-esc="replaySteps.length"/></span>
              <input class="rp-scrub" type="range" min="0" max="100" t-att-value="replayProgressPct()" t-on-input="replaySeekPct"/>
            </div>

            <div class="rp-body">
              <!-- LEFT: current-step spotlight -->
              <div class="rp-spotlight">
                <t t-if="replayCurrent">
                  <div t-attf-class="rp-spot-head g-{{ catKey(replayCurrent.group) }}">
                    <span class="rp-spot-col" t-esc="replayCurrent.col"/>
                    <div class="rp-spot-nm"><b t-esc="replayCurrent.name"/><small t-esc="replayCurrent.group"/></div>
                  </div>
                  <div class="rp-spot-formula">
                    <t t-foreach="replayCurrent.tokens" t-as="tk" t-key="tk_index">
                      <span t-if="tk.kind === 'ref'" class="rp-tok ref"><span class="rp-tok-col" t-esc="tk.col"/><t t-esc="tk.text"/></span>
                      <span t-elif="tk.kind === 'op'" class="rp-tok op" t-esc="tk.text"/>
                      <span t-elif="tk.kind === 'num'" class="rp-tok num" t-esc="tk.text"/>
                      <span t-else="" class="rp-tok src" t-esc="tk.text"/>
                    </t>
                  </div>
                  <div t-if="replayCurrent.inputs.length" class="rp-spot-inputs">
                    <div class="rp-spot-lbl">reads</div>
                    <div class="rp-inputs">
                      <t t-foreach="replayCurrent.inputs" t-as="iv" t-key="iv.col">
                        <div class="rp-input"><span class="rp-input-col" t-esc="iv.col"/><span class="rp-input-nm" t-esc="iv.name"/><span class="rp-input-val" t-esc="replayFmt(iv)"/></div>
                      </t>
                    </div>
                  </div>
                  <div class="rp-spot-result">
                    <span class="rp-spot-lbl">=</span>
                    <span t-attf-class="rp-result {{ replayCurrent.is_deduction ? 'ded' : '' }}"><t t-if="replayCurrent.is_deduction">−</t><t t-esc="replayFmt({number_format: replayCurrent.number_format, value: replayCurrent.result})"/></span>
                  </div>
                </t>
                <div t-else="" class="rp-idle">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
                  <b>Inputs are ready</b>
                  <span>Press play to watch each component compute in dependency order.</span>
                </div>
              </div>

              <!-- RIGHT: progressive ledger -->
              <div class="rp-ledger">
                <div class="rp-ledger-sec">Inputs &amp; constants · <t t-esc="state.replayData.seeded.length"/></div>
                <div t-foreach="state.replayData.seeded" t-as="sd" t-key="sd.col" class="rp-row seed">
                  <span class="rp-row-col" t-esc="sd.col"/>
                  <span class="rp-row-nm" t-esc="sd.name"/>
                  <span class="rp-row-val" t-esc="replayFmt(sd)"/>
                </div>
                <div class="rp-ledger-sec">Computed · <t t-esc="replaySteps.length"/></div>
                <div t-foreach="replaySteps" t-as="s" t-key="s.col" t-attf-class="rp-row {{ replayStepComputed(s_index) ? 'on' : 'pending' }} {{ s_index === state.replayStep ? 'current' : '' }}">
                  <span class="rp-row-col" t-esc="s.col"/>
                  <span class="rp-row-nm" t-esc="s.name"/>
                  <span t-if="replayStepComputed(s_index)" t-attf-class="rp-row-val {{ s.is_deduction ? 'ded' : '' }}"><t t-if="s.is_deduction">−</t><t t-esc="replayFmt({number_format: s.number_format, value: s.result})"/></span>
                  <span t-else="" class="rp-row-val pending">•••</span>
                </div>
              </div>
            </div>
          </t>
          <div t-elif="state.replayBusy" class="rp-loading">Tracing…</div>
          <div t-else="" class="rp-loading">No sample to replay. Add a test sample first.</div>
        </div>
      </div>

      <!-- What-if simulator overlay (B8) -->
      <div t-if="state.whatifOpen" class="pbfs-whatif-scrim" t-on-click="closeWhatif">
        <div class="pbfs-whatif" t-on-click="(ev) => ev.stopPropagation()">
          <div class="wi-head">
            <div class="wi-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="21" x2="4" y2="14"/><line x1="4" y1="10" x2="4" y2="3"/><line x1="12" y1="21" x2="12" y2="12"/><line x1="12" y1="8" x2="12" y2="3"/><line x1="20" y1="21" x2="20" y2="16"/><line x1="20" y1="12" x2="20" y2="3"/><line x1="1" y1="14" x2="7" y2="14"/><line x1="9" y1="8" x2="15" y2="8"/><line x1="17" y1="16" x2="23" y2="16"/></svg>
              <div>
                <b>What-if simulator</b>
                <small>Slide a rate and project the payroll cost across everyone</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeWhatif"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="wi-body" t-if="state.whatifData and state.whatifData.components.length">
            <!-- controls -->
            <div class="wi-controls">
              <label class="wi-pick"><span>Vary this rate / constant</span>
                <select t-on-change="onWhatifTargetChange">
                  <option t-foreach="state.whatifData.components" t-as="c" t-key="c.code" t-att-value="c.code" t-att-selected="c.code === state.whatifTarget"><t t-esc="c.name"/> (<t t-esc="c.col"/>)</option>
                </select>
              </label>
              <div class="wi-slider-block" t-if="whatifComp">
                <div class="wi-slider-vals">
                  <span class="wi-from">now <b t-esc="whatifFmtVal(state.whatifBase)"/></span>
                  <span t-attf-class="wi-to {{ state.whatifMult > 1 ? 'up' : (state.whatifMult &lt; 1 ? 'dn' : '') }}"><b t-esc="whatifFmtVal(whatifCandidate)"/> <span class="wi-pct" t-esc="(whatifPctChange() >= 0 ? '+' : '') + whatifPctChange() + '%'"/></span>
                </div>
                <input class="wi-slider" type="range" min="0" max="200" step="5" t-att-value="Math.round(state.whatifMult * 100)" t-on-input="onWhatifSlide"/>
                <div class="wi-slider-scale"><span>0×</span><span>1×</span><span>2×</span></div>
              </div>
            </div>

            <!-- results -->
            <t t-if="state.whatifResult and !state.whatifResult.empty">
              <div class="wi-cards">
                <div class="wi-card">
                  <span class="wi-card-lbl">Current <t t-esc="state.whatifHeadlineName"/> total</span>
                  <span class="wi-card-val" t-esc="vnd(state.whatifResult.baseline_total)"/>
                </div>
                <div class="wi-card accent">
                  <span class="wi-card-lbl">Projected total</span>
                  <span class="wi-card-val" t-esc="vnd(state.whatifResult.candidate_total)"/>
                  <span t-attf-class="wi-card-delta {{ state.whatifResult.delta_total > 0 ? 'up' : (state.whatifResult.delta_total &lt; 0 ? 'dn' : '') }}" t-esc="fmtSigned(state.whatifResult.delta_total)"/>
                </div>
                <div class="wi-card">
                  <span class="wi-card-lbl">Annualized impact (×12)</span>
                  <span t-attf-class="wi-card-val {{ state.whatifResult.annualized_delta > 0 ? 'up' : (state.whatifResult.annualized_delta &lt; 0 ? 'dn' : '') }}" t-esc="fmtSigned(state.whatifResult.annualized_delta)"/>
                </div>
                <div class="wi-card">
                  <span class="wi-card-lbl">Employees affected</span>
                  <span class="wi-card-val" t-esc="state.whatifResult.employees_changed + ' / ' + state.whatifResult.employees_total"/>
                </div>
              </div>

              <div t-attf-class="wi-scope {{ state.whatifResult.sampled ? 'sampled' : 'exact' }}">
                <t t-if="state.whatifResult.sampled">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                  Sampled preview (<t t-esc="state.whatifResult.employees_total"/> employees) — press <b>Run on everyone</b> for the exact figure.
                  <button class="pbfs-btn primary sm" t-att-disabled="state.whatifBusy" t-on-click="whatifRunFull">Run on everyone</button>
                </t>
                <t t-else="">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>
                  Exact — computed across all <t t-esc="state.whatifResult.employees_total"/> employees.
                </t>
              </div>

              <!-- take-home delta histogram -->
              <div t-if="state.whatifResult.histogram" class="wi-hist">
                <div class="wi-hist-title">Distribution of the change</div>
                <div class="wi-hist-bars">
                  <t t-foreach="state.whatifResult.histogram" t-as="b" t-key="b.bucket">
                    <div class="wi-hist-col">
                      <div class="wi-hist-stack">
                        <div class="wi-hb pos" t-attf-style="height:{{ whatifBarPct(b.pos) }}%" t-att-title="b.pos + ' up'"/>
                        <div class="wi-hb neg" t-attf-style="height:{{ whatifBarPct(b.neg) }}%" t-att-title="b.neg + ' down'"/>
                      </div>
                      <span class="wi-hb-lbl" t-esc="whatifHistLabels[b.bucket]"/>
                    </div>
                  </t>
                </div>
              </div>

              <!-- biggest movers -->
              <div t-if="state.whatifResult.movers and state.whatifResult.movers.length" class="wi-movers">
                <div class="wi-movers-title">Biggest movers</div>
                <div t-foreach="state.whatifResult.movers.slice(0, 8)" t-as="m" t-key="m_index" class="wi-mover">
                  <span class="wi-mover-nm" t-esc="m.emp"/>
                  <span class="wi-mover-b" t-esc="vnd(m.baseline)"/>
                  <span class="wi-mover-arrow">→</span>
                  <span class="wi-mover-c" t-esc="vnd(m.candidate)"/>
                  <span t-attf-class="wi-mover-d {{ m.delta > 0 ? 'up' : 'dn' }}" t-esc="fmtSigned(m.delta)"/>
                </div>
              </div>
            </t>
            <div t-elif="state.whatifBusy" class="wi-loading">Simulating…</div>
            <div t-else="" class="wi-loading">Move the slider to project the impact.</div>
          </div>
          <div t-elif="state.whatifData" class="wi-loading">This configuration has no rate/constant components to vary.</div>
          <div t-else="" class="wi-loading">Loading…</div>
        </div>
      </div>

      <!-- Release bundles + sign-off overlay (B3) -->
      <div t-if="state.releaseOpen" class="pbfs-release-scrim" t-on-click="closeReleases">
        <div class="pbfs-release" t-on-click="(ev) => ev.stopPropagation()">
          <div class="rl-head">
            <div class="rl-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>
              <div>
                <b>Releases</b>
                <small>Review and sign off the formula changes since the last milestone</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeReleases"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="rl-tabs">
            <button t-attf-class="rl-tab {{ state.releaseTab === 'pending' ? 'on' : '' }}" t-on-click="() => this.setReleaseTab('pending')">
              Pending<span t-if="state.releaseData and state.releaseData.change_count" class="rl-tab-badge" t-esc="state.releaseData.change_count"/>
            </button>
            <button t-attf-class="rl-tab {{ state.releaseTab === 'history' ? 'on' : '' }}" t-on-click="() => this.setReleaseTab('history')">
              History<span t-if="state.releaseList.length" class="rl-tab-badge muted" t-esc="state.releaseList.length"/>
            </button>
          </div>

          <div class="rl-body">
            <!-- ===== PENDING ===== -->
            <t t-if="state.releaseTab === 'pending'">
              <div t-if="state.releaseBusy and !state.releaseData" class="rl-loading">Building release…</div>
              <t t-if="state.releaseData and state.releaseData.ok">
                <t t-if="state.releaseData.change_count">
                  <div class="rl-since">
                    <t t-if="state.releaseData.from_milestone">Since <b t-esc="state.releaseData.from_milestone.name"/> · <t t-esc="state.releaseData.from_milestone.date"/></t>
                    <t t-else="">Since the configuration was created</t>
                    <span class="rl-since-count"><t t-esc="state.releaseData.change_count"/> component<t t-if="state.releaseData.change_count !== 1">s</t> changed</span>
                  </div>

                  <div class="rl-narrative">
                    <div class="rl-narrative-head"><span>Changelog</span><span class="rl-ai-tag">auto-drafted · editable</span></div>
                    <textarea class="rl-narrative-ta" t-att-value="state.releaseNarrative" t-on-input="onReleaseNarrative"/>
                  </div>

                  <div class="rl-changes">
                    <div t-foreach="state.releaseData.changes" t-as="c" t-key="c.rule_id" class="rl-change">
                      <div class="rl-change-head">
                        <span class="rl-change-col" t-esc="c.col"/>
                        <span class="rl-change-nm" t-esc="c.name"/>
                        <span t-attf-class="rl-reason {{ c.reason }}" t-esc="reasonLabel(c.reason)"/>
                      </div>
                      <div t-if="!c.type or c.formula_changed !== false" class="rl-diff">
                        <t t-foreach="c.runs" t-as="run" t-key="run_index">
                          <t t-if="run.op === 'equal'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok eq" t-esc="tk"/></t>
                          <t t-elif="run.op === 'insert'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/></t>
                          <t t-elif="run.op === 'delete'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/></t>
                          <t t-elif="run.op === 'replace'">
                            <span t-foreach="run.old" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/>
                            <span class="hs-arrow">→</span>
                            <span t-foreach="run.new" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                          </t>
                        </t>
                      </div>
                      <div t-if="c.constant_changed" class="rl-diff">
                        <span class="hs-tok del" t-esc="fmtSigned(c.old_constant)"/><span class="hs-arrow">→</span><span class="hs-tok ins" t-esc="fmtSigned(c.cur_constant)"/>
                      </div>
                    </div>
                  </div>

                  <div class="rl-actions" t-if="state.releaseData.can_edit">
                    <button class="pbfs-btn primary" t-att-disabled="state.releaseBusy" t-on-click="approveRelease">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/><path d="m9 12 2 2 4-4"/></svg>Sign off &amp; seal release
                    </button>
                  </div>
                </t>
                <div t-else="" class="rl-empty">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="9"/></svg>
                  <b>Nothing to release</b>
                  <span>No formula changes since the last milestone. Every edit is already sealed.</span>
                </div>
              </t>
            </t>

            <!-- ===== HISTORY ===== -->
            <t t-else="">
              <div t-if="!state.releaseList.length" class="rl-empty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/></svg>
                <b>No releases yet</b>
                <span>Sign off the pending changes to seal your first release.</span>
              </div>
              <div t-foreach="state.releaseList" t-as="rel" t-key="rel.id" class="rl-rel">
                <div class="rl-rel-head" t-on-click="() => this.toggleReleaseDetail(rel)">
                  <div class="rl-rel-main">
                    <div class="rl-rel-nm"><b t-esc="rel.name"/><span class="rl-rel-cnt" t-esc="rel.change_count + (rel.change_count === 1 ? ' change' : ' changes')"/></div>
                    <div class="rl-rel-meta">signed off by <t t-esc="rel.approved_by"/> · <t t-esc="rel.approved_date"/></div>
                  </div>
                  <svg t-attf-class="rl-rel-chev {{ state.releaseExpandId === rel.id ? 'open' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                </div>
                <div t-if="rel.narrative" class="rl-rel-narr" t-esc="rel.narrative"/>
                <div t-if="state.releaseExpandId === rel.id" class="rl-rel-detail">
                  <t t-if="state.releaseDetail and state.releaseDetail.changes">
                    <div t-foreach="state.releaseDetail.changes" t-as="c" t-key="c.rule_id" class="rl-change">
                      <div class="rl-change-head"><span class="rl-change-col" t-esc="c.col"/><span class="rl-change-nm" t-esc="c.name"/><span t-attf-class="rl-reason {{ c.reason }}" t-esc="reasonLabel(c.reason)"/></div>
                      <div t-if="!c.type or c.formula_changed !== false" class="rl-diff">
                        <t t-foreach="c.runs" t-as="run" t-key="run_index">
                          <t t-if="run.op === 'equal'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok eq" t-esc="tk"/></t>
                          <t t-elif="run.op === 'insert'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/></t>
                          <t t-elif="run.op === 'delete'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/></t>
                          <t t-elif="run.op === 'replace'">
                            <span t-foreach="run.old" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/><span class="hs-arrow">→</span><span t-foreach="run.new" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                          </t>
                        </t>
                      </div>
                      <div t-if="c.constant_changed" class="rl-diff">
                        <span class="hs-tok del" t-esc="fmtSigned(c.old_constant)"/><span class="hs-arrow">→</span><span class="hs-tok ins" t-esc="fmtSigned(c.cur_constant)"/>
                      </div>
                    </div>
                  </t>
                  <div t-else="" class="rl-rel-loading">…</div>
                  <!-- W86 — rollback available on the latest release only -->
                  <div t-if="rel.is_latest and state.canEdit" class="rl-rollback-row">
                    <button class="pbfs-btn danger sm" t-on-click.stop="() => this.openRollback(rel)">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7v6h6"/><path d="M21 17a9 9 0 0 0-15-6.7L3 13"/></svg>
                      Roll back this release
                    </button>
                    <span class="rl-rollback-hint">Reverts every component to its state before this release.</span>
                  </div>
                </div>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- W86 — rollback dialog: change list → simulate → type-to-confirm → apply -->
      <div t-if="state.rollbackOpen" class="pbfs-rollback-scrim" t-on-click="closeRollback">
        <div class="pbfs-rollback" t-on-click="(ev) => ev.stopPropagation()">
          <div class="rb-head">
            <div class="rb-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7v6h6"/><path d="M21 17a9 9 0 0 0-15-6.7L3 13"/></svg>
              <div>
                <b>Roll back <t t-esc="state.rollbackData and state.rollbackData.release ? state.rollbackData.release.name : ''"/></b>
                <small t-if="state.rollbackData and state.rollbackData.ok !== false"><t t-esc="state.rollbackData.change_count"/> component<t t-if="state.rollbackData.change_count !== 1">s</t> will be restored</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeRollback"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>
          <div class="rb-body">
            <div t-if="state.rollbackBusy and !state.rollbackData" class="rb-loading">Building rollback preview…</div>
            <t t-elif="state.rollbackData and state.rollbackData.ok !== false">
              <!-- block reason -->
              <div t-if="!state.rollbackData.eligible" class="rb-blocked">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg>
                <span t-esc="state.rollbackData.block_reason"/>
              </div>
              <!-- empty -->
              <div t-elif="!state.rollbackData.change_count" class="rb-empty">This release changed nothing to roll back.</div>
              <t t-else="">
                <!-- change list -->
                <div class="rb-changes">
                  <div t-foreach="state.rollbackData.changes" t-as="c" t-key="c.rule_id" class="rb-change">
                    <div class="rb-change-head">
                      <span class="rb-change-col" t-esc="c.col"/>
                      <span class="rb-change-nm" t-esc="c.name"/>
                      <span t-if="c.constant_changed" class="rb-tag const">constant</span>
                    </div>
                    <div t-if="c.formula_changed" class="rb-diff">
                      <t t-foreach="c.runs" t-as="run" t-key="run_index">
                        <t t-if="run.op === 'equal'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok eq" t-esc="tk"/></t>
                        <t t-elif="run.op === 'insert'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/></t>
                        <t t-elif="run.op === 'delete'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/></t>
                        <t t-elif="run.op === 'replace'">
                          <span t-foreach="run.old" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/><span class="hs-arrow">→</span><span t-foreach="run.new" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                        </t>
                      </t>
                    </div>
                    <div t-if="c.constant_changed" class="rb-const">
                      <span class="hs-tok del" t-esc="fmtSigned(c.cur_constant)"/><span class="hs-arrow">→</span><span class="hs-tok ins" t-esc="fmtSigned(c.old_constant)"/>
                    </div>
                  </div>
                </div>
                <!-- simulate-first -->
                <div class="rb-sim">
                  <div class="rb-sim-head">
                    <b>Impact preview</b>
                    <button class="pbfs-btn soft sm" t-on-click="runRollbackSim" t-att-disabled="state.rollbackSimBusy">
                      <t t-if="state.rollbackSimBusy">Simulating… <t t-esc="state.rollbackSimProgress"/>%</t>
                      <t t-else="">Simulate impact</t>
                    </button>
                  </div>
                  <div t-if="state.rollbackSimResult and !state.rollbackSimResult.empty" class="rb-sim-out">
                    <div class="rb-sim-stat"><b t-esc="state.rollbackSimResult.employees_changed.toLocaleString('en-US')"/><span>of <t t-esc="state.rollbackSimResult.employees_total.toLocaleString('en-US')"/> employees affected</span></div>
                    <div class="rb-sim-stat"><b t-esc="fmtSigned(state.rollbackSimResult.delta_total)"/><span><t t-esc="state.rollbackSimResult.headline"/> total Δ</span></div>
                  </div>
                  <div t-elif="state.rollbackSimResult and state.rollbackSimResult.empty" class="rb-sim-empty">No historical payslips to simulate against.</div>
                </div>
                <!-- type-to-confirm + apply -->
                <div class="rb-confirm" t-if="state.rollbackData.eligible and state.rollbackData.can_edit">
                  <label>Type <b t-esc="state.rollbackData.release.name"/> to confirm</label>
                  <input type="text" class="rb-confirm-in" t-att-value="state.rollbackConfirm" t-on-input="onRollbackConfirm" placeholder="Release name"/>
                  <button class="pbfs-btn danger-solid" t-att-disabled="!rollbackConfirmOk or state.rollbackApplying" t-on-click="applyRollback">
                    <t t-if="state.rollbackApplying">Rolling back…</t>
                    <t t-else="">Roll back now</t>
                  </button>
                </div>
              </t>
            </t>
            <div t-else="" class="rb-empty">Could not load the rollback preview.</div>
          </div>
        </div>
      </div>

      <!-- Bureau cockpit overlay (B6) -->
      <div t-if="state.bureauOpen" class="pbfs-bureau-scrim" t-on-click="closeBureau">
        <div class="pbfs-bureau" t-on-click="(ev) => ev.stopPropagation()">
          <div class="bu-head">
            <div class="bu-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="9" rx="1"/><rect x="14" y="3" width="7" height="5" rx="1"/><rect x="14" y="12" width="7" height="9" rx="1"/><rect x="3" y="16" width="7" height="5" rx="1"/></svg>
              <div>
                <b>Bureau cockpit</b>
                <small t-if="state.bureauData"><t t-esc="state.bureauData.company"/> · every configuration at a glance</small>
              </div>
            </div>
            <div class="bu-summary" t-if="state.bureauData and state.bureauData.cards.length">
              <div class="bu-stat"><b t-esc="bureauSummary.configs"/><span>configs</span></div>
              <div class="bu-stat"><b t-esc="bureauSummary.active"/><span>active</span></div>
              <div t-attf-class="bu-stat {{ bureauSummary.withErrors ? 'err' : '' }}"><b t-esc="bureauSummary.withErrors"/><span>with errors</span></div>
              <div t-attf-class="bu-stat {{ bureauSummary.withPending ? 'warn' : '' }}"><b t-esc="bureauSummary.withPending"/><span>unreleased</span></div>
              <div class="bu-stat"><b t-esc="bureauSummary.employees.toLocaleString('en-US')"/><span>employees</span></div>
            </div>
            <button class="hs-x" t-on-click="closeBureau"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="bu-body">
            <div t-if="state.bureauBusy and !state.bureauData" class="bu-loading">Gathering health across all configurations…</div>
            <div t-elif="state.bureauData and state.bureauData.cards.length" class="bu-grid">
              <t t-foreach="state.bureauData.cards" t-as="c" t-key="c.id">
              <div t-attf-class="bu-card {{ (c.problem_counts.error || 0) ? 'attn-err' : (c.pending_changes ? 'attn-warn' : '') }}">
                <div class="bu-card-top">
                  <div class="bu-card-nm" t-esc="c.name"/>
                  <span t-attf-class="bu-state {{ c.state }}" t-esc="bureauStateLabel(c.state)"/>
                </div>
                <div class="bu-card-chips">
                  <span t-if="c.division" class="bu-chip" t-esc="c.division"/>
                  <span class="bu-chip cyc" t-esc="bureauCycleLabel(c.cycle_type)"/>
                </div>
                <div class="bu-card-mid">
                  <div class="bu-ring">
                    <svg width="60" height="60" viewBox="0 0 42 42">
                      <circle cx="21" cy="21" r="19" fill="none" stroke="#EEF1F5" stroke-width="4"/>
                      <circle cx="21" cy="21" r="19" fill="none" t-attf-stroke="{{ c.score >= 80 ? '#059669' : (c.score >= 50 ? '#D97706' : '#DC2626') }}" stroke-width="4" stroke-linecap="round"
                              t-att-stroke-dasharray="ring(c.score).dash" t-att-stroke-dashoffset="ring(c.score).offset" transform="rotate(-90 21 21)"/>
                    </svg>
                    <span class="bu-ring-val" t-esc="c.score"/>
                  </div>
                  <div class="bu-metrics">
                    <div class="bu-metric"><span class="bu-m-val" t-esc="c.rule_count"/><span class="bu-m-lbl">components</span></div>
                    <div class="bu-metric"><span class="bu-m-val" t-esc="c.employees.toLocaleString('en-US')"/><span class="bu-m-lbl">employees</span></div>
                  </div>
                </div>
                <div class="bu-flags">
                  <span t-if="(c.problem_counts.error || 0)" class="bu-flag err"><t t-esc="c.problem_counts.error"/> error<t t-if="c.problem_counts.error !== 1">s</t></span>
                  <span t-if="(c.problem_counts.warning || 0)" class="bu-flag warn"><t t-esc="c.problem_counts.warning"/> warning<t t-if="c.problem_counts.warning !== 1">s</t></span>
                  <span t-if="(c.problem_counts.hint || 0)" class="bu-flag hint"><t t-esc="c.problem_counts.hint"/> hint<t t-if="c.problem_counts.hint !== 1">s</t></span>
                  <span t-if="c.pending_changes" class="bu-flag pending"><t t-esc="c.pending_changes"/> unreleased</span>
                  <span t-if="c.release_count" class="bu-flag rel"><t t-esc="c.release_count"/> release<t t-if="c.release_count !== 1">s</t></span>
                  <span t-if="!(c.problem_counts.error || 0) and !c.pending_changes" class="bu-flag ok"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>clean</span>
                </div>
                <div class="bu-card-actions">
                  <button class="pbfs-btn soft sm" t-on-click="() => this.bureauOpenConfig(c)">Open</button>
                  <button t-if="state.bureauData.can_edit" class="pbfs-btn ghost sm" t-on-click="() => this.bureauClone(c)">Clone</button>
                </div>
              </div>
              </t>
            </div>
            <div t-else="" class="bu-loading">No configurations found.</div>
          </div>
        </div>
      </div>

      <!-- ============ LEGISLATION PACKS (B4) ============ -->
      <div t-if="state.legisOpen" class="pbfs-legis-scrim" t-on-click="closeLegislation">
        <div class="pbfs-legis" t-on-click="(ev) => ev.stopPropagation()">
          <div class="lg-head">
            <div class="lg-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v18M4 7l8-4 8 4M6 21h12M5 7l-2 6a4 4 0 0 0 8 0L9 7M19 7l-2 6a4 4 0 0 0 8 0l-2-6"/></svg>
              <div>
                <b>Legislation packs</b>
                <small t-if="state.legisCoverage and state.legisCoverage.pack">Roll a statutory change across every configuration — reviewed, versioned, signed off</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeLegislation"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="lg-body">
            <div t-if="state.legisBusy and !state.legisPacks.length" class="lg-loading">Loading legislation packs…</div>

            <t t-if="state.legisPacks.length">
            <!-- LEFT: pack list -->
            <div class="lg-rail">
              <t t-foreach="state.legisPacks" t-as="p" t-key="p.id">
              <div t-attf-class="lg-pack {{ p.id === state.legisSel ? 'sel' : '' }} {{ p.drift ? 'has-drift' : '' }}" t-on-click="() => this.legisSelect(p.id)">
                <div class="lg-pack-top">
                  <span class="lg-pack-nm" t-esc="p.name"/>
                  <span t-attf-class="lg-ver">v<t t-esc="p.version"/></span>
                </div>
                <div class="lg-pack-meta">
                  <span t-attf-class="lg-pill {{ p.state }}" t-esc="legisStateLabel(p.state)"/>
                  <span class="lg-country" t-esc="p.country"/>
                  <span t-if="p.effective_date" class="lg-eff" t-esc="p.effective_date"/>
                </div>
                <div class="lg-pack-cov">
                  <span t-if="p.drift" class="lg-cov drift"><t t-esc="p.drift"/> to update</span>
                  <span t-if="p.aligned" class="lg-cov ok"><t t-esc="p.aligned"/> aligned</span>
                  <span t-if="p.na and !p.drift and !p.aligned" class="lg-cov na">not applicable</span>
                </div>
              </div>
              </t>
            </div>

            <!-- RIGHT: selected pack detail + coverage -->
            <div class="lg-main">
              <div t-if="!state.legisDetail" class="lg-loading">Loading pack…</div>
              <t t-if="state.legisDetail and state.legisDetail.ok">
                <!-- pack header -->
                <div class="lg-detail-head">
                  <div class="lg-dh-title">
                    <b t-esc="state.legisDetail.pack.name"/>
                    <span t-attf-class="lg-pill {{ state.legisDetail.pack.state }}" t-esc="legisStateLabel(state.legisDetail.pack.state)"/>
                    <span class="lg-ver big">v<t t-esc="state.legisDetail.pack.version"/></span>
                  </div>
                  <div class="lg-dh-sub">
                    <span t-if="state.legisDetail.pack.effective_date"><b>Effective</b> <t t-esc="state.legisDetail.pack.effective_date"/></span>
                    <span t-if="state.legisDetail.pack.authority" class="lg-auth" t-esc="state.legisDetail.pack.authority"/>
                  </div>
                  <div t-if="state.legisDetail.pack.description" class="lg-desc" t-esc="state.legisDetail.pack.description"/>
                </div>

                <!-- coverage summary strip -->
                <div t-if="state.legisCoverage and state.legisCoverage.ok" class="lg-summary">
                  <div class="lg-sum-stats">
                    <div t-attf-class="lg-sum {{ state.legisCoverage.summary.drift ? 'drift' : '' }}"><b t-esc="state.legisCoverage.summary.drift"/><span>need update</span></div>
                    <div class="lg-sum ok"><b t-esc="state.legisCoverage.summary.aligned"/><span>aligned</span></div>
                    <div class="lg-sum muted"><b t-esc="state.legisCoverage.summary.na"/><span>n/a</span></div>
                    <div t-if="state.legisCoverage.summary.drift" class="lg-sum"><b t-esc="state.legisCoverage.summary.employees_affected.toLocaleString('en-US')"/><span>employees affected</span></div>
                  </div>
                  <button t-if="state.legisCanEdit and state.legisCoverage.summary.drift" class="pbfs-btn primary lg-applyall" t-att-disabled="state.legisApplying" t-on-click="legisApplyAll">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                    Roll out to all <t t-esc="state.legisCoverage.summary.drift"/>
                  </button>
                  <div t-elif="!state.legisCoverage.summary.drift" class="lg-allgood">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>
                    All configurations aligned
                  </div>
                </div>

                <!-- statutory values in the pack -->
                <div class="lg-sec">Statutory values <span class="lg-secn" t-esc="state.legisDetail.items.length"/></div>
                <div class="lg-values">
                  <t t-foreach="state.legisDetail.items" t-as="it" t-key="it.code">
                  <div class="lg-val">
                    <div class="lg-val-lbl"><b t-esc="it.label"/><code t-esc="it.code"/></div>
                    <div class="lg-val-amt" t-esc="legisVal(it.number_format, it.value)"/>
                    <div t-if="it.note" class="lg-val-note" t-esc="it.note"/>
                  </div>
                  </t>
                </div>

                <!-- per-config coverage board -->
                <div class="lg-sec">Configurations</div>
                <div t-if="state.legisCoverage and state.legisCoverage.ok" class="lg-board">
                  <t t-foreach="state.legisCoverage.board" t-as="cf" t-key="cf.config_id">
                  <div t-attf-class="lg-row {{ cf.status }}">
                    <div class="lg-row-main">
                      <div class="lg-row-nm">
                        <span t-attf-class="lg-dot {{ cf.status }}"/>
                        <b t-esc="cf.name"/>
                        <span class="lg-emp"><t t-esc="cf.employees.toLocaleString('en-US')"/> emp</span>
                      </div>
                      <div class="lg-row-status">
                        <span t-if="cf.status === 'aligned'" class="lg-badge ok">Aligned</span>
                        <span t-elif="cf.status === 'drift'" class="lg-badge drift"><t t-esc="cf.changed"/> to update</span>
                        <span t-else="" class="lg-badge na">Not applicable</span>
                        <button t-if="cf.status === 'drift' and state.legisCanEdit" class="pbfs-btn soft sm" t-att-disabled="state.legisApplying" t-on-click="() => this.legisApplyOne(cf.config_id)">Apply</button>
                      </div>
                    </div>
                    <div t-if="cf.status === 'drift' and cf.diffs.length" class="lg-diffs">
                      <t t-foreach="cf.diffs" t-as="d" t-key="d.code">
                      <div class="lg-diff">
                        <span class="lg-diff-lbl" t-esc="d.label"/>
                        <span class="lg-diff-old" t-esc="legisVal(d.number_format, d.current)"/>
                        <svg class="lg-arw" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                        <span class="lg-diff-new" t-esc="legisVal(d.number_format, d.target)"/>
                      </div>
                      </t>
                    </div>
                  </div>
                  </t>
                </div>
              </t>
            </div>
            </t>
          </div>
        </div>
      </div>

      <!-- ============ CONFIG BRANCHES (B2) ============ -->
      <div t-if="state.branchOpen" class="pbfs-branch-scrim" t-on-click="closeBranches">
        <div class="pbfs-branch" t-on-click="(ev) => ev.stopPropagation()">
          <div class="br-head">
            <div class="br-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></svg>
              <div>
                <b>Branches</b>
                <small>Fork a live configuration, trial a change in isolation, merge it back as a release</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeBranches"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="br-body">
            <div t-if="state.branchBusy and !state.branchData" class="br-loading">Loading branches…</div>

            <t t-if="state.branchData and state.branchData.ok">
              <!-- viewing-a-branch banner -->
              <div t-if="state.branchData.config.is_branch" class="br-banner">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></svg>
                <div class="br-banner-txt">
                  You are viewing a branch of <b t-esc="state.branchData.config.parent_name"/>.
                  <span t-if="state.branchData.config.branch_state === 'merged'"> This branch has been merged.</span>
                </div>
                <button class="pbfs-btn ghost sm" t-on-click="openParentConfig">Open parent</button>
              </div>

              <!-- create a new branch (off the mainline) -->
              <div t-if="!state.branchData.config.is_branch and state.branchData.can_edit" class="br-new">
                <div class="br-new-title">New branch of <b t-esc="state.branchData.config.name"/></div>
                <div class="br-new-row">
                  <input type="text" class="br-input" placeholder="Branch name (e.g. Trial 2026 tax relief)" t-att-value="state.branchNewName" t-on-input="onBranchName"/>
                  <button class="pbfs-btn primary" t-att-disabled="state.branchCreating" t-on-click="createBranch">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                    Create branch
                  </button>
                </div>
                <input type="text" class="br-input note" placeholder="Why this branch exists (optional)" t-att-value="state.branchNewNote" t-on-input="onBranchNote"/>
              </div>

              <!-- branch list -->
              <div class="br-sec" t-if="state.branchData.branches.length">
                Branches of <t t-esc="state.branchData.config.is_branch ? state.branchData.config.parent_name : state.branchData.config.name"/>
                <span class="br-secn" t-esc="state.branchData.branches.length"/>
              </div>
              <div t-if="!state.branchData.branches.length and !state.branchData.config.is_branch" class="br-empty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></svg>
                No branches yet. Create one above to trial a change without touching the live configuration.
              </div>

              <div class="br-list">
                <t t-foreach="state.branchData.branches" t-as="b" t-key="b.id">
                <div t-attf-class="br-item {{ b.id === state.branchExpandId ? 'open' : '' }} {{ b.branch_state }}">
                  <div class="br-item-head" t-on-click="() => this.toggleBranchDiff(b)">
                    <svg t-attf-class="br-caret {{ b.id === state.branchExpandId ? 'down' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
                    <div class="br-item-main">
                      <div class="br-item-nm">
                        <b t-esc="b.name"/>
                        <span t-attf-class="br-pill {{ b.branch_state }}" t-esc="b.branch_state"/>
                      </div>
                      <div class="br-item-sub">
                        <span t-if="b.note" class="br-note" t-esc="b.note"/>
                        <span class="br-emp"><t t-esc="b.employees.toLocaleString('en-US')"/> emp</span>
                        <span t-if="b.created_by">· <t t-esc="b.created_by"/></span>
                      </div>
                    </div>
                    <div class="br-item-chips">
                      <span t-if="b.conflicts" class="br-chip conflict"><t t-esc="b.conflicts"/> conflict<t t-if="b.conflicts !== 1">s</t></span>
                      <span t-if="b.changed" class="br-chip changed"><t t-esc="b.changed"/> changed</span>
                      <span t-if="b.added" class="br-chip added"><t t-esc="b.added"/> added</span>
                      <span t-if="b.removed" class="br-chip removed"><t t-esc="b.removed"/> removed</span>
                      <span t-if="!b.changed and !b.added and !b.removed" class="br-chip nochg">no changes</span>
                    </div>
                  </div>

                  <!-- expanded diff -->
                  <div t-if="b.id === state.branchExpandId" class="br-diff">
                    <div t-if="!state.branchDiff" class="br-loading sm">Computing diff…</div>
                    <t t-if="state.branchDiff and state.branchDiff.ok">
                      <div t-if="!state.branchDiff.changed.length and !state.branchDiff.added.length and !state.branchDiff.removed.length" class="br-nodiff">This branch is identical to its parent.</div>

                      <!-- changed components -->
                      <t t-foreach="state.branchDiff.changed" t-as="ch" t-key="ch.code">
                      <div t-attf-class="br-drow {{ ch.conflict ? 'conflict' : '' }}">
                        <div class="br-drow-top">
                          <span class="br-col" t-esc="ch.col"/>
                          <b class="br-dname" t-esc="ch.name"/>
                          <span t-if="ch.conflict" class="br-conflict-tag" title="The parent changed this component after the branch was cut">parent moved</span>
                        </div>
                        <div t-if="ch.kind === 'formula'" class="br-tokens">
                          <t t-foreach="ch.runs" t-as="run" t-key="run_index">
                            <t t-if="run.op === 'equal'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok eq" t-esc="tk"/></t>
                            <t t-elif="run.op === 'insert'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/></t>
                            <t t-elif="run.op === 'delete'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/></t>
                            <t t-elif="run.op === 'replace'">
                              <span t-foreach="run.old" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/><span class="hs-arrow">→</span><span t-foreach="run.new" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                            </t>
                          </t>
                        </div>
                        <div t-else="" class="br-valchg">
                          <span class="br-old" t-esc="legisVal(ch.number_format, ch.old_value)"/>
                          <svg class="br-arw" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                          <span class="br-new" t-esc="legisVal(ch.number_format, ch.new_value)"/>
                        </div>
                      </div>
                      </t>

                      <!-- added / removed (informational — not auto-merged) -->
                      <div t-if="state.branchDiff.added.length" class="br-addrem">
                        <span class="br-ar-lbl added">Added in branch:</span>
                        <t t-foreach="state.branchDiff.added" t-as="ad" t-key="ad.code"><span class="br-ar-chip added" t-esc="ad.name"/></t>
                      </div>
                      <div t-if="state.branchDiff.removed.length" class="br-addrem">
                        <span class="br-ar-lbl removed">Removed in branch:</span>
                        <t t-foreach="state.branchDiff.removed" t-as="rm" t-key="rm.code"><span class="br-ar-chip removed" t-esc="rm.name"/></t>
                      </div>
                      <div t-if="state.branchDiff.added.length or state.branchDiff.removed.length" class="br-note-merge">Only changed components merge back; added/removed are shown for review.</div>

                      <!-- actions -->
                      <div class="br-actions">
                        <button class="pbfs-btn soft sm" t-on-click="() => this.openBranchConfig(b)">Open branch</button>
                        <button t-if="state.branchData.can_edit and b.branch_state === 'open' and state.branchDiff.changed.length" class="pbfs-btn primary sm" t-att-disabled="state.branchBusy" t-on-click="() => this.mergeBranch(b)">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="18" r="3"/><circle cx="6" cy="6" r="3"/><path d="M6 21V9a9 9 0 0 0 9 9"/></svg>
                          Merge into <t t-esc="state.branchDiff.parent.name"/>
                        </button>
                        <button t-if="state.branchData.can_edit and b.branch_state === 'open'" class="pbfs-btn ghost sm danger" t-on-click="() => this.discardBranch(b)">Discard</button>
                      </div>
                    </t>
                  </div>
                </div>
                </t>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- ============ SCHEME VARIANTS (B5) ============ -->
      <div t-if="state.variantOpen" class="pbfs-variant-scrim" t-on-click="closeVariants">
        <div class="pbfs-variant" t-on-click="(ev) => ev.stopPropagation()">
          <div class="vr-head">
            <div class="vr-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
              <div>
                <b>Scheme variants</b>
                <small>One master scheme, many variants — edit once, push everywhere, keep local overrides</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeVariants"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="vr-body">
            <div t-if="state.variantBusy and !state.variantData" class="vr-loading">Loading variants…</div>

            <t t-if="state.variantData and state.variantData.ok">
              <!-- master strip -->
              <div class="vr-master">
                <div class="vr-master-info">
                  <span class="vr-master-tag">Master</span>
                  <b t-esc="state.variantData.master.name"/>
                  <span t-if="state.variantData.role === 'variant'" class="vr-master-note">· you are viewing one of its variants</span>
                </div>
                <div class="vr-master-actions">
                  <button t-if="state.variantData.role === 'variant'" class="pbfs-btn ghost sm" t-on-click="openMasterConfig">Open master</button>
                  <button t-if="state.variantData.can_edit and state.variantData.variants.length" class="pbfs-btn primary sm" t-att-disabled="state.variantSyncing" t-on-click="pushToVariants">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
                    Push to all <t t-esc="state.variantData.variants.length"/>
                  </button>
                </div>
              </div>

              <!-- new variant -->
              <div t-if="state.variantData.can_edit" class="vr-new">
                <input type="text" class="vr-input" placeholder="New variant name (e.g. Hanoi branch, Night-shift plant)" t-att-value="state.variantNewName" t-on-input="onVariantName"/>
                <button class="pbfs-btn soft" t-att-disabled="state.variantCreating" t-on-click="createVariant">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                  New variant
                </button>
              </div>

              <div t-if="!state.variantData.variants.length" class="vr-empty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
                No variants yet. Create one to spin off a synced copy of this master for a site, entity, or worker group.
              </div>

              <div class="vr-sec" t-if="state.variantData.variants.length">Variants <span class="vr-secn" t-esc="state.variantData.variants.length"/></div>
              <div class="vr-list">
                <t t-foreach="state.variantData.variants" t-as="v" t-key="v.id">
                <div t-attf-class="vr-item {{ v.id === state.variantExpandId ? 'open' : '' }} {{ v.id === state.variantData.current_id ? 'current' : '' }}">
                  <div class="vr-item-head" t-on-click="() => this.toggleVariantDiff(v)">
                    <svg t-attf-class="vr-caret {{ v.id === state.variantExpandId ? 'down' : '' }}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
                    <div class="vr-item-main">
                      <div class="vr-item-nm">
                        <b t-esc="v.name"/>
                        <span t-if="v.id === state.variantData.current_id" class="vr-here">viewing</span>
                      </div>
                      <div class="vr-item-sub"><t t-esc="v.employees.toLocaleString('en-US')"/> emp</div>
                    </div>
                    <div class="vr-item-chips">
                      <span t-if="v.in_sync and !v.overrides" class="vr-chip sync"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>in sync</span>
                      <span t-if="v.drift" class="vr-chip drift"><t t-esc="v.drift"/> to sync</span>
                      <span t-if="v.overrides" class="vr-chip override"><t t-esc="v.overrides"/> override<t t-if="v.overrides !== 1">s</t></span>
                      <span t-if="v.added" class="vr-chip added"><t t-esc="v.added"/> added</span>
                      <span t-if="v.removed" class="vr-chip removed"><t t-esc="v.removed"/> removed</span>
                    </div>
                  </div>

                  <div t-if="v.id === state.variantExpandId" class="vr-diff">
                    <div t-if="!state.variantDiff" class="vr-loading sm">Comparing to master…</div>
                    <t t-if="state.variantDiff and state.variantDiff.ok">
                      <div t-if="!state.variantDiff.changed.length and !state.variantDiff.added.length and !state.variantDiff.removed.length" class="vr-nodiff">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>
                        Fully in sync with the master.
                      </div>

                      <t t-foreach="state.variantDiff.changed" t-as="ch" t-key="ch.code">
                      <div t-attf-class="vr-drow {{ ch.overridden ? 'ov' : (ch.drift ? 'drift' : '') }}">
                        <div class="vr-drow-top">
                          <span class="vr-col" t-esc="ch.col"/>
                          <b class="vr-dname" t-esc="ch.name"/>
                          <span t-if="ch.drift" class="vr-tag drift">will sync from master</span>
                          <button t-if="state.variantData.can_edit" t-attf-class="vr-ovbtn {{ ch.overridden ? 'on' : '' }}" t-on-click="() => this.toggleOverride(v, ch)">
                            <svg t-if="ch.overridden" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="11" width="14" height="10" rx="2"/><path d="M8 11V7a4 4 0 0 1 8 0v4"/></svg>
                            <svg t-else="" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="11" width="14" height="10" rx="2"/><path d="M8 11V7a4 4 0 0 1 8-1"/></svg>
                            <t t-if="ch.overridden">Protected</t><t t-else="">Inherits</t>
                          </button>
                        </div>
                        <div t-if="ch.kind === 'formula'" class="vr-tokens">
                          <t t-foreach="ch.runs" t-as="run" t-key="run_index">
                            <t t-if="run.op === 'equal'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok eq" t-esc="tk"/></t>
                            <t t-elif="run.op === 'insert'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/></t>
                            <t t-elif="run.op === 'delete'"><span t-foreach="run.tokens" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/></t>
                            <t t-elif="run.op === 'replace'">
                              <span t-foreach="run.old" t-as="tk" t-key="tk_index" class="hs-tok del" t-esc="tk"/><span class="hs-arrow">→</span><span t-foreach="run.new" t-as="tk" t-key="tk_index" class="hs-tok ins" t-esc="tk"/>
                            </t>
                          </t>
                        </div>
                        <div t-else="" class="vr-valchg">
                          <span class="vr-mtag">master</span>
                          <span class="vr-old" t-esc="legisVal(ch.number_format, ch.old_value)"/>
                          <svg class="vr-arw" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                          <span class="vr-new" t-esc="legisVal(ch.number_format, ch.new_value)"/>
                          <span class="vr-vtag">this variant</span>
                        </div>
                      </div>
                      </t>

                      <div t-if="state.variantDiff.added.length" class="vr-addrem">
                        <span class="vr-ar-lbl added">Variant-only:</span>
                        <t t-foreach="state.variantDiff.added" t-as="ad" t-key="ad.code"><span class="vr-ar-chip added" t-esc="ad.name"/></t>
                      </div>
                      <div t-if="state.variantDiff.removed.length" class="vr-addrem">
                        <span class="vr-ar-lbl removed">Missing vs master:</span>
                        <t t-foreach="state.variantDiff.removed" t-as="rm" t-key="rm.code"><span class="vr-ar-chip removed" t-esc="rm.name"/></t>
                      </div>

                      <div class="vr-actions">
                        <button class="pbfs-btn soft sm" t-on-click="() => this.openVariantConfig(v)">Open variant</button>
                        <button t-if="state.variantData.can_edit and v.drift" class="pbfs-btn primary sm" t-att-disabled="state.variantSyncing" t-on-click="() => this.syncVariant(v)">
                          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2v6h-6M3 12a9 9 0 0 1 15-6.7L21 8M3 22v-6h6M21 12a9 9 0 0 1-15 6.7L3 16"/></svg>
                          Sync from master
                        </button>
                        <button t-if="state.variantData.can_edit" class="pbfs-btn ghost sm danger" t-on-click="() => this.detachVariant(v)">Detach</button>
                      </div>
                    </t>
                  </div>
                </div>
                </t>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- ============ CLIENT REVIEW SHARES (B7) ============ -->
      <div t-if="state.shareOpen" class="pbfs-share-scrim" t-on-click="closeShare">
        <div class="pbfs-share" t-on-click="(ev) => ev.stopPropagation()">
          <div class="sh-head">
            <div class="sh-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5 15.4 17.5M15.4 6.5 8.6 10.5"/></svg>
              <div>
                <b>Client review links</b>
                <small>Share a read-only trust surface — no login, revocable, with optional release sign-off</small>
              </div>
            </div>
            <button class="hs-x" t-on-click="closeShare"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="sh-body">
            <div t-if="state.shareBusy and !state.shareData" class="sh-loading">Loading links…</div>

            <t t-if="state.shareData and state.shareData.ok">
              <!-- create -->
              <div t-if="state.shareData.can_edit" class="sh-new">
                <div class="sh-new-row">
                  <input type="text" class="sh-input" placeholder="Client name (who is this link for?)" t-att-value="state.shareNewClient" t-on-input="onShareClient"/>
                  <select class="sh-input sh-sel" t-on-change="onShareRelease">
                    <option value="">No release to sign off</option>
                    <t t-foreach="state.shareData.releases" t-as="r" t-key="r.id">
                      <option t-att-value="r.id" t-esc="'Sign off: ' + r.name"/>
                    </t>
                  </select>
                  <button class="pbfs-btn primary" t-att-disabled="state.shareCreating" t-on-click="createShare">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7 0l3-3a5 5 0 0 0-7-7l-1 1"/><path d="M14 11a5 5 0 0 0-7 0l-3 3a5 5 0 0 0 7 7l1-1"/></svg>
                    Create link
                  </button>
                </div>
              </div>

              <div t-if="!state.shareData.shares.length" class="sh-empty">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5 15.4 17.5M15.4 6.5 8.6 10.5"/></svg>
                No review links yet. Create one to give your client a read-only view of this configuration.
              </div>

              <div class="sh-list">
                <t t-foreach="state.shareData.shares" t-as="s" t-key="s.id">
                <div t-attf-class="sh-item {{ s.status }}">
                  <div class="sh-item-top">
                    <div class="sh-item-nm">
                      <b t-esc="s.client_name || 'Untitled link'"/>
                      <span t-attf-class="sh-pill {{ s.status }}" t-esc="shareStatusLabel(s.status)"/>
                    </div>
                    <div class="sh-item-meta">
                      <span t-if="s.release" class="sh-rel"><t t-esc="s.release"/></span>
                      <span t-if="s.view_count"><t t-esc="s.view_count"/> view<t t-if="s.view_count !== 1">s</t></span>
                      <span t-if="s.comment_count"><t t-esc="s.comment_count"/> comment<t t-if="s.comment_count !== 1">s</t></span>
                    </div>
                  </div>
                  <div t-if="s.signed_off_name" class="sh-signed">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 5 5 9-11"/></svg>
                    Signed off by <b t-esc="s.signed_off_name"/> · <t t-esc="s.signed_off_date"/>
                  </div>
                  <div class="sh-url-row">
                    <input class="sh-url" type="text" readonly="readonly" t-att-value="s.url"/>
                    <button class="pbfs-btn soft sm" t-on-click="() => this.copyShare(s)">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="11" height="11" rx="2"/><path d="M5 15V5a2 2 0 0 1 2-2h10"/></svg>
                      <t t-if="state.shareCopied === s.token">Copied</t><t t-else="">Copy</t>
                    </button>
                    <a class="pbfs-btn ghost sm" t-att-href="s.url" target="_blank">Open</a>
                    <button t-if="state.shareData.can_edit and s.status !== 'revoked'" class="pbfs-btn ghost sm danger" t-on-click="() => this.revokeShare(s)">Revoke</button>
                  </div>
                </div>
                </t>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- ============ DEPENDENCY MAP (B9) ============ -->
      <div t-if="state.depOpen" class="pbfs-dep-scrim" t-on-click="closeDepMap">
        <div class="pbfs-dep" t-on-click="(ev) => ev.stopPropagation()">
          <div class="dp-head">
            <div class="dp-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="5" cy="6" r="2.4"/><circle cx="5" cy="18" r="2.4"/><circle cx="19" cy="12" r="2.4"/><path d="M7.3 6.6 16.7 11M7.3 17.4 16.7 13"/></svg>
              <div>
                <b>Dependency map</b>
                <small><t t-esc="state.config.name"/> · every component, in computation order</small>
              </div>
            </div>
            <div class="dp-stats">
              <div class="dp-stat"><b t-esc="depStats.nodes"/><span>components</span></div>
              <div class="dp-stat"><b t-esc="depStats.depth"/><span>layers deep</span></div>
              <div class="dp-stat"><b t-esc="depStats.critical"/><span>longest chain</span></div>
              <div t-attf-class="dp-stat {{ depStats.cycles ? 'err' : '' }}"><b t-esc="depStats.cycles"/><span>cycles</span></div>
            </div>
            <button class="hs-x" t-on-click="closeDepMap"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="dp-toolbar">
            <span class="dp-tb-lbl">Show</span>
            <t t-foreach="depGroupsPresent" t-as="g" t-key="g">
              <button t-attf-class="dp-chip dg-{{ catKey(g) }} {{ depHiddenGroup(g) ? 'off' : '' }}" t-on-click="() => this.toggleDepGroup(g)">
                <span class="dp-dot"/><t t-esc="g"/><b t-esc="depGroupCount(g)"/>
              </button>
            </t>
            <button t-attf-class="dp-chip dp-crit {{ state.depCriticalOn ? 'on' : '' }}" t-on-click="toggleDepCritical">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m13 2-9 12h7l-1 8 9-12h-7z"/></svg>
              Critical path
            </button>
            <div class="dp-zoom">
              <button t-on-click="depZoomOut" title="Zoom out"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"><path d="M5 12h14"/></svg></button>
              <button class="dp-fit" t-on-click="fitDepMap">Fit</button>
              <button t-on-click="depZoomIn" title="Zoom in"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></button>
            </div>
          </div>

          <div t-attf-class="dp-canvas {{ state.depDragging ? 'dragging' : '' }}" t-ref="depCanvas"
               t-on-mousedown="onDepDown" t-on-mousemove="onDepMove" t-on-mouseup="onDepUp" t-on-mouseleave="onDepUp" t-on-wheel="onDepWheel">
            <svg class="dp-svg" width="100%" height="100%">
              <defs>
                <marker id="dep-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto-start-reverse">
                  <path d="M0 0 L10 5 L0 10 z" fill="context-stroke"/>
                </marker>
              </defs>
              <g t-attf-transform="{{ depTransform }}">
                <path t-foreach="state.depEdges" t-as="e" t-key="e_index" t-att-d="depEdgePath(e)" t-attf-class="{{ depEdgeCls(e) }}" marker-end="url(#dep-arrow)"/>
                <g t-foreach="state.depNodes" t-as="n" t-key="n.col" t-attf-class="{{ depNodeCls(n) }}"
                   t-attf-transform="translate({{ n.x }},{{ n.y }})"
                   t-on-click="() => this.setDepFocus(n)" t-on-dblclick="() => this.openFromMap(n)">
                  <rect class="dn-box" t-att-width="n.w" t-att-height="n.h" rx="10"/>
                  <rect class="dn-accent" width="4" t-att-height="n.h" rx="2"/>
                  <text class="dn-col" x="15" y="20" t-esc="n.col"/>
                  <text class="dn-type" t-att-x="n.w - 12" y="20" text-anchor="end" t-esc="n.type"/>
                  <text class="dn-name" x="15" y="35" t-esc="depClip(n.name)"/>
                </g>
              </g>
            </svg>
            <div t-if="!state.depNodes.length" class="dp-empty">No components to map yet.</div>
            <div class="dp-legend">
              <span><i class="lg-crit"/>Critical path</span>
              <span><i class="lg-cycle"/>Circular</span>
              <span class="dp-legend-hint">Click to trace · double-click to open · drag to pan</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Simulate-before-activate modal (F8) -->
      <div t-if="state.simOpen" class="pbfs-sim-scrim" t-on-click="closeSimulate">
        <div class="pbfs-sim" t-on-click="(ev) => ev.stopPropagation()">
          <div class="sm-head">
            <div class="sm-title">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="M7 14l4-4 3 3 5-6"/></svg>
              <div>
                <b>Simulate before activate</b>
                <small>This configuration vs the last actual payrun · every employee, real inputs</small>
              </div>
            </div>
            <button class="sm-x" t-on-click="closeSimulate"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>

          <div class="sm-body">
            <!-- progress while the chunked drive runs -->
            <div t-if="!state.simResult" class="sm-progress">
              <div class="sm-spin"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12a9 9 0 1 1-6.2-8.5"/></svg></div>
              <div class="sm-plabel">Recomputing every employee through the engine…</div>
              <div class="sm-pbar"><span t-attf-style="width:{{ state.simProgress }}%"/></div>
              <div class="sm-ppct"><t t-esc="state.simProgress"/>%</div>
            </div>

            <t t-if="state.simResult and state.simResult.empty">
              <div class="sm-empty">No historical payslips to simulate against for this configuration.</div>
            </t>

            <t t-if="state.simResult and !state.simResult.empty">
              <!-- headline banner: how many payslips change -->
              <div class="sm-banner" t-att-class="state.simResult.employees_changed ? 'changed' : 'clean'">
                <div class="sm-bignum"><t t-esc="state.simResult.employees_unchanged.toLocaleString()"/></div>
                <div class="sm-bantext">
                  <b>of <t t-esc="state.simResult.employees_total.toLocaleString()"/> payslips unchanged</b>
                  <small><t t-esc="state.simResult.pct_unchanged"/>% identical ·
                    <t t-esc="state.simResult.employees_changed.toLocaleString()"/> would change</small>
                </div>
                <div class="sm-headchip" t-if="state.simResult.headline">Take-home: <b t-esc="state.simResult.headline"/></div>
              </div>

              <!-- histogram of take-home deltas -->
              <div t-if="state.simResult.employees_changed" class="sm-hist">
                <div class="sm-sec">Distribution of take-home change</div>
                <div class="sm-hrows">
                  <div t-foreach="state.simResult.histogram" t-as="b" t-key="b.bucket" class="sm-hrow">
                    <div class="sm-hlabel" t-esc="simHistLabels[b.bucket]"/>
                    <div class="sm-hbar">
                      <span class="neg" t-attf-style="width:{{ simBarPct(b.neg) }}%" t-att-title="b.neg + ' lower'"/>
                      <span class="pos" t-attf-style="width:{{ simBarPct(b.pos) }}%" t-att-title="b.pos + ' higher'"/>
                    </div>
                    <div class="sm-hcount"><t t-if="b.neg" t-esc="'−' + b.neg"/><t t-if="b.neg and b.pos">/</t><t t-if="b.pos" t-esc="'+' + b.pos"/></div>
                  </div>
                </div>
                <div class="sm-legend"><span class="dot neg"/>lower<span class="dot pos"/>higher</div>
              </div>

              <!-- components that moved -->
              <div t-if="state.simResult.components.length" class="sm-comp">
                <div class="sm-sec">Components affected</div>
                <table class="sm-ctab">
                  <thead><tr><th>Component</th><th class="num">Employees</th><th class="num">Avg change</th><th class="num">Largest</th></tr></thead>
                  <tbody>
                    <tr t-foreach="state.simResult.components" t-as="c" t-key="c.code">
                      <td><b t-esc="c.code"/></td>
                      <td class="num" t-esc="c.employees.toLocaleString()"/>
                      <td t-attf-class="num {{ c.avg_delta >= 0 ? 'up' : 'dn' }}" t-esc="fmtSigned(c.avg_delta)"/>
                      <td class="num muted" t-esc="fmtSigned(c.max_abs)"/>
                    </tr>
                  </tbody>
                </table>
              </div>

              <!-- biggest movers -->
              <div t-if="state.simResult.movers.length" class="sm-mov">
                <div class="sm-sec">Biggest movers <small>(top <t t-esc="state.simResult.movers.length"/> by take-home change)</small></div>
                <table class="sm-mtab">
                  <thead><tr><th>Employee</th><th class="num">Before</th><th class="num">After</th><th class="num">Change</th></tr></thead>
                  <tbody>
                    <tr t-foreach="state.simResult.movers" t-as="m" t-key="m_index">
                      <td><b t-esc="m.emp"/> <span class="sm-ref" t-esc="m.ref"/></td>
                      <td class="num muted" t-esc="Math.round(m.baseline).toLocaleString()"/>
                      <td class="num" t-esc="Math.round(m.candidate).toLocaleString()"/>
                      <td t-attf-class="num {{ m.delta >= 0 ? 'up' : 'dn' }}" t-esc="fmtSigned(m.delta)"/>
                    </tr>
                  </tbody>
                </table>
              </div>

              <div class="sm-foot">
                <span class="sm-note">Nothing was written — this is a preview. Activate when you're confident.</span>
                <button class="pbfs-btn ghost" t-on-click="closeSimulate">Close</button>
              </div>
            </t>
          </div>
        </div>
      </div>

      <!-- Command Center (visual tool launcher) -->
      <div t-if="state.cmdOpen" class="pbfs-cmd-scrim" t-on-click="closeCommand">
        <div class="pbfs-cmd" t-on-click.stop="">
          <div class="pbfs-cmd-head">
            <div class="pbfs-cmd-kbd"><span class="pbfs-kbd">esc</span></div>
            <div class="eyebrow">
              <span class="badge"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg></span>
              <div>
                <h2>Command Center</h2>
                <div class="sub">Every tool for <b t-esc="state.config.name"/> · score <b t-esc="state.config.score"/></div>
              </div>
            </div>
            <div class="pbfs-cmd-search">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="7"/><path d="m21 21-4.3-4.3"/></svg>
              <input t-ref="cmdInput" type="text" placeholder="Search tools…" t-att-value="state.cmdQuery" t-on-input="setCmdQuery" t-on-keydown="onCmdKey"/>
            </div>
          </div>
          <div class="pbfs-cmd-body">
            <t t-foreach="commandView.lanes" t-as="lane" t-key="lane.id">
              <div class="pbfs-cmd-lane">
                <div class="lane-h"><span t-esc="lane.label"/><span class="n" t-esc="lane.tools.length"/></div>
                <div class="pbfs-cmd-grid">
                  <t t-foreach="lane.tools" t-as="tool" t-key="tool.key">
                    <button t-attf-class="pbfs-cmd-card a-{{ tool.accent }} {{ tool.fi === state.cmdActive ? 'active' : '' }}"
                            t-attf-style="animation-delay: {{ tool.fi * 28 }}ms"
                            t-on-click="() => this.runCommand(tool)"
                            t-on-mouseenter="() => this.state.cmdActive = tool.fi">
                      <span class="ic"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><t t-set="icon" t-value="tool.icon"/><t t-call="pb_formula_studio.CmdIco"/></svg></span>
                      <span class="tx">
                        <span class="nm"><span t-esc="tool.name"/><span t-if="tool.stat" class="stat" t-esc="tool.stat"/></span>
                        <span class="dc" t-esc="tool.desc"/>
                      </span>
                      <span class="go"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
                    </button>
                  </t>
                </div>
              </div>
            </t>
            <div t-if="!commandView.flat.length" class="pbfs-cmd-empty">No tools match “<t t-esc="state.cmdQuery"/>”.</div>
          </div>
          <div class="pbfs-cmd-foot">
            <div class="hints"><span><b>↑↓</b> navigate</span><span><b>↵</b> open</span><span><b>esc</b> close</span></div>
            <button class="searchall" t-on-click="cmdToPalette">Search everything <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></button>
          </div>
        </div>
      </div>

      <!-- PayAI slide-in -->
      <div t-attf-class="pbfs-ai-scrim {{ state.aiOpen ? 'open' : '' }}" t-on-click="closeAI"/>
      <aside t-attf-class="pbfs-ai-panel {{ state.aiOpen ? 'open' : '' }}">
        <div class="ai-top">
          <div class="ico"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg></div>
          <div><b>PayAI Formula Assistant</b><small>Describe a rule in plain English</small></div>
          <button class="x" t-on-click="closeAI"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
        </div>
        <div class="ai-body">
          <div class="ai-msg"><div class="who">PayAI</div>
            <div class="ai-bubble bot">Tell me what a component should compute and I'll write the formula, mapping it to your existing columns. Try:</div>
            <div class="ai-chips">
              <span class="ai-chip" t-on-click="() => this.aiAskChip('Net is gross minus all deductions')">Net = gross − deductions</span>
              <span class="ai-chip" t-on-click="() => this.aiAskChip('Explain Net Salary')">Explain a formula</span>
            </div>
          </div>
          <t t-foreach="state.aiMsgs" t-as="m" t-key="m_index">
            <div class="ai-msg"><div class="who" t-esc="m.who === 'you' ? 'You' : 'PayAI'"/>
              <div t-attf-class="ai-bubble {{ m.who === 'you' ? 'user' : 'bot' }}" t-esc="m.text"/></div>
          </t>
          <div t-if="state.aiProposal" class="ai-proposal">
            <div class="ph">Proposed formula<t t-if="state.aiProposal.target_col"><t t-esc="' · column ' + state.aiProposal.target_col"/></t></div>
            <div class="pf"><t t-esc="state.aiProposal.human"/></div>
            <div class="map">
              <t t-foreach="state.aiProposal.mapping" t-as="mp" t-key="mp_index">
                <span class="mp"><b t-esc="mp.name"/> → <t t-esc="mp.col"/></span>
              </t>
            </div>
            <div class="pa">
              <button class="pbfs-btn primary" style="flex:1" t-on-click="applyProposal">
                <t t-if="state.aiProposal.target_id">Apply to <t t-esc="state.aiProposal.target_name"/></t>
                <t t-else="">Create as new component</t>
              </button>
              <button class="pbfs-btn ghost" t-on-click="discardProposal">Discard</button>
            </div>
          </div>
        </div>
        <div class="ai-input pbfs-ai-input">
          <input placeholder="e.g. overtime is 1.5 times hourly rate" t-on-keydown="aiAskInput"/>
          <button class="send" t-on-click="(ev) => this.aiAsk(ev.target.closest('.ai-input').querySelector('input').value)">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="m4 12 16-8-6 16-3-7z"/></svg>
          </button>
        </div>
      </aside>

      <!-- Flowchart modal (Expand / drill-down) -->
      <div t-if="state.flowOpen" class="fc-scrim" t-on-click="closeFlow">
        <div class="fc-modal" t-on-click.stop="() => {}">
          <div class="fc-top">
            <div class="ico"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/><path d="M10 6.5h6a1 1 0 0 1 1 1V14"/></svg></div>
            <div><b t-esc="modalComp ? modalComp.name : 'Formula flow'"/><small>Calculation flow · = <t t-esc="modalComp ? previewVal(modalComp.col) : ''"/> for <t t-esc="sampleName"/></small></div>
            <div class="fc-crumbs">
              <t t-foreach="modalCrumbs" t-as="cb" t-key="cb_index">
                <span t-if="cb_index > 0">›</span>
                <span t-attf-class="cb {{ cb_index === modalCrumbs.length - 1 ? 'cur' : '' }}" t-on-click="() => this.crumbTo(cb_index)" t-esc="cb.name"/>
              </t>
            </div>
            <div class="fc-zoom">
              <button t-on-click="zoomOut" title="Zoom out"><svg viewBox="0 0 24 24" fill="none" stroke-width="2.4"><path d="M5 12h14"/></svg></button>
              <button class="zfit" t-on-click="zoomFit" title="Fit to window"><span class="zlabel">Fit</span></button>
              <button t-on-click="zoomIn" title="Zoom in"><svg viewBox="0 0 24 24" fill="none" stroke-width="2.4"><path d="M12 5v14M5 12h14"/></svg></button>
            </div>
            <button class="fc-x" t-on-click="closeFlow"><svg viewBox="0 0 24 24" fill="none" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button>
          </div>
          <div class="fc-legend">
            <span class="lg"><span class="sw" style="background:#0E7490"/>Input / info</span>
            <span class="lg"><span class="sw" style="background:#4F46E5"/>Earning</span>
            <span class="lg"><span class="sw" style="background:#B45309"/>Deduction / decision</span>
            <span class="lg"><span class="sw" style="background:#059669"/>Total</span>
            <span class="lg" style="margin-left:auto;color:#4F46E5">▤ click a formula box to drill in</span>
          </div>
          <div class="fc-canvas" t-on-wheel="onWheel" t-on-pointerdown="onPanStart" t-on-pointermove="onPanMove" t-on-pointerup="onPanEnd" t-on-pointercancel="onPanEnd">
            <div class="fc-zoomwrap"><t t-call="pb_formula_studio.Flow"><t t-set="flow" t-value="modalFlow"/></t></div>
          </div>
        </div>
      </div>

      <!-- First-setup wizard -->
      <div t-if="state.wizardOpen" class="pbfs-wz-scrim">
        <div class="pbfs-wz">
          <div class="wz-rail">
            <h3>New Formula Config</h3>
            <div class="rsub">Set up a payroll calculation in 5 quick steps.</div>
            <t t-foreach="[[1,'Basics','Name, country, cycle'],[2,'Components','Pick a starter set'],[3,'Formulas','Review what gets created'],[4,'Test','Sample data'],[5,'Activate','Create &amp; open']]" t-as="st" t-key="st[0]">
              <div t-attf-class="wz-step {{ state.wizardStep === st[0] ? 'active' : (state.wizardStep > st[0] ? 'done' : '') }}">
                <div class="num"><t t-if="state.wizardStep > st[0]">✓</t><t t-else="" t-esc="st[0]"/></div>
                <div><div class="stt" t-esc="st[1]"/><div class="sd" t-esc="st[2]"/></div>
              </div>
            </t>
          </div>
          <div class="wz-main">
            <div class="wz-content">
              <!-- Step 1: Basics -->
              <t t-if="state.wizardStep === 1">
                <h2>The basics</h2>
                <p class="lead">Name the configuration and choose its country and pay cycle.</p>
                <div class="wz-fields">
                  <div class="field full"><label>Configuration name</label>
                    <input class="wz-input" t-att-value="state.wizardForm.name" t-on-input="(ev) => this.wizardSet('name', ev)" placeholder="e.g. Vietnam Monthly 2026"/>
                  </div>
                  <div class="field"><label>Country</label>
                    <select class="wz-input" t-on-change="(ev) => this.wizardSet('country_code', ev)">
                      <option value="VN" selected="1">Vietnam</option>
                      <option value="ID">Indonesia</option>
                      <option value="IN">India</option>
                      <option value="SG">Singapore</option>
                      <option value="TH">Thailand</option>
                      <option value="KH">Cambodia</option>
                      <option value="MY">Malaysia</option>
                    </select>
                  </div>
                  <div class="field"><label>Cycle type</label>
                    <select class="wz-input" t-on-change="(ev) => this.wizardSet('cycle_type', ev)">
                      <option value="regular" selected="1">Regular</option>
                      <option value="mid_cycle">Mid-Cycle</option>
                      <option value="end_cycle">End-Cycle</option>
                      <option value="full_final">Full &amp; Final</option>
                    </select>
                  </div>
                </div>
              </t>
              <!-- Step 2: Components -->
              <t t-elif="state.wizardStep === 2">
                <h2>How do you want to start?</h2>
                <p class="lead">Pick a starting point. You can add, remove or rewrite any component afterwards.</p>
                <div class="wz-tmpl">
                  <div t-foreach="state.wizardTemplates" t-as="tpl" t-key="tpl.key"
                       t-attf-class="tmpl-card {{ state.wizardForm.template === tpl.key ? 'sel' : '' }}"
                       t-on-click="() => this.pickTemplate(tpl.key)">
                    <b><t t-if="tpl.flag"><t t-esc="tpl.flag"/> </t><t t-esc="tpl.name"/>
                      <span t-if="tpl.certified" class="tmpl-badge cert" title="Country-reviewed &amp; certified">✓ Certified</span>
                      <span t-elif="tpl.state === 'draft'" class="tmpl-badge draft" title="Structure validated; statutory items pending review">Draft</span>
                    </b><p t-esc="tpl.desc"/>
                    <small t-if="tpl.version and tpl.version !== 'legacy'" class="tmpl-ver">v<t t-esc="tpl.version"/><t t-if="tpl.effective_date"> · from <t t-esc="tpl.effective_date"/></t></small>
                  </div>
                  <div class="tmpl-card" t-on-click="importExcel">
                    <b>Import from Excel</b><p>Upload a spreadsheet — headers, columns and formulas detected automatically.</p>
                  </div>
                </div>
              </t>
              <!-- Step 3: Formulas review -->
              <t t-elif="state.wizardStep === 3">
                <h2>What's inside <t t-esc="wizardTpl.name"/></h2>
                <p class="lead">These components will be created. You'll tune them in the cockpit.</p>
                <!-- F113 registry template: full component list + rate tables + legislation refs -->
                <div t-if="wizardTpl.components and wizardTpl.components.length" class="wz-inside">
                  <div class="wz-complist">
                    <div t-foreach="wizardTpl.components" t-as="c" t-key="c_index" class="cpr">
                      <span class="c" t-esc="c.col"/><t t-esc="c.name"/>
                      <span t-attf-class="f {{ c.type }}" t-esc="c.type"/>
                    </div>
                  </div>
                  <div t-if="wizardTpl.rate_tables and wizardTpl.rate_tables.length" class="wz-rates">
                    <h3>Rate tables</h3>
                    <div t-foreach="wizardTpl.rate_tables" t-as="rt" t-key="rt_index" class="rate-row">
                      <b t-esc="rt.code"/> <span t-esc="rt.name"/>
                      <span class="rb"><t t-esc="rt.brackets.length"/> brackets</span>
                    </div>
                  </div>
                  <div t-if="wizardTpl.refs and wizardTpl.refs.length" class="wz-refs">
                    <h3>Legislation</h3>
                    <div t-foreach="wizardTpl.refs" t-as="r" t-key="r_index" class="ref-row">
                      <a t-if="r.url" t-att-href="r.url" target="_blank" t-esc="r.title || r.ref"/>
                      <span t-else="" t-esc="r.title || r.ref"/>
                      <span t-if="r.effective_date" class="rd"> · <t t-esc="r.effective_date"/></span>
                    </div>
                  </div>
                </div>
                <!-- legacy built-in template: short preview -->
                <div t-elif="wizardTpl.preview and wizardTpl.preview.length" class="wz-preview">
                  <div t-foreach="wizardTpl.preview" t-as="p" t-key="p_index" class="cpr">
                    <span class="c" t-esc="p.col"/><t t-esc="p.name"/><span class="f" t-esc="p.f"/>
                  </div>
                  <div class="cpr more">…and the full statutory set</div>
                </div>
                <p t-else="" class="wz-blank">You'll start with a blank canvas and add components in the cockpit — or ask PayAI to draft them.</p>
              </t>
              <!-- Step 4: Test -->
              <t t-elif="state.wizardStep === 4">
                <h2>Test data</h2>
                <t t-if="wizardTpl.builtin and wizardTpl.key === 'vn_standard'">
                  <p class="lead">We'll seed a sample employee so the live preview works immediately. Generate more from real staff anytime, then run tests from the cockpit.</p>
                  <div class="wz-note"><b>Sample — Standard</b> (Basic ₫15,000,000) will be added so you can see values right away.</div>
                </t>
                <t t-elif="wizardTpl.builtin">
                  <p class="lead">A blank canvas starts without test data — generate samples from real staff anytime, then run tests from the cockpit.</p>
                </t>
                <t t-else="">
                  <p class="lead">The template ships its certification test suite. Every test is seeded into the Test workbench with its expected values, so you can re-run and verify anytime.</p>
                  <div class="wz-note"><b><t t-esc="wizardTpl.name"/></b> certification tests will be added, pre-wired with expected results.</div>
                </t>
              </t>
              <!-- Step 5: Activate -->
              <t t-else="">
                <h2>Ready to create</h2>
                <p class="lead">Review and create. The configuration opens in the cockpit where you validate, test and activate.</p>
                <div class="wz-summary">
                  <div class="sr"><span>Name</span><b t-esc="state.wizardForm.name || '—'"/></div>
                  <div class="sr"><span>Country</span><b t-esc="state.wizardForm.country_code"/></div>
                  <div class="sr"><span>Cycle</span><b t-esc="state.wizardForm.cycle_type"/></div>
                  <div class="sr"><span>Starting point</span><b t-esc="wizardTpl.name || state.wizardForm.template"/></div>
                </div>
              </t>
            </div>
            <div class="wz-foot">
              <button class="pbfs-btn ghost" t-on-click="closeWizard">Cancel</button>
              <div style="margin-left:auto;display:flex;gap:10px;">
                <button t-if="state.wizardStep > 1" class="pbfs-btn ghost" t-on-click="wizardBack">Back</button>
                <button t-if="state.wizardStep &lt; 5" class="pbfs-btn primary" t-on-click="wizardNext">Continue</button>
                <button t-else="" class="pbfs-btn primary" t-on-click="wizardCreate" t-att-disabled="state.wizardBusy">
                  <t t-if="state.wizardBusy">Creating…</t><t t-else="">Create &amp; open</t>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>

    <!-- WP-A — Studio Command Layer overlays (W14 · W99 · W100) -->
    <FindReplace t-if="state.findOpen and !state.empty" index="searchIndex" canEdit="state.canEdit"
                 onClose="() => this.closeFind()" onValidate="(f) => this.gridValidateLive(f)"
                 onCommit="(items, note) => this.findCommit(items, note)"
                 onRename="(id) => this.findRename(id)" onJump="(id) => this.findJump(id)"/>
    <CommandPalette t-if="state.paletteOpen and !state.empty" commands="paletteCommands" onClose="() => this.closePalette()"/>
    <HoverCard t-if="state.hoverCard and hoverCardData" data="hoverCardData" style="hoverCardStyle"/>

    <!-- WP-F · W18 — keyboard-shortcuts overlay (registry-driven, zero RPC) -->
    <div t-if="state.shortcutsOpen" class="pbfs-shortcuts-scrim" t-on-click="closeShortcuts" data-coach="shortcuts">
      <div class="pbfs-shortcuts" t-on-click="(ev) => ev.stopPropagation()">
        <div class="pbsc-h">
          <div class="pbsc-title">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="5" width="20" height="14" rx="2"/><path d="M6 9h.01M10 9h.01M14 9h.01M18 9h.01M6 13h.01M18 13h.01M9 13h6"/></svg>
            Keyboard shortcuts
          </div>
          <button class="pbsc-x" t-on-click="closeShortcuts" title="Close (Esc)">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
          </button>
        </div>
        <div class="pbsc-body">
          <div t-foreach="shortcutSections" t-as="sec" t-key="sec.title" class="pbsc-sec">
            <div class="pbsc-sec-t" t-esc="sec.title"/>
            <div t-foreach="sec.rows" t-as="row" t-key="row_index" class="pbsc-row">
              <span class="pbsc-keys">
                <t t-foreach="row.keys" t-as="k" t-key="k_index"><kbd class="pbsc-kbd" t-esc="k"/></t>
              </span>
              <span class="pbsc-lbl" t-esc="row.label"/>
            </div>
          </div>
        </div>
        <div class="pbsc-foot"><b>?</b> opens this panel · <b>Esc</b> closes</div>
      </div>
    </div>

    <!-- WP-H · W95 — budget editor overlay (managers only) -->
    <div t-if="state.budgetEditOpen" class="pbfs-budget-scrim" t-on-click="closeBudgetEditor">
      <div class="pbfs-budget" t-on-click="(ev) => ev.stopPropagation()">
        <div class="pbbud-head">
          <div class="pbbud-titles">
            <b t-esc="state.budgetEdit and state.budgetEdit.id ? 'Edit budget' : 'New budget'"/>
            <small t-esc="state.config.name"/>
          </div>
          <button class="pbbud-x" t-on-click="closeBudgetEditor" title="Close">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
          </button>
        </div>

        <div t-if="state.budgetEdit" class="pbbud-body">
          <div class="pbbud-meta">
            <label>Name
              <input class="ce-input" t-att-value="state.budgetEdit.name" t-on-input="(ev) => this.onBudgetHead('name', ev)" placeholder="e.g. FY2026 Q3 target"/></label>
            <label>Period label
              <input class="ce-input" t-att-value="state.budgetEdit.period_label" t-on-input="(ev) => this.onBudgetHead('period_label', ev)" placeholder="optional"/></label>
            <label>Note
              <input class="ce-input" t-att-value="state.budgetEdit.note" t-on-input="(ev) => this.onBudgetHead('note', ev)" placeholder="optional"/></label>
          </div>

          <div class="pbbud-seed">
            <span class="pbbud-seed-lbl">Seed amounts from</span>
            <select t-on-change="(ev) => this.onSeedRunPick(ev)">
              <option t-foreach="state.cmpRuns" t-as="r" t-key="r.id" t-att-value="r.id" t-att-selected="r.id === state.budgetSeedRun" t-esc="r.name"/>
            </select>
            <button class="pbfs-btn soft sm" t-att-disabled="state.budgetBusy" t-on-click="seedBudgetFromRun">Seed from payrun</button>
            <button t-if="canSeedFromCompare" class="pbfs-btn ghost sm" t-on-click="seedBudgetFromCompare">Seed from comparison</button>
          </div>

          <div class="pbbud-rows">
            <t t-foreach="budgetEditByGroup" t-as="grp" t-key="grp.group">
              <div class="pbbud-grp" t-esc="grp.group"/>
              <div t-foreach="grp.rows" t-as="row" t-key="row.code" class="pbbud-row">
                <span class="pbbud-code" t-esc="row.code"/>
                <span class="pbbud-name" t-esc="row.name"/>
                <input class="ce-input pbbud-amt" type="number" step="any" t-att-value="row.amount" t-on-input="(ev) => this.onBudgetAmount(row.code, ev)"/>
              </div>
            </t>
            <t t-if="state.budgetEdit.orphans.length">
              <div class="pbbud-grp gone">Orphan lines (component gone)</div>
              <div t-foreach="state.budgetEdit.orphans" t-as="o" t-key="o.code" class="pbbud-row orphan">
                <span class="pbbud-code gone" t-esc="o.code"/>
                <span class="pbbud-name">no longer in this configuration</span>
                <input class="ce-input pbbud-amt" type="number" step="any" t-att-value="o.amount" t-on-input="(ev) => this.onBudgetAmount(o.code, ev)"/>
                <button class="pbbud-drop" t-on-click="() => this.dropOrphan(o.code)" title="Remove line">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
                </button>
              </div>
            </t>
          </div>
        </div>

        <div class="pbbud-foot">
          <button t-if="state.budgetEdit and state.budgetEdit.id" class="pbfs-btn ghost sm pbbud-del" t-att-disabled="state.budgetBusy" t-on-click="() => this.deleteBudget(state.budgetEdit)">Delete</button>
          <span class="pbbud-spacer"/>
          <button class="pbfs-btn ghost sm" t-on-click="closeBudgetEditor">Cancel</button>
          <button class="pbfs-btn primary sm" t-att-disabled="state.budgetBusy" t-on-click="saveBudget">
            <t t-if="state.budgetBusy">Saving…</t><t t-else="">Save budget</t>
          </button>
        </div>
      </div>
    </div>

    <!-- WP-H · W98 — offer calculator overlay (read-only, in-memory) -->
    <div t-if="state.offerOpen" class="pbfs-offer-scrim" t-on-click="closeOfferCalc">
      <div class="pbfs-offer" t-on-click="(ev) => ev.stopPropagation()">
        <div class="pbof-head">
          <div class="pbof-titles">
            <b>Offer calculator</b>
            <small>Type hypothetical inputs — the full breakdown runs through the live configuration. Nothing is saved.</small>
          </div>
          <button class="pbof-x" t-on-click="closeOfferCalc" title="Close">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
          </button>
        </div>

        <div class="pbof-body">
          <!-- inputs -->
          <div class="pbof-left">
            <div class="pbof-sec-h">
              <span>Inputs</span>
              <select t-if="state.offerSamples.length" class="pbof-sample" t-on-change="(ev) => this.offerFromSample(ev)">
                <option value="">Start from sample…</option>
                <option t-foreach="state.offerSamples" t-as="s" t-key="s.id" t-att-value="s.id" t-esc="s.name"/>
              </select>
            </div>
            <div t-if="!state.offerInputs.length" class="pbof-noinputs">This configuration has no input components.</div>
            <div t-foreach="state.offerInputs" t-as="i" t-key="i.code" class="pbof-input">
              <label>
                <span class="pbof-icode" t-esc="i.code"/>
                <span class="pbof-iname" t-esc="i.name"/>
              </label>
              <input class="ce-input" type="text" inputmode="decimal" t-att-value="i.value" t-on-input="(ev) => this.onOfferInput(i.code, ev)"/>
            </div>
          </div>

          <!-- breakdown -->
          <div class="pbof-right">
            <t t-if="state.offerResult">
              <div class="pbof-net">
                <span class="pbof-net-l"><t t-esc="state.offerResult.net_code || 'Net'"/></span>
                <span class="pbof-net-v" t-esc="offerMoney(state.offerResult.net_value)"/>
              </div>
              <div t-if="state.offerResult.subtotals.length" class="pbof-subs">
                <div t-foreach="state.offerResult.subtotals" t-as="st" t-key="st.group" class="pbof-sub">
                  <span class="pbof-sub-g" t-esc="st.group"/>
                  <span class="pbof-sub-v" t-esc="offerMoney(st.value)"/>
                </div>
              </div>
              <div class="pbof-break">
                <t t-foreach="offerGroups" t-as="grp" t-key="grp.group">
                  <div class="pbof-grp" t-esc="grp.group"/>
                  <div t-foreach="grp.rows" t-as="row" t-key="row.code" t-attf-class="pbof-row {{ row.appears_on_payslip ? 'onslip' : '' }}">
                    <span class="pbof-rcode" t-esc="row.code"/>
                    <span class="pbof-rname" t-esc="row.name"/>
                    <span class="pbof-rval" t-esc="offerVal(row)"/>
                  </div>
                </t>
              </div>
              <div class="pbof-honesty">Reports net + per-group subtotals only — no employer-cost figure is invented.</div>
            </t>
            <div t-elif="state.offerBusy" class="pbof-idle">Calculating…</div>
            <div t-else="" class="pbof-idle">Enter inputs to see the breakdown.</div>
          </div>
        </div>

        <div class="pbof-foot">
          <span class="pbof-foot-note" t-if="state.offerBusy">Calculating…</span>
          <span class="pbbud-spacer"/>
          <button class="pbfs-btn soft sm" t-att-disabled="!state.offerResult" t-on-click="copyOfferSummary">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
            Copy summary
          </button>
          <button class="pbfs-btn ghost sm" t-on-click="closeOfferCalc">Close</button>
        </div>
      </div>
    </div>

    <!-- WP-F · W104 — manage-snippets overlay (managers only) -->
    <div t-if="state.snipManageOpen" class="pbfs-snipmgr-scrim" t-on-click="closeSnipManage" data-coach="snippets">
      <div class="pbfs-snipmgr" t-on-click="(ev) => ev.stopPropagation()">
        <div class="pbsc-h">
          <div class="pbsc-title">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 18l6-6-6-6M8 6l-6 6 6 6"/></svg>
            Snippet library
          </div>
          <button class="pbsc-x" t-on-click="closeSnipManage" title="Close (Esc)">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
          </button>
        </div>
        <div class="snm-body">
          <div class="snm-list">
            <button class="pbfs-btn primary sm snm-new" t-on-click="newSnippet">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
              New snippet
            </button>
            <div t-if="!state.snippets.length" class="snm-empty">No snippets yet.</div>
            <div t-foreach="state.snippets" t-as="s" t-key="s.id"
                 t-attf-class="snm-row {{ state.snipEdit and state.snipEdit.id === s.id ? 'active' : '' }}"
                 t-on-click="() => this.editSnippet(s)">
              <div class="snm-row-main">
                <span class="snm-name" t-esc="s.name"/>
                <span class="snm-cat" t-esc="s.category"/>
              </div>
              <code class="snm-body-preview" t-esc="s.body"/>
              <button class="snm-del" t-on-click.stop="() => this.deleteSnippet(s)" title="Delete snippet">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
              </button>
            </div>
          </div>
          <div class="snm-edit" t-if="state.snipEdit">
            <label class="snm-f"><span>Name</span>
              <input class="ce-input" t-att-value="state.snipEdit.name" t-on-input="(ev) => this.onSnipField('name', ev)"/></label>
            <label class="snm-f"><span>Category</span>
              <select class="ce-input" t-on-change="(ev) => this.onSnipField('category', ev)">
                <option t-foreach="snipCategories" t-as="c" t-key="c[0]" t-att-value="c[0]"
                        t-att-selected="state.snipEdit.category === c[0]" t-esc="c[1]"/>
              </select></label>
            <label class="snm-f"><span>Body — use ${CODE} placeholders</span>
              <input class="ce-input snm-bodyinput" t-att-value="state.snipEdit.body" t-on-input="(ev) => this.onSnipField('body', ev)"/></label>
            <label class="snm-f"><span>Description</span>
              <input class="ce-input" t-att-value="state.snipEdit.description" t-on-input="(ev) => this.onSnipField('description', ev)"/></label>
            <div class="snm-edit-foot">
              <button class="pbfs-btn ghost sm" t-on-click="cancelSnipEdit">Cancel</button>
              <button class="pbfs-btn primary sm" t-att-disabled="state.snipBusy" t-on-click="saveSnippet">
                <t t-if="state.snipBusy">Saving…</t><t t-else="">Save snippet</t>
              </button>
            </div>
          </div>
          <div class="snm-edit snm-hint" t-else="">
            Select a snippet to edit, or create a new one. Insert snippets from the ⌘K palette or by typing their name in a formula cell.
          </div>
        </div>
      </div>
    </div>
    </div>
  </t>

  <!-- Reusable flowchart renderer -->
  <t t-name="pb_formula_studio.Flow">
    <div t-if="flow" class="fc-stage" t-attf-style="min-width:{{ flow.width }}px;min-height:{{ flow.height }}px;">
      <svg class="fc-edges" t-att-width="flow.width" t-att-height="flow.height">
        <t t-foreach="flow.edges" t-as="e" t-key="e_index">
          <path t-att-d="e.d" fill="none" t-att-stroke="e.color" stroke-width="2" class="fc-edge"/>
        </t>
      </svg>
      <t t-foreach="flow.nodes" t-as="n" t-key="n.id">
        <div class="fc-node" t-attf-style="left:{{ n.x }}px;top:{{ n.y }}px;">
          <t t-if="n.kind === 'if'">
            <div class="node-diamond"><div class="dmd" t-attf-style="border-color:{{ n.color }}"/><div class="dtext" t-attf-style="color:{{ n.color }}" t-esc="n.label"/></div>
          </t>
          <t t-elif="n.kind === 'result'">
            <div class="node-box node-result"><div class="nsub">result</div><div class="nlabel" t-esc="n.label"/><div class="nval" t-esc="n.value"/></div>
          </t>
          <t t-elif="n.kind === 'op'">
            <div class="node-box op-node">
              <div t-attf-class="op-badge {{ n.opCls }}" t-esc="n.label"/>
              <div class="nval" t-esc="n.value"/>
            </div>
          </t>
          <t t-else="">
            <div t-attf-class="node-box {{ n.formula ? 'clickable' : '' }}" t-attf-style="--nc:{{ n.color }};border-color:{{ n.color }}"
                 t-on-click="(ev) => this.flowNodeClick(ev, n)">
              <div t-if="n.sub" class="nsub" t-esc="n.sub"/>
              <div class="nlabel" t-esc="n.label"/>
              <div class="nval" t-esc="n.value"/>
              <div t-if="n.formula" class="drill">▤ open formula</div>
            </div>
          </t>
        </div>
      </t>
      <t t-foreach="flow.labels" t-as="l" t-key="l_index">
        <div t-attf-class="edge-label {{ l.cls }}" t-attf-style="left:{{ l.x }}px;top:{{ l.y }}px;" t-esc="l.text"/>
      </t>
    </div>
  </t>

  <!-- Rich hover tooltip for the cockpit toolbar icons (name + brief description).
       Rendered by the web tooltip service via data-tooltip-template; `info` keys
       (name, desc) are spread into the render context. -->
  <t t-name="pb_formula_studio.BtnTip">
    <div class="pbfs-btntip">
      <b t-esc="name"/>
      <span class="d" t-esc="desc"/>
    </div>
  </t>

  <!-- Command Center card icons, keyed by tool.icon (inner SVG shapes only;
       the wrapping <svg> with stroke settings lives in the card template). -->
  <t t-name="pb_formula_studio.CmdIco">
    <t t-if="icon === 'replay'"><polygon points="5 3 19 12 5 21 5 3"/></t>
    <t t-elif="icon === 'whatif'"><line x1="4" y1="21" x2="4" y2="14"/><line x1="4" y1="10" x2="4" y2="3"/><line x1="12" y1="21" x2="12" y2="12"/><line x1="12" y1="8" x2="12" y2="3"/><line x1="20" y1="21" x2="20" y2="16"/><line x1="20" y1="12" x2="20" y2="3"/><line x1="1" y1="14" x2="7" y2="14"/><line x1="9" y1="8" x2="15" y2="8"/><line x1="17" y1="16" x2="23" y2="16"/></t>
    <t t-elif="icon === 'depmap'"><circle cx="5" cy="6" r="2.4"/><circle cx="5" cy="18" r="2.4"/><circle cx="19" cy="12" r="2.4"/><path d="M7.3 6.6 16.7 11M7.3 17.4 16.7 13"/></t>
    <t t-elif="icon === 'offer'"><rect x="4" y="2" width="16" height="20" rx="2"/><path d="M8 6h8M8 10h.01M12 10h.01M16 10h.01M8 14h.01M12 14h.01M16 14h4M8 18h.01M12 18h.01"/></t>
    <t t-elif="icon === 'payslip'"><path d="M6 2h9l3 3v17l-3-2-3 2-3-2-3 2V2z"/><path d="M9 7h6M9 11h6M9 15h4"/></t>
    <t t-elif="icon === 'mapping'"><circle cx="6" cy="6" r="2.5"/><circle cx="6" cy="18" r="2.5"/><circle cx="18" cy="12" r="2.5"/><path d="M8.5 6.5C13 8 12 12 15.6 12M8.5 17.5C13 16 12 12 15.6 12"/></t>
    <t t-elif="icon === 'rates'"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M3 15h18M9 3v18"/></t>
    <t t-elif="icon === 'problems'"><path d="M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/><path d="M12 9v4M12 17h.01"/></t>
    <t t-elif="icon === 'branches'"><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></t>
    <t t-elif="icon === 'variants'"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></t>
    <t t-elif="icon === 'legislation'"><path d="M12 3v18M4 7l8-4 8 4M6 21h12M5 7l-2 6a4 4 0 0 0 8 0L9 7M19 7l-2 6a4 4 0 0 0 8 0l-2-6"/></t>
    <t t-elif="icon === 'releases'"><path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></t>
    <t t-elif="icon === 'share'"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5 15.4 17.5M15.4 6.5 8.6 10.5"/></t>
    <t t-elif="icon === 'payai'"><circle cx="12" cy="12" r="3.2"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></t>
  </t>

</templates>
